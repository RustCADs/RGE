# Correction Packet

DISPATCH_ID: ISSUE-35
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_04-29-51+0300
RELATED_FILES:
- crates/cad-projection/tests/render_adapter_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-35_TASK_2026-05-19_04-20-47+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-35_EXEC_2026-05-19_04-28-50+0300.md`
- Latest Verification Log: `.ai/dispatch-ISSUE-35/verification.round0.log`
- Review Report: none. The orchestrator stopped before Codex control review because canonical verification failed.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction - nothing more, nothing less.
The correction references the post-execution verification finding that blocked
the dispatch before control review.

1. **Satisfy split-exemption lint for the expanded smoke file** - Canonical
   verification failed in `cargo run -q -p rge-tool-architecture-lints -- all`
   with `[split-exemption] FAIL`:
   `crates\cad-projection\tests\render_adapter_smoke.rs:1001: file is 1132 lines (>1000) without // SPLIT-EXEMPTION: annotation`.
   Required change: edit only `crates/cad-projection/tests/render_adapter_smoke.rs`
   to add one top-of-file `// SPLIT-EXEMPTION:` annotation with a concrete
   reason tied to this cohesive render-adapter smoke aggregation and the
   Task Packet's explicit requirement not to add another integration test file.
   Do not split the file, add a new test file, change test behavior, change
   production code, change Cargo metadata, or edit documentation/scripts/lints.
   Acceptance: `cargo run -q -p rge-tool-architecture-lints -- all` exits 0
   with `[split-exemption] PASS`, and the canonical dispatch verification gate
   proceeds past architecture lints.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control review findings exist for this run because verification
failed before control review. The only approved correction is the split-exemption
annotation required to make the existing verified test change pass the
architecture gate.

## Updated Acceptance Criteria

Unchanged from the Task Packet except for this correction-round allowance:
`crates/cad-projection/tests/render_adapter_smoke.rs` may gain exactly one
`// SPLIT-EXEMPTION:` annotation explaining why this existing integration smoke
file remains cohesive after adding the Issue #35 LoftOp smoke. No other
behavioral or structural change is approved.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0,
  including `[split-exemption] PASS`.
- `cargo test -p rge-cad-projection --test render_adapter_smoke -- --nocapture`
  -> expected exit 0 with 8 passed / 0 failed / 0 ignored.
- `git diff --check -- crates/cad-projection/tests/render_adapter_smoke.rs`
  -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0. This is the dispatch-passing
  gate; document any environment-only inability to run it, but the dispatch
  cannot pass until this canonical gate exits 0.
- `git status --short --untracked-files=all` -> expected no unexpected changes
  outside `crates/cad-projection/tests/render_adapter_smoke.rs` and the required
  ISSUE-35 execution-report packet for the correction round.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with one correction-round clarification: halt
and route back to Planner if making architecture lints pass appears to require
anything beyond the single approved `// SPLIT-EXEMPTION:` annotation in
`crates/cad-projection/tests/render_adapter_smoke.rs`.

## Planner Notes

The prior execution already reported the focused render-adapter smoke passing
and `cargo +nightly fmt --all -- --check` passing. The canonical gate failed
only because the added Issue #35 smoke pushed the existing aggregated
`render_adapter_smoke.rs` file past the repository's 1000-line split lint.
Splitting is not approved here because the Task Packet explicitly required the
smoke to live in the existing file and forbade adding another integration test
file. A justified top-of-file split exemption is therefore the minimal
correction needed to keep scope intact and satisfy CI.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-35
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
