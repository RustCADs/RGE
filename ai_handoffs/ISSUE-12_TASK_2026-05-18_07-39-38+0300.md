# Task Packet

DISPATCH_ID: ISSUE-12
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_07-39-38+0300
RELATED_FILES:
- crates/cad-core/src/operators/transform.rs
STATUS: OPEN

## Goal

Add focused test-only coverage for `TransformOp` rotation, non-uniform scale, and structural hashing in `crates/cad-core/src/operators/transform.rs` for GitHub issue #12. The dispatch should increase confidence that the existing TRS evaluation handles known 90-degree Y rotation and non-uniform scale cases, and that `structural_hash` distinguishes rotation-only and scale-only changes, without changing runtime behavior.

## Scope

### MAY edit
- crates/cad-core/src/operators/transform.rs

### MUST NOT edit
- Any file other than `crates/cad-core/src/operators/transform.rs`.
- Production/runtime implementation outside the existing `#[cfg(test)] mod tests` in `crates/cad-core/src/operators/transform.rs`.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`, or any other handoff packet.

### MAY add new files
- None.

### MUST NOT add new files
- No new source files, test files, fixtures, docs, schemas, scripts, Cargo entries, or architecture-lint files. The protocol-required `EXECUTION_REPORT` packet is the only non-implementation artifact the Executor may create.

## Deliverables

- Add or revise unit tests inside the existing `#[cfg(test)] mod tests` in `crates/cad-core/src/operators/transform.rs`.
- Cover a 90-degree positive Y-axis rotation using known input vertices and hard-coded expected output positions derived from the standard right-handed rotation formula, not by reusing `TransformOp::evaluate` logic to compute expectations.
- Cover non-uniform scale on known vertices, including at least one vertex with non-zero Z so X, Y, and Z scale factors are all observable.
- Assert transformed positions with an absolute tolerance of `1e-5`.
- Extend `structural_hash` test coverage so changing only `rotation_quat_xyzw` and changing only `scale` each produce hashes distinct from the base hash.

## Acceptance Criteria

- The only implementation file changed is `crates/cad-core/src/operators/transform.rs`.
- All new or revised code is test-only and remains inside `#[cfg(test)] mod tests`.
- No changes are made to `TransformOp`, `Operator` implementation logic, `Tessellation`, production code, Cargo configuration, or repository documentation.
- Rotation coverage uses expected values independent of `TransformOp::evaluate`; for a +90 degree Y rotation, `(x, y, z)` should be asserted against `(z, y, -x)` within `1e-5`.
- Non-uniform scale coverage uses expected values computed directly from per-axis multiplication, for example `(x * sx, y * sy, z * sz)`, with hard-coded expected positions asserted within `1e-5`.
- `structural_hash` coverage includes assertions that a base transform, a rotation-only variant, and a scale-only variant do not collapse to the same hash.
- Existing transform tests continue to pass.

## Constraints / Non-Goals

- This is not a runtime behavior change. Do not modify `TransformOp::evaluate`, `TransformOp::structural_hash`, trait implementations, public APIs, serialization, or tessellation validation.
- Do not introduce new dependencies or test fixtures.
- Do not broaden the task into label propagation, normal handling, matrix composition changes, or transform semantics changes.
- Do not rewrite unrelated tests or refactor the operator module beyond the minimal helper/assertion code needed for the requested coverage.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-cad-core` -> expected exit 0
- `cargo test -p rge-cad-core --lib operators::transform --no-fail-fast` -> expected exit 0, including the new rotation, scale, and hash coverage
- `cargo test -p rge-cad-core --lib --no-fail-fast` -> expected exit 0

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested tests fail against the existing production implementation and passing them would require changing runtime code.
- Any required edit outside `crates/cad-core/src/operators/transform.rs` appears necessary.
- Any new dependency, fixture, generated file, or Cargo manifest change appears necessary.
- The expected rotation convention appears ambiguous after inspecting the existing `glam::Quat::from_xyzw` usage.
- Formatting or tests fail for reasons unrelated to the test-only changes and cannot be isolated within the allowed scope.

## Planner Notes

- Use the existing local test style in `transform.rs`; adding small test-only helper functions is acceptable if they stay inside `#[cfg(test)] mod tests`.
- The rotation input quaternion may be expressed as `[0.0, sqrt(0.5), 0.0, sqrt(0.5)]` or an equivalent literal for a +90 degree Y rotation, but expected positions should remain hard-coded from the independent formula.
- For scale coverage, choose vertices that make all three scale axes visible; the current `quad()` helper has `z = 0` for every vertex, so either add a test-local mesh/helper with non-zero Z or otherwise ensure Z scale is actually asserted.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-12
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
