# Task Packet

DISPATCH_ID: ISSUE-225
AUTHOR: Codex
TIMESTAMP: 2026-05-27_07-55-15+0300
RELATED_FILES:
- editor/rge-editor/Cargo.toml
- Cargo.lock
- editor/rge-editor/src/main.rs
- editor/rge-editor/tests/
- ai_handoffs/ISSUE-225_TASK_2026-05-27_07-55-15+0300.md
- ai_handoffs/ISSUE-225_EXEC_*.md
- ai_handoffs/ISSUE-225_*.meta.json
- ai_dispatch_logs/log_*ISSUE-225*.md
STATUS: OPEN

## Goal

Implement the next bounded Phase 9 editor-usability step for GitHub issue #225: add `rge-editor --scene <path>` so the editor can load existing RGE scene/project data through `rge-scene-loader` and construct an `EditorShell` from the resulting ECS `World`. This is a manually filed queue issue to run in branch mode / `-NoPublish`; do not merge, fast-forward, or push to `main` from inside the dispatch.

## Scope

### MAY edit
- `editor/rge-editor/Cargo.toml`
- `Cargo.lock` only for the unavoidable edge-only metadata update to the `rge-editor` package dependency list for the dependency edges named in this packet; no package versions, checksums, source URLs, or other package nodes may change.
- `editor/rge-editor/src/main.rs`
- `editor/rge-editor/tests/*.rs`
- `ai_handoffs/ISSUE-225_TASK_2026-05-27_07-55-15+0300.md`
- `ai_handoffs/ISSUE-225_EXEC_*.md`
- `ai_handoffs/ISSUE-225_*.meta.json`
- `ai_dispatch_logs/log_*ISSUE-225*.md`
- `.ai/dispatch-ISSUE-225/**`

### MUST NOT edit
- root `Cargo.toml`
- any `Cargo.lock` package node other than the `rge-editor` dependency list, and any `Cargo.lock` version, checksum, or source entry
- `plans/BASELINE.md`, `plans/IMPLEMENTATION.md`, or `plans/PLAN.md`
- `Status.md`, `HANDOFF.md`, `change.md`, or `README.md`
- `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`, `.ai/*.schema.json`, or scheduler configuration
- `Invoke-AiDispatchLoop.ps1`, `Invoke-AiDispatchQueue.ps1`, `Invoke-AiDispatchAuto.ps1`, `Register-AiDispatchSchedule.ps1`, `Get-AiDispatchHealth.ps1`, `Watch-AiDispatch.ps1`, `Wait-GitHubActions.ps1`, or `new-handoff.ps1`
- Rust crates outside `editor/rge-editor/`, including `crates/**`, `kernel/**`, `runtime/**`, and `tools/**`
- architecture-lint source, GitHub workflows, ADRs, docs, or fixtures under `golden-projects/**`
- any `ai_handoffs/` packet or sidecar outside this dispatch's `ISSUE-225_*` files

### MAY add new files
- `editor/rge-editor/tests/*.rs`
- `ai_handoffs/ISSUE-225_EXEC_*.md`
- `ai_handoffs/ISSUE-225_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-225_TASK_*.meta.json`
- `ai_dispatch_logs/log_*ISSUE-225*.md`
- `.ai/dispatch-ISSUE-225/**`

### MUST NOT add new files
- New Rust crates, docs, ADRs, architecture lints, workflows, scripts, fixtures, schemas, scheduler files, or plan/status/handoff/change files.
- New files outside `editor/rge-editor/tests/*.rs` and this dispatch's generated handoff/log/scratch artifacts.

## Deliverables

- Add only the dependency edges needed by `rge-editor` to load RGE scene/project data. Expected direct edges are `rge-data`, `rge-scene-loader`, and `ron` if needed, following the existing workspace/path dependency conventions in `editor/rge-editor/Cargo.toml`.
- If Cargo updates `Cargo.lock`, keep that diff limited to adding the named dependency strings to the existing `rge-editor` package dependency list. Do not move versions or add new package nodes.
- Extend the private CLI model in `editor/rge-editor/src/main.rs` with `scene_path: Option<PathBuf>` and parse `--scene <path>` beside the existing `--glb <path>` path without introducing a third-party CLI parser.
- Make `--scene` and `--glb` mutually exclusive with a clear CLI error and one-line stderr message. Preserve existing no-arg and `--glb <path>` behavior except for the new conflict error.
- Add a small testable scene-load helper in `main.rs` or a private module under `editor/rge-editor/`. It must load a `.rge-project` by parsing `rge_data::Project`, resolving the first scene relative to the project directory, parsing `rge_data::Scene`, then calling `rge_scene_loader::load_scene_into_world`; it must load a `.rge-scene` by parsing that file directly as `rge_data::Scene`, then calling `load_scene_into_world`.
- Wire `main` so `--scene <path>` constructs the editor with `EditorShell::with_world(world)` from the helper result. Do not use `EditorShell::with_world_projection_graph` for the scene load-only path.
- Keep the existing default cuboid demo and `--glb <path>` render-only path behavior byte-for-byte except where parser conflict handling requires the new error.
- Add parser and load-helper coverage either in the existing inline `#[cfg(test)]` module or in a new integration test under `editor/rge-editor/tests/`.

## Acceptance Criteria

- `parse_args` has coverage proving `--scene <path>` captures a `PathBuf` in `Cli::scene_path`.
- `parse_args` has conflict coverage for `--scene <path>` plus `--glb <path>`; either flag order may be covered, and covering both orders is acceptable.
- At least one test loads the tracked `golden-projects/simple-scene/.rge-project` through the new helper and verifies the resulting `World` has exactly two entities, matching the invariant in `crates/rge-scene-loader/tests/simple_scene.rs`.
- If the implementation adds a missing-path error for `--scene`, it has parser coverage and display text consistent with the existing `--glb` error style.
- The `--scene` branch in `main` constructs `EditorShell::with_world(world)` and does not route through CAD projection, `BRepHandle`, `CadProjection`, or `EditorShell::with_world_projection_graph`.
- The default no-arg cuboid demo still uses `build_cuboid_demo_shell()` and the existing CAD projection graph path.
- The `--glb <path>` branch still uses the existing glTF import, mesh conversion, watcher, and render-mesh shell construction path except for rejecting simultaneous `--scene`.
- No file outside the MAY edit / MAY add surface changes.
- Any `Cargo.lock` diff is limited to the `rge-editor` package dependency list and records no version, checksum, source, or package-node movement.

## Constraints / Non-Goals

- Treat this dispatch as load-only, headless constructor-level validation. Do not promise visible rendering for the golden simple-scene fixture; it has no CAD entity, `BRepHandle`, `CadProjection`, or prebuilt render mesh content today.
- Do not modify `rge-scene-loader`, `rge-data`, ECS, editor-shell, render crates, CAD crates, fixtures, architecture lints, docs, plans, status files, automation scripts, or workflows.
- Do not implement CadCheckpoint integration, MenuRegistry / File Open Scene UI, hierarchy panel UI, save for `.rge-scene` or `.rge-project`, workspace/project selection UI, drag-drop, file dialogs, recent files, hot reload for `.rge-scene`, BRep/CAD projection generation for simple-scene entities, or any change to `rge-scene-loader`.
- Do not add `clap`, `argh`, `pico-args`, or any other CLI parsing dependency.
- Do not use `EditorShell::with_world_projection_graph` for the load-only scene path.

## Review-Gate Strings

The TASK packet, EXEC report, and final control review must preserve these strings exactly:

```text
MUST add a --scene <path> CLI path to rge-editor without changing the existing default cuboid or --glb behavior
MUST load .rge-project by resolving its first scene and .rge-scene directly via rge_scene_loader::load_scene_into_world
MUST construct scene-loaded editor state with EditorShell::with_world and MUST NOT use with_world_projection_graph for the load-only scene path
MUST add parser conflict coverage for --scene plus --glb
MUST add golden-project simple-scene load coverage expecting two entities
MUST NOT edit plans Status HANDOFF change README automation scripts workflows ADRs architecture lints fixtures or crates outside editor/rge-editor
MUST keep this as load-only headless constructor-level validation and defer CadCheckpoint MenuRegistry hierarchy panel Save workspace UI drag-drop and CAD projection work
```

## Verification Gates

The Executor MUST run and document the result of each gate in the `EXECUTION_REPORT`:

- `git diff --check` - expected exit 0.
- `cargo test -p rge-editor --all-targets --no-fail-fast` - expected exit 0.
- Static inspection with `git diff --name-only` - expected tracked changes are limited to `editor/rge-editor/Cargo.toml`, `Cargo.lock`, `editor/rge-editor/src/main.rs`, optional `editor/rge-editor/tests/*.rs`, and this dispatch's own generated `ai_handoffs/ISSUE-225_*` / `ai_dispatch_logs/log_*ISSUE-225*` artifacts.
- Static inspection of `Cargo.lock` - expected only the `rge-editor` package dependency list grows for the named direct dependency edges; no version, checksum, source URL, or other package node changes.
- Static inspection with `git diff --name-only` - expected no changes to plans, `Status.md`, `HANDOFF.md`, `change.md`, `README.md`, automation scripts, workflows, ADRs, architecture lints, docs, fixtures, root `Cargo.toml`, or crates outside `editor/rge-editor`.
- If the orchestrator runs `.ai/dispatch.verify.ps1`, it must pass or the executor must halt with a narrow explanation that does not expand scope outside this issue.

## Halt Conditions

The Executor MUST halt without committing and request planner or human guidance if any of the following occur:

- The implementation needs to edit any file outside the MAY edit / MAY add surface.
- The requested dependency edges require any `Cargo.lock` change beyond adding dependency strings to the `rge-editor` package node, or require any package version, checksum, source, or new package-node change.
- `EditorShell::with_world` is unavailable or cannot construct the scene-loaded shell without changing crates outside `editor/rge-editor`.
- Loading the golden simple-scene project requires changing `rge-scene-loader`, `rge-data`, ECS, or any fixture under `golden-projects/**`.
- Passing the required tests requires adding visible rendering, CAD projection, BRep generation, file-open UI, save UI, hot reload, or any explicitly deferred feature.
- The canonical dispatch verifier fails on unrelated workspace surfaces and the only apparent fix is outside this dispatch's allowed scope.

## Planner Notes

The prior preflight gate identified that adding the required manifest dependency edges will necessarily update `Cargo.lock` metadata for the `rge-editor` package. This revision allows only that edge-only lockfile delta and preserves the original prohibition on dependency version movement or broader lockfile churn.

Existing `editor/rge-editor/src/main.rs` already owns CLI parsing, `main` dispatch, and inline private-helper tests, so inline tests are acceptable. A focused integration test under `editor/rge-editor/tests/` is also allowed if that is cleaner for accessing the helper. Use `crates/rge-scene-loader/tests/simple_scene.rs` as the behavioral reference for loading `golden-projects/simple-scene/.rge-project`, but do not copy its component-level assertions unless needed; the acceptance gate for this dispatch is the two-entity loaded-world count.

`editor/rge-editor/src/main.rs` already imports `rge_io_gltf::Scene`; avoid a `Scene` name collision by qualifying `rge_data::Scene` or importing it under a distinct alias such as `DataScene`.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-225
AUTHOR: Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
