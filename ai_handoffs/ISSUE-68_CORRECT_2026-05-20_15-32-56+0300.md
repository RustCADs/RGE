# Correction Packet

DISPATCH_ID: ISSUE-68
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20T15:32:56+03:00
RELATED_FILES:
- crates/script-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-68_TASK_2026-05-20_15-23-25+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-68_EXEC_2026-05-20_15-31-54+0300.md`
- Latest Review Report: Not applicable; the orchestrator verification gate failed before Codex control review.
- Prior Correction Packets: none.
- Verification Log: `.ai/dispatch-ISSUE-68/verification.round0.log`

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections, nothing more and nothing
less.

1. **Normalize formatter newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with "Incorrect newline style"
   in `crates/script-graph/src/lib.rs`. Required change: normalize only
   `crates/script-graph/src/lib.rs` to the newline style accepted by the
   repository's nightly rustfmt configuration. Do not change Rust semantics,
   identifiers, tests, comments, imports, manifests, config, or any other file.
   Acceptance: `cargo +nightly fmt --all -- --check` exits 0, and
   `git diff -- crates/script-graph/src/lib.rs` shows only newline-style
   normalization relative to the already-executed implementation.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred. There was no post-execution review packet because the
canonical verification gate failed before control review.

## Updated Acceptance Criteria

Unchanged from the Task Packet. The dispatch also must pass the canonical
post-execution verification gate after the newline-style correction.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-script-graph` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with this clarification: halt if passing
formatting appears to require editing any file other than
`crates/script-graph/src/lib.rs`.

## Planner Notes

The implementation itself is not being reopened by this correction packet. The
only approved change is to make the already-modified `crates/script-graph/src/lib.rs`
acceptable to the repository's canonical formatting check by fixing its newline
style. Do not add `.gitattributes`, change formatter configuration, touch
workspace metadata, or broaden the implementation.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-68
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
