# Correction Packet

DISPATCH_ID: ISSUE-225
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_08-31-16+0300
RELATED_FILES:
- ai_handoffs/ISSUE-225_TASK_2026-05-27_07-55-15+0300.md
- ai_handoffs/ISSUE-225_EXEC_2026-05-27_08-19-21+0300.md
- editor/rge-editor/src/main.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-225_TASK_2026-05-27_07-55-15+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-225_EXEC_2026-05-27_08-19-21+0300.md`
- Latest Review Report: none generated. The post-execution canonical verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more.

1. **Apply required rustfmt formatting in `main.rs`** - Verification gate finding: `cargo +nightly fmt --all -- --check` failed because `editor/rge-editor/src/main.rs` is not formatted at the `--scene` load error branch. Required change: in `editor/rge-editor/src/main.rs`, reformat only the `eprintln!` call in the `Err(e)` arm near line 927 so it matches rustfmt's multi-line output:

   ```rust
   eprintln!(
       "rge-editor: failed to load --scene {}: {e}",
       scene_path.display()
   );
   ```

   Do not change CLI behavior, scene-loading logic, tests, Cargo metadata, comments, imports, or any other file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and the only source correction is the rustfmt-required formatting change above.

## Deferred Findings (NOT Approved for This Round)

None. No review findings were produced; the only known blocker is the canonical format gate failure above.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-specific clarification: `editor/rge-editor/src/main.rs` may receive only the rustfmt-required formatting change in the `--scene` load error branch. The existing ISSUE-225 implementation must not be expanded or behaviorally changed.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` - expected exit 0.
- `.\.ai\dispatch.verify.ps1` - expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with these correction-round additions:

- Halt and route back to Planner if making `cargo +nightly fmt --all -- --check` pass would require any semantic Rust change, any Cargo metadata change, any test change, any documentation change, or any edit outside `editor/rge-editor/src/main.rs`.
- Halt and route back to Planner if the canonical verification gate fails after this formatting correction for a reason other than the approved rustfmt diff above.

## Planner Notes

This correction is deliberately narrow. The execution report indicates the ISSUE-225 `--scene PATH` implementation and local checks landed, but the orchestrator's canonical verification failed at the first formatter gate before Codex control review. Apply only the rustfmt output shown in the verification log, then rerun the canonical verification gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-225
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
