# Task Packet

DISPATCH_ID: ISSUE-396
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_00-03-01+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- ai_handoffs/ISSUE-396_TASK_2026-06-15_00-03-01+0300.md
- crates/editor-shell/src/lifecycle/**
- crates/editor-shell/src/render_path.rs
- crates/editor-shell/src/camera.rs
- crates/editor-ui/src/menus/**
- crates/editor-egui-host/src/**
- crates/editor-actions/src/**
- crates/cad-projection/src/**
- crates/plugin-discovery/src/**
- runtime/**
STATUS: OPEN

## Goal

Run GitHub issue #396 / task 142 as a docs/source-read-only Phase 9 audit after task 141's focus-loss double-click reset. Use current local source and status-doc reads for repository evidence, and use only the dispatcher-provided GitHub-state snapshot for queue/already-filed evidence. Compare the remaining keybinding/remap, host-shell routing, plugin execution, OS/typed clipboard, CAD/CommandBus mutation, and camera/navigation follow-up candidates. Then append exactly one bounded FEATURE task as task 143, carrying the same Self-re-arm final-step requirement, or append one source-grounded `NEEDS_HUMAN_RECORDED:` line containing the actual ISO date, a hyphen separator, and the reason if no bounded in-policy task exists.

This is an audit and task-selection dispatch. Do not implement task 143.

## Task 143 Selection Marker

TASK_143_SELECTION: EXECUTOR_TO_DECIDE_FROM_LOCAL_AUDIT

The executor must choose the task 143 outcome from fresh local source/status reads during this dispatch. The marker above is the packet's selection policy for task 143; it is not an implementation title, candidate winner, or source-gap conclusion.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-396_EXEC_*.md`
- `ai_handoffs/ISSUE-396_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-396/**`
- `ai_dispatch_logs/log_ISSUE-396*.md`

### MUST NOT edit
- Rust source or tests under `crates/**`, `editor/**`, `kernel/**`, or `runtime/**`.
- Cargo manifests, `Cargo.lock`, or dependency metadata.
- GitHub workflows, schemas, ADR files, packet templates, architecture-lint rules/config, or dispatch automation scripts.
- Existing handoff packets, run logs, claim artifacts, or audit artifacts unrelated to ISSUE-396.
- Plugin runtime/discovery/loading code, command routing, shortcut execution, remapping/persistence/fatal policy, OS clipboard behavior, CAD/projection/CommandBus mutation, undo/dirty/save-load authority, camera/navigation behavior, camera math, viewport hit testing, face-pick policy, wheel zoom, right-button orbit math, or middle-button pan math.

### MAY add new files
- `ai_handoffs/ISSUE-396_EXEC_*.md`
- `ai_handoffs/ISSUE-396_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-396/**`
- `ai_dispatch_logs/log_ISSUE-396*.md`

### MUST NOT add new files
- New Rust files, tests, crates, fixtures, Cargo files, workflows, schemas, ADRs, architecture-lint files, packet templates, task 144 artifacts, or generated artifacts for any dispatch other than ISSUE-396.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-396_EXEC_*.md
  - ai_handoffs/ISSUE-396_EXEC_*.meta.json
  - .ai/dispatch-ISSUE-396/**
  - ai_dispatch_logs/log_ISSUE-396*.md
MUST_NOT_EDIT:
  - crates/**
  - editor/**
  - kernel/**
  - runtime/**
  - tools/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/**
  - .ai/*.schema.json
  - .ai/dispatch.verify.ps1
  - Invoke-*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-*.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A completed source-read-only audit recorded under task 142 in `.ai/dispatch.tasks.md`, including the pre-edit task-heading check, dispatcher snapshot evidence, status-doc cross-check, and source evidence for each candidate class.
- Exactly one of these outcomes:
  - task 142 is marked done and exactly one bounded FEATURE task 143 is appended with explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions` sections, and with the copied Self-re-arm final-step requirement; or
  - one source-grounded `NEEDS_HUMAN_RECORDED:` line is appended with the actual ISO date, a hyphen separator, and the reason explaining why no bounded in-policy task 143 can be safely delegated.
- `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md` updated to reflect the audit result and the task 143 or `NEEDS_HUMAN_RECORDED` outcome.
- An ISSUE-396 EXECUTION_REPORT packet documenting commands run, evidence gathered, files changed, verification results, final status, and the self-rearm outcome.

## Acceptance Criteria

- No live `gh`, browser, network, or GitHub API command is used for queue or already-filed evidence. Queue/already-filed claims are limited to the dispatcher-provided snapshot generated at `2026-06-15T00:02:36.6430933+03:00`; that snapshot reports no open `ai-dispatch` issue before ISSUE-396 was created, no open failed autonomous issue, and autonomous issues already filed through closed #395.
- The audit compares all six required candidate classes: keybinding/remap/preferences/fatal policy, host-shell routing, plugin execution, OS/typed clipboard, CAD/CommandBus mutation, and camera/navigation.
- Every candidate conclusion has positive source references. Any negative or unchanged source claim records an explicit falsifying `rg`, `git grep`, or local read command and observed result.
- The self-rearm outcome appends exactly one bounded FEATURE task 143, or exactly one `NEEDS_HUMAN_RECORDED:` line. No task 144 is appended.
- The task 143 feature, if appended, includes explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions` sections and carries this same Self-re-arm final-step requirement so the chain continues.
- No implementation work for task 143 is performed.
- `git diff --name-only` shows only the MAY-edit documentation/status files and ISSUE-396 generated artifacts.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion this packet makes about current repository state lists its inverse-search command and observed result here. The executor must re-run and record its own pre-edit and post-edit evidence before relying on any claim.

- Claim: The task brief currently has task 140 done, task 141 done, task 142 open, and no task 143 heading.
  Falsifying search: `rg -n "^140\\.|^141\\.|^142\\.|^143\\." .ai/dispatch.tasks.md` -> observed matches only for task 140 at line 13308, task 141 at line 13514, and task 142 at line 13600; no `^143.` match.
- Claim: The allowed status docs identify ISSUE-395 / task 141 as complete and task 142 as the single open post-focus-loss audit.
  Falsifying search: `rg -n "ISSUE-395|task 141|task 142|task 143|focus-loss|double-click reset|Post-focus-loss" Status.md HANDOFF.md plans/BASELINE.md change.md .ai/dispatch.tasks.md` -> observed current matches in `Status.md:3`, `HANDOFF.md:3`, `plans/BASELINE.md:5-15`, `change.md:1929`, and `.ai/dispatch.tasks.md:13514-13712`, including task 142 as `Post-focus-loss-double-click-reset Phase 9 next-task source audit`.
- Claim: The Planner Notes' focus-loss / focus-gain context is current-source grounded: `handle_window_focus_change(false)` resets pending viewport left-double-click state and cancels active viewport drags, while `handle_window_focus_change(true)` does not enter that reset/cancel branch and has focused-true test coverage preserving active drag state.
  Falsifying search: `rg -n "handle_window_focus_change|if !focused|reset_viewport_left_double_click|cancel_viewport_drags_for_focus_loss|viewport_focus_loss_(active_orbit|active_pan|combined_drag|focused_true|resets_stale_left_double_click)" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs` -> observed current matches in `crates/editor-shell/src/lifecycle/mod.rs:2299-2302` showing the `if !focused` branch calling `reset_viewport_left_double_click()` and `cancel_viewport_drags_for_focus_loss()`, `crates/editor-shell/src/lifecycle/mod.rs:2306` for the focus-loss cancel helper, `crates/editor-shell/src/lifecycle/tests.rs:1712`, `:1737`, and `:1762` for active orbit/pan/combined focus-loss cancellation, `crates/editor-shell/src/lifecycle/tests.rs:1796` and `:1802` for focused-true active-drag preservation, and `crates/editor-shell/src/lifecycle/tests.rs:1813` and `:1834` for focus-loss stale left-double-click reset coverage.
- Claim: The task 143 selection policy is `EXECUTOR_TO_DECIDE_FROM_LOCAL_AUDIT`.
  Falsifying search: `rg -n "^TASK_143_SELECTION:" ai_handoffs/ISSUE-396_TASK_2026-06-15_00-03-01+0300.md` -> this completed TASK packet contains exactly one `TASK_143_SELECTION: EXECUTOR_TO_DECIDE_FROM_LOCAL_AUDIT` marker.
- Claim: Queue/already-filed state must not be fetched live inside the executor sandbox.
  Falsifying read: the dispatcher snapshot embedded in the ISSUE-396 goal states it was generated by `Invoke-AiDispatchAuto.ps1` at `2026-06-15T00:02:36.6430933+03:00` before ISSUE-396 was created, for repo `RustCADs/RGE`, and reports no open `ai-dispatch` issues, no open failed autonomous issues, and autonomous issues already filed through closed #395; this packet makes live GitHub access a halt condition rather than a verification step.

## Constraints / Non-Goals

- This is an audit and task-selection dispatch, not an implementation dispatch.
- Local source/status reads are allowed; source, Cargo, workflow, schema, automation, ADR, architecture-lint, and packet-template edits are not.
- Do not mutate or test candidate behavior while auditing. Greps, file reads, and status-doc reads are sufficient unless a narrow local command is needed only to inspect current behavior without changing files.
- Do not use live GitHub, `gh`, browser, or network calls to confirm queue state or already-filed autonomous tasks.
- Do not broaden the selected follow-up into a batch. If the coherent next step requires multiple implementation tasks, append `NEEDS_HUMAN_RECORDED` instead of a broad task.
- Do not change scheduler behavior, publish mode, queue semantics, verification gates, labels, claims, automation policy, or any source behavior.

## Verification Gates

The executor MUST run and document the result of each applicable gate in the EXECUTION_REPORT:

- `rg -n "^140\\.|^141\\.|^142\\.|^143\\." .ai/dispatch.tasks.md` before edits and after edits.
- `rg -n "ISSUE-395|task 141|task 142|task 143|focus-loss|double-click reset|Post-focus-loss" Status.md HANDOFF.md plans/BASELINE.md change.md .ai/dispatch.tasks.md`.
- Keybinding/remap/preferences/fatal-policy source grep, for example `rg -n "shortcut.*remap|remap.*shortcut|keymap|shortcut.*preference|preferences|fatal|conflict" crates/editor-ui/src/menus crates/editor-egui-host/src crates/editor-shell/src/lifecycle`.
- Host-shell routing source grep, for example `rg -n "MenuCommandHandoff|route_menu_command|drain_and_route_menu_commands|Command::" crates/editor-shell/src crates/editor-ui/src/menus crates/editor-egui-host/src`.
- Plugin execution source grep, for example `rg -n "ExtensionCommand|extension_command|plugin-discovery|plugin_discovery|PluginRegistry|PluginDiscovery|wasmtime|Command::Plugin|Command::Custom" crates/editor-shell/src crates/editor-ui/src/menus crates/plugin-discovery/src runtime crates`.
- OS/typed clipboard source grep, for example `rg -n "arboard|copypasta|Clipboard|entity_clipboard|copy_selected|paste|typed.*clipboard|clipboard.*type" Cargo.toml crates/editor-shell/src crates/editor-ui/src crates/editor-egui-host/src`.
- CAD/CommandBus mutation source grep, for example `rg -n "CommandBus|submit_action|undo_command|redo_command|mark_saved|CadProjection::tick|render_mesh_for|save_source|dirty|CommandBus" crates/editor-shell/src crates/editor-actions/src crates/cad-projection/src`.
- Camera/navigation source grep, for example `rg -n "ViewportLeftDoubleClick|handle_window_focus_change|reset_viewport_left_double_click|selected_cad_scene_bounds|reset_camera|zoom_camera|orbit|pan|CursorLeft|Focused\\(false\\)|CursorGrabMode|set_cursor_grab|viewport_navigation" crates/editor-shell/src crates/editor-ui/src/menus crates/editor-egui-host/src`.
- `rg -n "^143\\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` after edits, expected to show exactly one task 143 heading or exactly one new `NEEDS_HUMAN_RECORDED:` line for this dispatch outcome.
- `rg -n "^144\\." .ai/dispatch.tasks.md`, expected no matches.
- `git diff --name-only`, expected only paths within the MAY-edit list plus ISSUE-396 generated artifacts.
- `git diff --check`, expected exit 0.
- `.\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`, where `$execPacket` is the actual ISSUE-396 EXECUTION_REPORT path.

## Halt Conditions

The executor MUST halt and write an EXECUTION_REPORT with `STATUS: NEEDS_HUMAN` or `STATUS: BLOCKED` if any of the following occur:

- The dispatcher-provided GitHub-state snapshot is unavailable from the issue/packet context, and confirming queue/already-filed state would require live `gh`, browser, network, or GitHub API access.
- A task 143 heading or a new `NEEDS_HUMAN_RECORDED:` line already exists before edits, making the self-rearm state ambiguous.
- The audit cannot compare all six required candidate classes using local source/status reads.
- The selected follow-up would require implementation edits during this dispatch or edits outside the MAY-edit surface.
- More than one feature follow-up would be required to make the selected next task coherent.
- The audit result cannot be recorded as either exactly one bounded task 143 or one `NEEDS_HUMAN_RECORDED:` line without crossing a policy, architecture, ownership, or verification boundary.
- Verification shows a diff outside the MAY-edit surface or any task 144 heading.
- Verification fails for reasons inside the MAY-edit surface and cannot be fixed without expanding scope.
- Verification fails only because of an unrelated pre-existing issue outside this dispatch's scope; document the failure and halt rather than editing outside scope.

## Self-Rearm Requirements For Task 143

After the audit is complete and verified, follow the Self-re-arm protocol in `.ai/dispatch.tasks.md`: append exactly one bounded FEATURE task as task 143, carrying this same Self-re-arm final-step requirement so the chain continues, or, if no bounded in-policy next task exists, append one `NEEDS_HUMAN_RECORDED:` line with the actual ISO date, a hyphen separator, and the source-grounded reason filled in.

The task 143 feature must include explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions` sections. Its final step must require appending exactly one bounded next-task-source AUDIT task as task 144, or appending one source-grounded `NEEDS_HUMAN_RECORDED:` line if no bounded in-policy next task exists. Copy this Self-rearm final-step requirement into task 143. Edit `.ai/dispatch.tasks.md` to do this; it is already in this packet's MAY-edit list.

## Planner Notes

Task 141 shipped as ISSUE-395: `WindowEvent::Focused(false)` / `handle_window_focus_change(false)` now resets pending `ViewportLeftDoubleClick` state before preserving existing focus-loss drag cancellation and cursor-grab release behavior, and `handle_window_focus_change(true)` remains a no-op for active drag state. Focused lifecycle tests cover stale scene-wide frame prevention and stale selected-CAD frame prevention across first viewport left press, focus loss, and second in-threshold viewport left press, plus existing focus-loss orbit/pan/cursor-grab cases.

The dispatcher snapshot supplied with ISSUE-396 is the only allowed GitHub queue evidence for this audit. It was generated by `Invoke-AiDispatchAuto.ps1` at `2026-06-15T00:02:36.6430933+03:00` before ISSUE-396 was created, for repo `RustCADs/RGE`, and reports no open `ai-dispatch` issues, no open failed autonomous issues, and autonomous issues already filed through closed #395. Treat that snapshot as authoritative for queue/already-filed evidence in this sandbox.

The likely shape is another narrow Phase 9 editor-usability implementation task, but the executor must decide from current local source evidence whether the next bounded task belongs to keybinding/remap policy, host-shell routing, plugin execution, clipboard, CAD mutation, or camera/navigation. If all coherent candidates require broader product or architecture decisions, append `NEEDS_HUMAN_RECORDED` instead of forcing task 143.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-396
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
