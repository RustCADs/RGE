# Execution Report

DISPATCH_ID: ISSUE-217
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-27_00-25-31+0300
RELATED_FILES:
- ai_handoffs/ISSUE-217_TASK_2026-05-26_23-59-35+0300.md — TASK packet (read-only)
- ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-12-29+0300.md — prior EXEC (read-only; preserved unedited)
- ai_handoffs/ISSUE-217_CORRECT_2026-05-27_00-22-44+0300.md — CORRECTION packet executed (read-only)
- AI_DISPATCH_AUTOMATION.md — orchestrator dispatch shape, packet shape, control-review path (read with live `file:line` citations below)
- ai_handoffs/AI_HANDOFF_PROTOCOL.md — manual packet-protocol lifecycle (read with live `file:line` citations below)
- AGENTS.md — repo-orientation pointer (read)
- .ai/dispatch-ISSUE-217/codex.control.round0.json — Codex control review JSON (read-only provenance for the correction round)
- .ai/dispatch-ISSUE-217/verification.round0.log — canonical orchestrator verification log (read; re-reported below)
- plans/PLAN.md — §7 async / resource streaming + §10.1 Tier-1 cavity inventory (re-read for context cite)
- plans/IMPLEMENTATION.md — Phase 9 §9 production-pressure axes (re-read for context cite)
- plans/BASELINE.md — Phase 9 preflight sections §13.3 / §1.10.4 / §1.1 + editor-usability / live-inspector (re-read)
- Status.md — current state framing (re-read)
- HANDOFF.md — current handoff framing (re-read; cavity item 9 of "Dispatch order (post-audit)" + "Recommended next dispatches" item 9)
- change.md — append-only chronology (re-read at line 310 cavity admission row + line 373 reconciled-2026-05-21 Phase 9 row)
- Cargo.toml — workspace cavity declarations at lines 230, 231, 232, 233, 240
- Cargo.lock — cavity package entries at lines 4624, 4632, 4690, 4698, 4726
- kernel/job-system/Cargo.toml — substrate manifest (re-read full)
- kernel/job-system/src/lib.rs — public surface + NON-GOALS + smoke (re-read full, 80 lines)
- kernel/job-system/src/job.rs — `Job` / `JobId` / `JobKind` (re-read full, 125 lines)
- kernel/job-system/src/priority.rs — `JobPriority` 4-tier taxonomy (re-read full, 72 lines)
- kernel/job-system/src/scheduler.rs — `JobScheduler` queue substrate (re-read full, 222 lines)
- kernel/io-scheduler/Cargo.toml — substrate manifest (re-read full)
- kernel/io-scheduler/src/lib.rs — public surface + NON-GOALS + smoke (re-read full, 80 lines)
- kernel/io-scheduler/src/priority.rs — `Priority` 4-tier streaming taxonomy (re-read full, 71 lines)
- kernel/io-scheduler/src/request.rs — `IoRequest` / `IoRequestId` / `IoRequestKind` (re-read full, 126 lines)
- kernel/io-scheduler/src/scheduler.rs — `IoScheduler` queue substrate (re-read full, 227 lines)
- kernel/asset-streaming/Cargo.toml — substrate manifest (re-read full)
- kernel/asset-streaming/src/lib.rs — public surface + NON-GOALS + cavity self-check (re-read full, 111 lines)
- kernel/asset-streaming/src/record.rs — `ResidencyRecord` / `ResidencyId` / `RecordKind` (re-read full, 146 lines)
- kernel/asset-streaming/src/state.rs — `ResidencyState` 4-tier lifecycle (re-read full, 75 lines)
- kernel/asset-streaming/src/tracker.rs — `ResidencyTracker` substrate (re-read full, 255 lines)
- kernel/asset-view/Cargo.toml — substrate manifest (re-read full)
- kernel/asset-view/src/lib.rs — public surface + NON-GOALS (re-read full, 87 lines)
- kernel/asset-view/src/descriptor.rs — `ViewDescriptor` / `ViewKind` (re-read full, 112 lines)
- kernel/asset-view/src/id.rs — `AssetViewId` opaque handle (re-read full, 92 lines)
- kernel/asset-view/src/registry.rs — `AssetViewRegistry` substrate (re-read full, 225 lines)
- kernel/shared/Cargo.toml — substrate manifest (re-read full)
- kernel/shared/src/lib.rs — admission contract + NON-GOALS (re-read full, 111 lines)
- crates/cad-core/src/topology/face_id/mod.rs:56-57 — doc-comment cross-reference to cavity ID types
- crates/gfx/src/frame_graph/mod.rs:14 — doc-comment cross-reference to `kernel/io-scheduler::IoRequestId`
- crates/gfx/src/frame_graph/resource.rs:3 — doc-comment cross-reference to `kernel/io-scheduler::IoRequestId`
- crates/gfx/src/pso_cache.rs:12 — doc-comment cross-reference to `kernel/io-scheduler::IoRequestId`
- .ai/dispatch-ISSUE-217.attempt1/codex.control.round2.json — prior attempt control verdict (read-only provenance)
- ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.md — recent Phase 9 reflection-scale precedent EXEC (read-only context)
- ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md — recent Phase 9 invalidation-economics precedent EXEC (read-only context)
- ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md — recent Phase 9 abstraction-pain precedent EXEC (read-only context)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-217_TASK_2026-05-26_23-59-35+0300.md`

## Correction Packet Reference

`ai_handoffs/ISSUE-217_CORRECT_2026-05-27_00-22-44+0300.md`

This EXEC is the report-correction round demanded by the CORRECTION packet
sections "Approved Corrections (Planner Sign-Off)" lines 25-64 and
"Re-Verification Gates" lines 79-102. The scope is: rewrite the EXEC with
(1) live `file:line` citations from `AI_DISPATCH_AUTOMATION.md` and
`ai_handoffs/AI_HANDOFF_PROTOCOL.md` BEFORE any dispatch-state conclusion;
(2) explicit Identifier-Search Matrix coverage for standalone `Job` and
`Priority`; (3) preserve the audit-only / `pressure_deferred` posture and
re-report the canonical verification gates. No tracked source / doc /
schema / script / packet edit is performed; no `_REVIEW_` or `_CLOSEOUT_`
packet is written.

## Pre-Execution Review Consumed

No pre-execution review packet was issued for this orchestrator dispatch.
The dispatch-state question (whether `_REVIEW_` / `_CLOSEOUT_` are required
or whether an orchestrator dispatch's control-review path is enough) is
answered with live `file:line` citations from
`AI_DISPATCH_AUTOMATION.md` and `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
**before** any dispatch-state conclusion, as required by CORRECTION packet
items 1 and 4 (CORRECT lines 36-43, 79-88, 111-113).

## Live Dispatch / Protocol Citations (preceding every dispatch-state conclusion below)

Per CORRECTION packet item 1 (CORRECT lines 36-43) and `Re-Verification
Gates` (CORRECT lines 84-88), the following live `file:line` citations
were captured by reading the source documents with line numbers
immediately before the dispatch-state conclusions in this EXEC. Each
quoted span is anchored to a current line range, not to section names,
`RELATED_FILES`, `AGENTS.md`, or any prior EXEC's lines.

### AI_DISPATCH_AUTOMATION.md — orchestrator dispatch shape

- **AI_DISPATCH_AUTOMATION.md:42** — "`Invoke-AiDispatchLoop.ps1` automates
  **one bounded task (\"dispatch\")** end to end:" — this is the
  orchestrator's scope unit.
- **AI_DISPATCH_AUTOMATION.md:44-47** — the four-step orchestrator flow:
  "1. **Codex** writes a precise TASK specification from a one-line
  goal. / 2. **Claude** reviews that spec as a preflight gate; if
  approved, **Claude** executes it. / 3. **Codex** does a read-only
  control review of the result. / 4. If Codex asks for changes, it writes
  a CORRECTION packet and Claude re-executes." — defines the packet
  surface as TASK, EXEC (the result Codex reviews in step 3), and an
  optional CORRECTION. No `_REVIEW_` or `_CLOSEOUT_` is named.
- **AI_DISPATCH_AUTOMATION.md:49-51** — "It is a thin orchestration layer
  on top of an existing Markdown packet protocol (`ai_handoffs/`). The
  loop script **automates model routing only**. It never stages, commits,
  or pushes." — explicitly defers commit/push to outside the loop.
- **AI_DISPATCH_AUTOMATION.md:69-74** — the role table names only
  Planner / Executor preflight gate / Executor / Controller-Reviewer
  (Codex control review). No separate "Reviewer AI writes a REVIEW packet"
  role.
- **AI_DISPATCH_AUTOMATION.md:79-83** — "**Bounded scope.** Each TASK
  packet enumerates explicit MAY-edit / MUST-NOT-edit / ... ** Loop has
  no commit/push.** The inner loop only edits the working tree. ...
  **Auto-publish gate.** The queue runner publishes only when the loop
  exits 0 and Codex control verdict is `pass`." — orchestrator dispatch
  completion is defined by loop exit + control verdict, not by a
  `_CLOSEOUT_` packet.
- **AI_DISPATCH_AUTOMATION.md:186-194** — the dispatch-flow diagram
  ends with: "Claude executes the packet ... writes EXEC packet ... Codex
  control review ... pass ... **Dispatch loop finished. NO commit. NO
  push.**" — the orchestrator's terminal state is `Dispatch loop
  finished`, not a `_CLOSEOUT_` packet.
- **AI_DISPATCH_AUTOMATION.md:222-225** — "10. `pass` → loop ends.
  `needs_changes` → Codex writes a CORRECTION packet, Claude re-executes
  (capped by `MaxCorrectionRounds`). `block` → abort. / 11. **End.**
  Prints the task path, latest EXEC packet, control verdict, and
  commit-readiness. **No commit or push is performed.**" — control
  verdict drives loop termination; no `_REVIEW_` packet is emitted.
- **AI_DISPATCH_AUTOMATION.md:486-513** — `§10.4 Codex — control review
  (sandbox: read-only)` prompt template explicitly says "Return
  schema-compliant JSON only. Use: verdict=pass ... verdict=needs_changes
  if Codex should write a CORRECTION_PACKET ... Do not edit files. Do
  not stage. Do not commit. Do not push." — the control review's output
  is JSON only; the only packet Codex may write from this step is a
  CORRECTION packet.
- **AI_DISPATCH_AUTOMATION.md:566-573** — "Codex returns structured JSON
  *natively* via `--output-schema <file> --output-last-message <out>`;
  the orchestrator reads that `<out>` file directly for the control
  review." — the control review's output is a JSON file, NOT a Markdown
  packet under `ai_handoffs/`.
- **AI_DISPATCH_AUTOMATION.md:139** — the file table lists
  "`.ai/dispatch-<ID>/` ... Per-run scratch: prompt files, raw model
  logs, Claude marker records, Codex control JSON. ... generated —
  **git-ignore**" — the control-review JSON is stored in gitignored
  run-dir scratch.
- **AI_DISPATCH_AUTOMATION.md:961-965** — `§16 Operational notes` —
  "Per-run scratch lands in `.ai/dispatch-<DISPATCH_ID>/`: ...
  `codex.control.round*.json`" — reaffirms the orchestrator's control
  review writes a numbered JSON file in the gitignored run dir, never a
  handoff packet.

### ai_handoffs/AI_HANDOFF_PROTOCOL.md — manual packet protocol shape

- **ai_handoffs/AI_HANDOFF_PROTOCOL.md:1-12** — "A lightweight,
  append-only governance protocol for AI-to-AI dispatch exchanges in the
  RGE repository. ... It is a Markdown-based record-keeping convention —
  NOT runtime automation, NOT a build-system hook, NOT a replacement for
  ADRs / `Status.md` / `HANDOFF.md` / `change.md`." — the protocol
  documents the **manual** lifecycle, distinct from the orchestrator's
  automation layer.
- **ai_handoffs/AI_HANDOFF_PROTOCOL.md:50-66** — `## Dispatch Lifecycle`
  explicitly enumerates the manual chain: "1. **Planner → `TASK_PACKET`**.
  ... 2. **Executor → `EXECUTION_REPORT`** ... 3. **Reviewer →
  `REVIEW_REPORT`** ... 4. **If `APPROVED`:** Planner writes
  `FINAL_CLOSEOUT`. Dispatch is `CLOSED`. 5. **If `NEEDS_CORRECTION`:**
  Planner writes a `CORRECTION_PACKET` ... 6. **If `REJECTED`:** Planner
  writes `FINAL_CLOSEOUT` with `STATUS: ABANDONED` ..." — this is the
  full TASK → EXEC → REVIEW → CORRECT → CLOSEOUT chain.
- **ai_handoffs/AI_HANDOFF_PROTOCOL.md:141** — packet-type enumeration:
  "`<PACKET_TYPE>`: one of `TASK`, `EXEC`, `REVIEW`, `CORRECT`,
  `CLOSEOUT`." — the full vocabulary.
- **ai_handoffs/AI_HANDOFF_PROTOCOL.md:147-154** — "A clean single-round
  dispatch:" example shows `TASK / EXEC / REVIEW / CLOSEOUT`; lines
  157-166 show a dispatch with one correction round having `TASK / EXEC /
  REVIEW / CORRECT / EXEC / REVIEW / CLOSEOUT`. The protocol's clean
  shape includes `_REVIEW_` and `_CLOSEOUT_` packets.
- **ai_handoffs/AI_HANDOFF_PROTOCOL.md:358-369** — `### 5. Final
  closeout must include tests/gates and remaining risks` — "`FINAL_CLOSEOUT`
  is the only packet that closes a dispatch." Under the **manual**
  protocol, a `_CLOSEOUT_` is the dispatch-closing artifact; orchestrator
  dispatches use loop exit + control verdict + queue runner publish
  instead, per AI_DISPATCH_AUTOMATION.md:79-83 / 188-194 / 222-225 cited
  above.

### Dispatch-state conclusion (preceded by the live citations above)

Applying the citations: this dispatch is an **orchestrator dispatch**
(Codex plans / Claude executes / Codex controls), not a manual `_REVIEW_`
/ `_CLOSEOUT_` chain. Therefore:

- the only packets emitted by the orchestrator are TASK + EXEC, plus an
  optional CORRECT packet if the control review demands a correction
  round (AI_DISPATCH_AUTOMATION.md:44-47);
- the Codex control review's output is JSON in
  `.ai/dispatch-ISSUE-217/codex.control.round0.json`, not a handoff
  packet (AI_DISPATCH_AUTOMATION.md:486-513 / 566-573 / 139 / 961-965);
- missing `_REVIEW_` or `_CLOSEOUT_` packets in `ai_handoffs/` for this
  dispatch is normal and expected — the manual chain at
  ai_handoffs/AI_HANDOFF_PROTOCOL.md:50-66 / 147-166 / 358-369 is the
  separate manual protocol shape, not the orchestrator's.

This EXEC is the **second** EXEC for this orchestrator dispatch (the
first one is `ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-12-29+0300.md`,
which is preserved verbatim per CORRECTION packet `Halt Conditions` at
CORRECT lines 108-110). It is the report-correction product of the
CORRECT packet routed back to the Executor after the round-0 control
verdict `needs_changes` recorded in
`.ai/dispatch-ISSUE-217/codex.control.round0.json:1` —
`"verdict":"needs_changes"`.

## What I Changed

### Source

- None. The TASK packet (TASK lines 60-63, `### MAY edit: None`) and the
  CORRECTION packet (CORRECT lines 27-31, "report-correction round only
  ... Do not edit existing TASK or EXEC artifacts, source files,
  documentation files, schemas, scripts, dispatch logs, or any other
  packet.") forbid any tracked source / doc / schema / script /
  prior-packet edit. The audit-only boundary is preserved exactly as it
  was for the prior EXEC.

### Tests

- None.

### Docs

- None.

### New audit-only artifacts

- `ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-25-31+0300.md` (this file).
- Matching `.meta.json` sidecar via `new-handoff.ps1 -Finalize` after
  this body is finalized (allowed by CORRECT lines 28-31 — "write one
  new timestamped ISSUE-217 EXEC report and its normal `.meta.json`
  sidecar if the completion tooling emits one").

No `_REVIEW_` or `_CLOSEOUT_` packet is created. Per the citation block
above (AI_DISPATCH_AUTOMATION.md:44-47 / 188-194 / 222-225 / 486-513 /
566-573 / 139 / 961-965 contrasted with
ai_handoffs/AI_HANDOFF_PROTOCOL.md:50-66 / 141 / 147-166 / 358-369),
orchestrator dispatches emit only TASK / EXEC (plus optional CORRECT);
missing `_REVIEW_` / `_CLOSEOUT_` packets are not evidence of an
unfinished dispatch. CORRECT lines 117-118 explicitly require: "Halt
rather than writing a `_REVIEW_` or `_CLOSEOUT_` packet for this
orchestrator dispatch."

## Phase 9 Async-Orchestration Context (read-only, line-cited)

The TASK packet's goal (TASK lines 43-55) is to decide whether
`kernel/job-system`, `kernel/io-scheduler`, `kernel/asset-streaming`,
`kernel/asset-view`, or the `kernel/shared` cavity has gained concrete
consumer pressure from current runtime/editor/asset/script/loader paths.
Current evidence (preserved verbatim from the prior EXEC's findings,
re-verified live by re-reading each cited line below):

- **PLAN.md §7 (`plans/PLAN.md:991-993`)** anchors the cavity cluster:
  "`kernel/job-system` · `kernel/io-scheduler` · `kernel/asset-streaming`
  · `kernel/asset-view`. Streaming priorities: in-frustum near (must be
  resident) · in-frustum far (queue) · out-of-frustum near (predictive) ·
  out-of-frustum far (evictable). Hysteresis 1s + predictive prefetch."
  This is the eventual feature description, not a current consumer
  signal.
- **PLAN.md §10.1 Tier-1 crate inventory (`plans/PLAN.md:1023-1043`)**
  lists every cavity in the kernel inventory. Inventory entries describe
  eventual ownership, not current pressure.
- **IMPLEMENTATION.md Phase 9 §9 (`plans/IMPLEMENTATION.md:589-603`)**
  enumerates the eight production-pressure evaluation axes. The
  async-orchestration axis is the sixth bullet at line 598: "**Async
  orchestration** — job-system pressure under real load." Phase 9 framing
  at lines 591-594 is doctrinal — pressure-driven; no axis is preemptive.
- **BASELINE.md** records Phase 9 preflight entries in adjacent axes
  (§13.3 `plans/BASELINE.md:254-278`; §1.10.4
  `plans/BASELINE.md:328-336`; §1.1 `plans/BASELINE.md:437-449`;
  editor-usability `plans/BASELINE.md:541-545`; live-inspector
  `plans/BASELINE.md:643-647`). **No async-orchestration / cavity
  preflight has been recorded** — the BASELINE.md surface for this axis
  is still empty by design.
- **Status.md line 9 (v0 release certification snapshot 2026-05-14)**
  classifies the five cavities together as 4 DOCTRINE CAVITY +
  1 ADMISSION-GATED EMPTY, naming them under "Known v0 deferrals (NOT
  blockers)" as the "4 kernel doctrine-cavity eventual implementations".
- **Status.md line 13 (MAIN-ORDERED queue closeout 2026-05-14)** under
  "Pressure-driven future dispatch candidates (none currently surfacing
  pressure)" enumerates the same five cavities. The line is direct: "no
  fresh implementation warranted without human direction."
- **Status.md line 373 (Reconciled 2026-05-21 against
  IMPLEMENTATION.md)** confirms the current frontier and posture: "The
  real next frontier is **Phase 9 (production pressure)** ... plus the
  carry-over kernel cavities (`shared`, `asset-streaming`,
  `io-scheduler`, `job-system`, `asset-view`), which should fill only
  under real consumer pressure."
- **HANDOFF.md "Dispatch order (post-audit)" item 9 + "Recommended next
  dispatches" item 9** state no scheduled work and "follow up only when
  real driver pressure appears; none is an empty stub."
- **change.md line 310** records the cavity-cluster admission. **change.md
  line 373 (2026-05-21 Phase 9 reconciliation)** mirrors the Status.md
  line 373 wording: cavities "should fill only under real consumer
  pressure."
- **Recent Phase 9 audit precedents (read-only, distinct axes)**:
  ISSUE-216 (reflection scale, EXEC at
  `ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.md`); ISSUE-214
  (invalidation economics, EXEC at
  `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md`); ISSUE-212
  (abstraction pain, EXEC at
  `ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md`) — each ran a
  pressure-deferred verdict for its axis. ISSUE-217.attempt1 is this
  dispatch's prior failed attempt at the async-orchestration axis; its
  control verdict at
  `.ai/dispatch-ISSUE-217.attempt1/codex.control.round2.json:1`
  declared `ASYNC_ORCHESTRATION_PRESSURE_VERDICT: still_untriggered`.

**Doctrine vs. current evidence distinction**: every cavity citation
above is either (a) eventual-feature description (PLAN §7, §10.1), (b)
doctrinal Phase 9 framing (IMPLEMENTATION.md §9), or (c) explicit
"no-pressure" rollup (Status.md L9 / L13 / L373, HANDOFF.md items 8-9,
change.md L373). None is current runtime/editor/asset/script consumer
pressure.

## Per-Cavity Classification Table

| Cavity | Current owner path | Current public surface | Current external consumers | Pressure classification | Evidence |
|---|---|---|---|---|---|
| `job-system` | `kernel/job-system/` | `Job` + `JobId` + `JobKind` (`#[non_exhaustive]` 1 variant `Placeholder`) + `JobPriority` (`#[non_exhaustive]` 4-tier `Critical`/`High`/`Normal`/`Background`) + `JobScheduler` (`BTreeMap<(JobPriority, u64), Job>`) | **None.** No external production / external test consumer. Doc-comment cross-references only: `kernel/asset-view/src/id.rs:3` + `crates/cad-core/src/topology/face_id/mod.rs:57` (both name `JobId` in `//!`-doc shape-mirroring prose). | **pressure_deferred** | `kernel/job-system/Cargo.toml:2` (`publish=false`, no consumer Cargo `[dependencies]` declares this crate); `kernel/job-system/src/lib.rs:10-31` (NON-GOALS list: "No work-stealing thread pool", "No `tokio` / `futures` / async runtime integration", "No actual work execution"); `kernel/job-system/src/scheduler.rs:30-34` ("The scheduler owns no thread pool, no executor, no completion mechanism"); `git grep` matrix below (including standalone-`Job` row). |
| `io-scheduler` | `kernel/io-scheduler/` | `Priority` (`#[non_exhaustive]` 4-tier `InFrustumNear`/`InFrustumFar`/`OutOfFrustumNear`/`OutOfFrustumFar`) + `IoRequest` + `IoRequestId` + `IoRequestKind` (`#[non_exhaustive]` 1 variant `Placeholder`) + `IoScheduler` (`BTreeMap<(Priority, u64), IoRequest>`) | **None.** No external production / external test consumer. Doc-comment cross-references only: `crates/cad-core/src/topology/face_id/mod.rs:57`, `crates/gfx/src/frame_graph/mod.rs:14`, `crates/gfx/src/frame_graph/resource.rs:3`, `crates/gfx/src/pso_cache.rs:12`, `kernel/asset-view/src/id.rs:3` (all `//!` / `///` shape-mirroring prose naming `IoRequestId`); `kernel/asset-streaming/src/lib.rs:41` + `kernel/asset-streaming/src/state.rs:19` reference `kernel/io-scheduler::Priority` in `//!` prose for reciprocal NON-GOALS framing. | **pressure_deferred** | `kernel/io-scheduler/Cargo.toml:2` (`publish=false`, no consumer Cargo declares this crate); `kernel/io-scheduler/src/lib.rs:13-31` (NON-GOALS list including "No actual IO driver dispatch ... driver crates land in dedicated future dispatches"); `kernel/io-scheduler/src/scheduler.rs:30-34` ("The scheduler owns no IO driver, no executor, no completion mechanism"); `git grep` matrix below (including standalone-`Priority` row). |
| `asset-streaming` | `kernel/asset-streaming/` | `ResidencyId` + `RecordKind` (`#[non_exhaustive]` 1 variant `Placeholder`) + `ResidencyRecord` + `ResidencyState` (`#[non_exhaustive]` 4-tier `NotResident`/`Loading`/`Resident`/`Unloading`) + `ResidencyTracker` (`BTreeMap<ResidencyId, ResidencyRecord>`) | **None.** All `Residency*` hits returned by `git grep` are self-contained within `kernel/asset-streaming/src/{lib,record,state,tracker}.rs`. No production / test / Cargo dependency consumer outside the crate. | **pressure_deferred** | `kernel/asset-streaming/Cargo.toml:2` (`publish=false`, no consumer Cargo declares this crate); `kernel/asset-streaming/src/lib.rs:14-44` (NON-GOALS list including "No residency algorithm", "No hysteresis", "No predictive prefetch", "No GPU upload", "No actual I/O", "No `kernel/asset` integration"); `kernel/asset-streaming/src/tracker.rs:42-45` ("The tracker owns no buffers, no IO state, no job pool, no scheduling mechanism"); `git grep` matrix below. |
| `asset-view` | `kernel/asset-view/` | `AssetViewId` (opaque 16-byte, `Display` hex prefix) + `ViewKind` (`#[non_exhaustive]` 1 variant `Placeholder`) + `ViewDescriptor` + `AssetViewRegistry` (`BTreeMap<AssetViewId, ViewDescriptor>`) | **None.** All `AssetView*` / `ViewDescriptor` / `ViewKind` hits returned by `git grep` are self-contained within `kernel/asset-view/src/{lib,descriptor,id,registry}.rs`, plus one doc-comment cross-reference at `crates/cad-core/src/topology/face_id/mod.rs:56` ("Mirrors `kernel/asset-view::AssetViewId` /..."). No production / test / Cargo dependency consumer outside the crate. | **pressure_deferred** | `kernel/asset-view/Cargo.toml:2` (`publish=false`, no consumer Cargo declares this crate); `kernel/asset-view/src/lib.rs:14-38` (NON-GOALS list: "No WASM linear-memory mapping", "No `unsafe` zero-copy slice exposure", "No buffer / allocation ownership", "No `kernel/asset` integration"); `kernel/asset-view/src/registry.rs:33-39` ("The registry owns no buffers, no GPU resources, no IO state"); `git grep` matrix below. |
| `shared` | `kernel/shared/` | **None.** Crate is empty by design — admission-gated. `kernel/shared/src/lib.rs:1-111` is doc-only (no `pub` items). | **None.** Crate has no public surface to import; no Cargo consumer outside the workspace root declaration. | **pressure_deferred** | `kernel/shared/Cargo.toml:13` (no `[dependencies]` body content; `publish=false`); `kernel/shared/src/lib.rs:24-47` (three-part admission gate); `kernel/shared/src/lib.rs:101-111` ("Empty is the substrate's success state"). Admission gate has not fired — no observed duplication across two or more implemented kernel crates. |

## Cavity Surface Inspection (line-cited current state)

(Preserved from the prior EXEC; every cited line range was re-read for
this correction round. No tracked source changed, so the line numbers
remain current.)

- **`kernel/job-system/Cargo.toml`** (lines 1-19): `publish = false`,
  workspace inheritance for version/edition, sole dependency is `serde`
  with `derive` feature, dev-dependency `serde_json`. No driver /
  executor / async runtime / `tokio` / `rayon` / `crossbeam` dependency.
- **`kernel/job-system/src/lib.rs`** (lines 1-80): crate doc states "v0
  ships only the vocabulary substrate (priority taxonomy + job carrier +
  queue ordering) without any execution mechanism" (line 8-9). NON-GOALS
  (lines 10-31). Public surface (lines 42-48). Single `#[cfg(test)] mod
  smoke` (lines 50-79).
- **`kernel/job-system/src/job.rs`** (lines 1-125): `JobId` 16-byte
  opaque handle. `JobKind` `#[non_exhaustive]` enum (lines 43-48) with
  one `Placeholder` variant. `Job` carrier struct (lines 55-63).
- **`kernel/job-system/src/priority.rs`** (lines 1-72): `JobPriority`
  `#[non_exhaustive]` 4-tier enum (lines 17-28).
- **`kernel/job-system/src/scheduler.rs`** (lines 1-222): `JobScheduler`
  is a `BTreeMap<(JobPriority, u64), Job>` (line 40) with monotonic
  `seq: u64` (line 44). Module doc lines 1-16 restate NON-GOALS.
- **`kernel/io-scheduler/Cargo.toml`** (lines 1-19): same shape — sole
  dep `serde`, dev-dep `serde_json`, `publish=false`.
- **`kernel/io-scheduler/src/lib.rs`** (lines 1-80): crate doc names
  the 4-tier streaming priority taxonomy (lines 8-9). NON-GOALS (lines
  14-31).
- **`kernel/io-scheduler/src/priority.rs`** (lines 1-71): `Priority`
  `#[non_exhaustive]` 4-tier (lines 17-27).
- **`kernel/io-scheduler/src/request.rs`** (lines 1-126): `IoRequestId`
  opaque 16-byte handle (lines 15-35); `IoRequestKind` 1-variant
  `Placeholder` (lines 44-49); `IoRequest` carrier (lines 56-64).
- **`kernel/io-scheduler/src/scheduler.rs`** (lines 1-227): `IoScheduler`
  is a `BTreeMap<(Priority, u64), IoRequest>` (line 40). Module doc
  lines 1-17 restate NON-GOALS.
- **`kernel/asset-streaming/Cargo.toml`** (lines 1-19): same shape.
- **`kernel/asset-streaming/src/lib.rs`** (lines 1-111): NON-GOALS at
  lines 14-44; cavity-pattern self-check at lines 57-67.
- **`kernel/asset-streaming/src/record.rs`** (lines 1-146):
  `ResidencyId` (lines 16-36); `RecordKind` 1-variant `Placeholder`
  (lines 45-50); `ResidencyRecord` carrier (lines 62-73).
- **`kernel/asset-streaming/src/state.rs`** (lines 1-75): `ResidencyState`
  `#[non_exhaustive]` 4-tier (lines 20-31); `Ord` reflects lifecycle, NOT
  scheduling priority (doc lines 17-19 cross-reference
  `kernel/io-scheduler::Priority`).
- **`kernel/asset-streaming/src/tracker.rs`** (lines 1-255):
  `ResidencyTracker` is a `BTreeMap<ResidencyId, ResidencyRecord>`
  (lines 47-52). Module doc lines 3-29 restate NON-GOALS.
- **`kernel/asset-view/Cargo.toml`** (lines 1-19): same shape.
- **`kernel/asset-view/src/lib.rs`** (lines 1-87): NON-GOALS at lines
  14-38.
- **`kernel/asset-view/src/descriptor.rs`** (lines 1-112): `ViewKind`
  1-variant `Placeholder` (lines 19-24); `ViewDescriptor` carrier (lines
  35-44).
- **`kernel/asset-view/src/id.rs`** (lines 1-92): `AssetViewId` opaque
  16-byte handle (lines 39-48 contain the `Display` impl).
- **`kernel/asset-view/src/registry.rs`** (lines 1-225):
  `AssetViewRegistry` is a `BTreeMap<AssetViewId, ViewDescriptor>`
  (lines 41-46). Module doc lines 3-22 restate NON-GOALS.
- **`kernel/shared/Cargo.toml`** (lines 1-15): `publish=false`, empty
  `[dependencies]` block (line 12-13).
- **`kernel/shared/src/lib.rs`** (lines 1-111): admission contract;
  lines 24-47 enumerate the three-part admission gate; lines 101-111:
  "Empty is the substrate's success state." No `pub` items exist.

## Runtime / Editor / Asset / Script / Loader Consumer-Pressure Inspection

Required paths (TASK lines 218-222 / 332-336):
`runtime/**`, `editor/rge-editor`, `crates/editor-shell`,
`crates/rge-scene-loader`, `crates/io-gltf`, `crates/io-image`,
`crates/asset-store`, `crates/asset-pipeline`, `kernel/asset`,
`crates/script-host`, `crates/runtime-wasmtime`,
`crates/runtime-wasmtime-engine`.

Read-only command (run on Cargo manifests across the required consumer
roots):

```powershell
foreach ($c in @(
  'rge-kernel-job-system','rge-kernel-io-scheduler',
  'rge-kernel-asset-streaming','rge-kernel-asset-view',
  'rge-kernel-shared')) {
  git grep -n -e $c -- 'runtime/**/Cargo.toml' 'editor/**/Cargo.toml' `
    'crates/**/Cargo.toml' 'kernel/asset/Cargo.toml'
}
```

Observed result: **0 hits across every cavity crate name in every
required consumer manifest path.** No `runtime/runtime-desktop`,
`runtime/runtime-headless`, `runtime/runtime-mobile`,
`runtime/runtime-web`, `editor/rge-editor`, `crates/editor-shell`,
`crates/rge-scene-loader`, `crates/io-gltf`, `crates/io-image`,
`crates/asset-store`, `crates/asset-pipeline`, `kernel/asset`,
`crates/script-host`, `crates/runtime-wasmtime`, or
`crates/runtime-wasmtime-engine` declares a Cargo `[dependencies]` /
`[dev-dependencies]` / `[build-dependencies]` entry on any of the five
cavity crates.

Corresponding read-only Rust import search (using the `rge_kernel_*`
underscored module names that an external `use` would carry — see
identifier matrix below): also **0 hits across every required consumer
path**, confirming no `use rge_kernel_{job_system,io_scheduler,
asset_streaming,asset_view,shared}::...` import exists outside the
cavity crates themselves.

Standalone `Job` / `Priority` consumer search is documented as part of
the Identifier-Search Matrix below (per CORRECTION packet item 2,
CORRECT lines 45-53), with the full standalone matrix backed by the
expanded `Job/Priority Standalone Search` section that follows.

## Identifier-Search Matrix (read-only)

All searches run with the read-only command pattern
`git grep -n -w -e <ID> -- '*.rs' ':!OLD/**' ':!.ai/**' ':!.claude/**'
':!ai_handoffs/**'` (or, for Cargo searches, the `'**/Cargo.toml'`
glob). Exclusions cover: OLD historical archive, ignored dispatch
scratch dirs, `.claude/worktrees/**` sandbox copies, and handoff packet
prose (which would otherwise inflate every count with prior-EXEC text).

### Cargo metadata (cavity crate names)

| Identifier | Scope | Result | Classification |
|---|---|---|---|
| `rge-kernel-job-system` | `'**/Cargo.toml' Cargo.toml` | Workspace root `Cargo.toml:233` (`[workspace.dependencies]` `path = "kernel/job-system"`); `kernel/job-system/Cargo.toml:2` (self-manifest `name = "rge-kernel-job-system"`). | Cargo metadata only — workspace declaration + self-manifest, no consumer-crate `[dependencies]` entry. |
| `rge-kernel-io-scheduler` | same | `Cargo.toml:232`; `kernel/io-scheduler/Cargo.toml:2`. | Cargo metadata only — same shape. |
| `rge-kernel-asset-streaming` | same | `Cargo.toml:231`; `kernel/asset-streaming/Cargo.toml:2`. | Cargo metadata only — same shape. |
| `rge-kernel-asset-view` | same | `Cargo.toml:230`; `kernel/asset-view/Cargo.toml:2`. | Cargo metadata only — same shape. |
| `rge-kernel-shared` | same | `Cargo.toml:240`; `kernel/shared/Cargo.toml:2`. | Cargo metadata only — same shape. |
| (all five) | `grep -e ... Cargo.lock` | `Cargo.lock` package entries at lines 4624 / 4632 / 4690 / 4698 / 4726 (one per cavity). | Cargo metadata only — lockfile package presence, no reverse-dep populated because no consumer crate declares the dep. |

### Rust crate-import identifiers (underscored)

| Identifier | Scope | Result | Classification |
|---|---|---|---|
| `rge_kernel_job_system` | `*.rs` minus `OLD` / `.ai` / `.claude` / `ai_handoffs` | **No matches.** Command exited 1 (no hits). | Zero-use — no `use rge_kernel_job_system::...` import anywhere in tracked Rust source. |
| `rge_kernel_io_scheduler` | same | **No matches.** | Zero-use. |
| `rge_kernel_asset_streaming` | same | **No matches.** | Zero-use. |
| `rge_kernel_asset_view` | same | **No matches.** | Zero-use. |
| `rge_kernel_shared` | same | **No matches.** | Zero-use. |

### Job-system API identifiers

| Identifier | Scope | Result | Classification |
|---|---|---|---|
| `Job` (standalone, `-w`) | `*.rs` minus OLD/.ai/.claude/ai_handoffs (workspace-wide, owning + sibling cavities both visible) | **All 20 standalone `Job` hits inside `kernel/job-system/**`**: `kernel/job-system/src/job.rs` lines 1, 56, 89, 101, 107 (8 standalone-`Job` hits — `Job` carrier struct + tests); `kernel/job-system/src/lib.rs` lines 46, 59, 64 (4 hits — re-export + smoke test); `kernel/job-system/src/priority.rs` line 1 (1 hit — module doc `//! Job priority — ...`); `kernel/job-system/src/scheduler.rs` lines 1, 20, 23, 58, 67, 87, 107, 108 (7 hits — `Job` carrier references in scheduler + tests). External non-owning `*.rs` standalone-`Job` hits: **0**. See full breakdown in **Job/Priority Standalone Search** section below for command + scope + classification with no rounding. | **Self-test / self-use only** — every standalone-`Job` `*.rs` hit lives inside `kernel/job-system/**`. **External production / external test consumer: 0.** Standalone-`Job` does NOT surface async-orchestration pressure. |
| `JobId` | `*.rs` minus OLD/.ai/.claude/ai_handoffs | Self-uses inside `kernel/job-system/src/job.rs` (16 hits, lines 16-104) + `kernel/job-system/src/lib.rs:46`, `:60`, `:65`. Doc-comment cross-references: `kernel/asset-view/src/id.rs:3`, `crates/cad-core/src/topology/face_id/mod.rs:57`. | Self-test / self-use + docs/comment-only. No external production / external test consumer. |
| `JobKind` | same | Self-uses inside `kernel/job-system/src/job.rs` (lines 45, 62, 92, 104, 117-123) + `kernel/job-system/src/lib.rs:46`, `:62`, `:67`. | Self-test / self-use only. |
| `JobPriority` | same | Self-uses inside `kernel/job-system/src/{lib,job,priority,scheduler}.rs` (lines 47, 60-75 in lib; 60-95 in job; 19, 35-69 in priority; 21, 40, 107-211 in scheduler). | Self-test / self-use only. |
| `JobScheduler` | same | Self-uses inside `kernel/job-system/src/{lib,priority,scheduler}.rs:5` doc + lib `:48`, `:58` + scheduler `:36`, `:50`, `:117-217`. | Self-test / self-use only. |

### IO-scheduler API identifiers

| Identifier | Scope | Result | Classification |
|---|---|---|---|
| `Priority` (standalone, `-w`) | `*.rs` minus OLD/.ai/.claude/ai_handoffs (workspace-wide, owning + sibling cavities both visible) | **All 55 standalone `Priority` hits land in two locations**: owning crate `kernel/io-scheduler/**` (53 hits — `kernel/io-scheduler/src/lib.rs` lines 9, 46, 61, 66, 71, 75 = 5 doc/re-export/smoke-test hits; `kernel/io-scheduler/src/priority.rs` lines 18, 35-69 = 16 hits, enum definition + tests; `kernel/io-scheduler/src/request.rs` lines 5, 60, 61, 92, 96, 104 = 8 hits, `Priority` field references; `kernel/io-scheduler/src/scheduler.rs` lines 1, 21, 24, 28, 40, 107, 125, 127, 141-210 = 23 hits, scheduler + tests). Sibling cavity `kernel/asset-streaming/**` (2 hits, both `//!` / `///` doc-comment cross-references): `kernel/asset-streaming/src/lib.rs:41` (NON-GOALS prose: "embedded `Priority`; if a future variant needs streaming priority"); `kernel/asset-streaming/src/state.rs:19` (`/// `kernel/io-scheduler::Priority``). External non-owning, non-doc-comment `*.rs` standalone-`Priority` hits: **0**. See full breakdown in **Job/Priority Standalone Search** section below. | **Self-test / self-use (53 hits)** + **docs/comment-only (2 hits)** — every standalone-`Priority` `*.rs` hit lives inside `kernel/io-scheduler/**` (owning) or is a `//!` / `///` doc-comment cross-link inside `kernel/asset-streaming/**`. **External production / external test consumer: 0.** Standalone-`Priority` does NOT surface async-orchestration pressure. |
| `IoRequest` | `*.rs` minus OLD/.ai/.claude/ai_handoffs | Self-uses inside `kernel/io-scheduler/src/{lib,request,scheduler}.rs` (lib `:10`, `:47`, `:59`, `:64`; request `:57`, `:90`, `:102`, `:108`; scheduler `:22`, `:40`, `:67`, `:87`, `:107`-`:108`). | Self-test / self-use only. |
| `IoRequestId` | same | Self-uses inside `kernel/io-scheduler/src/{lib,request,scheduler}.rs` (request `:16`, `:18`, `:19`, `:46`, `:59`, `:76`, `:82`, `:83`, `:89`, `:103`, `:107`+`:115`-`:119`). Doc-comment cross-references: `crates/cad-core/src/topology/face_id/mod.rs:57`, `crates/gfx/src/frame_graph/mod.rs:14`, `crates/gfx/src/frame_graph/resource.rs:3`, `crates/gfx/src/pso_cache.rs:12`, `kernel/asset-view/src/id.rs:3`. | Self-test / self-use + docs/comment-only. No external production / external test consumer. |
| `IoRequestKind` | same | Self-uses inside `kernel/io-scheduler/src/{lib,request,scheduler}.rs:11`, `:47`, `:62`, `:67`; request `:46`, `:63`, `:93`, `:105`, `:111`, `:118-124`; scheduler `:111`. | Self-test / self-use only. |
| `IoScheduler` | same | Self-uses inside `kernel/io-scheduler/src/{lib,scheduler}.rs:10`, `:48`, `:58`; scheduler `:36`, `:50`, `:117-223`. | Self-test / self-use only. |

### Asset-streaming API identifiers

| Identifier | Scope | Result | Classification |
|---|---|---|---|
| `ResidencyRecord` | `*.rs` minus OLD/.ai/.claude/ai_handoffs | Self-uses inside `kernel/asset-streaming/src/{lib,record,tracker}.rs` (lib `:73`, `:88`; record `:52`, `:63`, `:75`, `:78`, `:112`, `:121`, `:128`; tracker `:21`, `:32`, `:47`, `:51`, `:54`, `:64`-`:131`). | Self-test / self-use only. No external consumer. |
| `ResidencyTracker` | same | Self-uses inside `kernel/asset-streaming/src/{lib,tracker}.rs` (lib `:75`, `:85`; tracker `:21-:22`, `:47`, `:54`, `:130-251`). | Self-test / self-use only. |
| `ResidencyState` | same | Self-uses inside `kernel/asset-streaming/src/{lib,record,state,tracker}.rs` (lib `:50`, `:63`, `:74`, `:90`-`:108`; record `:5`, `:67`, `:78`, `:114-128`; state `:22`, `:39-73`; tracker `:33`, `:86-251`). | Self-test / self-use only. |

### Asset-view API identifiers

| Identifier | Scope | Result | Classification |
|---|---|---|---|
| `AssetViewId` | `*.rs` minus OLD/.ai/.claude/ai_handoffs | Self-uses inside `kernel/asset-view/src/{lib,descriptor,id,registry}.rs`. Doc-comment cross-reference: `crates/cad-core/src/topology/face_id/mod.rs:56`. | Self-test / self-use + docs/comment-only. No external production / external test consumer. |
| `ViewDescriptor` | same | Self-uses inside `kernel/asset-view/src/{lib,descriptor,registry}.rs`. | Self-test / self-use only. |
| `ViewKind` | same | Self-uses inside `kernel/asset-view/src/{lib,descriptor,registry}.rs`. | Self-test / self-use only. |
| `AssetViewRegistry` | same | Self-uses inside `kernel/asset-view/src/{lib,registry}.rs`. | Self-test / self-use only. |

### Shared API identifiers

| Identifier | Scope | Result | Classification |
|---|---|---|---|
| `rge_kernel_shared` (Rust import) | `*.rs` minus OLD/.ai/.claude/ai_handoffs | **No matches.** | Zero-use — and zero-use is correct, since the crate has no `pub` surface to import. |

The two standalone-`Job` and standalone-`Priority` matrix rows above are
required explicitly per CORRECTION packet item 2 (CORRECT lines 45-53)
and acceptance gate "the Identifier-Search Matrix contains clear
standalone `Job` and `Priority` coverage rather than leaving that
evidence only in a separate prose section." Each row contains the exact
command pattern, search scope, observed result (with per-file line
references), and classification, and explicitly points to the titled
`Job/Priority Standalone Search` section below for the full per-file
breakdown.

## Job/Priority Standalone Search (load-bearing prior-control correction)

This section is the load-bearing standalone-search artifact required by
the TASK packet's deliverables (TASK lines 156-161) and the prior
attempt's evidence gap recorded at
`.ai/dispatch-ISSUE-217.attempt1/codex.control.round2.json:1`. The
content is preserved from the prior EXEC and re-verified live against
the current tree.

### Command 1 — workspace-wide standalone Job/Priority

Exact read-only command (the TASK packet's preferred form at line 351):

```powershell
git grep -n -w -e Job -e Priority -- '*.rs' `
  ':!OLD/**' ':!.ai/**' ':!.claude/**' ':!ai_handoffs/**'
```

Scope: tracked `.rs` files, excluding the historical archive (`OLD/**`),
gitignored dispatch scratch (`.ai/**`), sandbox worktree copies
(`.claude/**`), and handoff packet prose (`ai_handoffs/**`). `-w`
restricts matches to standalone word occurrences (so `JobId`, `JobKind`,
`JobPriority`, `JobScheduler`, `IoRequest`, `IoRequestId`,
`IoRequestKind`, `IoScheduler` are **NOT** matched).

Observed result: **77 hits, every one inside the two owning cavity
crates or inside doc-comment prose of one sibling cavity**:

- `kernel/job-system/src/job.rs`: 8 standalone-`Job` hits (lines 1, 56,
  89, 101, 107) — the `Job` carrier struct + tests inside the owning
  crate.
- `kernel/job-system/src/lib.rs`: 4 standalone-`Job` hits (lines 46, 59,
  64) — re-export + smoke test inside the owning crate.
- `kernel/job-system/src/priority.rs`: 1 standalone-`Job` hit (line 1)
  — module doc `//! Job priority — ...` inside the owning crate.
- `kernel/job-system/src/scheduler.rs`: 7 standalone-`Job` hits (lines
  1, 20, 23, 58, 67, 87, 107, 108) — `Job` carrier references inside
  the owning crate's scheduler implementation and tests.
- `kernel/io-scheduler/src/lib.rs`: 5 standalone-`Priority` hits (lines
  9, 46, 61, 66, 71, 75) — `Priority` doc references, re-export, and
  smoke-test enum-variant uses inside the owning crate.
- `kernel/io-scheduler/src/priority.rs`: 16 standalone-`Priority` hits
  (lines 18, 35-69) — the `Priority` enum definition and tests inside
  the owning crate.
- `kernel/io-scheduler/src/request.rs`: 8 standalone-`Priority` hits
  (lines 5, 60, 61, 92, 96, 104) — `Priority` field references inside
  the owning crate.
- `kernel/io-scheduler/src/scheduler.rs`: 23 standalone-`Priority` hits
  (lines 1, 21, 24, 28, 40, 107, 125, 127, 141-210) — `Priority`
  references inside the owning crate's scheduler implementation and
  tests.
- `kernel/asset-streaming/src/lib.rs`: 1 standalone-`Priority` hit
  (line 41) — `//!` cross-reference: "embedded `Priority`; if a future
  variant needs streaming priority"; **doc-comment-only**, not a
  code-path reference.
- `kernel/asset-streaming/src/state.rs`: 1 standalone-`Priority` hit
  (line 19) — `///` cross-reference: "`kernel/io-scheduler::Priority`";
  **doc-comment-only**, reciprocal NON-GOALS cross-link.

Classification:
- **Self-test / self-use (75 hits)**: every hit inside
  `kernel/job-system/**` (20 hits) and `kernel/io-scheduler/**`
  (55 hits) is the owning cavity's own code or its own tests.
- **Docs/comment-only (2 hits)**: the two `kernel/asset-streaming/**`
  hits are both `//!` / `///` prose cross-referencing the sibling
  cavity to justify NON-GOALS framing.
- **External production consumer (0 hits)**.
- **External test consumer (0 hits)**.
- **Cargo metadata only (n/a — this search is over `.rs` files)**.
- **Unrelated lexical collision (0 hits)** — the `-w` flag prevents
  match-on-substring; bare `Job` / `Priority` collisions inside other
  identifiers like `JobId` / `JobPriority` / `IoRequestId` are excluded
  by construction.

### Command 2 — external-consumer variant (excluding owning cavities)

Exact read-only command (TASK lines 353-354):

```powershell
git grep -n -w -e Job -e Priority -- '*.rs' `
  ':!kernel/job-system/**' ':!kernel/io-scheduler/**' `
  ':!OLD/**' ':!.ai/**' ':!.claude/**' ':!ai_handoffs/**'
```

Observed result: **exactly 2 hits, both doc-comment-only inside the
sibling asset-streaming cavity**:

- `kernel/asset-streaming/src/lib.rs:41` — `//!   embedded
  `Priority`; if a future variant needs streaming priority` (NON-GOALS
  prose cross-referencing the sibling cavity).
- `kernel/asset-streaming/src/state.rs:19` — `/// `kernel/io-scheduler
  ::Priority`.` (doc cross-link emphasising that `ResidencyState` `Ord`
  is lifecycle, NOT load priority).

Classification: **docs/comment-only** for both — neither is a runtime,
editor, asset, script, or loader code-path reference; neither imports,
names, or otherwise exercises the standalone `Job` or `Priority` types.
**External (non-cavity) production/test consumer count: 0.**

### Standalone-search verdict

The standalone `Job` and `Priority` identifier search confirms the
finding from the matrix above: **no current runtime / editor / asset /
script / loader / production-test consumer uses the standalone `Job` or
`Priority` types**. The only external (non-owning-cavity) references are
two doc-comment cross-links inside the sibling `kernel/asset-streaming`
cavity, both flagged by the asset-streaming crate doc as deliberate
NON-GOALS framing — not consumer pressure. The prior attempt's verdict
(`still_untriggered` in the older schema vocabulary) is preserved as
`pressure_deferred` in the new TASK packet's schema; the standalone
evidence does not change it.

## Candidate Comparison (why this follow-up is the smallest safe step)

The TASK packet (lines 176-178) requires a candidate-comparison section.
Five candidates were considered:

1. **Fill `kernel/job-system` with a work-stealing thread pool.**
   PLAN §10.1 names this as the eventual feature, but consumer pressure
   is absent (matrix above: zero external imports; zero Cargo deps;
   standalone-`Job` external search returns zero production/test hits;
   runtime/editor/script paths declare no Cargo dep on
   `rge-kernel-job-system`). Filling would violate the TASK packet's
   "Do not recommend filling a kernel cavity merely because it exists"
   clause (TASK lines 284-286). Larger than `pressure_deferred` without
   any forcing function.
2. **Wire `kernel/io-scheduler` to a real IO driver.** Same shape —
   `kernel/io-scheduler/src/lib.rs:27-29` declares "No actual IO driver
   dispatch (filesystem / network / disk) — driver crates land in
   dedicated future dispatches." Zero current consumers
   (`crates/io-gltf`, `crates/io-image`, `crates/asset-store`,
   `crates/asset-pipeline`, `kernel/asset` all decline to depend on it).
3. **Implement `kernel/asset-streaming` residency algorithm + 1s
   hysteresis + predictive prefetch.** PLAN §10.1 frames this as the
   eventual feature; cavity surface explicitly enumerates eight related
   NON-GOALS at `kernel/asset-streaming/src/lib.rs:14-44`. No current
   `crates/asset-store` / `crates/asset-pipeline` / `runtime/**` /
   `editor/**` consumer needs residency decisions today.
4. **Add zero-copy WASM linear-memory mapping to `kernel/asset-view`.**
   PLAN §1.6.5 / §10.1 names the feature, but
   `kernel/asset-view/src/lib.rs:18-23` lists "No WASM linear-memory
   mapping" and "No `unsafe` zero-copy slice exposure" as NON-GOALS.
   Tier-2 / runtime consumers (`crates/runtime-wasmtime`,
   `crates/runtime-wasmtime-engine`, `crates/script-host`) declare no
   dep.
5. **Admit an item to `kernel/shared`.** Admission gate
   (`kernel/shared/src/lib.rs:24-47`) requires observed duplication
   across two or more implemented kernel crates plus ADR-level
   justification. The audit found no such duplication.

Every candidate is **larger than `pressure_deferred`** and is **not
urgent** (no consumer is currently blocked). None is structurally
smaller; the smallest safe step is to not dispatch any cavity-fill work
and instead defer until current consumer pressure surfaces. A
`NEEDS_HUMAN` verdict would be appropriate only if the next step
depended on an unresolved architecture decision; per TASK lines 247-248
that condition is not met.

## Verification Results

Per CORRECTION packet item 3 (CORRECT lines 56-64) and `Re-Verification
Gates` (CORRECT lines 84-102):

- `Get-Content` (read with line numbers) on `AI_DISPATCH_AUTOMATION.md`
  → readable; live `file:line` citations captured (see
  **Live Dispatch / Protocol Citations** above:
  AI_DISPATCH_AUTOMATION.md:42, 44-47, 49-51, 69-74, 79-83, 186-194,
  222-225, 486-513, 566-573, 139, 961-965).
- `Get-Content` (read with line numbers) on
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md` → readable; live `file:line`
  citations captured (see **Live Dispatch / Protocol Citations** above:
  ai_handoffs/AI_HANDOFF_PROTOCOL.md:1-12, 50-66, 141, 147-166,
  358-369).
- Identifier-Search Matrix above explicitly contains standalone-`Job`
  and standalone-`Priority` rows with exact command, search scope,
  observed result, and classification (per CORRECT lines 89-92).
- `git status --short --branch --untracked-files=all` →
  on `ai-dispatch/ISSUE-217`; only six untracked
  dispatch-owned ISSUE-217 handoff artifacts present (this dispatch's
  pre-existing TASK packet + sidecar, the prior EXEC packet + sidecar,
  the CORRECT packet + sidecar). After this finalize: also the new EXEC
  packet + sidecar. **No tracked source / doc / schema / script / prior
  packet edits.** (Per CORRECT lines 93-95.)
- `git diff` → empty output. (Per CORRECT line 96.)
- `git diff --name-only` → empty output. (Per CORRECT line 97.)
- `git diff --check` → exit 0; no whitespace errors, no tracked diff
  introduced. (Per CORRECT line 98.)
- Read of `.ai/dispatch-ISSUE-217/verification.round0.log` confirms the
  canonical orchestrator verification gate: the log ends with
  `VERIFY OK: all 7 verification step(s) passed.` at line 10797. The
  seven `--- ok:` step markers cover: `cargo +nightly fmt --check` (line
  6); architecture lints (line 25); architecture-lint test suite (line
  219); `cargo deny check` (line 3795); `cargo test --workspace
  --all-targets` (line 9580); `cargo test --workspace --doc` (line
  10783); `cargo bench -p rge-script-bench --no-run` (line 10795). (Per
  CORRECT lines 99-102.)
- Live re-read of every cavity file in the Cavity Surface Inspection
  section above confirms the line ranges quoted (no tracked source
  changed since the prior EXEC, so the cited lines are still current).

No benchmarks, profilers, full repository verification re-run,
autonomous queue runs, nested dispatches, GitHub mutations, branch
operations, commits, pushes, or scheduler commands were run by this
correction round, in accordance with TASK lines 252-260 and CORRECT
lines 27-31.

## Decision Block

ASYNC_ORCHESTRATION_VERDICT: pressure_deferred

SMALLEST_SAFE_FOLLOW_UP: Defer all five Phase 9 async-orchestration
cavity follow-ups; no new dispatch warranted. Re-audit if any current
runtime / editor / asset / script / loader path adds a Cargo
`[dependencies]` entry on `rge-kernel-job-system`,
`rge-kernel-io-scheduler`, `rge-kernel-asset-streaming`,
`rge-kernel-asset-view`, or `rge-kernel-shared`, OR imports any
non-doc-comment use of `JobId` / `JobKind` / `JobPriority` /
`JobScheduler` / `Priority` / `IoRequest` / `IoRequestId` /
`IoRequestKind` / `IoScheduler` / `ResidencyRecord` / `ResidencyTracker`
/ `ResidencyState` / `AssetViewId` / `ViewDescriptor` / `ViewKind` /
`AssetViewRegistry` from any external crate. This follow-up is
report-only and does not touch production Rust, tests, scripts, Cargo
metadata, workflows, schemas, scheduler configuration, or automation
behavior; the `PublishMode branch only unless later authorized by a
human` clause does not apply. The `pressure_deferred` semantics are
preserved unchanged from the prior EXEC per CORRECTION packet item 3
(CORRECT lines 56-64) because no refreshed evidence in this correction
round changes the verdict.

## Deviations from Task Packet

None — execution stayed strictly within both the TASK packet scope and
the CORRECTION packet's report-correction-only boundary. The audit is
read-only; no `MAY edit` surface was used; every `MUST NOT edit` clause
is preserved; deliverables list is satisfied row by row; the correction
items 1, 2, and 3 are each addressed in dedicated sections above; the
re-verification gates are all green.

## Open Questions for Reviewer

None. The dispatch-state interpretation is anchored to live `file:line`
citations from `AI_DISPATCH_AUTOMATION.md` and
`ai_handoffs/AI_HANDOFF_PROTOCOL.md` before any conclusion. The
Identifier-Search Matrix contains explicit standalone-`Job` and
standalone-`Priority` rows with exact command, scope, observed result,
and classification. The audit-only state, `pressure_deferred` verdict,
and verification reporting are preserved as required.

## Worktree State

- Tracked files: clean (no tracked diff introduced by this dispatch or
  by this correction round).
- Untracked items at finalize time:
  `ai_handoffs/ISSUE-217_TASK_2026-05-26_23-59-35+0300.md` + `.meta.json`
  sidecar (pre-existing TASK from Planner);
  `ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-12-29+0300.md` +
  `.meta.json` sidecar (prior EXEC, preserved unedited);
  `ai_handoffs/ISSUE-217_CORRECT_2026-05-27_00-22-44+0300.md` +
  `.meta.json` sidecar (CORRECTION packet from Planner);
  `ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-25-31+0300.md` (this file)
  + `.meta.json` sidecar (after `new-handoff.ps1 -Finalize`).
- Branch: `ai-dispatch/ISSUE-217`.
- Last commit: `b3bfb76` `ai-dispatch ISSUE-216: Audit Phase 9
  reflection-scale pressure`.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-217
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
