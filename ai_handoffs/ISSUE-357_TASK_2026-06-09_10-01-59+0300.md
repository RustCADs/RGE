# Task Packet

DISPATCH_ID: ISSUE-357
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-09_10-01-59+0300
RELATED_FILES:
- ai_handoffs/ISSUE-357_TASK_2026-06-09_10-01-59+0300.md
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- crates/editor-egui-host/src/**
- crates/editor-shell/src/**
- crates/editor-ui/src/**
- crates/editor-actions/src/**
- editor/rge-editor/src/**
- crates/cad-core/src/**
- crates/cad-projection/src/**
- kernel/**
- runtime/**
STATUS: OPEN

## Goal

Run the post-palette Phase 9 editor-usability source/docs audit after task 107
and update the task brief with exactly one bounded implementation follow-up as
task 109, or record `NEEDS_HUMAN` if current source does not justify a safe
autonomous implementation task. This is a selection/audit dispatch only:
read/search current editor, plugin, CAD, runtime, and status surfaces; do not
implement the selected task.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`

### MUST NOT edit
- Rust source or tests under `crates/**`, `kernel/**`, `runtime/**`, or `editor/**`.
- Cargo manifests or lockfiles: `Cargo.toml`, `Cargo.lock`, `crates/**/Cargo.toml`, `kernel/**/Cargo.toml`, `runtime/**/Cargo.toml`, `editor/**/Cargo.toml`.
- GitHub workflows under `.github/workflows/**`.
- Dispatch automation, guard, queue, scheduler, verification, watcher, or packet tooling scripts, including `Invoke-AiDispatchLoop.ps1`, `Invoke-AiDispatchQueue.ps1`, `Invoke-AiDispatchAuto.ps1`, `Register-AiDispatchSchedule.ps1`, `Get-AiDispatchHealth.ps1`, `Watch-AiDispatch.ps1`, `Wait-GitHubActions.ps1`, `.ai/dispatch.verify.ps1`, and `new-handoff.ps1`.
- Schemas, architecture-lint rules/config, ADR files, packet templates, or governance docs, including `.ai/*.schema.json`, `tools/architecture-lints/**`, `docs/adr/**`, `ai_handoffs/templates/**`, `AI_DISPATCH_AUTOMATION.md`, and `ai_handoffs/AI_HANDOFF_PROTOCOL.md`.
- Plugin runtime/discovery/loading implementation code, including `crates/plugin-discovery/**`, `crates/runtime-wasmtime/**`, `crates/runtime-wasmtime-engine/**`, and any editor integration that would wire real plugin discovery/loading/runtime execution.
- Any existing handoff packet, including `ai_handoffs/ISSUE-357_TASK_2026-06-09_10-01-59+0300.md`.
- Do not rewrite packet `.meta.json` sidecars. The TASK sidecar is orchestration-finalization output, not executor work product.
- Existing handoff, closeout, review, execution, correction, queue-log, or run-dir artifacts for any dispatch other than ISSUE-357.

### MAY add new files
- `ai_handoffs/ISSUE-357_EXEC_*.md`
- `ai_handoffs/ISSUE-357_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-357_CORRECT_*.md`
- `ai_handoffs/ISSUE-357_CORRECT_*.meta.json`
- `ai_dispatch_logs/log_ISSUE-357*.md`
- `.ai/dispatch-ISSUE-357/**`

Orchestration finalization may create
`ai_handoffs/ISSUE-357_TASK_2026-06-09_10-01-59+0300.meta.json` from this
completed TASK before executor work starts. That sidecar is finalizer output,
not executor edit/add scope.

### MUST NOT add new files
- Any new Rust source or test file under `crates/**`, `kernel/**`, `runtime/**`, or `editor/**`.
- Any new Cargo manifest, dependency metadata, workflow, schema, ADR, architecture-lint file, dispatch script, packet template, plugin runtime/discovery/loading file, or generated artifact for another dispatch.
- Any new roadmap or doctrine document outside the five MAY-edit docs.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-357_EXEC_*.md
  - ai_handoffs/ISSUE-357_EXEC_*.meta.json
  - ai_handoffs/ISSUE-357_CORRECT_*.md
  - ai_handoffs/ISSUE-357_CORRECT_*.meta.json
  - ai_dispatch_logs/log_ISSUE-357*.md
  - .ai/dispatch-ISSUE-357/**
MUST_NOT_EDIT:
  - crates/**/*.rs
  - kernel/**/*.rs
  - runtime/**/*.rs
  - editor/**/*.rs
  - Cargo.toml
  - Cargo.lock
  - crates/**/Cargo.toml
  - kernel/**/Cargo.toml
  - runtime/**/Cargo.toml
  - editor/**/Cargo.toml
  - .github/workflows/**
  - Invoke-AiDispatchLoop.ps1
  - Invoke-AiDispatchQueue.ps1
  - Invoke-AiDispatchAuto.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Watch-AiDispatch.ps1
  - Wait-GitHubActions.ps1
  - .ai/dispatch.verify.ps1
  - new-handoff.ps1
  - .ai/*.schema.json
  - tools/architecture-lints/**
  - docs/adr/**
  - ai_handoffs/templates/**
  - AI_DISPATCH_AUTOMATION.md
  - ai_handoffs/AI_HANDOFF_PROTOCOL.md
  - ai_handoffs/ISSUE-357_TASK_2026-06-09_10-01-59+0300.md
  - crates/plugin-discovery/**
  - crates/runtime-wasmtime/**
  - crates/runtime-wasmtime-engine/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A docs/source-read audit recorded in the EXEC packet, including the exact
  searches and source/doc reads used to falsify stale claims before selection.
- Exactly one of these outcomes:
  - append one and only one new task entry numbered `109.` to `.ai/dispatch.tasks.md`;
  - or record `NEEDS_HUMAN` with concrete source evidence and append no task 109.
- If task 109 is appended, it must be a single bounded Phase 9
  editor-usability implementation follow-up with:
  - a precise MAY-edit and MUST-NOT-edit envelope;
  - falsifying searches for every current-state or negative premise;
  - required behavior;
  - done criteria;
  - verification gates;
  - halt conditions;
  - explicit non-goals preventing implementation drift.
- Status bookkeeping in `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and
  `change.md` that records the audit outcome and the selected task 109 or
  `NEEDS_HUMAN`.
- An EXEC packet for ISSUE-357 describing the audit, candidate comparison,
  selected/deferred outcome, verification, deviations, and open questions.

## Acceptance Criteria

- The executor reads/searches current docs and source before choosing. Do not
  infer from stale backlog prose, task headings, or `ai_handoffs/` filenames.
- The candidate comparison explicitly covers all of these classes:
  host-shell routing/FIFO replacement, extension/plugin command execution,
  keybinding or shortcut-conflict policy, unsaved close/quit UX, OS or typed
  clipboard behavior, CAD graph/projection mutation with undo/dirty
  integration, camera/navigation UI, and any smaller source-backed
  editor-usability candidate found during the audit.
- `.ai/dispatch.tasks.md` ends with exactly one active outcome:
  - task 108 marked done or completed with a result and exactly one new task
    `109.` appended;
  - or task 108 marked with `NEEDS_HUMAN` and no task `109.` appended.
- A selected task 109 is implementation-sized, not a project. It must fit a
  narrow file surface and must not require Cargo changes, workflows, schemas,
  ADRs, dispatch automation edits, real plugin runtime/discovery/loading,
  command-route replacement, OS clipboard integration, authoritative CAD
  mutation, or undo/dirty policy unless the audit proves that exact slice is
  already safe and bounded.
- If no candidate meets that bar, the executor records `NEEDS_HUMAN` with the
  concrete policy or architecture decision needed and does not manufacture a
  task.
- No Rust source/tests, Cargo files, workflows, dispatch automation scripts,
  schemas, ADRs, packet templates, architecture-lint files, plugin
  runtime/discovery/loading code, or unrelated handoff/log artifacts are edited.
- The executor does not modify this TASK packet after approval; the TASK
  `.meta.json` sidecar, if present, is created only by orchestration
  finalization before executor work starts.
- `git diff --name-only` contains only the MAY-edit docs plus ISSUE-357
  generated artifacts.
- `git diff --check` exits 0.

## Current-State Claims: Falsification Required

Every negative, unchanged, or already-complete claim in this task and in the
executor's selected task 109 must be backed by an inverse source/doc search.
The executor MUST re-run and record the relevant commands and observed results
in the EXEC packet before relying on them.

- Claim: task 107 is complete, task 108 is the only currently armed
  post-palette audit, and no numbered task `109.` has yet been selected.
  Falsifying search: `git grep -n -E "^107\\.|^108\\.|^109\\.|task 107|task 108|876c3ed|palette_pinned|NEEDS_HUMAN" -- .ai/dispatch.tasks.md Status.md HANDOFF.md plans/BASELINE.md change.md`
  -> Planner preflight observed docs stating task 107 landed as `876c3ed`,
  task 108 is open, and no current numbered task `109.` entry exists.

- Claim: command-palette fuzzy search, recent persistence, and pinned
  favorites are already complete enough that task 109 must not reselect another
  palette fuzzy/recent/pinned slice.
  Falsifying search: `git grep -n -E "palette_recent|palette_pinned|command_palette_recent_command_ids|command_palette_pinned_command_ids|filter_command_palette_entries_with_pinned_and_recents|toggle_command_palette_pinned_command|enqueue_command_palette_activation" -- crates/editor-egui-host/src Status.md HANDOFF.md plans/BASELINE.md .ai/dispatch.tasks.md`
  -> Planner preflight observed `palette_recent.rs`, `palette_pinned.rs`,
  host fields for recent/pinned ids, ordering helpers, toggle helpers, and
  focused tests.

- Claim: menu clicks, command-palette activations, and enabled keyboard
  accelerators still cross the host-shell boundary through
  `MenuCommandHandoff` and `EditorShell::route_menu_command`; replacing that
  route is broader than a docs-only selection task.
  Falsifying search: `git grep -n -E "MenuCommandHandoff|drain_and_route_menu_commands|route_menu_command|command_palette_window|enqueue_command_palette_activation|enabled_command_for_shortcut|default_editor_menu" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src editor/rge-editor/src`
  -> Planner preflight observed the host FIFO, the shell drain method, the
  shared `route_menu_command` sink, canonical menu registry, and
  enabled-command accelerator lookup.

- Claim: extension/plugin command activations currently stop at the task-102
  injected handler seam in the editor route; real plugin runtime, discovery,
  loading, or WASM execution is not wired into editor command execution.
  Falsifying search: `git grep -n -E "ExtensionCommandHandler|ExtensionCommandEvent|Command::Custom|Command::Plugin|PluginHost|PluginContext|plugin-discovery|runtime-wasmtime|rge_kernel_plugin_host" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src editor/rge-editor/src runtime crates`
  -> Planner preflight observed editor-shell extension handler events and
  plugin command projection, plus plugin-host/runtime/discovery surfaces in
  other crates; no source-backed reason to treat real editor plugin loading as
  already wired.

- Claim: shortcut conflict handling is already represented as registry data and
  host diagnostics, not as a fatal policy or keybinding-editor surface.
  Falsifying search: `git grep -n -E "Shortcut Conflicts|shortcut_conflicts|ShortcutConflict|conflict|enabled_command_for_shortcut|command_for_shortcut|KeyboardInput|Modifiers|Shortcut" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src crates/editor-actions/src editor/rge-editor/src`
  -> Planner preflight observed host conflict projection, a `Shortcut
  Conflicts` menu diagnostic, shortcut conflict tests, and registry comments
  leaving fatality to the host.

- Claim: close/quit commands currently do not prompt for unsaved changes,
  while dirty state is surfaced through save-status/window-title paths.
  Falsifying search: `git grep -n -E "prompt|Prompt|confirm|Confirm|unsaved|dirty|CloseRequested|handle_close_file_request|handle_quit_request|take_quit_request|Command::Close|Command::Quit|SaveStatusSnapshot" -- crates/editor-shell/src crates/editor-ui/src crates/editor-egui-host/src editor/rge-editor/src`
  -> Planner preflight observed comments that close and quit do not prompt for
  unsaved changes, plus dirty/save-status/window-title plumbing.

- Claim: current clipboard behavior is shell-local entity clipboard behavior,
  not OS clipboard integration and not authoritative typed CAD/projection clone
  behavior.
  Falsifying search: `git grep -n -E "OS clipboard|system clipboard|arboard|clipboard-win|copypasta|EntityClipboardItem|entity_clipboard|has_clipboard_entities|shell-local entity clipboard|authoritative CAD" -- crates/editor-shell/src crates/editor-ui/src crates/editor-egui-host/src editor/rge-editor/src Cargo.toml crates/**/Cargo.toml`
  -> Planner preflight observed `EntityClipboardItem`, `entity_clipboard`,
  `has_clipboard_entities`, and explicit comments that this is not OS clipboard
  or authoritative CAD/projection cloning.

- Claim: current Edit commands operate on wrapper-world selection/entities and
  do not yet provide authoritative CAD graph/projection mutation with undo/dirty
  integration.
  Falsifying search: `git grep -n -E "CadGraph|cad_graph|cad_world|CadProjection|OperatorGraph|Command::Cut|Command::Copy|Command::Paste|Command::Delete|Command::Duplicate|UndoStack|CommandBus|route_menu_command|authoritative CAD|face selection" -- crates/editor-shell/src crates/editor-actions/src crates/cad-core/src crates/cad-projection/src crates/editor-ui/src crates/editor-egui-host/src editor/rge-editor/src`
  -> Planner preflight observed CAD substrates and editor Edit command tests,
  with current Edit command comments describing wrapper-world and shell-local
  behavior rather than authoritative CAD mutation.

- Claim: camera reset and zoom commands already exist and are routed; broader
  camera/navigation UI must be justified as a smaller source-backed slice, not
  assumed from old roadmap gaps.
  Falsifying search: `git grep -n -E "Command::ResetCamera|Command::ZoomIn|Command::ZoomOut|reset_camera|zoom_in|zoom_out|Reset Camera|Zoom In|Zoom Out|has_camera_bounds|camera" -- crates/editor-shell/src crates/editor-ui/src crates/editor-egui-host/src editor/rge-editor/src`
  -> Planner preflight observed View menu camera commands, Home/PageUp/PageDown
  accelerators, `EditorShell::reset_camera`, zoom helpers, and routing tests.

## Constraints / Non-Goals

- Do not implement task 109 in this dispatch. The only code-facing work is
  reading/searching source and writing the selected task description or
  `NEEDS_HUMAN`.
- Do not edit Rust source/tests, Cargo metadata, workflows, schemas, ADRs,
  architecture lints, dispatch automation, scheduler configuration, packet
  templates, or plugin runtime/discovery/loading implementation code.
- Do not select a task by copying stale backlog phrasing. Current source wins
  over backlog prose.
- Do not select another command-palette fuzzy/recent/pinned persistence task
  unless the audit proves a new, distinct, non-overlapping palette slice exists.
- Do not choose host-shell FIFO replacement, real plugin runtime execution,
  keybinding policy/fatality, unsaved prompt policy, OS clipboard integration,
  authoritative CAD mutation, or undo/dirty integration unless the audit proves
  the chosen task is a narrow implementation slice with explicit source-backed
  safety and halt conditions.
- Do not add a batch of tasks. The result is exactly one task 109 or
  `NEEDS_HUMAN`.

## Verification Gates

The Executor MUST run and document the result of each of these in the
`EXECUTION_REPORT`:

- Required source/doc `git grep` searches listed in `Current-State Claims` and
  any additional searches needed to support the selected task 109.
- Focused source reads of the files surfaced by those searches, sufficient to
  compare the candidate classes instead of only counting matches.
- `git diff --name-only` -> expected only the five MAY-edit docs plus
  ISSUE-357 generated artifacts.
- `git diff --check` -> expected exit 0.
- No Cargo build/test/fmt gate is required for this audit-only selection
  dispatch because no Rust source, tests, manifests, or lockfiles may change.
  If any Rust/Cargo change appears necessary, halt instead of running code
  verification.

## Halt Conditions

The Executor MUST halt and request fresh guidance if any of the following occur:

- Selecting task 109 would require implementation edits during this dispatch.
- Current source shows all plausible next slices require human
  product/architecture policy before implementation.
- The selected follow-up would need Cargo/workflow/automation/schema/ADR edits,
  real plugin runtime/discovery/loading, command-route replacement, OS
  clipboard integration, authoritative CAD mutation, or undo/dirty policy, and
  the audit cannot prove the exact implementation slice is already narrow and
  safe.
- The audit cannot compare every required candidate class with current
  source/docs evidence.
- Any required falsifying search cannot be run or yields contradictory evidence
  that invalidates this packet's premise.
- `git diff --name-only` shows files outside the MAY-edit/generated-artifact
  envelope.
- `git diff --check` fails for a reason unrelated to the permitted docs edits.

## Planner Notes

Task 107 closed the command-palette pinned-favorites slice and full verify
passed before task 108 was armed. The remaining Phase 9/editor-usability
surface is now mostly policy-heavy or substrate-heavy. This packet deliberately
does not preselect task 109: the executor must redo the source/docs audit and
compare the candidate classes from current repository state. If the smallest
honest outcome is an arbitration question, `NEEDS_HUMAN` is the correct result.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-357
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
