# Correction Packet

DISPATCH_ID: ISSUE-230
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_14-59-36+0300
RELATED_FILES:
- Invoke-AiDispatchQueue.ps1
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-230_TASK_2026-05-27_14-16-42+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-230_EXEC_2026-05-27_14-38-51+0300.md`
- Latest Review Result: Codex control review JSON for round 0 (`.ai/dispatch-ISSUE-230/codex.control.round0.json`); orchestrator dispatches do not write `_REVIEW_` packets.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction - nothing more, nothing less.

1. **Require complete PR metadata before PR-mode publish success** - Codex control finding: PR mode can mark publish as successful after `gh pr create` even when neither stdout parsing nor `gh pr view` yields both PR number and URL. Required change: in `Invoke-AiDispatchQueue.ps1` around the PR-mode publish block currently near lines 1931-1959, do not set `$published = $true` unless the queue has both a PR number and a PR URL. A straightforward implementation is to always run `gh pr view` for the dispatch branch with `--json number,url` after `gh pr create` and require both fields from that result. If PR metadata lookup or parsing fails, treat it as a publish-pipeline failure: set `$publishFailed = $true`, set `$publishHardFailed = $true`, leave `$published = $false`, preserve the dispatch branch for human recovery, and do not report PR-mode success in the final issue comment. Acceptance: PR-mode success is possible only when the final issue comment includes both the PR number and URL; missing PR metadata produces the existing publish-failure path and never falls back to pushing `origin/main` or closing the source issue.

## Deferred Findings (NOT Approved for This Round)

None. The control review identified one required correction, and it is approved above.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this clarification for the existing PR-mode success criterion: successful PR creation is not considered published unless the queue has both the PR number and PR URL and includes both in the final issue comment.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Invoke-Pester -Path .\tools\dispatch-tests -CI"` - expected exit 0.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\.ai\dispatch.verify.ps1` - expected exit 0.
- `git diff --check` - expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet. In particular, halt if the correction appears to require live GitHub writes from tests, pushing `origin/main`, closing the source issue in PR mode, or editing files outside the Task Packet MAY-edit list.

## Planner Notes

The only approved correction is the PR metadata success gate. The prior implementation already routes branch-push and PR-create failures into the publish-pipeline failure taxonomy; this correction extends that same hard-failure treatment to missing or incomplete PR metadata so the final issue comment cannot claim PR success without the required PR number and URL.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-230
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
