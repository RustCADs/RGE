# Correction Packet

DISPATCH_ID: ISSUE-43
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_09-00-20+0300
RELATED_FILES:
- crates/cad-projection/tests/face_picking_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-43_TASK_2026-05-19_08-53-38+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-43_EXEC_2026-05-19_08-59-27+0300.md`
- Latest Review Report: none; this correction is driven by the canonical post-execution verification failure.
- Verification failure: `.ai/dispatch.verify.ps1` failed at step `[2] architecture lints`; `[split-exemption] FAIL` reported `crates\cad-projection\tests\face_picking_smoke.rs:1001: file is 1049 lines (>1000) without // SPLIT-EXEMPTION: annotation`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction and nothing more.

1. **Add split-exemption annotation for the enlarged smoke-test file** - Verification gate finding `[split-exemption] FAIL`. Required change: in `crates/cad-projection/tests/face_picking_smoke.rs`, add this exact top-of-file annotation before the existing `//!` module docs:

   ```rust
   // SPLIT-EXEMPTION: cohesive face-picking smoke aggregation with shared
   // picker fixtures and invariants; ISSUE-43 explicitly extends this existing
   // integration test file instead of adding or splitting test files.
   ```

   Do not split the file, add files, delete the new Transform picker smoke, weaken assertions, or touch any production code. Acceptance: `cargo run -q -p rge-tool-architecture-lints -- all` exits 0 and the canonical verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. No reviewer findings are deferred; the only blocking issue is the split-exemption lint failure above.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with one verification-specific clarification: because the accepted implementation pushed `crates/cad-projection/tests/face_picking_smoke.rs` over the 1000-line architecture-lint threshold, the file must now contain a justified `// SPLIT-EXEMPTION:` annotation and the full canonical dispatch verification must pass.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0, including `[split-exemption] PASS`.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet. Halt and request fresh Planner guidance if passing verification appears to require editing any file other than `crates/cad-projection/tests/face_picking_smoke.rs`, changing production code, adding or splitting test files, or weakening/removing the ISSUE-43 Transform picker coverage.

## Planner Notes

The implementation itself passed formatting and the focused `face_picking_smoke` test. The dispatch is blocked only because the additive smoke made the existing aggregation file exceed the architecture lint's 1000-line threshold without the required `// SPLIT-EXEMPTION:` annotation. The approved correction is therefore limited to that annotation and its verification.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-43
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
