# Task Packet

DISPATCH_ID: ISSUE-418
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-18_23-22-33+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- ai_handoffs/ISSUE-417_TASK_2026-06-18_11-14-23+0300.md
- ai_handoffs/ISSUE-417_EXEC_2026-06-18_11-54-27+0300.md
- crates/editor-actions/src/action.rs
- crates/editor-actions/src/bus.rs
- crates/editor-actions/src/lib.rs
- crates/editor-actions/src/undo_stack.rs
- crates/editor-actions/tests/test_actions.rs
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/mod.rs
- crates/editor-shell/src/lifecycle/tests.rs
STATUS: OPEN

## Goal

Perform the task 161 gated audit created by task 160 / ISSUE-417: audit the
post-bus-routed first CAD mutation boundary without changing source, tests,
docs, Cargo metadata, scripts, workflows, schemas, or packet templates. Confirm
from current source and tests that `rge-editor-actions` remains a generic
CommandBus/action-context layer, while `rge-editor-shell` owns the CAD
action/context and keeps the bus-routed CAD mutation bounded to the first
empty-scene cuboid add. End by recording exactly one human-decision marker and
an immediately following recommendation block in `.ai/dispatch.tasks.md`; do
not append task 162 and do not approve or implement the recommendation.

This dispatch implements GitHub issue #418, selected from task 161 in
`.ai/dispatch.tasks.md`.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md`
- `ai_handoffs/ISSUE-418_*.md`
- `ai_handoffs/ISSUE-418_*.meta.json`
- `ai_handoffs/claims/ISSUE-418_*.json`
- `ai_dispatch_logs/log_*.md`

The `.ai/dispatch.tasks.md` edit is limited to the task 161 audit conclusion:
exactly one `NEEDS_HUMAN_RECORDED:` marker with the actual local ISO execution
date, immediately followed by the required `Recommendation for human approval`
block. The generated handoff/audit/log paths are limited to current-dispatch
ISSUE-418 artifacts.

### MUST NOT edit
- `crates/**`
- `editor/**`
- `kernel/**`
- `runtime/**`
- `tools/**`
- `docs/**`
- `plans/**`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `AI_DISPATCH_AUTOMATION.md`
- `AI_DISPATCH_PARALLEL.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/**`
- `.ai/*.schema.json`
- `.ai/dispatch.verify.ps1`
- `Invoke-*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-*.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/templates/**`
- `ai_handoffs/ISSUE-417_*`
- `ai_handoffs/ISSUE-415_*`
- `ai_handoffs/ISSUE-414_*`
- Any handoff, sidecar, claim, or dispatch-log artifact not generated for
  current dispatch ISSUE-418.

### MAY add new files
- `ai_handoffs/ISSUE-418_*.md`
- `ai_handoffs/ISSUE-418_*.meta.json`
- `ai_handoffs/claims/ISSUE-418_*.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files
- No Rust source or test files, docs, ADRs, plans, status/history files, crates,
  Cargo metadata, workflows, scripts, schemas, architecture lints, packet
  templates, or task-162 / feature-task artifacts.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - ai_handoffs/ISSUE-418_*.md
  - ai_handoffs/ISSUE-418_*.meta.json
  - ai_handoffs/claims/ISSUE-418_*.json
  - ai_dispatch_logs/log_*.md
MUST_NOT_EDIT:
  - crates/**
  - editor/**
  - kernel/**
  - runtime/**
  - tools/**
  - docs/**
  - plans/**
  - Status.md
  - HANDOFF.md
  - change.md
  - AI_DISPATCH_AUTOMATION.md
  - AI_DISPATCH_PARALLEL.md
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/**
  - .ai/*.schema.json
  - .ai/dispatch.verify.ps1
  - Invoke-*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-*.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
  - ai_handoffs/ISSUE-417_*
  - ai_handoffs/ISSUE-415_*
  - ai_handoffs/ISSUE-414_*
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A source-grounded audit report in the EXEC packet covering the
  post-ISSUE-417 / task-160 CommandBus and CAD action/context boundary.
- A `.ai/dispatch.tasks.md` task 161 conclusion containing exactly one marker
  beginning `NEEDS_HUMAN_RECORDED: `, followed by the actual local ISO
  execution date, a dash, and a source-grounded reason.
- An immediately following `Recommendation for human approval` block covering:
  proposed next feature, exact edit surface, risks, verification, and why the
  proposal is the smallest coherent next step.
- Verification output proving no task 162 was appended, no source/docs/Cargo
  scope drift occurred, and only allowed files changed.

## Acceptance Criteria

- The audit cites source/tests, not inference, for the generic
  `rge-editor-actions` boundary: `ActionContext`, `ActionContextFamily`,
  `WorldActionContext`, `ActionView`, `ActionViewRef`, `Action`, `BusEntry`,
  `CommandBus`, and `UndoStack` remain generic. The crate must not depend on or
  import editor-shell, cad-core, cad-projection, B-Rep, render, or
  shell-specific types. If broad text searches surface the existing
  `CadCheckpoint` future-variant comment or audit-ledger "projection" wording,
  classify those explicitly rather than claiming zero broad CAD/projection text.
- The audit cites source/tests for the shell-owned CAD boundary:
  `EditorShellActionContext`, `ShellActionContext`, `CadMutationState`,
  `WorldOnlyShellAction`, `AddCadCuboidToEmptyScene`, and
  `EditorShell::add_cad_cuboid_to_empty_scene` live in editor-shell and route
  only the first empty-scene cuboid add through `command_bus.submit`.
- The audit confirms no UI/menu/shortcut, render-path, CAD-core,
  cad-projection, Cargo, save/load, persistence, camera, viewport, deletion,
  duplication, transform, parameter-editing, multi-root CAD, plugin, or OS
  clipboard scope was added by the boundary under review. Because lifecycle
  files already contain many older UI/render/save/menu references, this must be
  proven by local ISSUE-417 commit/file-surface evidence plus targeted reads of
  the CAD-add path, not by treating every existing lifecycle grep hit as new.
- The audit confirms undo/redo and dirty/save-mark semantics from source/tests:
  undo restores the pre-add checkpoint state, leaves no live spawned B-Rep
  entity and no stale render-mesh lookup for that entity, and redo re-adds
  coherent CAD graph/projection/world/tracked-entity state. Dirty/save-mark
  movement remains owned by the CommandBus.
- The `.ai/dispatch.tasks.md` edit records exactly one new
  `NEEDS_HUMAN_RECORDED:` marker for this audit with the actual local ISO
  execution date, immediately followed by a `Recommendation for human approval`
  block with the five required parts.
- No task 162, feature task, implementation approval, source edit, test edit,
  docs edit, Cargo edit, workflow edit, script edit, schema edit, template edit,
  or unrelated handoff/log edit is made.
- Queue/already-filed claims use only the dispatcher snapshot facts recorded in
  this TASK packet or an exact local issue-body artifact. The executor must not
  run `gh`, browser, network, GitHub API, or live remote lookup commands.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion in this packet is backed by an inverse
search or source read below. The Executor must re-run relevant searches after
the audit edit before making final negative claims in the EXEC packet.

- Claim: current `rge-editor-actions` source/tests remain generic and do not
  import, depend on, or type-reference editor-shell, cad-core, cad-projection,
  CAD graph/projection/world, B-Rep, render, or shell-specific concepts. Broad
  CAD/projection words are not zero: they must be classified.
  Falsifying search:
  `rg -n "CAD|Cad|cad|graph|projection|BRep|render|shell|editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection" crates/editor-actions/src crates/editor-actions/Cargo.toml crates/editor-actions/tests`
  -> observed matches only at
  `crates/editor-actions/tests/audit_ledger_projection.rs:1` for the test file's
  audit-ledger projection title, `crates/editor-actions/src/bus.rs:20` for the
  `CadCheckpoint` example in a `#[non_exhaustive]` future-variant comment, and
  `crates/editor-actions/src/bus.rs:192` for an audit-ledger projection comment.
  `crates/editor-actions/Cargo.toml` has no CAD/editor-shell/render dependency
  matches. No observed match is an editor-shell import, CAD crate dependency,
  CAD type reference, B-Rep reference, render reference, or shell-specific
  action/context reference.
- Claim: current `rge-editor-actions` exposes a generic CommandBus context
  contract rather than shell-owned CAD context.
  Source/read evidence: `crates/editor-actions/src/action.rs:52-83` defines the
  minimal `ActionContext`, `ActionContextFamily`, and default
  `WorldActionContext`; `action.rs:119-164` defines `ActionViewRef` and
  `Action<F: ActionContextFamily = WorldActionContext>`; `bus.rs:23-28` and
  `bus.rs:87-107` define `BusEntry<F>` and `CommandBus<F>` over the context
  family. Search:
  `rg -n "ActionContext|ActionContextFamily|WorldActionContext|ActionView|ActionViewRef|Box<dyn Action|BusEntry|CommandBus<|UndoStack<|new_for_context" crates/editor-actions/src crates/editor-actions/tests`
  -> observed matches are in `action.rs`, `bus.rs`, `undo_stack.rs`,
  `compound.rs`, `lib.rs`, and generic tests, with no shell CAD context type.
- Claim: the shell-owned CAD action/context boundary is in editor-shell and is
  bounded to the first empty-scene cuboid add.
  Source/read evidence: `crates/editor-shell/src/lifecycle/commands.rs:163-199`
  defines `EditorShellActionContext`, `ShellActionContext`, and
  `CadMutationState`; `commands.rs:203-227` wraps existing World-only actions
  with `WorldOnlyShellAction`; `commands.rs:416-473` defines
  `AddCadCuboidToEmptyScene` as `Action<EditorShellActionContext>`;
  `commands.rs:710-743` documents and implements the headless
  `add_cad_cuboid_to_empty_scene` guard, rejects populated CAD/render state,
  then submits `AddCadCuboidToEmptyScene`; `commands.rs:879-899` builds
  `ShellActionContext` and calls `command_bus.submit(action, &mut context)`.
  Search:
  `rg -n "EditorShellActionContext|ShellActionContext|CadMutationState|WorldOnlyShellAction|AddCadCuboidToEmptyScene|submit_shell_action|add_cad_cuboid_to_empty_scene|undo_command|redo_command|mark_saved_command|CommandBusView|UndoStackView|command_bus\.submit|submit_action\(" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> observed matches are the shell command/context definitions, the typed bus
  field in `lifecycle/mod.rs`, and lifecycle tests.
- Claim: the boundary under review did not add CAD-core, cad-projection,
  render-path, UI/menu/shortcut, save/load, Cargo, or other out-of-scope edit
  surfaces for ISSUE-417. This claim is about the local ISSUE-417 change
  surface, not about the absence of older lifecycle code.
  Local provenance: `git show --name-only --format="%H%n%s" HEAD` on this
  worktree reports commit `851a8962e4e84f404db5d70bc3d77eca2c2551d3`
  `ai-dispatch ISSUE-417: Route first CAD cuboid add through CommandBus`, with
  source changes only in `crates/editor-actions/src/action.rs`,
  `crates/editor-actions/src/bus.rs`, `crates/editor-actions/src/lib.rs`,
  `crates/editor-actions/src/undo_stack.rs`,
  `crates/editor-actions/tests/test_actions.rs`,
  `crates/editor-shell/src/lifecycle/commands.rs`,
  `crates/editor-shell/src/lifecycle/mod.rs`, and
  `crates/editor-shell/src/lifecycle/tests.rs`, plus task/handoff/log artifacts.
  The same file list appears in
  `ai_handoffs/ISSUE-417_EXEC_2026-06-18_11-54-27+0300.md:6-15`.
  Targeted noisy search:
  `rg -n "route_menu_command|EditorKeyCommand::|Shortcut|Command::|init_render_state|render_frame|with_render_mesh|save_source|handle_save|Cargo.toml|cad-core|cad-projection" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> observed many existing lifecycle UI/render/save/menu references, so the
  executor must classify them as pre-existing context unless a targeted source
  read shows the CAD-add path uses them.
- Claim: undo/redo and dirty/save-mark semantics for the bus-routed cuboid add
  are currently covered by source/tests.
  Source/read evidence: `commands.rs:583-609` reverts by restoring the captured
  `pre_add_head`, despawning the tracked B-Rep entity, ticking projection, and
  clearing `cad_entity`; `commands.rs:909-960` routes undo, redo, and save marks
  through the `CommandBus`. Test evidence:
  `crates/editor-shell/src/lifecycle/tests.rs:2449-2568`
  `bus_routed_cad_cuboid_add_undo_redo_keeps_scene_coherent_and_moves_dirty_mark`
  asserts one bus action after add, dirty state after add, clean state after
  `mark_saved_command`, undo cursor movement to 0, no tracked CAD entity, graph
  restored to `pre_add_head`, zero live B-Rep entities, zero projection render
  meshes, no stale `render_mesh_for(added.entity, cad_world)` lookup, redo
  cursor movement to 1, live tracked entity, one B-Rep entity, one projection
  render mesh, and no uploaded/prebuilt render meshes. Test evidence:
  `tests.rs:2571-2600`
  `bus_routed_cad_cuboid_rejected_non_empty_add_does_not_grow_bus_stack` asserts
  a rejected second add preserves stack length, cursor, clean saved state, and
  CAD inspection.
- Claim: task 161 exists and task 162 is not currently appended in
  `.ai/dispatch.tasks.md`.
  Falsifying search:
  `rg -n "^160\.|^161\.|^162\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
  -> observed task 160 heading at line 16028 and task 161 heading at line
  16164; no `^162.` heading appeared. The other matches are historical or
  task-instruction `NEEDS_HUMAN_RECORDED` references, not a completed marker for
  task 161.
- Claim: the audit must not use live GitHub state.
  Falsifying source: this TASK packet records the required dispatcher GitHub
  snapshot facts supplied with the issue body. The snapshot was generated by
  `Invoke-AiDispatchAuto.ps1` at `2026-06-18T22:53:59.1460845+03:00` before
  issue #418 was created. The recorded facts are: repo `RustCADs/RGE`, open
  ai-dispatch issues before this issue was created: none; open failed
  autonomous issues: none; autonomous issues already filed include closed #417
  "Route first CAD cuboid add through CommandBus". Use these recorded facts or
  an exact local issue-body artifact for queue/already-filed claims; no `gh`,
  browser, network, GitHub API, or live remote lookup is allowed. If the audit
  needs a broader already-filed issue claim than these recorded facts support,
  halt unless an exact local issue-body artifact is available.
- Claim: the audit can be completed without source/docs edits outside the
  final task marker/recommendation.
  Falsifying search to run before final EXEC: `git diff --name-only` -> all
  changed paths must be `.ai/dispatch.tasks.md` plus current-dispatch ISSUE-418
  handoff/sidecar/claim/log artifacts only.

## Constraints / Non-Goals

- This is a source/docs-read-only audit of production/test/docs surfaces. It is
  not a feature, fix, refactor, cleanup, test-addition, or docs-update dispatch.
- Do not edit Rust source or tests. Read source and tests only.
- Do not edit docs, ADRs, plans, status/history files, automation scripts,
  workflows, schemas, packet templates, Cargo manifests, or `Cargo.lock`.
- Do not append task 162 or any feature/implementation task.
- Do not approve, implement, or partially implement the recommendation.
- Do not broaden the CAD action boundary into UI/menu/shortcut routing,
  render-path refresh, CAD-core/cad-projection changes, save/load persistence,
  multi-root CAD composition, deletion, duplication, transforms, parameter
  editing, camera/viewport behavior, plugin execution, OS clipboard behavior,
  or Cargo/dependency work.
- Do not use `gh`, browser, network, GitHub API, or live remote lookup for
  GitHub queue or already-filed claims.

## Verification Gates

The Executor MUST run and document the result of each of these in the
EXECUTION_REPORT:

- `rg -n "^160\.|^161\.|^162\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> expected task 160 and task 161 headings, exactly one new audit marker for this dispatch, and no `^162.` heading.
- `rg -n "CAD|Cad|cad|graph|projection|BRep|render|shell|editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection" crates/editor-actions/src crates/editor-actions/Cargo.toml crates/editor-actions/tests` -> expected only the existing `CadCheckpoint` future-variant comment in `bus.rs` plus audit-ledger projection wording; no editor-shell import, CAD crate dependency, CAD type, B-Rep, render, or shell-specific action/context references in editor-actions.
- `rg -n "ActionContext|ActionContextFamily|WorldActionContext|ActionView|ActionViewRef|Box<dyn Action|BusEntry|CommandBus<|UndoStack<|new_for_context" crates/editor-actions/src crates/editor-actions/tests` -> expected generic CommandBus/action-context surface only.
- `rg -n "EditorShellActionContext|ShellActionContext|CadMutationState|WorldOnlyShellAction|AddCadCuboidToEmptyScene|submit_shell_action|add_cad_cuboid_to_empty_scene|undo_command|redo_command|mark_saved_command|CommandBusView|UndoStackView" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs` -> expected shell-owned CAD action/context and bus adapter surface.
- `rg -n "bus_routed_cad_cuboid_add_undo_redo_keeps_scene_coherent_and_moves_dirty_mark|bus_routed_cad_cuboid_rejected_non_empty_add_does_not_grow_bus_stack|render_mesh_for|live_brep_entity_count|projection_render_mesh_count|mark_saved_command|undo_command|redo_command" crates/editor-shell/src/lifecycle/tests.rs` -> expected source/test evidence for undo/redo, no stale render-mesh lookup, and dirty/save-mark movement.
- `rg -n "route_menu_command|EditorKeyCommand::|Shortcut|Command::|init_render_state|render_frame|with_render_mesh|save_source|handle_save|Cargo.toml|cad-core|cad-projection" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs` -> record read-only evidence for whether task 160 changed only the bounded lifecycle/command surfaces and did not add a CAD UI/render/save/Cargo scope.
- Read the dispatcher snapshot facts recorded in this TASK packet or exact local issue-body artifact and record that no live GitHub lookup was used.
- `git diff --name-only` -> expected only `.ai/dispatch.tasks.md` plus current-dispatch ISSUE-418 handoff/sidecar/claim/log artifacts.
- `git diff --check` -> expected exit 0.

The orchestrator's canonical `.ai/dispatch.verify.ps1` gate may run after this
read-only audit. If it fails on an out-of-scope target and a correction would
require forbidden edits, halt per the halt conditions rather than expanding
scope.

## Halt Conditions

The Executor MUST halt, write the EXEC packet with the appropriate blocked or
needs-human status, and not force the audit conclusion if any of these occur:

- The audit cannot cite the dispatcher-provided GitHub snapshot from this TASK
  packet or an exact local issue-body artifact without using `gh`, browser,
  network, GitHub API, or a live remote lookup.
- The audit would require editing Rust source/tests, docs, automation,
  workflows, schemas, Cargo metadata, packet templates, or any path outside the
  explicit MAY edit/add surface.
- The audit cannot make one source-grounded recommendation without resolving
  more than one coherent next feature boundary.
- Recording exactly one `NEEDS_HUMAN_RECORDED:` marker plus the required
  recommendation block would disturb existing task provenance.
- The executor discovers task 162 is already appended before its work begins or
  that another concurrent edit has already recorded the task 161 human marker.
- Any verification gate shows source/docs/Cargo/workflow/script/schema/template
  changes or generated artifacts outside current-dispatch ISSUE-418 scope.

## Planner Notes

This packet intentionally mirrors task 161 in `.ai/dispatch.tasks.md` and the
user-provided ISSUE-418 goal. The point is to pause after task 160, audit the
new generic CommandBus context boundary and shell-owned CAD context boundary,
and leave a human approval record for the next coherent feature. The executor
should treat the final recommendation as a proposal only; it must not self-arm
task 162 and must not implement the proposed next feature.

The dispatcher GitHub state snapshot facts recorded in this TASK packet are
the only GitHub evidence available here. The snapshot was generated at
`2026-06-18T22:53:59.1460845+03:00`, before issue #418 was created, and states
that there were no open ai-dispatch issues and no open failed autonomous issues
at that time. It also lists prior autonomous issue #417 as closed. This is
enough for the queue/already-filed claims required by this audit; do not query
live GitHub. If broader already-filed issue evidence becomes necessary, use an
exact local issue-body artifact or halt.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-418
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
