# Execution Report

DISPATCH_ID: ISSUE-423
AUTHOR: Executor / Codex
TIMESTAMP: 2026-06-19_08-59-09+0300
RELATED_FILES:
- crates/editor-shell/src/render_path.rs - routed Command::Delete to CAD delete only for exact single tracked-CAD selection
- crates/editor-shell/src/lifecycle/tests.rs - added focused route/delete/cut regression coverage
- .ai/dispatch.tasks.md - appended gated audit task 165
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-423_TASK_2026-06-19_08-44-01+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## Current-State Claims: Falsification Re-run

- Claim: The current menu Delete route went directly to wrapper-world selected entity deletion.
  Re-run before edits: `rg -n "Command::Delete =>|self\\.delete_selected_entities\\(\\)" crates/editor-shell/src/render_path.rs` -> found `Command::Delete` at `crates/editor-shell/src/render_path.rs:453` and `self.delete_selected_entities();` at `crates/editor-shell/src/render_path.rs:454`.
- Claim: The audited CAD delete entry point and action already existed in `editor-shell`.
  Re-run: `rg -n "struct DeleteCurrentCadCuboid|fn delete_current_cad_cuboid" crates/editor-shell/src/lifecycle/commands.rs` -> found `struct DeleteCurrentCadCuboid` at `crates/editor-shell/src/lifecycle/commands.rs:511` and `pub fn delete_current_cad_cuboid` at `crates/editor-shell/src/lifecycle/commands.rs:1218`.
- Claim: Existing menu Delete/Cut tests covered the wrapper-world route.
  Re-run before edits: `rg -n "fn menu_delete_command_deletes_selected_entities_and_prunes_selection|fn menu_cut_command_copies_then_deletes_selected_legacy_blobs" crates/editor-shell/src/lifecycle/tests.rs` -> found both pre-existing tests at the task-packet lines before they were renamed to include the required filters.
- Claim: `crates/editor-actions` had no CAD/editor-shell coupling by the guard pattern.
  Re-run: `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> no matches; command exited 1, which is the expected zero-match `rg` result.
- Claim: Before this dispatch, task 165 and task 166 headings were not present in `.ai/dispatch.tasks.md`.
  Re-run before appending task 165: the heading grep found only `164.` and no `165.` or `166.` heading. Final verification now finds exactly one `164.` and one `165.`, with no `166.` heading.

Additional unchanged-surface checks:
- `git diff -- crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs` -> no output.
- `git diff -- crates/editor-actions crates/editor-ui crates/editor-egui-host Cargo.toml Cargo.lock` -> no output.
- `git diff -- crates/editor-shell/src/render_path.rs | rg -n "Command::Cut|cut_selected_entities"` -> no matches; command exited 1.

## What I Changed

### Source

- `crates/editor-shell/src/render_path.rs`: added `delete_menu_selection_is_exact_tracked_cad_entity`, a direct ID predicate requiring selection length 1 and `self.cad_entity == Some(selected)`.
- `crates/editor-shell/src/render_path.rs`: changed only the `Command::Delete` arm so exact tracked-CAD selection calls `delete_current_cad_cuboid`; rejected CAD preflight errors are warn-logged to `rge::editor-shell::menu` and swallowed without wrapper fallback. All other selections still call `delete_selected_entities`.

### Tests

- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_delete_current_cad_cuboid_for_exact_tracked_selection`: exact tracked-CAD Delete restores the empty parent checkpoint, clears `cad_entity`, prunes entity/face selection, advances the bus, and leaves no stale `render_mesh_for`.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_delete_current_cad_cuboid_undo_redo_uses_fresh_entity`: routed CAD Delete undo/redo restores a fresh entity, re-deletes it cleanly, and preserves dirty/save cursor semantics.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_delete_selected_non_cad_preserves_tracked_cad_scene`: single non-CAD selection uses wrapper-world Delete and leaves CAD graph/projection/world state intact.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_delete_selected_mixed_selection_preserves_cad_scene`: mixed tracked-CAD plus non-CAD selection uses wrapper-world Delete and leaves the CAD cuboid alive.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_delete_selected_empty_selection_preserves_tracked_cad_scene`: empty selection no-ops through the wrapper path and does not delete CAD.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_delete_current_cad_cuboid_stale_selection_swallow_no_fallback`: exact stale tracked-CAD selection preserves selection/face selection and does not grow or move the bus stack.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_delete_selected_entities_and_prunes_selection`: renamed existing wrapper Delete test so the `delete_selected` and `route_menu_command` filters run meaningful coverage.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_cut_selected_copies_then_deletes_selected_legacy_blobs`: renamed existing wrapper Cut test so the `cut_selected` and `route_menu_command` filters run meaningful coverage.
- `crates/editor-shell/src/lifecycle/tests.rs::route_menu_command_cut_selected_exact_tracked_cad_stays_wrapper_world_only`: exact tracked-CAD Cut remains wrapper-world only and does not route to CAD delete.

### Docs

- `.ai/dispatch.tasks.md`: appended exactly one task 165 gated audit block, carrying the required self-rearm instruction verbatim; no task 166 was appended and no task-164 direct `NEEDS_HUMAN_RECORDED` marker was added.

## Per-File Summary

- `crates/editor-shell/src/render_path.rs`: the routing change is local to `Command::Delete`; `Command::Cut` and extension/menu surfaces are untouched.
- `crates/editor-shell/src/lifecycle/tests.rs`: coverage now exercises CAD route, stale no-fallback, non-CAD/mixed/empty wrapper path preservation, existing wrapper Delete/Cut behavior, and route filter names.
- `.ai/dispatch.tasks.md`: task 165 is an audit-only human-gated pause with read-only source scope and no feature/task-166 rearm.

## Verification Results

- `cargo test -p rge-editor-shell --lib -- delete_current_cad_cuboid` -> exit 0; 7 passed, 0 failed, 327 filtered out.
- `cargo test -p rge-editor-shell --lib -- add_cad_cuboid_to_empty_scene` -> exit 0; 4 passed, 0 failed, 330 filtered out.
- `cargo test -p rge-editor-shell --lib -- route_menu_command` -> exit 0; 9 passed, 0 failed, 325 filtered out.
- `cargo test -p rge-editor-shell --lib -- delete_selected` -> exit 0; 4 passed, 0 failed, 330 filtered out.
- `cargo test -p rge-editor-shell --lib -- cut_selected` -> exit 0; 2 passed, 0 failed, 332 filtered out.
- `cargo check -p rge-editor-actions -p rge-editor-shell` -> exit 0.
- `cargo +nightly fmt --all -- --check` -> initially reported one wrapping diff in the new test; fixed it. Final rerun exit 0.
- `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> no matches; exit 1 as expected for zero matches.
- `git diff --name-only` -> exit 0; tracked diff only `.ai/dispatch.tasks.md`, `crates/editor-shell/src/lifecycle/tests.rs`, and `crates/editor-shell/src/render_path.rs`.
- `git diff --check` -> exit 0; no whitespace errors. Git printed the existing LF-to-CRLF working-copy warning for `.ai/dispatch.tasks.md`.
- `rg -n "^164\\.|^165\\.|^166\\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> exit 0; one `164.` heading at line 16455, one `165.` heading at line 16590, no `166.` heading, and no task-164 direct marker. Historical/instructional `NEEDS_HUMAN_RECORDED` references remain as provenance.

## Deviations from Task Packet

None - execution stayed strictly within the Task Packet scope.

## Open Questions for Reviewer

None. Please focus review on the exact-selection predicate and the stale selected-CAD no-fallback behavior.

## Worktree State

- Tracked files: modified `.ai/dispatch.tasks.md`, `crates/editor-shell/src/lifecycle/tests.rs`, `crates/editor-shell/src/render_path.rs`.
- Untracked items: `ai_handoffs/ISSUE-423_TASK_2026-06-19_08-44-01+0300.md`, `ai_handoffs/ISSUE-423_TASK_2026-06-19_08-44-01+0300.meta.json`, `ai_handoffs/ISSUE-423_EXEC_2026-06-19_08-59-09+0300.md`, `ai_handoffs/ISSUE-423_EXEC_2026-06-19_08-59-09+0300.meta.json` after finalize, and pre-existing `ai_handoffs/claims/ISSUE-423_2026-06-19_08-43-59-8182743+0300_claim.json`.
- Branch: `ai-dispatch/ISSUE-423`
- Last commit: `90afb96` Re-arm dispatch with approved task 164 (route menu Delete to headless CAD-cuboid delete)

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-423
AUTHOR: Executor / Codex
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
