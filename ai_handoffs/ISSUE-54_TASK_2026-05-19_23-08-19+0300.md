# Task Packet

DISPATCH_ID: ISSUE-54
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_23-08-19+0300
RELATED_FILES:
- crates/cad-core/tests/operator_graph_snapshot_smoke.rs
- crates/cad-core/src/graph/operator_graph.rs
- crates/cad-core/src/operators/mod.rs
- kernel/graph-foundation/src/snapshot.rs
- kernel/graph-foundation/src/graph.rs
STATUS: OPEN

## Goal

Add one focused cad-core integration smoke test for GitHub issue #54 proving that `OperatorGraph` can reuse graph-foundation snapshots for a CAD operator graph. The test should build a small `Cuboid -> Transform` operator graph, capture `graph.inner()` with `GraphSnapshot`, round-trip the snapshot through RON, materialize it back to a graph, and assert that counts plus exact node and edge payloads survive unchanged.

## Scope

### MAY edit
- `crates/cad-core/tests/operator_graph_snapshot_smoke.rs` only.

### MUST NOT edit
- Any existing file, including `crates/cad-core/src/graph/operator_graph.rs`.
- `crates/cad-core/src/operators/**`; these files may be inspected only to construct public `CuboidOp`, `TransformOp`, `OperatorNode`, and `EdgeKind` values.
- `kernel/graph-foundation/**`; these files may be inspected only to understand `GraphSnapshot`, `Graph`, `EdgeRecord`, `NodeId`, and `EdgeId`.
- `crates/cad-core/Cargo.toml`, `Cargo.toml`, `Cargo.lock`, or any manifest/lockfile.
- `crates/material-graph/**`, `crates/anim-graph/**`, graph-viewer/editor/runtime/renderer/asset/ECS crates, tooling, docs, schemas, scripts, CI files, `.gitignore`, architecture-lint tooling, or any existing handoff packet.

### MAY add new files
- `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`.
- Protocol output only: the Executor's `ai_handoffs/ISSUE-54_EXEC_*.md` report and its `.meta.json` sidecar if finalized by `new-handoff.ps1`.

### MUST NOT add new files
- Any source file outside `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`.
- New fixtures, snapshots/goldens, generated artifacts, crates, modules, docs, ADRs, schemas, scripts, Cargo entries, task/correction/review/closeout packets, or files under graph-foundation/material-graph/anim-graph.

## Deliverables

- Add exactly one new cad-core integration test file: `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`.
- Add exactly one test in that file. Required test name: `operator_graph_snapshot_round_trip_preserves_nodes_and_edges`.
- The test must construct an `OperatorGraph` using only public cad-core APIs.
- The graph must contain exactly two operator nodes:
  - one `OperatorNode::Cuboid(CuboidOp { ... })`
  - one `OperatorNode::Transform(TransformOp { ... })`
- The graph must contain exactly one edge created with `OperatorGraph::connect(cuboid_id, transform_id, 0)`.
- The test must capture the original graph with `GraphSnapshot::from_graph(graph.inner())`.
- The test must serialize that snapshot with `GraphSnapshot::to_ron`, deserialize it with `GraphSnapshot::from_ron`, and materialize the deserialized snapshot with `to_graph()`.
- The test must assert the original snapshot, deserialized snapshot, and restored graph all report exactly two nodes and one edge.
- The test must assert the original `NodeId` values returned by `add_operator` still look up the exact expected `OperatorNode` payloads after the RON round-trip and restore.
- The test must assert the original `EdgeId` returned by `connect` still looks up an `EdgeRecord` whose `src`, `dst`, and `data` are exactly the expected `cuboid_id`, `transform_id`, and `EdgeKind::Input(0)`.
- The test should compare concrete records, not only counts. A suitable shape is collecting `GraphSnapshot::nodes()` and `GraphSnapshot::edges()` before and after RON into owned `(NodeId, OperatorNode)` and `(EdgeId, EdgeRecord<EdgeKind>)` vectors, plus direct restored-graph lookups by id.

## Acceptance Criteria

- The only implementation file added or changed is `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`.
- No existing source file, manifest, lockfile, docs file, script, schema, CI file, or packet is changed.
- The test uses `rge_kernel_graph_foundation::GraphSnapshot` against `OperatorGraph::inner()`; it does not add a new snapshot wrapper API to cad-core.
- The test uses the existing `GraphSnapshot::to_ron`, `GraphSnapshot::from_ron`, and `GraphSnapshot::to_graph` paths.
- The test validates exact `NodeId -> OperatorNode` and `EdgeId -> EdgeRecord<EdgeKind>` preservation across the RON round-trip, not just successful serialization.
- The restored graph is obtained from the deserialized graph-foundation snapshot, not by cloning the original `OperatorGraph` or by using `CadGraph` checkpoint APIs.
- `cargo test -p rge-cad-core --test operator_graph_snapshot_smoke` exits 0.

## Constraints / Non-Goals

- This dispatch is test coverage only.
- Do not implement, modify, test, or expand `VizAdapter`; GitHub issue #53 owns that separately.
- Do not change `OperatorGraph`, `OperatorNode`, `CuboidOp`, `TransformOp`, `EdgeKind`, graph-foundation `Graph`, graph-foundation `GraphSnapshot`, `EdgeRecord`, id types, serialization behavior, or public APIs.
- Do not add traversal, evaluation, cycle detection, diffing, editor behavior, renderer behavior, graph-viewer behavior, runtime behavior, checkpoint behavior, cache behavior, persistence behavior, ECS behavior, asset behavior, or material/animation graph behavior.
- Do not add dependencies, feature flags, manifests, lockfile updates, generated files, architecture-lint policy, docs, ADRs, or CI changes.
- Do not broaden this into workspace-wide tests, structural-diff tests, checkpoint restore tests, corruption-recovery tests, or VizAdapter coverage.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-cad-core --test operator_graph_snapshot_smoke` -> expected exit 0.

The Executor SHOULD also run and document this scoped formatting check if the nightly formatter is available:

- `cargo +nightly fmt --package rge-cad-core -- --check` -> expected exit 0.

If the local shell cannot find `cargo`, the Executor may use the repository's established Rust toolchain location (`A:\RustCache\cargo\bin\cargo.exe`) if present, matching `.ai/dispatch.verify.ps1`; otherwise halt and report the missing tool.

If a broader workspace verification wrapper or workspace formatting check fails solely because of pre-existing out-of-scope files, the Executor must not fix those files. Record the out-of-scope failure clearly in the execution report after the required scoped cad-core test gate has been attempted.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The change cannot be completed by adding or editing only `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`.
- The implementation appears to require edits to `OperatorGraph`, graph-foundation, operator definitions, manifests, lockfiles, docs, scripts, schemas, CI files, architecture-lint tooling, or any existing file.
- The test cannot use public cad-core and graph-foundation APIs to build the graph, snapshot it, round-trip through RON, and restore it.
- The solution requires implementing or modifying `VizAdapter` or relying on any behavior owned by GitHub issue #53.
- The solution requires changing serialization derives, graph id derivation, edge record shapes, snapshot APIs, checkpoint APIs, traversal/evaluation behavior, or public method signatures.
- Cargo attempts to modify any manifest, lockfile, generated file, or file outside the MAY edit scope.
- `cargo test -p rge-cad-core --test operator_graph_snapshot_smoke` fails after the scoped test addition and the failure is not an obvious typo that can be fixed within `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`.
- A pre-existing environment problem, such as missing cargo after checking the established RustCache path, prevents running the required verification gate.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #54. Revision number: 0. There is no prior Claude gate result.

Useful public imports for the integration test are expected to include `rge_cad_core::{CuboidOp, EdgeKind, OperatorGraph, OperatorNode, TransformOp}` and `rge_kernel_graph_foundation::{EdgeRecord, GraphSnapshot}`. `CuboidOp` and `TransformOp` both implement `Default`; using a non-default transform translation is fine as long as the expected `OperatorNode` payload is cloned before insertion and compared after restore. Keep the test small: two nodes, one `Input(0)` edge, one RON round-trip, exact payload assertions.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-54
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
