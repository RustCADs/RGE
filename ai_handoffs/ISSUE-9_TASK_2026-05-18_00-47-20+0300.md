# Task Packet

DISPATCH_ID: ISSUE-9
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_00-47-20+0300
RELATED_FILES:
- AI_DISPATCH_AUTOMATION.md
- Invoke-AiDispatchQueue.ps1
- Invoke-AiDispatchAuto.ps1
- Register-AiDispatchSchedule.ps1
STATUS: OPEN

## Goal

Document the unattended dispatch stack requested by GitHub issue #9 by adding a
focused subsection, `7.1 Unattended operation`, under section 7 of
`AI_DISPATCH_AUTOMATION.md`. The new subsection should explain how the queue
runner, autonomous driver, and Windows scheduler fit together so an operator can
understand the layers without reading full parameter tables or reverse
engineering the scripts.

## Scope

### MAY edit
- AI_DISPATCH_AUTOMATION.md

### MUST NOT edit
- Invoke-AiDispatchQueue.ps1
- Invoke-AiDispatchAuto.ps1
- Register-AiDispatchSchedule.ps1
- AI_DISPATCH_PARALLEL.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- ai_handoffs/templates/*
- .ai/*
- .gitignore
- Any source, schema, script, packet, log, status, handoff, or documentation
  file other than `AI_DISPATCH_AUTOMATION.md`

### MAY add new files
- none

### MUST NOT add new files
- Any new file or directory

## Deliverables

- A new `### 7.1 Unattended operation` subsection in
  `AI_DISPATCH_AUTOMATION.md`, placed after the existing section 7 modes table
  and before `## 8. Parameters`.
- Prose of roughly 20-30 Markdown source lines describing the three unattended
  layers: `Invoke-AiDispatchQueue.ps1`, `Invoke-AiDispatchAuto.ps1`, and
  `Register-AiDispatchSchedule.ps1`.
- The subsection cross-references those three script names exactly and explains
  their responsibilities without duplicating their full parameter tables.

## Acceptance Criteria

- `AI_DISPATCH_AUTOMATION.md` contains exactly one heading line:
  `### 7.1 Unattended operation`.
- The new subsection is directly under section 7, after the existing Modes
  table and before `## 8. Parameters`.
- The subsection names all three scripts exactly:
  `Invoke-AiDispatchQueue.ps1`, `Invoke-AiDispatchAuto.ps1`, and
  `Register-AiDispatchSchedule.ps1`.
- The subsection describes the queue runner as the GitHub issue queue layer,
  the autonomous driver as the task-selection layer, and the scheduler as the
  recurring Windows Scheduled Task trigger layer.
- The subsection summarizes operator-relevant behavior, including one-run-per
  invocation, publish gating, branch or main publish mode at the autonomous
  layer, recurring ticks, and overlap prevention.
- No full parameter tables are added or duplicated.
- No top-level table of contents entry is added for `7.1`; the existing table of
  contents lists top-level sections only.
- No file other than `AI_DISPATCH_AUTOMATION.md` is modified as task output.

## Constraints / Non-Goals

- This is a documentation-only dispatch.
- Do not change script behavior, defaults, labels, publish policy, scheduler
  registration, lock handling, or any PowerShell implementation.
- Do not add command examples beyond short references already needed for the
  prose.
- Do not add, remove, or renumber top-level sections.
- Do not rewrite unrelated sections of `AI_DISPATCH_AUTOMATION.md` except for
  the minimal local insertion needed to introduce `7.1`.
- Do not run broad Rust build or test suites for this docs-only change unless
  another tool already requires them.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Select-String -Path AI_DISPATCH_AUTOMATION.md -Pattern '^### 7\.1 Unattended operation$','Invoke-AiDispatchQueue\.ps1','Invoke-AiDispatchAuto\.ps1','Register-AiDispatchSchedule\.ps1'`
  -> expected: one heading match and at least one match for each required
  script name.
- `$lines = Get-Content -LiteralPath 'AI_DISPATCH_AUTOMATION.md'; $start = ($lines | Select-String -Pattern '^### 7\.1 Unattended operation$').LineNumber; $end = ($lines | Select-String -Pattern '^## 8\. Parameters$').LineNumber; $end - $start - 1`
  -> expected: a value roughly in the 20-30 line range, allowing normal Markdown
  wrapping and blank lines.
- `git diff -- AI_DISPATCH_AUTOMATION.md` -> expected: a localized insertion of
  the new subsection only.
- `git diff --name-only` -> expected: `AI_DISPATCH_AUTOMATION.md` is the only
  tracked file changed by the task itself.

## Halt Conditions

The Executor MUST halt (without committing) and request fresh guidance if any
of the following occur:

- Section 7 or `## 8. Parameters` cannot be identified clearly enough to place
  the subsection without restructuring the document.
- The script headers or existing documentation contradict the requested summary
  in a way that would require changing PowerShell behavior or issue-queue
  policy to make the prose truthful.
- Satisfying the goal appears to require editing any file other than
  `AI_DISPATCH_AUTOMATION.md`.
- The required subsection would need full parameter tables or a new top-level
  section to be understandable.
- Verification shows unexpected tracked changes outside
  `AI_DISPATCH_AUTOMATION.md` that were made during this task.

## Planner Notes

Issue #9 asks for an operator-facing subsection, not new automation behavior.
The current document already names the scripts in the components table and
parameter sections; this task should synthesize the unattended stack in one
place under Modes. Use the script headers as read-only context if needed, but
the final deliverable is limited to documentation in `AI_DISPATCH_AUTOMATION.md`.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-9
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
