# Correction Packet

DISPATCH_ID: ISSUE-76
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_21-35-06+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
- kernel/graph-foundation/tests/round_trip_ron.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-76_TASK_2026-05-20_21-26-58+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-76_EXEC_2026-05-20_21-34-15+0300.md`
- Latest Review Report: none; the post-execution verification gate failed before Codex control review.
- Prior Correction Packets: none.
- Verification failure: `.ai/dispatch.verify.ps1` exited 1 during `cargo +nightly fmt --all -- --check`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more.

1. **Normalize formatter-reported newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` reported incorrect newline style in
   `kernel/graph-foundation/tests/diff_correctness.rs`. Required change:
   normalize this file to the newline style expected by rustfmt. Acceptance:
   `cargo +nightly fmt --all -- --check` no longer reports
   `diff_correctness.rs`.

2. **Apply rustfmt to the new round-trip test file** - Verification gate
   finding: `cargo +nightly fmt --all -- --check` reported a diff in
   `kernel/graph-foundation/tests/round_trip_ron.rs` around the
   `GraphSnapshot::from_ron(&ron).expect("deserialize")` assignment in the new
   test. Required change: apply rustfmt's formatting only to this file, including
   the verifier-requested single-line assignment layout. Acceptance:
   `cargo +nightly fmt --all -- --check` no longer reports
   `round_trip_ron.rs`.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred. The only reported blocker is the formatting failure
above, and verification did not proceed to later gates.

## Updated Acceptance Criteria

Unchanged from Task Packet, except this correction round permits only the
formatting and newline normalization needed for the two files reported by the
verification gate. No test logic, assertions, production code, manifests,
schemas, scripts, docs, or CI files may be changed.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Additionally, halt and report back if passing
verification appears to require any semantic code change, any new test, or any
file edit beyond the two formatter-reported test files listed in
`RELATED_FILES`.

## Planner Notes

This correction is limited to making the canonical verification gate pass after
the executor's test-only change. The executor should prefer `cargo +nightly fmt --all`
or the repository's standard formatter path over manual style edits, then confirm
the full canonical gate exits 0 before writing the follow-up execution report.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-76
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
