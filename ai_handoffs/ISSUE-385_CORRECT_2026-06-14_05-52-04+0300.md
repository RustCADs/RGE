# Correction Packet

DISPATCH_ID: ISSUE-385
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-14_05-52-04+0300
RELATED_FILES:
- ai_handoffs/ISSUE-385_TASK_2026-06-14_05-23-28+0300.md
- ai_handoffs/ISSUE-385_EXEC_2026-06-14_05-45-04+0300.md
- .ai/dispatch-ISSUE-385/verification.round0.log
- crates/editor-egui-host/src/lib.rs
- crates/editor-egui-host/src/menu.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-385_TASK_2026-06-14_05-23-28+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-385_EXEC_2026-06-14_05-45-04+0300.md`
- Latest Review Report: none. The orchestrator verification gate failed before Codex control review, so no `_REVIEW_` packet or control-review JSON is expected for this round.
- Verification Failure Log: `.ai/dispatch-ISSUE-385/verification.round0.log`
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more.

1. **Resolve `lib.rs` split-exemption lint failure** - Verification gate finding: `cargo run -q -p rge-tool-architecture-lints -- all` failed `[split-exemption]` because `crates/editor-egui-host/src/lib.rs` was 1032 lines, above the 1000-line cap, without a `// SPLIT-EXEMPTION:` annotation. Required change: keep the ISSUE-385 behavior intact while bringing `crates/editor-egui-host/src/lib.rs` back to 1000 physical lines or fewer. Use only the already authorized files needed for this narrow source-shape fix, preferably by reducing repeated main-menu rendering code in `lib.rs` or moving reusable rendering shape into the existing `crates/editor-egui-host/src/menu.rs` module. Do not add a new module or file. Do not add a new `// SPLIT-EXEMPTION:` annotation to `lib.rs` for this narrow dispatch. Acceptance: `cargo run -q -p rge-tool-architecture-lints -- all` exits 0 and reports `[split-exemption] PASS`, with no new architecture-lint violations.

2. **Preserve ISSUE-385 behavior while making the lint fix** - Verification gate finding: the pre-gate task-specific checks passed before the split-exemption failure, so the correction must not reopen implementation scope. Required change: preserve the exact conflict-source rule, enabled-only rule, peer-entry order, menu item command enqueueing behavior, shortcut display text, disabled-item behavior, plugin projected-menu behavior, and docs/task status already reported in the EXEC packet. Acceptance: the focused `rge-editor-egui-host` checks from the Task Packet still pass, `.ai/dispatch.tasks.md` still has no `132.` heading, and `git diff --name-only` remains confined to the original Task Packet MAY-edit list plus normal generated handoff artifacts.

3. **Write a corrected follow-up EXEC report after the source-shape fix** - Verification gate finding: the current EXEC report claims verification success only for the pre-gate task-specific checks, while the canonical post-execution verification failed. Required change: after the correction and fresh verification, write one new `ai_handoffs/ISSUE-385_EXEC_*.md` report that records the source-shape correction, the fresh gate results, and the final worktree state. Do not rewrite the prior EXEC packet. Acceptance: the follow-up EXEC report truthfully records the prior verification failure, the correction, the fresh architecture-lint result, and the canonical verification result.

## Deferred Findings (NOT Approved for This Round)

None. There are no other approved fixes in this correction round. Do not perform cosmetic cleanup, unrelated refactors, UI redesign, additional tests beyond what is needed to preserve the existing coverage, documentation rewrites, task-list expansion, Cargo changes, workflow changes, or packet-template changes.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-specific addition: the dispatch is not acceptable until the canonical verification gate passes after the split-exemption failure is resolved. `crates/editor-egui-host/src/lib.rs` must not remain above the 1000-line cap without a pre-existing accepted exemption, and this correction does not approve adding a new `lib.rs` exemption.

## Re-Verification Gates

The Executor MUST re-run and document these gates after the correction:

- `cargo test -p rge-editor-egui-host --lib menu` -> expected exit 0.
- `cargo test -p rge-editor-egui-host --lib command_palette` -> expected exit 0.
- `cargo test -p rge-editor-egui-host --lib shortcut_conflict` -> expected exit 0.
- `cargo check -p rge-editor-egui-host --lib` -> expected exit 0.
- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0, including `[split-exemption] PASS`.
- `rg -n "^130\.|^131\.|^132\." .ai/dispatch.tasks.md` -> expected task 130 DONE, task 131 DONE via ISSUE-385, and no `132.` heading.
- `git diff --name-only` -> expected paths confined to the Task Packet MAY-edit list plus normal generated handoff artifacts.
- `git diff --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- Halt with `NEEDS_HUMAN` if making the architecture lint pass would require editing any Task Packet MUST-NOT path, adding a new Rust module/file, editing Cargo metadata, changing workflows, changing dispatch automation, appending task 132, or changing shortcut conflict policy/behavior.
- Halt with `NEEDS_HUMAN` if preserving the implemented ISSUE-385 behavior would require keeping `crates/editor-egui-host/src/lib.rs` above the 1000-line cap and adding a new split-exemption annotation.
- Halt with `NEEDS_HUMAN` if canonical verification still fails after the narrow split-exemption correction.

## Planner Notes

The only observed post-execution blocker is the canonical verification failure from the architecture lint. The correction should be a source-shape adjustment inside the existing ISSUE-385 edit envelope, not a feature change and not a documentation cleanup round.

The `menu` module already owns `menu_item` and main-menu projection helpers, and its file header records the precedent that menu code was extracted from `lib.rs` to keep the crate root under the 1000-line cap. Use that precedent when choosing the smallest implementation shape.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-385
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
