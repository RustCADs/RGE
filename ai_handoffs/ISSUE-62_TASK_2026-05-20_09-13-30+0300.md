# Task Packet

DISPATCH_ID: ISSUE-62
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_09-13-30+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_operator_smoke.rs
- crates/editor-ui/Cargo.toml
- Cargo.lock
- crates/editor-ui/src/widgets/node_graph.rs
- crates/cad-core/src/graph/operator_graph.rs
- crates/cad-core/src/operators/mod.rs
STATUS: OPEN

## Goal

Add the focused editor-ui integration smoke for GitHub issue #62 proving that `NodeGraphWidget` can consume a real `rge_cad_core::OperatorGraph` through the existing `rge_kernel_graph_foundation::VizAdapter` bridge. This is a test-only cross-crate smoke for the generic node-graph model already exercised with material and animation graphs; production `editor-ui` must remain domain-agnostic and must not gain CAD production coupling or behavior changes.

## Scope

### MAY edit
- `crates/editor-ui/Cargo.toml`, only inside `[dev-dependencies]`, and only to add the test-only dependency required for the new integration test to name `rge-cad-core`.
- `Cargo.lock`, only for the minimal dependency-list metadata Cargo writes as a consequence of the approved `rge-editor-ui` dev-dependency on the existing workspace `rge-cad-core` package. No external package changes are allowed.

### MUST NOT edit
- Any production source file, including `crates/editor-ui/src/widgets/node_graph.rs`, `crates/cad-core/src/**`, and `kernel/graph-foundation/**`.
- `crates/material-graph/**`, `crates/anim-graph/**`, `crates/script-graph/**`, renderer/gfx crates, runtime crates, editor-shell crates, or any graph-domain crate.
- Workspace-root `Cargo.toml`, any crate manifest other than `crates/editor-ui/Cargo.toml`, architecture-lint files, schemas, scripts, CI files, documentation, `.gitignore`, generated artifacts other than the narrow `Cargo.lock` update allowed above, or any existing handoff packet.

### MAY add new files
- `crates/editor-ui/tests/node_graph_operator_smoke.rs`

### MUST NOT add new files
- Any file other than `crates/editor-ui/tests/node_graph_operator_smoke.rs`.
- New crates, fixtures, snapshots, assets, docs, schemas, scripts, architecture-lint rules, CI files, or generated files.

## Deliverables

- Add `crates/editor-ui/tests/node_graph_operator_smoke.rs` as an integration test.
- Add exactly one editor-ui dev-dependency if needed for the test to compile: `rge-cad-core = { path = "../cad-core" }`. Do not add a production dependency and do not modify workspace dependencies.
- Let Cargo update `Cargo.lock` only as needed to reflect that editor-ui dev-dependency. The expected lockfile shape is a dependency-list update involving `rge-editor-ui` and `rge-cad-core`, with no external crate version changes.
- In the smoke test, build a small real `OperatorGraph` using public cad-core APIs such as `OperatorGraph::new`, `add_operator`, `connect`, `OperatorNode::Cuboid`, `CuboidOp`, `OperatorNode::Boolean`, and `BooleanOp::union`.
- Use two distinct cuboid operators feeding one Boolean operator so content-derived node ids do not collide. Connect the first cuboid to Boolean port `0` and the second cuboid to Boolean port `1`.
- Build a node-graph model through the existing editor-ui API, for example `NodeGraphWidget::new().model_from(&graph)` if `model_from` remains an instance method.
- Assert the model has exactly 3 nodes and 2 edges.
- Assert node records for the two cuboids expose `display_name == "Cuboid"` and `kind == "Cuboid"`, and the Boolean node exposes `display_name == "Boolean"` and `kind == "Boolean"`.
- Assert edge records expose the exact endpoints returned by the two `connect` calls: cuboid-left to Boolean with label `input[0]`, and cuboid-right to Boolean with label `input[1]`.
- Make assertions by the stable `NodeId` and `EdgeId` values returned from graph construction, or otherwise avoid depending on insertion order because the underlying adapter order is deterministic but sorted by ids.

## Acceptance Criteria

- `crates/editor-ui/tests/node_graph_operator_smoke.rs` exists and is the only new file.
- The new test uses a real `rge_cad_core::OperatorGraph`; it does not use a fake adapter or standalone graph-foundation test double.
- The test proves `NodeGraphWidget` consumes `OperatorGraph` through `VizAdapter` by compiling and calling the existing `model_from` bridge without changing production APIs.
- The test builds exactly the intended tiny graph shape: two distinct cuboid nodes connected to one Boolean union node on ports `0` and `1`.
- The test asserts exact node count, edge count, cuboid and Boolean node display strings, cuboid and Boolean node kind strings, both edge endpoint pairs, and both exact edge labels `input[0]` and `input[1]`.
- `crates/editor-ui/Cargo.toml` is changed only inside `[dev-dependencies]`, and only by adding the editor-ui dev-dependency on `rge-cad-core` if the integration test needs it.
- `Cargo.lock` is changed only if Cargo requires it for that dev-dependency, with no registry dependency upgrades, no new external packages, and no unrelated lockfile churn.
- No production `editor-ui` code mentions `rge-cad-core`, `rge-material-graph`, `rge-anim-graph`, renderer crates, runtime crates, or other graph-domain crates as part of this dispatch.
- No behavior or public API changes are made to `NodeGraphWidget`, `NodeGraphModel`, `OperatorGraph`, `VizAdapter`, cad-core operators, graph-foundation, renderer, runtime, architecture-lint, schema, script, documentation, or CI files.
- `cargo test -p rge-editor-ui --test node_graph_operator_smoke` exits 0.

## Constraints / Non-Goals

- This dispatch is test-only except for the editor-ui `[dev-dependencies]` entry and its narrow lockfile consequence.
- Do not change `NodeGraphWidget`, `NodeGraphModel`, `VizAdapter`, `OperatorGraph`, operator graph labels, operator graph ordering, graph-foundation ids, or any production API to make the test easier.
- Do not add rendering, egui UI, layout, pan/zoom, selection, ports UI, persistence, CAD editor behavior, graph traversal, evaluation, Boolean evaluation assertions, BRep/tessellation assertions, validation, mutation behavior, or any CAD-domain UI.
- Do not promote `rge-cad-core` to a production dependency of editor-ui.
- Do not add workspace dependency entries, feature flags, dependency version bumps, architecture-lint exemptions, or new graph-foundation APIs.
- Do not fix unrelated warnings, formatting churn, failing tests, or dependency metadata outside the listed scope.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-ui --test node_graph_operator_smoke` -> expected exit 0.

If time permits, the Executor MAY also run and document:

- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The smoke cannot be implemented entirely within `crates/editor-ui/tests/node_graph_operator_smoke.rs`, the `crates/editor-ui/Cargo.toml` `[dev-dependencies]` entry, and the narrow `Cargo.lock` metadata update allowed in this packet.
- The test appears to require any production source edit, production dependency, workspace manifest edit, feature flag, architecture-lint exemption, or change to `NodeGraphWidget`, `VizAdapter`, `OperatorGraph`, graph-foundation, cad-core, material graph, animation graph, script graph, renderer, runtime, schema, script, documentation, or CI files.
- Cargo wants to change `Cargo.lock` beyond the minimal editor-ui-to-cad-core dev-dependency metadata described above, including external package additions, removals, or version changes.
- The architecture lint gate, if run, rejects the editor-ui dev-dependency on `rge-cad-core` and resolving that rejection would require any change outside this packet's MAY-edit scope.
- The existing public `NodeGraphWidget` model does not expose node display names, node kinds, edge labels, edge endpoints, or counts strongly enough for the smoke to assert them without changing production APIs.
- The required verification gate fails for reasons unrelated to this task and the fix would require edits outside the MAY-edit or MAY-add scope.
- Existing uncommitted changes in any in-scope file make it unclear how to proceed without overwriting someone else's work.

## Planner Notes

Use `crates/editor-ui/tests/node_graph_material_smoke.rs` and `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs` as local patterns for the cross-crate smoke shape, but keep the assertions CAD-specific. The intended graph is small: create two distinct cuboid `OperatorNode`s, create a Boolean union `OperatorNode`, connect cuboid-left to Boolean port `0` and cuboid-right to Boolean port `1`, pass `&graph` to `NodeGraphWidget::model_from`, and assert the adapter-provided counts, node labels/kinds, edge endpoints, and `input[0]`/`input[1]` labels survive in the editor-ui model.

The production dependency direction remains clean because only the integration test target names `rge-cad-core`. The existing `crates/editor-ui/src/widgets/node_graph.rs` design is already generic over `&dyn VizAdapter`; this task should validate that bridge, not specialize it for CAD.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-62
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
