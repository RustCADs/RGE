# Task Packet

DISPATCH_ID: ISSUE-403
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_09-23-42+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- crates/editor-actions/src/lib.rs
- crates/editor-actions/src/action.rs
- crates/editor-actions/src/bus.rs
- crates/editor-actions/src/undo_stack.rs
- crates/editor-shell/src/lifecycle/mod.rs
- crates/editor-shell/src/lifecycle/open_request.rs
- crates/editor-shell/src/lifecycle/save_request.rs
- crates/editor-shell/src/render_path.rs
- crates/cad-projection/src/lib.rs
- crates/cad-projection/src/render_adapter.rs
STATUS: OPEN

## Goal

Perform a read-only CAD/CommandBus first-mutation preflight and recommend the
single smallest coherent editor/CAD mutation boundary for human approval. The
executor must answer Q1-Q5 from current local source, record the recommendation
as `NEEDS_HUMAN_RECORDED`, and must not implement a CAD mutation or append task
150.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`

### MUST NOT edit
- `crates/**`
- `editor/**`
- `kernel/**`
- `runtime/**`
- `tools/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/*.schema.json`
- `.ai/dispatch.verify.ps1`
- `*.ps1`
- `ai_handoffs/templates/**`
- source files
- test files
- schemas
- workflows
- scripts
- architecture-lint rules or config
- routing behavior
- plugin runtime, discovery, loading, or command execution behavior
- clipboard behavior
- shortcut remapping, persistence, conflict, or fatal-policy behavior
- camera or viewport-navigation behavior
- CAD, projection, CommandBus, undo, dirty-state, save-mark, open, save, or load behavior
- unrelated existing handoff, audit-log, or generated artifacts

### MAY add new files
- `ai_handoffs/ISSUE-403_EXEC_*.md`
- `ai_handoffs/ISSUE-403_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-403/**`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files
- `crates/**`
- `editor/**`
- `kernel/**`
- `runtime/**`
- `tools/**`
- `.github/workflows/**`
- schemas
- scripts
- source files
- test files
- Cargo files
- ADR files
- architecture-lint files
- task 150 or any other implementation-task entry

## Deliverables

- An EXEC packet that documents the read-only audit and answers Q1-Q5:
  - Q1: Current `CommandBus` public contract and `Action` context.
  - Q2: Current authority for CAD projection, selected-CAD bounds,
    open/save/load, undo, dirty state, and save marks.
  - Q3: Candidate first mutations that are possible, plus which candidates
    require wider product or architecture decisions.
  - Q4: The exact human decision needed before implementation.
  - Q5: The single smallest coherent recommended first mutation.
- A final `.ai/dispatch.tasks.md` append containing exactly one marker line:
  `NEEDS_HUMAN_RECORDED: 2026-06-15 - CAD/CommandBus first-mutation recommendation`.
- Immediately after that marker, a five-part `Recommendation for human approval`
  block with these exact part labels: `Proposed mutation`, `Exact edit surface`,
  `Risks`, `Verification`, and `Why smallest coherent boundary`.
- Optional concise status/history updates in the allowed governance docs above,
  only if they help a human find the recommendation.

## Acceptance Criteria

- The executor performs analysis only. No source, test, Cargo, workflow,
  script, schema, routing, plugin, clipboard, shortcut-policy, camera, CAD,
  projection, CommandBus, undo/dirty, save-mark, open, save, or load behavior is
  changed.
- Queue and already-filed task claims use only the dispatcher-provided snapshot
  embedded in the ISSUE-403 issue body or this TASK packet. The executor must
  not call `gh`, use a browser, use the network, or query live GitHub state for
  those claims.
- Q1-Q5 are answered with current source references, not stale task-brief
  assumptions.
- Negative source-state claims include falsifying `rg` searches where
  practical, with the command and observed result recorded.
- `.ai/dispatch.tasks.md` has exactly one appended
  `NEEDS_HUMAN_RECORDED: 2026-06-15 - CAD/CommandBus first-mutation recommendation`
  marker for this dispatch, followed by the required five-part
  `Recommendation for human approval` block.
- The recommendation block identifies one proposed mutation or explicitly
  states that no single coherent first mutation can be recommended without a
  broader human decision. In both cases, it must not append task 150.
- No `150.` task heading, implementation task, or feature follow-up is added.
- The final diff is limited to the allowed governance-doc files and generated
  ISSUE-403 dispatch artifacts.

## Current-State Claims: Falsification Required

This TASK packet makes no negative current-source-state claim as a premise for
the recommendation. The executor owns all current-state conclusions and must
attach falsifying searches for negative claims in the EXEC packet.

- Claim: GitHub queue and already-filed autonomous issue evidence for this
  dispatch is provided by the dispatcher snapshot embedded in the ISSUE-403
  issue body.
  Falsifying search: no source search applies; the executor must cite the
  dispatcher snapshot text and must not replace it with live `gh`, browser,
  network, or GitHub API output.

## Constraints / Non-Goals

- This is a docs/source-read-only preflight and recommendation task. It is not a
  CAD implementation task.
- Do not add an implementation task, task 150, task 151, or any self-rearm
  feature follow-up. The required terminal action is a `NEEDS_HUMAN_RECORDED`
  recommendation for human approval.
- Do not modify or prototype `CommandBus`, `Action`, `CadGraph`,
  `CadProjection`, editor shell lifecycle, projection caches, selection state,
  open/save/load, undo/dirty, save-mark, routing, plugin, clipboard, camera, or
  viewport behavior.
- Do not run live GitHub discovery. Use the dispatcher-provided snapshot for
  queue/already-filed evidence.
- Do not treat missing manual `_REVIEW_` or `_CLOSEOUT_` packets as evidence of
  an unfinished orchestrator dispatch.
- If canonical verification or a correction round asks for a source fix outside
  this scope, halt and record `NEEDS_HUMAN`; do not expand the audit into a
  source-fix dispatch.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- Before and after edits:
  `rg -n "^148\\.|^149\\.|^150\\.|^NEEDS_HUMAN_RECORDED|Recommendation for human approval" .ai/dispatch.tasks.md`
- Source-read audit greps, at minimum:
  `rg -n "CommandBus::submit|pub fn submit|trait Action|ActionResult|MergeOutcome|undo\\(|redo\\(|mark_saved|is_dirty" crates/editor-actions/src`
- Source-read audit greps, at minimum:
  `rg -n "submit_action|undo_command|redo_command|mark_saved_command|command_bus\\(|replace_world|handle_open_request|handle_save_request|selected_cad_scene_bounds|render_mesh_for" crates/editor-shell/src crates/cad-projection/src`
- Status-doc cross-check:
  `rg -n "ISSUE-402|ISSUE-403|task 148|task 149|task 150|CommandBus|CAD/CommandBus|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md Status.md HANDOFF.md plans/BASELINE.md change.md`
- `git diff --name-only`
- `git diff --check`
- `.\new-handoff.ps1 -Finalize -PacketPath` against the actual EXEC packet path
  with `-DryRun`

No cargo build, cargo test, formatting, or architecture-lint command is required
for this audit-only task unless the orchestrator runs the canonical verification
gate automatically. If it does, record the result without broadening scope.

## Halt Conditions

The Executor MUST halt without implementing anything and request fresh guidance
if any of the following occur:

- Answering Q1-Q5 would require editing any MUST-NOT path or changing source,
  tests, Cargo, workflows, scripts, schemas, routing, plugin, clipboard,
  shortcut-policy, camera, CAD, projection, CommandBus, undo/dirty, save-mark,
  open, save, or load behavior.
- The dispatcher-provided GitHub snapshot cannot be cited without live `gh`,
  browser, network, or GitHub API access.
- The final `.ai/dispatch.tasks.md` append cannot be made with exactly one
  ISSUE-403 `NEEDS_HUMAN_RECORDED` marker and the required five-part
  recommendation block.
- More than one coherent implementation task would be needed before a first CAD
  mutation could be safe; record that as the human decision in the
  recommendation rather than appending task 150.
- A correction packet or verification failure pressures the executor to fix
  source or tests outside this read-only recommendation scope.

## Planner Notes

Dispatcher snapshot evidence supplied with ISSUE-403: generated by
`Invoke-AiDispatchAuto.ps1` at `2026-06-15T09:23:15.7729314+03:00`; repo
`RustCADs/RGE`; no open `ai-dispatch` issues before ISSUE-403 was created; no
open failed autonomous issues; autonomous issues already filed through closed
ISSUE-402/task 148. The executor must use that snapshot for GitHub queue and
already-filed evidence.

Task 149 in `.ai/dispatch.tasks.md` is the controlling local brief. It requires
a recommendation-only CAD/CommandBus preflight, exactly one
`NEEDS_HUMAN_RECORDED` marker, the five-part `Recommendation for human approval`
block, and no task 150.

The executor should compare at least these candidate first-mutation families
before recommending one: adding a CAD primitive, deleting selected CAD geometry,
transforming selected CAD geometry, editing primitive parameters, clearing or
replacing the CAD graph, and treating open/new/save state transitions as the
first mutation boundary. The recommendation must explain why the selected
boundary is smaller and more coherent than the alternatives.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-403
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
