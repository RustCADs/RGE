# Task Packet

DISPATCH_ID: ISSUE-43
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_08-53-38+0300
RELATED_FILES:
- crates/cad-projection/tests/face_picking_smoke.rs
STATUS: OPEN

## Goal

Implement GitHub issue #43 by adding one focused integration smoke test proving `CadProjection::pick_face` preserves upstream Cuboid face identity when the projected entity is bound to a non-identity `TransformOp` root. The dispatch should cover the picker path, not only face-ID lookup, by ray-picking a transformed Cuboid face and asserting the returned `BRepFaceId` is the upstream Cuboid face ID under the entity owner.

## Scope

### MAY edit
- `crates/cad-projection/tests/face_picking_smoke.rs`

### MUST NOT edit
- Any file other than `crates/cad-projection/tests/face_picking_smoke.rs`.
- Any production code in `crates/cad-core`, `crates/cad-projection`, or any other crate.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`, status files, or existing handoff packets.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-43_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, integration test files, fixtures, docs, schemas, scripts, Cargo entries, architecture-lint files, or generated artifacts.
- No handoff packets other than the protocol-required `ISSUE-43_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add exactly one new additive integration test to `crates/cad-projection/tests/face_picking_smoke.rs`.
- The test builds a `CadGraph` containing `CuboidOp -> TransformOp`, with `OperatorNode::Transform` set as the graph root.
- The `TransformOp` is non-identity. A translation-only transform is acceptable and preferred for a simple deterministic ray, for example `translation: [3.0, -2.0, 1.0]` with default rotation and scale.
- The projected entity is spawned from, and therefore bound to, the Transform root node, not the upstream Cuboid node.
- The entity's `BRepHandle.brep_owner` is set to `Some(ENTITY_OWNER)`.
- The test calls `CadProjection::pick_face` with a ray that intersects a transformed Cuboid face.
- The pick asserts the returned `entity`, `owner`, and `face_id`, where `face_id` is `BRepFaceId::for_cuboid_face(ENTITY_OWNER, CuboidFaceTag::PosZ)` if the ray targets the transformed top face.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- `crates/cad-projection/tests/face_picking_smoke.rs` is the only source or test file changed.
- The new smoke is additive: existing tests remain present and their intended assertions are not weakened or deleted.
- The test imports `TransformOp` only if needed and does not introduce new dependencies.
- The new test constructs a non-identity `TransformOp`; an identity transform is not acceptable.
- The graph root and spawned `BRepHandle` entity are the Transform node, proving the picker resolves through the projected Transform output rather than directly through the Cuboid node.
- The ray is aimed at transformed geometry coordinates, not at the untransformed Cuboid position by accident.
- `CadProjection::pick_face` returns `Some(FacePick)` for the transformed face.
- The returned `FacePick.entity` equals the Transform-root entity.
- The returned `FacePick.owner` equals `ENTITY_OWNER`.
- The returned `FacePick.face_id` equals the upstream Cuboid face ID minted under `ENTITY_OWNER`, proving Transform face labels survive the picker path.
- No runtime behavior, public APIs, production code, manifests, scripts, docs, or unrelated tests are changed.

## Constraints / Non-Goals

- This is a test-only dispatch.
- Do not modify `CadProjection::pick_face`, projection caching, label propagation, `TransformOp`, topology resolution, or any production/runtime implementation to satisfy the new smoke.
- Do not add a new test file; extend the existing `face_picking_smoke.rs` integration test only.
- Do not broaden the issue into Transform lookup coverage; `transform_brep_face_lookup_smoke.rs` already covers direct triangle face-ID lookup. This dispatch specifically covers `CadProjection::pick_face`.
- Do not change Cargo manifests, dependency lists, fixtures, generated assets, architecture lints, or repository automation.
- Do not commit or push.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-cad-projection` -> expected exit 0.
- `cargo test -p rge-cad-projection --test face_picking_smoke -- --nocapture` -> expected exit 0, with the new Transform picker smoke passing.
- `git diff -- crates/cad-projection/tests/face_picking_smoke.rs` -> expected diff is limited to one additive Transform picker smoke plus any necessary import and test-list comment updates in the same file.
- `git diff --check -- crates/cad-projection/tests/face_picking_smoke.rs` -> expected exit 0.
- `git status --short` -> expected implementation changes are limited to `crates/cad-projection/tests/face_picking_smoke.rs`, plus the protocol-required execution report packet and optional finalize-generated sidecar.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying production code and request fresh guidance if any of the following occur:

- The requested test cannot be implemented within `crates/cad-projection/tests/face_picking_smoke.rs` only.
- Passing the new smoke appears to require changing `CadProjection::pick_face`, `TransformOp`, topology resolution, projection cache behavior, face-label propagation, or any other production/runtime code.
- The picker resolves only when the entity is spawned from the upstream Cuboid node rather than the Transform root node.
- A non-identity Transform-root pick cannot be made deterministic without adding dependencies, fixtures, or broader test infrastructure.
- Any required edit outside the allowed file appears necessary.
- Formatting or tests fail for reasons unrelated to the requested additive smoke and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #43. Revision number: 0. There is no prior Claude gate result. Existing projection coverage already proves Transform-root triangle lookup can resolve upstream Cuboid face IDs; this dispatch intentionally adds the missing ray-driven `CadProjection::pick_face` smoke in the existing face-picking integration test file.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-43
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
