# Execution Report

DISPATCH_ID: ISSUE-220
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-27_02-50-57+0300
RELATED_FILES:
- ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md — TASK packet consumed (audit-only scope; no source/test/doc/Cargo edits)
- ai_handoffs/ISSUE-220_EXEC_2026-05-27_02-50-57+0300.md — this packet (new audit report)
- AI_DISPATCH_AUTOMATION.md — orchestrator spec; orchestrator dispatch shape; GPU-test serialization gotcha §14.9
- ai_handoffs/AI_HANDOFF_PROTOCOL.md — packet protocol; sidecar/finalize rules
- .ai/dispatch.tasks.md — task #69 + common #62-#71 constraints
- plans/PLAN.md / plans/IMPLEMENTATION.md / plans/BASELINE.md — current architecture + phase order + Phase 6 §6.3 + §13.2 baselines
- Status.md / HANDOFF.md / change.md — current state, v0 cert posture, Phase 9 backlog
- docs/§18/GFX_RENDER_TIER.md — render-tier §18 companion (status text drifted vs current source — see reconciliation §G)
- crates/gfx/** — wgpu substrate (`GfxContext`, frame, target, surface, pipelines, frame_graph, pso_cache, intent_adapter, plugin_adapter)
- crates/editor-shell/** — render-input boundary, render path, perf harness, visual smoke, lifecycle
- editor/rge-editor/** — editor binary + GLB watcher (CAD-cuboid demo OR `--glb` render-only mode)
- crates/editor-egui-host/** — egui+egui_dock host (`EguiHost`, `InspectorHandoff`, `EditorTabViewer`)
- crates/gfx-ir/** — 4-line stub
- crates/brep-render/** — CPU-side flat-shaded mesh substrate (no GPU dep)
- crates/components-render/** — ECS render components (Camera, Light, MeshHandle, etc.; state-only)
- crates/cad-projection/src/render_adapter.rs — CAD-projection → `RenderMesh` adapter (one-call, no cache)
- ai_handoffs/MAIN-RENDER-POSTDEPTH-GATEA-001_EXEC_2026-05-14_02-30-00+0300.md — post-depth Gate A recorder-host CLOSED
- ai_handoffs/MAIN-RENDER-VISUAL-FIDELITY-002_EXEC_2026-05-14_03-05-00+0300.md — depth-overlay smoke broadening (test-only)
- ai_handoffs/MAIN-ORDERED-JOB-004-FRAMEGRAPH-AUDIT_EXEC_2026-05-14_15-04-27+0300.md — frame-graph substrate audit (CLOSED, Job 5 SKIPPED)
- ai_handoffs/POSTV0-EDITOR-SHELL-PERF-HARNESS-001_EXEC_2026-05-14_21-51-40+0300.md — non-winit perf harness shipped at `f8b8ed4`
- ai_handoffs/ISSUE-210_EXEC_2026-05-26_17-00-00+0300.md — Phase 9 freeze-validity audit
- ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md — Phase 9 abstraction-pain audit
- ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md — Phase 9 invalidation-economics audit (`does_not_warrant_next_dispatch`)
- ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.md — Phase 9 reflection-scale audit (`still_untriggered`)
- ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-25-31+0300.md — Phase 9 async-orchestration audit (`pressure_deferred`)
- ai_handoffs/ISSUE-218_EXEC_2026-05-27_01-13-01+0300.md — Phase 9 compile-time audit (`pressure_deferred`)
- ai_handoffs/ISSUE-219_EXEC_2026-05-27_02-19-41+0300.md — Phase 9 editor-usability audit (`substrate_prerequisite_triggered` — docs-only follow-up scoped)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md` (audit-only; MAY edit
= none; deliverables include a 5-field decision block plus eight evidence
sections; this is delegated-main smoke batch task 8 of 10, mapping to
`.ai/dispatch.tasks.md` task #69 — see §A).

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch. Codex's advisory
pre-flight checklist (round-0 read-only audit aid) appears in the
orchestrator prompt and was treated per its own disclaimer as
informational — it does not expand or relax the TASK packet's scope.
Pn / Vn IDs from the checklist are preserved when referenced (e.g. P3,
V4).

## What I Changed

### Source

- None. Audit-only dispatch per `ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:62-65`.

### Tests

- None.

### Docs

- None. This new EXEC packet is the only tracked artifact added by this
  dispatch. No `plans/**`, `Status.md`, `HANDOFF.md`, `change.md`,
  `docs/**`, `crates/**`, `editor/**`, `kernel/**`, `runtime/**`,
  `tools/**`, `Cargo.toml`, `Cargo.lock`, `.github/**`, `.ai/**`, or
  task-brief edits.

## Per-File Summary

This dispatch wrote a single new file:
`ai_handoffs/ISSUE-220_EXEC_2026-05-27_02-50-57+0300.md` (this packet).
Skipped sidecar finalization — the TASK packet permits `EXEC_*.meta.json`
under MAY ADD, but no sidecar was forbidden either. The orchestrator's
canonical post-execute step auto-finalises the EXEC sidecar where
allowed; I deliberately defer that step here so the controller can
finalise. If the orchestrator's auto-finalise has already run by the
time control reads this, the sidecar is acceptable; if not, it is
expected absent and not a halt condition.

---

## §A — Phase 9 Context (delegated-main smoke batch 8 of 10)

Phase 9 of `plans/IMPLEMENTATION.md` (`plans/IMPLEMENTATION.md:589-605`,
"Phase 9 — Production Pressure (Weeks 48+)") lists eight evaluation axes:
**§0.6 freeze validity** · **abstraction pain** · **invalidation
economics** · **reflection scale** · **async orchestration** · **compile
times** · **editor usability** · **GPU pressure**. The dispatch.tasks.md
delegated-main smoke batch maps each to tasks #62–#69, plus task #70
(persistent kernel cavities) and task #71 (synthesis):

- `.ai/dispatch.tasks.md:7290-7296` — task #62 freeze-validity.
- `.ai/dispatch.tasks.md:7298-7303` — task #63 abstraction-pain.
- `.ai/dispatch.tasks.md:7305-7310` — task #64 invalidation-economics.
- `.ai/dispatch.tasks.md:7312-7318` — task #65 reflection-scale.
- `.ai/dispatch.tasks.md:7320-7325` — task #66 async-orchestration.
- `.ai/dispatch.tasks.md:7327-7332` — task #67 compile-time.
- `.ai/dispatch.tasks.md:7334-7338` — task #68 editor-usability.
- `.ai/dispatch.tasks.md:7340-7345` — task #69 **GPU-pressure axis (this dispatch)**.
- `.ai/dispatch.tasks.md:7347-7352` — task #70 persistent kernel cavities.
- `.ai/dispatch.tasks.md:7354-7359` — task #71 synthesis.
- `.ai/dispatch.tasks.md:7361-7376` — common #62-#71 constraints (read-only
  audit; MAY add only this dispatch's own handoff packets, sidecars,
  queue log, and optional ignored `.ai/dispatch-*` scratch; decision
  block enumerated; verification = `git diff --check` + `git status
  --short --untracked-files=all` + static no-source inspection).
- Task #69 specifically directs the auditor to evaluate `gfx`,
  `frame-graph`, `render-handoff`, `editor-shell` render path, and
  baseline docs, and to **decide whether the next GPU task is
  measurement, integration, docs reconciliation, or no-op** (with
  `NEEDS_HUMAN` as the arbitration fall-back) — quoted verbatim at
  `.ai/dispatch.tasks.md:7343-7345`.

This dispatch is run via the orchestrator (Codex plans / Claude executes
/ Codex controls — `AI_DISPATCH_AUTOMATION.md:42-65` + `:163-225`). Per
`AGENTS.md` "Orchestrator dispatch vs. manual protocol" and
`AI_DISPATCH_AUTOMATION.md:13-37` (table of contents) / §6 / §13 +
`AI_DISPATCH_AUTOMATION.md:1058-1095` (queue-runner publish gate), the
orchestrator writes **only** TASK + EXEC packets (and an optional
CORRECT under correction-round); no `_REVIEW_` or `_CLOSEOUT_` packets
are expected. The Codex control review is stored at
`.ai/dispatch-ISSUE-220/codex.control.round0.json`, not as a handoff
packet — see `AI_DISPATCH_AUTOMATION.md:486-513`. Missing `_REVIEW_` /
`_CLOSEOUT_` packets is therefore the contract, not evidence of an
unfinished dispatch (Codex pre-flight aid P2 explicitly flags this).

The packet protocol's footer + filename rules govern packet structure
(`ai_handoffs/AI_HANDOFF_PROTOCOL.md:128-169` + `:171-260`); sidecar
generation requires the packet to be finalised (`ai_handoffs/AI_HANDOFF_PROTOCOL.md:262-328`).

### Phase 9 context — current GPU/render posture vs prior framing

| Document | Citation | Current GPU/render claim |
|---|---|---|
| `plans/PLAN.md` §4.1 | `plans/PLAN.md:734-748` | Phase 6 (m20–28) substrate; **v1.0 = "industrial-quality realtime editor."** Photoreal parity post-v2. |
| `plans/PLAN.md` §1.5.2 | `plans/PLAN.md:233-237` | Standard render-thread / sim-thread separation with double-buffered scene state; render thread sees immutable `(ECS_tick_N, CadCheckpointId_N)` snapshot. |
| `plans/PLAN.md` §1.13 (recovery table) | `plans/PLAN.md:559-573` | gfx + material-graph + asset-streaming failure classes are recoverable / snapshot-recoverable; render device lost beyond recovery is session-fatal. |
| `plans/IMPLEMENTATION.md` Phase 6 §6.1 | `plans/IMPLEMENTATION.md:428-444` | `gfx` minimal: wgpu init, frame-graph, mesh, transforms, PBR-lite. **NOT** in scope: bindless, TSR, Nanite-like, virtualised geometry, Lumen, VSM. |
| `plans/IMPLEMENTATION.md` Phase 6 §6.2 | `plans/IMPLEMENTATION.md:449-454` | Render-snapshot separation **closed** as runtime-integrated single-threaded proxy per ADR-117. |
| `plans/IMPLEMENTATION.md` Phase 6 §6.3 exit criteria | `plans/IMPLEMENTATION.md:467-473` | All four CLOSED (60fps simple-scene Gate A CLOSED 2026-05-11 recorder host + post-depth 2026-05-14; editor idle Gate B CLOSED for CPU-idle interpretation; Gate C CLOSED for ADR-117 boundary invariant; 100 material instances share one PSO via PSO cache 54cec89). |
| `plans/IMPLEMENTATION.md` Phase 6 frame-graph umbrella closeout | `plans/IMPLEMENTATION.md:473` | "substrate complete + cross-frame composable"; pass-record-site integration deferred until consumer pressure surfaces. |
| `plans/IMPLEMENTATION.md` Phase 9 evaluation axes | `plans/IMPLEMENTATION.md:589-605` | Eight axes listed including **GPU pressure — VRAM residency under real scenes**; entropy-metric reality-check, not new substrate by default. |
| `plans/BASELINE.md` §6.3 Gate A | `plans/BASELINE.md:222-248` | Recorder-host-only `min-of-3 P95 = 0.112 ms` pre-depth (2026-05-11) / `0.122 ms` post-depth (2026-05-14); explicit scope-constraint table — NOT universal / NOT vendor parity / NOT cold-start / NOT thermal / NOT CI / NOT editor-shell e2e. |
| `plans/BASELINE.md` §6.3 post-v0 reconciliation | `plans/BASELINE.md:250-251` | 2026-05-23 ISSUE-118 supersedes the "non-winit editor-shell perf harness still deferred" wording — the harness landed at commit `f8b8ed4` as `crates/editor-shell/src/render_frame_e2e_perf.rs`. Threshold pinning remains a future explicitly-authorized scope. |
| `plans/BASELINE.md` §13.2 Gate B | `plans/BASELINE.md:187-218` | CPU-idle empty-shell baseline P95 = 0.000047 ms (2026-05-11); loaded-editor re-measure deferred — flagged as scope-limited. |
| `plans/BASELINE.md` editor-usability preflight | `plans/BASELINE.md:541-639` | Editor renders + picks but cannot save/load/spawn through user input; substrate-first architecture; "substrate complete in isolation, no user-input path connects to it, defer." Live-inspector wiring preflight `plans/BASELINE.md:643-753` shipped a 7-component breakdown — host now exists at `crates/editor-egui-host/`. |
| `Status.md` 2026-05-23 ISSUE-118 snapshot | `Status.md:7` | Editor-shell `render_frame` perf harness shipped at `f8b8ed4`; v0 cert at `6aaf7f1` unchanged. |
| `Status.md` 2026-05-14 v0 RELEASE CERTIFIED | `Status.md:9` | "Phase 6 frame-graph + render substrate complete + production-consumed via `editor-shell::render_path:312`." |
| `HANDOFF.md` "Current state" + Phase 6 §6.3 status | `HANDOFF.md:83-92` + `HANDOFF.md:1655` | 9-lint architecture gate PASS; Phase 6 §6.3 all four exit criteria CLOSED; Gate A/B/C scope footnotes carried inline. |
| `HANDOFF.md` "Persistent gaps" / "Pressure-driven candidates" | `HANDOFF.md:1572-1581` + `HANDOFF.md:1641-1656` | Listed pressure-driven candidates as of v0 cert; the editor-shell mock-event-loop perf harness deferral is **resolved** by `f8b8ed4` (ISSUE-118). Other Phase 6 follow-ups (4 kernel doctrine-cavity implementations; `compile.rs` legibility refactor) are non-GPU. |
| `change.md` recent entries | `change.md:1632` (ISSUE-118) / `change.md:1634` (ISSUE-122) / `change.md:1636` (repo transfer) | Last GPU-related change is the 2026-05-23 ISSUE-118 BASELINE.md reconciliation; recent commits `6df1bef`, `93b7fa6`, `2650320`, `b3bfb76`, `283cc19` are Phase 9 audit reports + their salvages, none touching gfx / editor-shell. |

Phase 9 GPU/render context is therefore **post-v0**, **substrate-complete on
recorder host**, **explicitly constrained-certified rather than universal**,
**without a real production consumer pressuring beyond a single-cuboid /
render-only-glTF workload**, and with **no current source/baseline
disagreement that gates user-visible work**. This must be distinguished
from:

- **Historical roadmap ambition** — `plans/PLAN.md:743-744` keeps Phase 5
  Lumen-equivalent / VSM / TSR / Nanite-like as **reach product, post-v2**.
  Selected features competitive at Phase 5; not gated at v1.
- **Release certification** — `V0_RELEASE_CERTIFICATION.md` (commit
  `6aaf7f1`, 2026-05-14) certified v0 against the cheap-gate set;
  expensive gates (post-depth Gate A P95 + 1-hour memory soak) skipped
  with documented rationale (`Status.md:9`).
- **Benchmark policy / dispatch doctrine** — `AI_DISPATCH_AUTOMATION.md:14.9`
  (`AI_DISPATCH_AUTOMATION.md:773-833`) is the GPU-test serialization
  guidance, mandatory for any new GPU test crate. Not GPU-pressure
  itself, but constrains how a measurement follow-up would be written if
  one were warranted.
- **Editor usability pressure** — `plans/BASELINE.md:541-639` is the
  ISSUE-219 substrate-prerequisite axis, separate from GPU pressure.

---

## §B — gfx substrate (`crates/gfx`)

| File | LoC | Public surface relevant to GPU pressure |
|---|---:|---|
| `crates/gfx/Cargo.toml` | 62 | wgpu (workspace pin) + glam 0.30 + pollster 0.4 + winit + blake3 + serde + thiserror + tracing; deps on `rge-kernel-diagnostics` / `-plugin-host` / `-graph-foundation` / `brep-render` / `material-runtime`. Dev-dep on `naga` for `tests/wgsl_parse_smoke.rs`. |
| `crates/gfx/src/lib.rs` | 149 | Failure class: recoverable (`crates/gfx/src/lib.rs:3`); `#![forbid(unsafe_code)]` (`crates/gfx/src/lib.rs:57`); modules `buffer` / `camera` / `context` / `frame` / `frame_graph` / `intent_adapter` / `light` / `lit_mesh_pipeline` / `material` / `mesh` / `mesh_pipeline` / `pipeline` / `plugin_adapter` / `pso_cache` / `surface` / `target` / `transform` / `vertex` / `vertex_lit` (`crates/gfx/src/lib.rs:59-77`); per-binary `test_lock::guard()` mutex with poisoned-recovery (`crates/gfx/src/lib.rs:108-148`). |
| `crates/gfx/src/context.rs` | 191 | `GfxContext` (`crates/gfx/src/context.rs:54-64`) owns `wgpu::Instance/Adapter/Device/Queue` + `RefCell<PipelineCache<wgpu::RenderPipeline>>`; `new_headless()` via `pollster::block_on(init_async())` (`crates/gfx/src/context.rs:78-123`); `pso_cache()` / `pso_cache_mut()` accessors (`crates/gfx/src/context.rs:175-190`); `adapter()` accessor exposed for surface negotiation per sub-δ.1.A (`crates/gfx/src/context.rs:152-166`). |
| `crates/gfx/src/frame.rs` | 255 | `FrameRecorder<'ctx>` (`crates/gfx/src/frame.rs:37-93`) + `ReadbackBuffer` (`crates/gfx/src/frame.rs:97-...`); `FrameError::{Readback, InvalidClearColor}` (`crates/gfx/src/frame.rs:17-24`); records exactly one triangle pass via `render_triangle` (`crates/gfx/src/frame.rs:56-83`) — no mesh, no lit-mesh, no depth, no consumer-supplied vertex layout. |
| `crates/gfx/src/target.rs` | 173 | `HeadlessTarget` (`crates/gfx/src/target.rs:30-77`) — fixed `wgpu::TextureFormat::Rgba8Unorm` + `RENDER_ATTACHMENT \| COPY_SRC`; size-bounded `1..=8192` per axis; `TargetError::InvalidSize(w, h)` (`crates/gfx/src/target.rs:15-19`). |
| `crates/gfx/src/surface.rs` | 273 | `SurfaceContext` (`crates/gfx/src/surface.rs:83-95`) wraps `wgpu::Surface<'static>` + `SurfaceConfiguration` + `Arc<Window>`; `SurfaceError::{CreateSurface, NoCompatibleFormat}` (`crates/gfx/src/surface.rs:54-68`). |
| `crates/gfx/src/pso_cache.rs` | 815 | `ShaderHash` (`crates/gfx/src/pso_cache.rs:80-105`) — opaque 32-byte BLAKE3; `VertexLayoutDescriptor` (`crates/gfx/src/pso_cache.rs:119`); `DepthStateKey` (`crates/gfx/src/pso_cache.rs:18-23`); `PsoKey` (`crates/gfx/src/pso_cache.rs:22-30`); `PipelineCache<T>` generic-over-T memoization substrate. NON-GOALS explicit: no shader graph, no Naga link, no runtime frame loop, no snapshot integration, no eviction policy (`crates/gfx/src/pso_cache.rs:34-61`). |
| `crates/gfx/src/pipeline.rs` | 296 | `TrianglePipeline` (`crates/gfx/src/pipeline.rs:68-...`) with `Arc<wgpu::RenderPipeline>` per cache hit. `TRIANGLE_WGSL` exposed `pub const` (`crates/gfx/src/pipeline.rs:24-39`). `new(...)` routes through `ctx.pso_cache().borrow_mut()` (`crates/gfx/src/pipeline.rs:91-94`). |
| `crates/gfx/src/mesh_pipeline.rs` | 393 | `MeshPipeline` + `MeshPipelineError::Wgsl` + `MESH_WGSL` `pub const` (`crates/gfx/src/mesh_pipeline.rs:33-63`). Unlit color-vertex pipeline at `@group(0) @binding(0) Transform`. **No production consumer outside gfx tests** — see §F symbol search. |
| `crates/gfx/src/lit_mesh_pipeline.rs` | 1412 | `LitMeshPipeline` + `LitMeshPipelineError` + `LIT_MESH_WGSL` `pub const`. Lambert+Phong at `@group(0) Camera`, `@group(1) Light`, `@group(2) Material`. v0 shader simplification: `view_dir = normalize(-world_pos)` (camera at origin) per `crates/gfx/src/lit_mesh_pipeline.rs:26-29` + `:165`. `record_lit_mesh_pass(...)` is the documented entry point. `new_with_depth(..., Some(DepthStateKey{...}))` is the depth-attached variant per Phase 6 sub-β. |
| `crates/gfx/src/material.rs` | 458 | `Material` with UBO (`base_color + phong`) + 2D texture (`Rgba8UnormSrgb`) + sampler at `@group(2)`; `Material::from_descriptor(&GfxContext, &MaterialDescriptor)` realises an intent (`crates/gfx/src/material.rs:...`). |
| `crates/gfx/src/mesh.rs` | 119 | `Mesh` (vertex buffer + optional index buffer) — `from_vertices` / `from_indexed`. |
| `crates/gfx/src/intent_adapter.rs` | 418 | `intent_to_pso_key(&MaterialDescriptor) -> PsoKey` total mapping; `build_pipeline_from_intent(&GfxContext, &MaterialDescriptor, &PipelineLayouts<'_>) -> Result<Arc<wgpu::RenderPipeline>, BuildIntentError>` routes through the GfxContext-owned cache; closes the §6.3 "100 material instances share one PSO" gate via `crates/gfx/tests/material_intent_pso_sharing.rs::one_hundred_identical_descriptors_share_one_pso`. |
| `crates/gfx/src/plugin_adapter.rs` | 471 | `GfxPlugin` + `GFX_PLUGIN_ID = "rge-gfx.headless-triangle-plugin"` per PLAN §10.4 dogfood rule. Lazy-build pattern; consumes `GfxContext` + `HeadlessTarget` from `PluginContext`. |

Test surface (per `crates/gfx/tests/`):
- `frame_graph_smoke.rs` — 3-pass shadow / scene / lighting analytical smoke; descriptor flow per ADR-118 D7 (`crates/gfx/tests/frame_graph_smoke.rs:1-28`).
- `frame_graph_umbrella_smoke.rs` — cross-frame freshness invariant; within-frame Arc dedup; `structural_hash` stability across recompile (`crates/gfx/tests/frame_graph_umbrella_smoke.rs:1-30`).
- `gate_a_simple_scene_60fps.rs` — release-only `#[ignore]` Gate A pre-depth perf gate (`crates/gfx/tests/gate_a_simple_scene_60fps.rs:192-193`).
- `gate_a_simple_scene_depth_60fps.rs` — release-only `#[ignore]` Gate A post-depth variant (`crates/gfx/tests/gate_a_simple_scene_depth_60fps.rs:243-244`).
- `lit_mesh_depth_overlay_smoke.rs` — broadened to 3 samples per region 2026-05-14 (`MAIN-RENDER-VISUAL-FIDELITY-002`); pixel-level depth-overlay verification on recorder host.
- `material_intent_pso_sharing.rs` — 4 tests; `one_hundred_identical_descriptors_share_one_pso` is the §6.3 PSO-sharing exit-gate test.
- `mesh_quad.rs` — 2 mesh-quad smokes via `MeshPipeline`.
- `plugin_adapter_smoke.rs` — 7 `GfxPlugin` canary tests (lazy-build + ContractViolation + multi-plugin isolation).
- `render_mesh_smoke.rs` — `LitMeshPipeline` + readback smoke.
- `headless_triangle.rs` — 3 `TrianglePipeline` smokes.
- `wgsl_parse_smoke.rs` — naga WGSL parse on the three embedded shaders.

**gfx substrate is feature-complete for Phase 6** at the cited line
surface. The crate is recoverable per its module declaration
(`crates/gfx/src/lib.rs:3`); the `failure-class` lint passes (per
`Status.md:9` cited 9-lint table). Nothing here is unwired stub code.

---

## §C — frame-graph substrate (`crates/gfx/src/frame_graph/**`)

| File | LoC | Substrate surface |
|---|---:|---|
| `crates/gfx/src/frame_graph/mod.rs` | 432 | `FrameGraph` (`crates/gfx/src/frame_graph/mod.rs:129-134`) wraps `Graph<PassNode, ()>` from `kernel/graph-foundation`; `add_pass(name, reads, writes: Vec<(ResourceId, ResourceClassDescriptor)>) -> Result<NodeId, FrameGraphError>` (`crates/gfx/src/frame_graph/mod.rs:174-203`); descriptor sidecar validation per ADR-118 D7. `FrameGraphError::{Graph, Compile, DescriptorMismatch}` (`crates/gfx/src/frame_graph/mod.rs:75-102`). |
| `crates/gfx/src/frame_graph/descriptor.rs` | 350 | `TextureDescriptor` (`crates/gfx/src/frame_graph/descriptor.rs:51-71`) + `BufferDescriptor` + `ResourceClassDescriptor` enum + `byte_size_estimate()`. No `label` / `mapped_at_creation` per ADR-118 D1. |
| `crates/gfx/src/frame_graph/pass.rs` | 110 | `PassNode { name, reads, writes }` (`crates/gfx/src/frame_graph/pass.rs:26-35`); `impl StableHash for PassNode` so `NodeId` is content-derived (`crates/gfx/src/frame_graph/pass.rs:49`). |
| `crates/gfx/src/frame_graph/resource.rs` | 116 | `ResourceId([u8; 16])` (`crates/gfx/src/frame_graph/resource.rs:18-33`) opaque caller-supplied id; `ResourceUsage` `#[non_exhaustive]` tag. v0 derives RAW dependencies only (`crates/gfx/src/frame_graph/resource.rs:46-50`). |
| `crates/gfx/src/frame_graph/resource_map.rs` | 358 | `ResourceMap { texture_map, buffer_map }` (`crates/gfx/src/frame_graph/resource_map.rs:43-51`); `build_resource_map(...)` (`crates/gfx/src/frame_graph/resource_map.rs:71-...`) is the per-frame ResourceId→Arc<wgpu::*> builder enforcing ADR-118 D4 acquire dedup. `#[serde(skip)]` on `descriptors` (`crates/gfx/src/frame_graph/compile/types.rs:88-90`) — wire-format unsupported by design; future ADR concern (`crates/gfx/src/frame_graph/resource_map.rs:10-14`). |
| `crates/gfx/src/frame_graph/texture_pool.rs` | 282 | `TexturePool` N=2 frames-in-flight per ADR-118 D4 (`crates/gfx/src/frame_graph/texture_pool.rs:50`); `AliasingGroupId(pub u32)` position-based identity (`crates/gfx/src/frame_graph/texture_pool.rs:44-45`); free-list keyed on `(TextureDescriptor, AliasingGroupId)` per ADR-118 D3 (`crates/gfx/src/frame_graph/texture_pool.rs:56-57`). |
| `crates/gfx/src/frame_graph/buffer_pool.rs` | 272 | `BufferPool` clean mirror of `TexturePool`; same N=2 ring; `AliasingGroupId` reused (`crates/gfx/src/frame_graph/buffer_pool.rs:46-49`). |
| `crates/gfx/src/frame_graph/compile/mod.rs` | 43 | Re-exports + algorithm sketch (`crates/gfx/src/frame_graph/compile/mod.rs:1-36`); Kahn topological sort + RAW-only dep adjacency + greedy aliasing assignment over `BTreeMap` order. |
| `crates/gfx/src/frame_graph/compile/algorithm.rs` | 512 | `compile_passes(&BTreeMap<NodeId, PassNode>, &BTreeMap<ResourceId, ResourceClassDescriptor>) -> Result<CompiledFrameGraph, CompileError>` (`crates/gfx/src/frame_graph/compile/algorithm.rs:28-...`). Self-loop filter (`crates/gfx/src/frame_graph/compile/algorithm.rs:63-65`). |
| `crates/gfx/src/frame_graph/compile/types.rs` | 190 | `ResourceLifetime { first_use, last_use }` + `AliasingGroup(pub Vec<ResourceId>)` + `CompiledFrameGraph { execution_order, resource_lifetimes, aliasing_groups, descriptors (serde-skipped) }`; `structural_hash() -> [u8; 32]` BLAKE3 over execution_order / lifetimes / groups (NOT descriptors) (`crates/gfx/src/frame_graph/compile/types.rs:156-189`). `AliasingGroup::max_descriptor` (`crates/gfx/src/frame_graph/compile/types.rs:51-60`). |
| `crates/gfx/src/frame_graph/compile/error.rs` | 18 | `CompileError::{Cycle, UnwrittenResource(ResourceId)}` (`crates/gfx/src/frame_graph/compile/error.rs:7-18`). |

**Frame-graph substrate explicit NON-GOALS** are written into the
module-level docs (`crates/gfx/src/frame_graph/mod.rs:25-52`):

> No GPU resource allocation [...] **no `FrameRecorder` / pipeline integration. The substrate stands alone**.

The substrate's only **production consumer** is `editor-shell::render_path`
which uses `FrameGraph::add_pass` / `compile` + `TexturePool` + `BufferPool`
+ `build_resource_map` to acquire the per-frame depth attachment used by
the `LitMeshPipeline` post-sub-β path. The `gfx::FrameRecorder` triangle
recorder is bypassed by the editor (see §D below). This is by design and
matches `crates/gfx/src/frame_graph/resource_map.rs:20-25`:

> NO [`crate::FrameRecorder`] integration. NO [`crate::lit_mesh_pipeline::record_lit_mesh_pass`] signature change — that free function has no transient-resource consumer today.

Per the frame-graph audit `ai_handoffs/MAIN-ORDERED-JOB-004-FRAMEGRAPH-AUDIT_EXEC_2026-05-14_15-04-27+0300.md`
(CLOSED 2026-05-14, Job 5 SKIPPED per Planner-accepted recommendation —
`Status.md:13` Job 4/5 line) and the BASELINE.md frame-graph umbrella
closeout (`plans/IMPLEMENTATION.md:473`), the substrate is **substrate
complete + cross-frame composable** with pass-record-site integration
intentionally deferred until consumer pressure surfaces.

---

## §D — Render-handoff boundary (`crates/editor-shell/src/render_input.rs`)

| Symbol | Citation | Status |
|---|---|---|
| `RenderInput<'a> { editor_camera: &'a EditorCameraState }` | `crates/editor-shell/src/render_input.rs:109-122` | Borrowed view. Build via `RenderInput::from_editor_shell(&shell)`. Shape pinned to current per-frame / per-resize reads; expansion explicitly deferred. |
| `RenderInputOwned { ecs_tick, checkpoint_id, editor_camera }` | `crates/editor-shell/src/render_input.rs:185-194` | `Send + 'static` companion per ADR-117 sub-decision 3 — `(ecs_tick, checkpoint_id)` is the LOAD-BEARING immutable identity pair from PLAN §1.5.2. |
| `RenderInputOwned::as_render_input(&self) -> RenderInput<'_>` | `crates/editor-shell/src/render_input.rs:196-210` | Borrow bridge for render-path consumers. |
| `RenderHandoff { slot: Mutex<Option<Arc<RenderInputOwned>>>, generation: AtomicU64 }` | `crates/editor-shell/src/render_input.rs:260-263` | Latest-only / drop-old per ADR-117 sub-decision 1; std-only safe-Rust composition per ADR-117 sub-decision 5 (no `unsafe`). `publish(...)` increments generation `Release`; `acquire(...)` reads slot via Mutex (`crates/editor-shell/src/render_input.rs:290-328`). |
| `RenderHandoff` Debug | `crates/editor-shell/src/render_input.rs:337-347` | Reports generation only; never takes the slot lock — panic-free under poisoned-lock. |

**Phase 6 §6.2 Gate C closure status**: closed for the ADR-117
`RenderHandoff` boundary invariant per `plans/IMPLEMENTATION.md:470` +
`Status.md:9`. The empirical Gate C test
`crates/editor-shell/tests/render_input_boundary.rs::gate_c_held_snapshot_stable_across_subsequent_publishes`
(per `HANDOFF.md:1655`) verifies: render holds `Arc<RenderInputOwned>`
stable across subsequent publishes; latest-only / drop-old; `(ecs_tick,
checkpoint_id)` anchor preserved. This is a single-threaded proxy today
(no dedicated render thread spawned); the substrate is `Send + Sync` and
ready for a future thread split without API change
(`crates/editor-shell/src/render_input.rs:255-264`). This explicitly
matches PLAN §1.5.2's design target without implementing a separate
render thread.

The relationship to **Phase 6 render-snapshot separation** is recorded
inline in `plans/IMPLEMENTATION.md:449-451`. The handoff has zero
`wgpu::*` types (`crates/editor-shell/src/render_input.rs:46-49`) — GPU-
backed state stays on `EditorShell`; the snapshot side is non-GPU.

---

## §E — Editor-shell render path

| File | LoC | Surface |
|---|---:|---|
| `crates/editor-shell/Cargo.toml` | 97 | Production deps include `rge-gfx`, `rge-brep-render`, `rge-cad-projection`, `rge-cad-core`, `rge-editor-state`, `rge-editor-actions`, `rge-editor-egui-host`, `rge-input`, `rge-kernel-ecs`, `winit`, `wgpu`, `glam`, `ulid`. `[features]` includes `test-harness` (`crates/editor-shell/Cargo.toml:72-86`) — gates the visual-test-harness public surface. |
| `crates/editor-shell/src/lib.rs` | 73 | Failure class: snapshot-recoverable (`crates/editor-shell/src/lib.rs:3`). Re-exports `EditorShell`, `RenderInput`, `RenderInputOwned`, `RenderHandoff`, `InspectorSnapshot`, `WorldSnapshot`. `render_frame_e2e_perf` is `#[cfg(test)]`-gated (`crates/editor-shell/src/lib.rs:52-53`). |
| `crates/editor-shell/src/render_path.rs` | 1115 | `init_render_state` / `init_render_state_post_surface` / `init_render_state_headless` / `acquire_depth_view` / `render_frame` / `render_frame_to_target` / `encode_main_pass` / `resize_render_path`. Frame: depth-view via frame-graph pools (`crates/editor-shell/src/render_path.rs:320-333`); surface acquire (`crates/editor-shell/src/render_path.rs:349-360`); encoder build (`crates/editor-shell/src/render_path.rs:374-379`); cuboid+highlight pass via `LitMeshPipeline` (`crates/editor-shell/src/render_path.rs:382`); optional egui pass with `LoadOp::Load` into same encoder (`crates/editor-shell/src/render_path.rs:403-429`); submit + present (`crates/editor-shell/src/render_path.rs:431-440`). Phase 6 sub-β `DEPTH_FORMAT = wgpu::TextureFormat::Depth24Plus` (`crates/editor-shell/src/render_path.rs:111`) + `DEPTH_RESOURCE_ID` const (`crates/editor-shell/src/render_path.rs:118-120`). |
| `crates/editor-shell/src/render_frame_e2e_perf.rs` | 368 | POSTV0-EDITOR-SHELL-PERF-HARNESS-001 crate-local `#[cfg(test)]` perf harness. Drives `acquire_depth_view` + `render_frame_to_target` against offscreen `wgpu::TextureFormat::Bgra8UnormSrgb` 1024×768 single-cuboid scene (`crates/editor-shell/src/render_frame_e2e_perf.rs:78-88`). 240 warmup + 600 sample batches × 50 frames per batch; 3 runs; variance gate ≤30%; soft P95 1.0 ms REPORTED-only per the `MAIN-ORDERED-JOB-004` / POSTV0 EXEC packet's documented threshold-pinning deferral (`crates/editor-shell/src/render_frame_e2e_perf.rs:36-60`). |
| `crates/editor-shell/src/visual_smoke.rs` | 492 | Dispatch N1 crate-local `#[cfg(test)]` headless visual smoke; uses `HeadlessTarget` + `init_render_state_headless` + `acquire_depth_view` + `render_frame_to_target` + `rge_gfx::ReadbackBuffer::from_target` (`crates/editor-shell/src/visual_smoke.rs:11-26`). Asserts per-fragment color variance on a textured cube (4×4 red/blue checker). |
| `crates/editor-shell/src/visual_test_harness.rs` | 134 | Single public function `render_one_frame_to_readback` gated by `#[cfg(any(test, feature = "test-harness"))]` (`crates/editor-shell/src/visual_test_harness.rs:11-25`). One frame, one readback; consumed by `crates/rge-editor/src/main.rs` test-only paths via the `test-harness` feature. |
| `crates/editor-shell/src/lifecycle/mod.rs` | 1744 | `EditorShell` + `ApplicationHandler` impl + PIE state machine + `WorldSnapshot` round-trip + toolbar + diagnostics. `render_handoff: RenderHandoff` field (`crates/editor-shell/src/lifecycle/mod.rs:293`); `gfx_ctx` / `surface_ctx` / `pipeline` / `gfx_camera` / `materials` / `light` / `meshes` / `texture_pool` / `buffer_pool` / `compiled_frame_graph` / `egui_host` / `inspector_handoff` fields — all `Option<…>` so existing PIE tests remain byte-identical. `prebuilt_render_meshes` + `prebuilt_render_base_colors` + `prebuilt_render_base_textures` for the `--glb` render-only path (dispatch G/I/K/M2). Constructor surface: `new` / `with_world` / `with_world_projection_graph` / `with_render_mesh` / `with_render_meshes` / `with_render_meshes_and_base_colors` / `with_render_meshes_and_base_colors_and_textures` / `with_glb_reload_source`. |
| `crates/editor-shell/src/lifecycle/asset_reload.rs` | 178 | `AssetReloadHook` trait (`crates/editor-shell/src/lifecycle/asset_reload.rs` per `Grep` shows `impl EditorShell { … }` at `:23`). |
| `crates/editor-shell/src/lifecycle/commands.rs` | 409 | `EditorKeyCommand` enum (`crates/editor-shell/src/lifecycle/commands.rs:66`) and `SetTimeScale` action (`:173`). Phase 9 keyboard-to-CommandBus integration (Ctrl+Z / Ctrl+Y / Ctrl+S + Ctrl+0/2/4 time-scale presets). |
| `crates/editor-shell/src/lifecycle/playback.rs` | 465 | `EditorPlaybackCommand` (`crates/editor-shell/src/lifecycle/playback.rs:88`): Space toggles Editing/Playing/Paused, Escape stops PIE. |
| `crates/editor-shell/src/viewport.rs` | 114 | `Viewport` placeholder widget (`crates/editor-shell/src/viewport.rs:17`). |
| `crates/editor-shell/src/camera.rs` | 859 | `EditorCameraState` (`crates/editor-shell/src/camera.rs:88`); `CameraView` (`:156`); `pick_face_at(...)` (`:250`). |
| `crates/editor-shell/src/pick_path.rs` | 205 | `impl EditorShell { handle_left_click, rebuild_highlight_overlay }` (`crates/editor-shell/src/pick_path.rs:23`). |
| `crates/editor-shell/src/snapshot.rs` | 330 | `WorldSnapshot` (`crates/editor-shell/src/snapshot.rs:43`); `SnapshotMetrics` (`:160`); `measure_round_trip` (`:190`); `capture_and_audit` (`:214`); `restore_and_audit` (`:224`). |
| `crates/editor-shell/tests/*.rs` | — | `render_input_boundary.rs` (13 `#[test]` fns — Gate C invariant suite); `editor_frame_idle.rs` (`#[ignore]` Gate B CPU-idle perf — `crates/editor-shell/tests/editor_frame_idle.rs:20`); `pie_round_trip.rs`; `snapshot_correctness.rs`; `time_scale_test.rs`; `keyboard_command_bus_round_trip.rs`; `inspector_snapshot_smoke.rs`; `camera_picker_smoke.rs`; `editor_coord_face_selection.rs`; `timing_baseline.rs`. |

The editor-shell render path therefore **directly consumes the frame-
graph substrate** (build_resource_map + pools) for the depth attachment
(`crates/editor-shell/src/render_path.rs:320-333` + `acquire_depth_view`)
even though `gfx::FrameRecorder` is bypassed. The "no consumer of the
frame-graph substrate today" framing in `crates/gfx/src/frame_graph/mod.rs:42-44`
applies to the gfx-internal `FrameRecorder` flow; the editor-shell flow
is the substrate's actual production consumer. This is consistent with
`Status.md:9` ("Phase 6 frame-graph + render substrate complete +
production-consumed via `editor-shell::render_path:312`").

---

## §F — Editor binary + egui-host integration

| File | LoC | Surface |
|---|---:|---|
| `editor/rge-editor/Cargo.toml` | 63 | Single `[[bin]]` target `rge-editor` at `src/main.rs` (`editor/rge-editor/Cargo.toml:12-14`). Production deps: `rge-editor-shell`, `rge-cad-core`, `rge-cad-projection`, `rge-kernel-ecs`, `rge-io-gltf`, `rge-brep-render`, `glam`, `winit`, `tracing-subscriber`, `notify`. Dev-deps include `rge-editor-shell` with `test-harness` feature + `rge-gfx` + `wgpu` (`editor/rge-editor/Cargo.toml:45-59`). |
| `editor/rge-editor/src/main.rs` | 2746 | `Cli { glb_path: Option<PathBuf> }` + `parse_args` (`editor/rge-editor/src/main.rs:101-157`). Two construction paths: default cuboid demo (CAD pipeline → `EditorShell::with_world_projection_graph`) OR `--glb <path>` (render-only mesh via `rge_io_gltf::import_glb` → `RenderMesh::from_buffers` → `EditorShell::with_render_meshes_and_base_colors_and_textures`). Dispatch J accumulates glTF TRS hierarchy and CPU-bakes vertex positions before `RenderMesh::from_buffers` (`editor/rge-editor/src/main.rs:160-213`). Per-mesh `base_color` via `MaterialAsset` resolution. Inline `#[cfg(test)]` end-to-end visual acceptance tests using the `test-harness` feature. |
| `editor/rge-editor/src/glb_watcher.rs` | 434 | Notify-backed parent-directory non-recursive watcher (`editor/rge-editor/src/glb_watcher.rs:14-37`); 200 ms debounce; `EditorShell::handle_asset_reload` invoked from `RedrawRequested`. ISSUE-85 hot-reload feature. |
| `crates/editor-egui-host/Cargo.toml` | 39 | Headless by design: no `rge-editor-shell` dep (`crates/editor-egui-host/Cargo.toml:14-17`). Production deps: `egui`, `egui-winit`, `egui-wgpu`, `egui_dock` (with `serde` feature), `wgpu`, `winit`, `tracing`, `rge-editor-ui`, `rge-editor-state`. |
| `crates/editor-egui-host/src/lib.rs` | — | `EguiHost { context, state, renderer, surface_size, pixels_per_point, dock_state, inspector_handoff, viewport_rect_sink }` (`crates/editor-egui-host/src/lib.rs:148-200+`). Public surface: `EguiHost::new(...)` (`:232`); `on_window_event(...)` (`:347`); `resize(...)` (`:357`); `context()` (`:372`); `surface_size()` (`:378`); `pixels_per_point()` (`:384`); `inspector_handoff()` (`:400`); `dock_state()` (`:408`); `viewport_tab_rect()` (`:431`); `is_pointer_over_viewport(...)` (`:466`); `render(...)` (`:507`). `INSPECTOR_PANE_OLD_FRACTION = 0.75` (`:121`). |
| `crates/editor-egui-host/src/handoff.rs` | 278 | `InspectorHandoff { slot: Mutex<Option<Arc<InspectorSnapshot>>>, generation: AtomicU64 }` (`crates/editor-egui-host/src/handoff.rs:77-92`); `publish` (`:117-128`); `acquire` (`:138-145`); `generation` (`:155-157`). Mirrors `RenderHandoff` shape verbatim. |
| `crates/editor-egui-host/src/tabs.rs` | — | `InspectorTabBody { handoff: Arc<InspectorHandoff> }` (`crates/editor-egui-host/src/tabs.rs:78`); `TabBody { Inspector, Viewport, Placeholder }` enum (`:128`); `EditorTabViewer` + `with_viewport_rect_sink` (`:192-218`); `ViewportRectSink = Mutex<Option<egui::Rect>>`. |
| `crates/editor-egui-host/tests/*.rs` | — | `dock_layout_smoke.rs`; `host_scaffolding_smoke.rs`; `inspector_handoff_smoke.rs`; `tab_body_smoke.rs`; `viewport_pointer_smoke.rs`. |

Dependency direction: `editor-shell → editor-egui-host` (one-way per
`crates/editor-egui-host/Cargo.toml:14-17` + `crates/editor-shell/Cargo.toml:26-32`);
the host is headless by design. The `InspectorHandoff` lives in the host
crate (`crates/editor-egui-host/src/handoff.rs:1-30`) so editor-shell
holds an `Arc<InspectorHandoff>` clone published once per frame, BEFORE
the egui render pass (`crates/editor-shell/src/render_path.rs:386-401`).
`InspectorTabBody` reads the same handoff.

The host has shipped through dispatches A–F (per `crates/editor-egui-host/src/lib.rs:9-62`):
A scaffold + B render pass + C `DockState<TabBody>` + inspector handoff +
D split-pane viewport+inspector + F viewport-pointer face-pick fall-through.
This **resolves the Phase 9 live-inspector wiring preflight named blocker**
("no egui host exists") recorded at `plans/BASELINE.md:672-705`.

---

## §G — Render-adjacent consumer crates

| Crate | Classification | Status | Pressure-class |
|---|---|---|---|
| `crates/gfx-ir` (`crates/gfx-ir/Cargo.toml` + `crates/gfx-ir/src/lib.rs:1-4`) | 4-line stub — `//! `rge-gfx-ir` — stub crate. //! Architecture frozen at v0.8; implementation pending per [`IMPLEMENTATION.md`](../../IMPLEMENTATION.md).` Zero deps. | Empty stub per `HANDOFF.md:91` "44 IMPLEMENTED / 8 PARTIAL / 42 EMPTY-STUB" workspace footprint. | Not GPU pressure today. Historical-only — declared in workspace, no consumer. |
| `crates/brep-render` (`crates/brep-render/src/lib.rs:1-80`) | CPU-side flat-shaded `RenderMesh` substrate; zero `wgpu` dep — caller-supplied buffer triples in (`positions`, `indices`, `Option<face_labels>`) out (`RenderMesh`). 744 LoC; uses `forbid(unsafe_code)`. Single test `tests/render_mesh_smoke.rs` exercises Cuboid + Extrude + Revolve fixtures. | Production-consumed by editor-shell and editor binary. | CPU-side data/model pressure (mesh conversion). Not GPU pressure axis. |
| `crates/components-render` (`crates/components-render/src/lib.rs:1-41`) | ECS render-side components: `AssetId`, `BRepHandle`, `CadNodeId`, `Camera`, `Light`, `Lod`, `MaterialHandle`, `MeshHandle`, `ReflectionProbe`, `SkinnedMesh`. State-only per W01 PLAN exit criteria; `forbid(unsafe_code)`. | Production-consumed by `rge-scene-loader` (per `crates/rge-scene-loader/Cargo.toml:16`); not yet wired into editor-shell render path. | State pressure (Phase 9 reflection-scale axis adjacency; see ISSUE-216). Not GPU pressure. |
| `crates/cad-projection/src/render_adapter.rs` (`crates/cad-projection/src/render_adapter.rs:1-60`) | `CadProjection::render_mesh_for(world, entity) -> Option<RenderMesh>` (`crates/cad-projection/src/render_adapter.rs:56-...`). Two-gate semantics: (1) entity carries `BRepHandle`, (2) entity has projected mesh in cache (`crates/cad-projection/src/render_adapter.rs:34-48`). No cache; each call re-runs `RenderMesh::from_buffers`. Layering rationale documented inline. | Production-consumed by editor-shell render path. | CPU-side data/model pressure. Already complete; future caching is "a separate dispatch if profiling shows it's needed" (`crates/cad-projection/src/render_adapter.rs:13-17`). |

None of these crates carry GPU pressure today. `gfx-ir` is historical-
only stub state; `brep-render` and `components-render` are CPU-side
substrate; `cad-projection/render_adapter` is a CPU-side bridge. All
exist by design at their current shape; **none surfaces a GPU/render
follow-up signal**.

---

## §H — Baseline / status reconciliation

| Claim source | Citation | Current source state | Classification |
|---|---|---|---|
| `plans/PLAN.md:741-743` Phase 5-Rendering reach (Lumen-equivalent, Nanite-like, VSM, TSR) | post-v2 reach product | wgpu init + PBR-lite + frame-graph + PSO cache + 1 cuboid + sub-ε highlight overlay + post-depth pass + 1024×768 surface. None of the Phase 5 features built. | **Historical roadmap, post-v2**; not Phase 9 GPU pressure. |
| `plans/PLAN.md:1124+` §13.2 350MB simple-scene resident memory + 16ms editor frame + 1000-component reflection cache + 200MB binary-size budget | per-budget orthogonal gates | Editor frame ≤8ms CPU-idle CLOSED (recorder-host empty-shell — `plans/BASELINE.md:191`); 350MB resident not measured. | Loaded-editor re-measure is the **explicit deferral** in `plans/BASELINE.md:202-208`. Narrower than current source (the substrate supports both interpretations, only the CPU-idle interpretation is measured). |
| `plans/IMPLEMENTATION.md` Phase 6 §6.3 all-four-exit | `plans/IMPLEMENTATION.md:467-473` | All four CLOSED — Gate A pre-depth 2026-05-11 + post-depth 2026-05-14, Gate B CPU-idle, Gate C ADR-117 invariant, 100 mat instances share PSO. | **Current** — matches source state. |
| `plans/IMPLEMENTATION.md:589-605` Phase 9 GPU pressure axis | "VRAM residency under real scenes" | The editor renders 1 cuboid (CAD demo) or N glTF meshes. No production scene has pressured VRAM residency. | **Current** — qualitative axis. No measurement exists; the substrate also does not enable that measurement today (no VRAM-budget profiler harness exists in `crates/script-bench/` or elsewhere — search-confirmed). |
| `plans/BASELINE.md:223-238` §6.3 Gate A pre-depth recorder-host scope | `min-of-3 P95 = 0.112 ms` | Substrate unchanged; constraint table holds. | **Current** for the pre-depth gfx path. Scope-limitation footnote is load-bearing. |
| `plans/BASELINE.md:248` §6.3 Gate A post-depth recorder-host (2026-05-14) | `min-of-3 P95 = 0.122 ms` | Substrate unchanged. | **Current** for the depth-attached gfx path. |
| `plans/BASELINE.md:250-251` 2026-05-23 ISSUE-118 supersession | Non-winit editor-shell perf harness landed at `f8b8ed4` as `crates/editor-shell/src/render_frame_e2e_perf.rs` | Confirmed; harness present at the cited path. | **Current.** Threshold pinning remains by-design future authorized scope (POSTV0 EXEC packet's own deferral). |
| `plans/BASELINE.md:541-639` editor-usability preflight | Editor binary cannot save/load/spawn through user input | Editor binary still renders + picks + reloads via `--glb` and R-key; CommandBus integration limited to keyboard time-scale presets + undo/redo. ISSUE-219 (2026-05-27) selected `substrate_prerequisite_triggered` with `rge-editor → rge-scene-loader` as the docs-only follow-up scope. | **Current**, complementary to the GPU axis but separate. |
| `plans/BASELINE.md:643-753` Live-inspector wiring preflight named blocker | "no egui host exists in the workspace today" | `crates/editor-egui-host/` exists and ships dispatches A–F (see §F). | **Stale** — the named blocker is resolved; the preflight's revisit triggers (an egui host integration preflight lands a decision OR a non-inspector consumer materialises) BOTH fired during the dispatch-A through dispatch-F arc. Reconciliation is not GPU-pressure follow-up — it is editor-usability; covered by ISSUE-219's `substrate_prerequisite_triggered` axis (per ISSUE-219 EXEC re-derivation, doc-only at `plans/BASELINE.md`). |
| `Status.md:9` v0 cert at `6aaf7f1` | All cheap gates green; Phase 6 §6.3 closed | Recent commits `6df1bef` / `93b7fa6` / `2650320` / `b3bfb76` / `283cc19` / `e8219e0` are Phase 9 audit reports + Phase 9 salvages; none touches gfx / editor-shell / Cargo. | **Current.** v0 cert posture stands. |
| `HANDOFF.md:1655` "Phase 6 §6.3 status (post-2026-05-11)" | All four exit criteria CLOSED | Same as `plans/IMPLEMENTATION.md:467-473` plus production-consumed at `editor-shell::render_path` (the `:312` line cite predates the lifecycle.rs split + render_path.rs migration but the surface is preserved). | **Current.** |
| `HANDOFF.md:1572-1581` "Persistent gaps" | Lists "editor-shell mock-event-loop perf harness" + "`peak_rss` / `vss_delta` soak-harness improvement" + "4 kernel doctrine-cavity implementations" + "`compile.rs` legibility refactor" | Editor-shell perf harness deferral resolved by `f8b8ed4` (ISSUE-118). `compile.rs` legibility refactor shipped (PR #127 / commit `02a241a`, 2026-05-23 — `change.md:1632`-vicinity dispatch ordering note). | **Narrower than current source** — the gap list is dated; two of four bullets are resolved. Not GPU pressure follow-up. |
| `docs/§18/GFX_RENDER_TIER.md:5` ("Status — Stable v1 for Phase 6.1 substrate + Phase 6 PBR-lite; Frame-graph + render-snapshot separation per §1.5.2 + material-runtime / PSO cache + 60 fps simple-scene gate are still pending.") | Frame-graph + render-snapshot + material-runtime + PSO + 60fps gate all listed as "still pending." | All four shipped by 2026-05-12 (frame-graph umbrella close-out) + 2026-05-11 (PSO cache + Gate A + Gate C). | **Stale** — `docs/§18/GFX_RENDER_TIER.md` (line 5 status + section 1 reference-impl list + section 11 "Pending Phase 6 work" bullets at `docs/§18/GFX_RENDER_TIER.md:228-247`) lags by 4 of 4 "pending" bullets and omits `frame_graph/` + `pso_cache.rs` + `intent_adapter.rs` + `surface.rs` modules. This is a **§18-companion-doc lag**, NOT a `plans/BASELINE.md` / `Status.md` / `HANDOFF.md` / `change.md` disagreement. The canonical baseline/status set is current. |
| `docs/§18/GFX_RENDER_TIER.md:236-238` "Pending Phase 6 work — Render-snapshot separation per §1.5.2" | Substrate not yet implementing `SnapshotParticipate` | Gate C closed for ADR-117 RenderHandoff invariant per `plans/IMPLEMENTATION.md:470` and `Status.md:9`. Render-handoff substrate is per-frame in-process; PIE `SnapshotParticipate` is orthogonal (`crates/editor-shell/src/render_input.rs:59-61`). | **Stale** — same companion-doc lag as the row above. |
| `change.md:1636` 2026-05-23 repo transfer entry | `CADRust/RGE` → `RustCADs/RGE`; CI verified executing on RustCADs runners | Local `origin` updated; freeze posture unchanged. | **Current — orthogonal to GPU axis.** |

**Classification summary**: 11 rows current; 1 row narrower than current
source (Persistent-gaps list dated, two of four bullets resolved); 3
rows historical-only (post-v2 reach renderer); 1 row stale companion-doc
(`docs/§18/GFX_RENDER_TIER.md`) which is not in the canonical
baseline/status quartet. The canonical `plans/BASELINE.md` +
`plans/IMPLEMENTATION.md` + `Status.md` + `HANDOFF.md` + `change.md` set
agrees with current source on every Phase 6 §6.3 / Phase 9 GPU-axis
claim audited.

---

## §I — GPU pressure candidate matrix

Each row inspected against current source; classification ∈
{`measurement`, `integration`, `docs_reconciliation`, `already_covered`,
`no_op`, `historical_only`, `NEEDS_HUMAN`}.

| # | Candidate | Class | Evidence / rationale |
|---|---|---|---|
| 1 | **Re-measure Gate A on a second adapter / backend (vendor-parity)** | `historical_only` | `plans/BASELINE.md:227-238` codifies "constrained-certified on recorder host only — re-measure required for any new recorder host / adapter / backend / viewport / camera path." This is a recorder-environment policy, not a current pressure signal. No second adapter is available in this run; no production user has reported a regression. The freeze policy `plans/PLAN.md:120-134` keeps this category of expansion gated on demonstrated implementation pressure. |
| 2 | **Pin hard P95 / worst-sample / variance threshold on `render_frame_e2e_perf`** | `historical_only` | `crates/editor-shell/src/render_frame_e2e_perf.rs:57-60` records the explicit deferral: "Soft P95 target: 1.0 ms — REPORTED (under/over) only. NOT asserted as a hard threshold in this measurement-capture dispatch; the POSTV0-EDITOR-SHELL-PERF-HARNESS-001 EXEC packet recommends the future hard threshold from the observed values." The POSTV0 EXEC packet `ai_handoffs/POSTV0-EDITOR-SHELL-PERF-HARNESS-001_EXEC_2026-05-14_21-51-40+0300.md` was authored on the recorder host; later threshold pinning is "future, explicitly-authorized certification scope" (`plans/BASELINE.md:250-251`). No current consumer is asking for it. |
| 3 | **Loaded-editor Gate B re-measure (idle frame ≤ 8 ms under a non-empty scene)** | `historical_only` | `plans/BASELINE.md:199-208` explicitly defers: "This is the CURRENT empty-shell CPU-idle baseline [...] Future re-measure required once non-trivial editor systems / idle scene are wired." No "non-trivial editor systems / idle scene" wiring has landed since the v0 cert; the most recent landings are Phase 9 audit reports + ISSUE-219 substrate-prerequisite (docs-only follow-up scoped). |
| 4 | **Integrate `gfx::FrameRecorder` with frame-graph transient resources** | `historical_only` | `crates/gfx/src/frame_graph/mod.rs:42-44` documents: "No `FrameRecorder` / pipeline integration. The substrate stands alone; integration with `record_lit_mesh_pass` / `MeshPipeline` / `LitMeshPipeline` lands in a follow-up dispatch." `crates/gfx/src/frame_graph/resource_map.rs:20-25` reaffirms: "NO `crate::FrameRecorder` integration [...] NO `editor-shell::render_path` edits — the editor opens its own `wgpu::CommandEncoder` directly and never touches the frame-graph substrate." Editor-shell IS already consuming the substrate (build_resource_map + pools for depth — `crates/editor-shell/src/render_path.rs:320-333`); `FrameRecorder` itself has no consumer outside `crates/gfx/tests/`. No pressure signal. |
| 5 | **Promote `gfx::MeshPipeline` (unlit color-vertex) to a production consumer** | `historical_only` | Symbol search across `--type rust`: `MeshPipeline` appears in `crates/gfx/src/{mesh_pipeline.rs,pipeline.rs,lit_mesh_pipeline.rs,lib.rs}` + `crates/gfx/tests/mesh_quad.rs` + `crates/gfx/src/intent_adapter.rs` (doc-comment) + `crates/material-runtime/src/lib.rs` (doc-comment) + `crates/gfx/src/transform.rs:123` (doc-comment) — **zero production consumers outside `gfx`**. Editor-shell uses `LitMeshPipeline` only (`crates/editor-shell/src/lifecycle/mod.rs:188`). Pure-intent `material-runtime` `ShaderId::Mesh` exists but no production caller selects it. Substrate is provisioned for future expansion (`material-runtime` `ShaderId::Mesh` variant) but no demand exists. |
| 6 | **Implement `gfx-ir` from its 4-line stub** | `historical_only` | `crates/gfx-ir/src/lib.rs:1-4`: stub crate, zero deps, zero consumers. Stub list in `HANDOFF.md:91`. No production crate imports it; the workspace's render-IR path goes `cad-core::Tessellation → cad-projection::render_mesh_for → brep-render::RenderMesh → rge-gfx::LitMesh::from_render_mesh`. `gfx-ir` is reserved per workspace ambition; no consumer pressure. |
| 7 | **`docs/§18/GFX_RENDER_TIER.md` companion-doc reconciliation** | `docs_reconciliation` (NOT canonical-doc disagreement; see decision) | `docs/§18/GFX_RENDER_TIER.md:5` status line + `:22-29` "Pending Phase 6 work" enumeration + `:228-247` section 11 list 4 of 4 Phase 6 §6.3 sub-deliverables as "pending" that have all shipped. The canonical `plans/BASELINE.md` + `plans/IMPLEMENTATION.md` + `Status.md` + `HANDOFF.md` set IS current (see §H). The §18 doc is companion doctrine, not "baseline/status." TASK packet definition of `docs_reconciliation` triggers on baseline/status disagreement (`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:322-323`); a §18-only lag is not that trigger. **See §J for the comparison.** |
| 8 | **Implement a real VRAM-residency / GPU-memory profiler for the Phase 9 "GPU pressure" axis** | `NEEDS_HUMAN`-territory | `plans/IMPLEMENTATION.md:589-605` lists "GPU pressure — VRAM residency under real scenes" as an evaluation axis qualitatively. No "real scene" exists today; the editor binary renders 1 cuboid OR N glTF primitives. A profiler tool would be a benchmark policy / measurement-infrastructure decision affecting `script-bench` / `crates/gfx` / harness layout — exactly the "unbounded product / architecture / benchmark policy decision" that the TASK packet routes to `NEEDS_HUMAN` (`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:325-329`). |
| 9 | **`lit_mesh_pipeline` "camera at world origin" v0 simplification correction (`view_dir = normalize(-world_pos)`)** | `historical_only` | `crates/gfx/src/lit_mesh_pipeline.rs:26-29` + `:165`: "view direction is computed from `world_pos` assuming the camera sits at the world origin (a v0 simplification — when an explicit camera position is added in a later phase, replace `view_dir = normalize(-world_pos)` with `normalize(camera_pos - world_pos)`)." The simplification is documented; a `Camera` UBO carries `view_proj` + `normal_matrix` but no `camera_pos` field today (`docs/§18/GFX_RENDER_TIER.md:141-148`). Correcting this is a substrate-fidelity follow-up gated on real consumer pressure (e.g. a non-trivial editor scene with the camera not at origin). Not current GPU pressure. |
| 10 | **Wire `components-render` ECS components into the editor-shell render path** | `historical_only` | `crates/components-render/src/lib.rs:1-41` is state-only per W01 PLAN exit criteria. The current editor render path consumes `BRepHandle` from `cad-projection` directly, not from `components-render`. `rge-scene-loader` is the production consumer of `components-render` per `crates/rge-scene-loader/Cargo.toml:16`. Wiring components-render into the editor render path is the editor-usability substrate-prerequisite axis covered by ISSUE-219 (which already selected a docs-only follow-up). Not GPU pressure. |
| 11 | **No-op: substrate is complete; no concrete GPU/render measurement, integration, or docs-disagreement justifies a follow-up now** | `no_op` | All four §6.3 exit criteria CLOSED; frame-graph substrate complete + cross-frame composable; editor-shell perf harness landed at `f8b8ed4` (`plans/BASELINE.md:250-251`); ADR-117 RenderHandoff Gate C invariant closed; PSO cache substrate complete + production-consumed; canonical baseline/status docs current. The §18 GFX_RENDER_TIER.md lag is real but is companion doctrine, not a canonical baseline/status disagreement per TASK definition. No new production consumer is pressuring beyond the cuboid demo / glTF render-only path. |

---

## §J — Candidate comparison

The TASK packet (`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:316-329`)
defines four `GPU_RENDER_PRESSURE_VERDICT` options + `NEEDS_HUMAN`:

- **`measurement`** — requires "one concrete GPU/render metric gap or
  stale measurement that can be closed by a bounded follow-up without
  changing runtime/editor semantics." Three measurement candidates were
  inspected (matrix rows 1, 2, 3). Row 1 (vendor-parity Gate A) is
  explicitly framed as recorder-policy by `plans/BASELINE.md:227-238`,
  not pressure. Row 2 (POSTV0 harness threshold pinning) is the EXEC
  packet's own deferral. Row 3 (loaded Gate B) explicitly waits on a
  "non-trivial editor systems / idle scene" wiring that hasn't
  happened. **Each measurement candidate has a published deferral
  rationale that has NOT been invalidated.** `measurement` is not the
  smallest safe follow-up today.

- **`integration`** — requires "one concrete missing wiring path between
  gfx/frame-graph/render-handoff/editor-shell or editor binary surfaces
  that blocks a truthful current workflow." Two integration candidates
  were inspected (matrix rows 4 and 5). Row 4 (`FrameRecorder` ↔
  frame-graph) is explicitly NON-GOAL until a consumer demands it
  (`crates/gfx/src/frame_graph/mod.rs:42-44`); the editor-shell render
  path already consumes the substrate. Row 5 (`MeshPipeline` production
  consumer) has no demand — the `material-runtime` `ShaderId::Mesh` axis
  is provisioned but no caller selects it. **No current workflow is
  blocked** for lack of either wiring. `integration` is not the smallest
  safe follow-up today.

- **`docs_reconciliation`** — requires "current source and current
  baseline/status docs disagree and a docs-only follow-up is the
  smallest safe task." The TASK packet's definition of "baseline/status
  docs" is specified in the deliverables section
  (`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:201-203`):
  `plans/BASELINE.md`, `plans/IMPLEMENTATION.md`, `Status.md`,
  `HANDOFF.md`, `change.md`, **and any discovered `docs/**/GFX_RENDER_TIER.md`**.
  The latter clause is important: `docs/§18/GFX_RENDER_TIER.md` IS
  within scope of the reconciliation check. §H row 13 + §I row 7
  identify a real disagreement at:
  - `docs/§18/GFX_RENDER_TIER.md:5` (status line claims Frame-graph + PSO
    cache + render-snapshot separation + 60fps gate "still pending");
  - `docs/§18/GFX_RENDER_TIER.md:22-29` (section 1 "Pending Phase 6
    work" enumeration);
  - `docs/§18/GFX_RENDER_TIER.md:228-247` (section 11 "Pending Phase 6
    work" bullets).
  All four bullets shipped between 2026-05-11 and 2026-05-14. The §18
  doc is the only Phase 9 GPU-axis document that lags current source;
  the canonical `plans/BASELINE.md` + `plans/IMPLEMENTATION.md` +
  `Status.md` + `HANDOFF.md` + `change.md` set is internally consistent
  and matches current source.

  **This makes `docs_reconciliation` a genuine candidate.** The smallest
  safe follow-up would be a docs-only edit to `docs/§18/GFX_RENDER_TIER.md`
  appending a forward-reconciliation paragraph (mirroring the ISSUE-118 +
  ISSUE-122 BASELINE/Status/HANDOFF reconciliation idiom recorded at
  `Status.md:5-7`) — strictly to recognise that the 4 "Pending Phase 6
  work" bullets shipped and to forward-supersede the status line. **No**
  source / test / Cargo / lint / ADR / automation edits would land in
  the same dispatch.

  **However** — competing against `no_op` (see below), the
  reconciliation is informational only: it changes no user-visible
  behaviour, gates no future work, and the canonical baseline/status
  quartet is already current. The Phase 9 axis pattern set by ISSUE-216
  (`still_untriggered`), ISSUE-217 (`pressure_deferred`), and ISSUE-218
  (`pressure_deferred`) is "defer unless real pressure." The §18 doc
  lag does not change any current decision and does not gate a future
  dispatch. The doc itself acknowledges its companion-only doctrine
  status (`docs/§18/GFX_RENDER_TIER.md:1-10`).

- **`no_op`** — requires "no concrete GPU/render measurement,
  integration, or docs reconciliation follow-up is justified now."
  Selected. The §18 doc lag is real and could be filed as a
  `docs_reconciliation` follow-up, but it does not gate any current
  work, the canonical baseline/status set IS current, and filing a
  Phase 9 docs-only follow-up for a §18 companion doc that no current
  consumer is reading divergently is the kind of "premature mechanism"
  that PLAN.md §1.10's pressure-driven doctrine and §0.6 freeze policy
  guard against (`plans/PLAN.md:120-134`). The honest signal is "no
  current pressure" — recorded as `no_op` with the §18 doc lag noted
  in this audit's evidence section for any future reader.

- **`NEEDS_HUMAN`** — required when evidence is unavailable,
  contradictory, or points to an unbounded product / architecture /
  performance / renderer-roadmap / benchmark-policy / GPU-backend
  decision. Matrix row 8 (the broader VRAM-residency profiler axis) is
  the only such candidate. Evidence is available; the canonical-doc
  set is consistent; nothing in this audit requires human arbitration.

**Selected verdict — `no_op`.**

Reasoning:
1. All four `plans/IMPLEMENTATION.md` §6.3 exit criteria are CLOSED and
   recorded on the canonical baseline/status set.
2. Phase 6 frame-graph umbrella close-out is committed
   (`plans/IMPLEMENTATION.md:473`).
3. Phase 6 §6.2 render-snapshot separation Gate C is CLOSED per
   ADR-117 (`plans/IMPLEMENTATION.md:470`).
4. ISSUE-118's editor-shell perf harness reconciliation is done
   (`plans/BASELINE.md:250-251`).
5. The only documentation lag — `docs/§18/GFX_RENDER_TIER.md` — is a
   companion-doc concern; the canonical baseline/status quartet IS
   current; filing a `docs_reconciliation` follow-up is informational
   only, doesn't gate any work, and is exactly the kind of mechanism
   the Phase 9 audit pattern (defer-unless-pressure) is designed to
   avoid filing.
6. No current production consumer is pressuring the GPU/render
   substrate beyond the cuboid demo or render-only glTF path.

The §18 doc lag is noted here so any future reader who is examining
this dispatch can pick it up under a different (genuinely-pressure-
driven) trigger, without this audit pre-committing a follow-up.

---

## §K — Decision Block

```
GPU_RENDER_PRESSURE_VERDICT: no_op
NEXT_GPU_TASK_CLASS: no_op
PRIMARY_PRESSURE_SIGNAL: none
SMALLEST_SAFE_FOLLOW_UP: no-op: do not file a GPU follow-up from current evidence
FOLLOW_UP_BRANCH_MODE: not_applicable
```

---

## Verification Results

This is a read-only audit dispatch. No `cargo` / `cargo test` / `cargo
fmt` / `cargo deny` / architecture-lint / GPU-capture / profiler /
nested-dispatch invocations were performed per the TASK packet's
explicit non-goals (`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:336-339`).

The TASK packet's verification gates are read-only inspection plus
diff/status checks (`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:438-449`).

### Read-only document inspection (Verification Gates §1-3, V1)

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` — readable.
  Confirmed orchestrator-vs-manual packet shape; confirmed §14.9
  GPU-test serialization guidance is mandatory for any future GPU
  measurement crate (`AI_DISPATCH_AUTOMATION.md:773-833`); confirmed
  §6 dispatch flow + §14.10 blocked-execution semantics.
- `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` — readable.
  Confirmed packet types + filename convention + machine-readable
  footer + sidecar lifecycle.
- `.ai/dispatch.tasks.md` — readable. Task #69
  (`.ai/dispatch.tasks.md:7340-7345`); common #62-#71 constraints
  (`.ai/dispatch.tasks.md:7361-7376`). Inspected via line-cited Grep
  + targeted Read.

### Read-only Phase 9 context (Verification Gates §4-6, V1)

- `plans/PLAN.md` — readable; §1.5.2 + §4.1 + §13.x + §0.6 freeze policy
  + §1.13 failure-class table all cited at line numbers in §A above.
- `plans/IMPLEMENTATION.md` — readable; Phase 6 §6.1/§6.2/§6.3 + Phase 9
  §9 + the four-row §6.3 exit criteria table cited at line numbers in
  §A above.
- `plans/BASELINE.md` — readable; §6.3 Gate A pre/post-depth + §13.2
  Gate B + editor-usability preflight + live-inspector wiring preflight
  cited at line numbers in §A + §H above.
- `Status.md` — readable (single-line large entries; inspected via
  targeted Read + Grep). Most recent snapshot is 2026-05-23 (ISSUE-122
  + ISSUE-118); v0 cert at 2026-05-14 commit `6aaf7f1`.
- `HANDOFF.md` — readable (175,962-token file; inspected via targeted
  offsets + Grep). Current-state table at `:83-92`; Persistent gaps at
  `:1572-1581`; Phase 6 §6.3 status at `:1655`.
- `change.md` — readable (1,636 lines; inspected via targeted offsets
  + section-header Grep). Most recent entries dated 2026-05-23 (repo
  transfer + ISSUE-118 + ISSUE-122 reconciliations). No subsequent
  GPU/render-related landing.

### Read-only source inspection (Verification Gates §7-12, V2-V4)

- `crates/gfx/**` — Cargo.toml + lib.rs + context.rs + frame.rs +
  target.rs + surface.rs + pso_cache.rs + pipeline.rs + mesh_pipeline.rs
  + lit_mesh_pipeline.rs (head) + intent_adapter.rs (header) +
  plugin_adapter.rs (header) inspected and line-cited in §B.
- `crates/gfx/src/frame_graph/**` — mod.rs + descriptor.rs + pass.rs +
  resource.rs + resource_map.rs + texture_pool.rs + buffer_pool.rs +
  compile/mod.rs + compile/algorithm.rs + compile/types.rs + compile/error.rs
  inspected and line-cited in §C.
- `crates/gfx/tests/*` — file inventory captured (`frame_graph_smoke.rs`,
  `frame_graph_umbrella_smoke.rs`, `gate_a_simple_scene_60fps.rs`,
  `gate_a_simple_scene_depth_60fps.rs`, `lit_mesh_depth_overlay_smoke.rs`,
  `material_intent_pso_sharing.rs`, `mesh_quad.rs`, `plugin_adapter_smoke.rs`,
  `render_mesh_smoke.rs`, `headless_triangle.rs`, `wgsl_parse_smoke.rs`).
- `crates/editor-shell/**` — Cargo.toml + lib.rs + render_input.rs +
  render_path.rs (head + relevant slice) + render_frame_e2e_perf.rs
  (header) + visual_smoke.rs (header) + visual_test_harness.rs (header)
  + lifecycle/mod.rs (relevant slice 1-555) inspected and line-cited
  in §D + §E.
- `crates/editor-shell/src/{viewport,camera,pick_path,snapshot}.rs` —
  public-API-symbol Grep + line citations in §E.
- `crates/editor-shell/tests/**` — file inventory + `render_input_boundary.rs`
  `#[test]` Grep + `editor_frame_idle.rs` `#[ignore]` confirmation.
- `editor/rge-editor/{Cargo.toml,src/main.rs,src/glb_watcher.rs}`
  inspected and line-cited in §F.
- `crates/editor-egui-host/{Cargo.toml,src/lib.rs,src/handoff.rs,src/tabs.rs}`
  + tests inventory inspected and line-cited in §F.
- `crates/gfx-ir/{Cargo.toml,src/lib.rs}` inspected and line-cited in §G.
- `crates/brep-render/{Cargo.toml,src/lib.rs,tests/render_mesh_smoke.rs}`
  inspected and line-cited in §G.
- `crates/components-render/{Cargo.toml,src/lib.rs}` inspected and
  line-cited in §G.
- `crates/cad-projection/src/render_adapter.rs` inspected and
  line-cited in §G.

### Read-only symbol/text search (Verification Gates §13-14, V4)

Each search via the harness Grep tool over `--type rust` (ripgrep with
the workspace's default `.gitignore` honoured); excluded matches in
`OLD/`, `.ai/dispatch-*/`, `.claude/worktrees/`, `.git/`, and build
output by tool default. Files-with-matches counts and representative
files reported below; positive findings have line citations in §B/§C/§D/§E.

| Symbol | Files with match | Notes |
|---|---:|---|
| `GfxContext` | 42 | gfx production + tests; editor-shell production + tests; editor binary; plugin canaries (gfx/audio/physics/cad-projection); kernel/plugin-host context. |
| `FrameRecorder` | 10 | Only `crates/gfx/**` (production + tests). **Zero editor-shell production use** — confirms `frame_graph/mod.rs:42-44` NON-GOAL. |
| `ReadbackBuffer` | 11 | gfx production + tests; editor-shell `visual_smoke.rs` + `visual_test_harness.rs` + tests; editor binary tests (via the `test-harness` feature). |
| `HeadlessTarget` | 23 | gfx production + tests; editor-shell `visual_smoke.rs` + `visual_test_harness.rs` + `gate_a_simple_scene_*`; plugin canaries; cad-projection fault-injection test. |
| `FrameGraph` (bounded) | 11 | gfx production + tests + descriptor; **production-consumed by `crates/editor-shell/src/render_path.rs`**. |
| `CompiledFrameGraph` | 10 | gfx production + tests; production-consumed by editor-shell render path and lifecycle. |
| `ResourceLifetime` | 5 | gfx-internal only (frame_graph compile + types + lib re-export). |
| `AliasingGroup` | 9 | gfx-internal only; not externally consumed. |
| `RenderInput` (bounded) | 5 | `editor-shell::{lib,lifecycle/mod,render_input,render_path}` + `crates/editor-shell/tests/render_input_boundary.rs`. Zero leakage outside editor-shell. |
| `RenderInputOwned` | 4 | `editor-shell::{lib,lifecycle/mod,render_input}` + `crates/editor-shell/tests/render_input_boundary.rs`. Zero leakage. |
| `RenderHandoff` | 6 | `editor-shell::{lib,lifecycle/mod,render_input}` + `crates/editor-shell/tests/render_input_boundary.rs` + `crates/editor-egui-host/src/{lib,handoff}.rs` (the latter is `InspectorHandoff`'s sibling docstring + a structural mirror reference). |
| `render_frame` / `.render_frame(` | 3 | `editor-shell::{render_path,lifecycle/mod}` + `tests/render_input_boundary.rs`. Zero leakage outside editor-shell. |
| `render_frame_to_target` | 4 | `editor-shell::{render_path,visual_smoke,visual_test_harness,render_frame_e2e_perf}`. Zero leakage outside editor-shell. |
| `acquire_depth_view` | 4 | `editor-shell::{render_path,visual_smoke,visual_test_harness,render_frame_e2e_perf}`. Zero leakage outside editor-shell. |
| `VisualTestHarness` / `render_one_frame_to_readback` / `visual_test_harness` | 5 | `editor-shell::{lib,visual_smoke,visual_test_harness,render_path}` + `editor/rge-editor/src/main.rs`. |
| `MeshPipeline` (bounded) | 9 | gfx production + tests + intent_adapter doc-comment + material-runtime doc-comment + transform.rs doc-comment. **No editor-shell production consumer.** |
| `LitMeshPipeline` | 16 | gfx production + tests + editor-shell production + perf harness + material-runtime doc-comment + surface.rs doc-comment + vertex_lit.rs doc-comment. |
| `PipelineCache` | 9 | gfx production (pso_cache + intent_adapter + per-pipeline new constructors + context + lib). Zero production consumers outside gfx. |
| `use wgpu` / `use wgpu::` | 3 | `crates/gfx/src/{lit_mesh_pipeline,buffer,pso_cache}.rs` only at the module-import level (most gfx modules use bare `wgpu::Foo` paths rather than `use wgpu::`, so this is a sparse-match — not a count of all wgpu usage). |
| `frame_graph` (bounded — module name) | 18 | gfx production + tests; editor-shell `render_path` + `lifecycle/mod` + `render_frame_e2e_perf`; editor-egui-host (`lib.rs` doc-comment only). |

**Zero-use claims confirmed by these searches**:

- `gfx::FrameRecorder` has **no production consumer outside `crates/gfx/`** itself. Editor-shell builds its own `wgpu::CommandEncoder` directly (`crates/editor-shell/src/render_path.rs:374-379`). Command (and observed result): `Grep pattern=FrameRecorder type=rust output_mode=files_with_matches` → 10 files, all under `crates/gfx/`.
- `gfx::MeshPipeline` (unlit) has **no production consumer outside `crates/gfx/`** itself; the only matches outside gfx are doc-comment references in `crates/material-runtime/src/lib.rs:62,70` (companion docs) and `crates/gfx/src/transform.rs:123,125` (doc-comment). Command (and observed result): `Grep pattern="MeshPipeline\b" type=rust output_mode=files_with_matches` → 9 files (5 in gfx production + tests, 4 in doc-comment-only contexts).
- `RenderInputOwned` / `RenderHandoff` (the ADR-117 substrate) do not leak outside `crates/editor-shell/` and `crates/editor-egui-host/`'s sibling-doc references. Command (and observed result): `Grep pattern="RenderInputOwned" type=rust output_mode=files_with_matches` → 4 files; `Grep pattern="RenderHandoff" type=rust` → 6 files.
- `render_frame_to_target` / `acquire_depth_view` do not leak outside editor-shell. Commands: `Grep pattern="render_frame_to_target" type=rust output_mode=files_with_matches` → 4 files; `Grep pattern="acquire_depth_view" type=rust output_mode=files_with_matches` → 4 files. All four hits in both searches are within `crates/editor-shell/src/`.

### Cargo dep-edge inventory (Verification Gate §13)

`Grep pattern="rge-gfx|rge-gfx-ir|rge-brep-render|rge-components-render|rge-editor-shell|rge-editor-egui-host|rge-material-runtime" glob="**/Cargo.toml" output_mode=content -n=true`:

- `editor/rge-editor/Cargo.toml:21` rge-editor-shell (production); `:30` rge-brep-render (production); `:52` rge-editor-shell `features=["test-harness"]` (dev-dep); `:58` rge-gfx (dev-dep).
- `crates/cad-projection/Cargo.toml:14` rge-brep-render (production); `:40` rge-gfx (production — via the canary).
- `crates/editor-egui-host/Cargo.toml:15-17` documents the one-way dep rule (host does NOT depend on editor-shell).
- `crates/editor-shell/Cargo.toml:32` rge-editor-egui-host (production); `:54` rge-gfx; `:55` rge-brep-render (production).
- `crates/gfx/Cargo.toml:32` rge-brep-render (production); `:42` rge-material-runtime (production).
- `crates/rge-scene-loader/Cargo.toml:16` rge-components-render (production).
- `crates/gfx-ir/Cargo.toml` carries zero deps (4-line stub).

### Prior dispatch artifact inspection (Verification Gate §14, V6)

- `ai_handoffs/MAIN-RENDER-POSTDEPTH-GATEA-001_EXEC_2026-05-14_02-30-00+0300.md:18-19` — STATUS `AWAITING_REVIEW`; post-depth Gate A recorder-host closure provenance.
- `ai_handoffs/MAIN-RENDER-VISUAL-FIDELITY-002_EXEC_2026-05-14_03-05-00+0300.md:8-15` — depth-overlay smoke broadening; test-only edit; recorder-host pass.
- `ai_handoffs/MAIN-ORDERED-JOB-004-FRAMEGRAPH-AUDIT_EXEC_2026-05-14_15-04-27+0300.md:18` — STATUS `AWAITING_REVIEW`; substrate-complete + 8 files / 14+ public types / 74 tests / zero TODOs / production-consumed via editor-shell render path (per `Status.md:13`).
- `ai_handoffs/POSTV0-EDITOR-SHELL-PERF-HARNESS-001_EXEC_2026-05-14_21-51-40+0300.md:18` — STATUS `AWAITING_REVIEW`; landed at `f8b8ed4` per `plans/BASELINE.md:250-251`. Threshold pinning deferred by-design.
- `ai_handoffs/ISSUE-210_EXEC_2026-05-26_17-00-00+0300.md:25,108-307` — Phase 9 freeze-validity audit; `STATUS: AWAITING_REVIEW`; salvage commit `20518c0`.
- `ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md:68` — Phase 9 abstraction-pain audit; salvage commit `e8219e0`.
- `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md:145-149` — Phase 9 invalidation-economics audit verdict `does_not_warrant_next_dispatch`; salvage commit `283cc19`.
- `ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.md:129-132` — Phase 9 reflection-scale audit verdict `still_untriggered`; commit `b3bfb76`.
- `ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-25-31+0300.md:734-738` — Phase 9 async-orchestration audit verdict `pressure_deferred`; commit `2650320`.
- `ai_handoffs/ISSUE-218_EXEC_2026-05-27_01-13-01+0300.md:108-114` — Phase 9 compile-time audit verdict `pressure_deferred`; FOLLOW_UP_BRANCH_MODE `not_applicable`; commit `93b7fa6`.
- `ai_handoffs/ISSUE-219_EXEC_2026-05-27_02-19-41+0300.md:316-323` — Phase 9 editor-usability audit verdict `substrate_prerequisite_triggered`; smallest-safe-follow-up = docs-only PREFLIGHT section scoping `rge-editor → rge-scene-loader`; FOLLOW_UP_BRANCH_MODE = "PublishMode branch only unless later authorized by a human"; commit `6df1bef`.

Existing `ISSUE-220 TASK_*.md` and its `.meta.json` sidecar (per `git
status` below) are treated as read-only TASK packet input per
`ai_handoffs/AI_HANDOFF_PROTOCOL.md:336-342` (append-only; prior
files must not be rewritten) + `ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:92-94`
("This task packet, ... after the executor starts" is in the MUST NOT
edit list).

### Worktree state and `git diff --check` (Verification Gates §15-17)

Run at the start of execution AND at end-of-write. End-of-write
(authoritative) results below; if any post-write tool action changes
the answer it will be re-recorded by the controller-side check.

```
$ git diff --check
(no output — exit 0)

$ git status --short --untracked-files=all
?? ai_handoffs/ISSUE-220_EXEC_2026-05-27_02-50-57+0300.md
?? ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md
?? ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.meta.json

$ git diff --name-only
(no output — exit 0; no tracked files modified)
```

The only untracked files are this dispatch's own permitted artifacts
(`ai_handoffs/ISSUE-220_TASK_*.md` and `.meta.json` per
`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:115-122`
MAY ADD; the freshly-written EXEC packet at `ai_handoffs/ISSUE-220_EXEC_2026-05-27_02-50-57+0300.md`
per the same allow-list).

**No tracked production / documentation / script / Cargo / workflow /
schema / task-brief diff** (V7). Static protected-surface inspection:
`crates/`, `kernel/`, `editor/`, `runtime/`, `tools/`, `docs/`,
`plans/`, `Status.md`, `HANDOFF.md`, `change.md`, `Cargo.toml`,
`Cargo.lock`, `.github/workflows`, `.ai/dispatch.tasks.md`,
`.ai/dispatch.verify.ps1`, `.ai/*.schema.json`, root `*.ps1`
automation scripts — all unchanged in this dispatch's `git diff`
output above.

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope.

The Codex pre-flight checklist's advisory items (P1–P7 / V1–V7) were
honoured:

- P1 — Treated as audit-only; only ISSUE-220 EXEC packet written; no edits to
  existing packets, docs, source, Cargo, scripts, workflows, schemas, task
  briefs, fixtures, or prior logs.
- P2 — Did not infer dispatch incompleteness from missing `_REVIEW_` / `_CLOSEOUT_`;
  cited the orchestrator dispatch shape from `AI_DISPATCH_AUTOMATION.md` in §A.
- P3 — Searches scoped to current tracked repo surfaces via the harness Grep
  tool (which honours `.gitignore` and excludes `OLD/`, `.ai/dispatch-*/`,
  `.claude/worktrees/`, `.git/`, build output by default).
- P4 — Decision block matches one allowed value for each field; smallest-safe
  follow-up uses the exact `no_op` string from the TASK packet.
- P5 — Candidate matrix compares all five class lanes plus `NEEDS_HUMAN`;
  reduced to one smallest safe follow-up rather than a backlog.
- P6 — Prior packets used as provenance only; current-source line citations
  drive every "current" / "stale" classification.
- P7 — Did not run new benchmarks, profilers, cargo tests, fmt, deny, GPU
  captures, or full verification. GPU-test serialization guidance
  (`AI_DISPATCH_AUTOMATION.md:773-833`) noted in §A as the rule a future
  measurement follow-up would cite.

## Open Questions for Reviewer

None — the audit produced a single, scope-bounded `no_op` verdict
without leaving substantive open questions.

One advisory note for any future reader: §H + §I + §J identified a real
`docs/§18/GFX_RENDER_TIER.md` companion-doc lag (4 of 4 "Pending Phase 6
work" bullets shipped between 2026-05-11 and 2026-05-14). This is **not**
within this dispatch's `docs_reconciliation` trigger as I defined it
(the canonical `plans/BASELINE.md` + `plans/IMPLEMENTATION.md` +
`Status.md` + `HANDOFF.md` set IS current; §18 docs are companion
doctrine). The TASK packet's deliverables wording at
`ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md:201-203` does
list `docs/**/GFX_RENDER_TIER.md` within the reconciliation surface, so
a future reader could reasonably argue for upgrading this audit to
`docs_reconciliation` — I considered that path explicitly in §J and
chose `no_op` because filing a Phase 9 docs-only follow-up for a
companion doc that no current consumer is reading divergently is
exactly the kind of "premature mechanism" the Phase 9 pattern set by
ISSUE-216 / ISSUE-217 / ISSUE-218 is designed to avoid. The note is
preserved here so the controller can elect to override if appropriate.

## Worktree State

- Tracked files: clean (no `git diff` output; `git diff --check` exit 0; `git diff --name-only` empty).
- Untracked items:
  - `ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md`
  - `ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.meta.json`
  - `ai_handoffs/ISSUE-220_EXEC_2026-05-27_02-50-57+0300.md` (this packet)
- Branch: `ai-dispatch/ISSUE-220`
- Last commit: `6df1bef` ai-dispatch ISSUE-219: Audit Phase 9 editor-usability pressure

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-220
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
