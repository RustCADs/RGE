# Task Packet

DISPATCH_ID: ISSUE-73
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_18-28-47+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs
- crates/editor-ui/tests/node_graph_script_graph_smoke.rs
STATUS: OPEN

## Goal

Extend the consolidated Phase 8 editor-ui node graph smoke so it proves the generic `NodeGraphWidget` path consumes `rge_script_graph::ScriptGraph` through `VizAdapter` alongside the existing MaterialGraph, AnimGraph, and OperatorGraph coverage. This closes GitHub issue #73 by folding the ScriptGraph projection assertions already proven in the single-domain smoke into the combined smoke test target, without changing production code.

## Scope

### MAY edit
- crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs

### MUST NOT edit
- crates/editor-ui/src/**
- crates/**/src/**
- Cargo.toml
- Cargo.lock
- crates/editor-ui/tests/node_graph_script_graph_smoke.rs
- crates/editor-ui/tests/node_graph_material_smoke.rs
- crates/editor-ui/tests/node_graph_anim_graph_smoke.rs
- crates/editor-ui/tests/node_graph_operator_smoke.rs
- crates/editor-ui/tests/phase8_graph_diff_smoke.rs
- docs/**
- ai_handoffs/**, except the Executor's own `ISSUE-73_EXEC_*.md` execution report packet required by the handoff protocol
- .ai/**
- .gitignore
- new-handoff.ps1
- Invoke-AiDispatch*.ps1
- Any production code, scripts, schemas, docs, or packet other than the Executor's own execution report

### MAY add new files
- ai_handoffs/ISSUE-73_EXEC_*.md for the Executor's execution report only.

### MUST NOT add new files
- Any new Rust source or test file
- Any new docs, ADRs, schemas, scripts, crates, Cargo entries, snapshots, fixtures, or handoff packets other than the Executor's own execution report

## Deliverables

- Update `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` so the combined smoke imports and exercises `rge_script_graph::{ScriptEdge, ScriptGraph}` in addition to the existing three graph domains.
- Add a ScriptGraph assertion path in the combined smoke by reusing the setup from `crates/editor-ui/tests/node_graph_script_graph_smoke.rs`: nodes `entry`, `body`, `exit`; edges `flow` and `done`; `NodeGraphWidget::new().model_from(&graph)`.
- Assert through the returned `NodeGraphModel` that ScriptGraph projection exposes 3 nodes, 2 edges, the expected node display names, node kind `ScriptNode`, exact edge endpoints, and edge labels `flow` and `done`.
- Update any in-file test name or comments in the combined smoke that would otherwise falsely claim only three domains are covered. Do not rename the file or change Cargo test target configuration.

## Acceptance Criteria

- `node_graph_phase8_three_domain_smoke.rs` still covers MaterialGraph, AnimGraph, and OperatorGraph exactly as before, with no weakened assertions.
- The combined smoke also covers ScriptGraph through `NodeGraphWidget` and the existing `VizAdapter` implementation; the test must not inspect ScriptGraph internals directly to bypass the widget/model projection.
- No production code is changed.
- No `Cargo.toml` or `Cargo.lock` change is required.
- No files outside `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` are modified, except the Executor's own execution report packet.
- `cargo test -p rge-editor-ui --test node_graph_phase8_three_domain_smoke` exits 0.

## Constraints / Non-Goals

- Do not add or alter any adapter implementation, graph type, editor-ui widget API, or production module.
- Do not change package dependencies. If the combined smoke cannot import `rge_script_graph` with the current dev-dependencies, halt rather than editing Cargo metadata.
- Do not remove or relax the existing single-domain ScriptGraph smoke.
- Do not broaden this dispatch into graph-diff, UI rendering, snapshot, workspace, or architecture-lint work.
- Do not rename `node_graph_phase8_three_domain_smoke.rs`; the requested scope is an in-place consolidation update.

## Verification Gates

The Executor MUST run and document the result of this gate in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-ui --test node_graph_phase8_three_domain_smoke` -> expected exit 0

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Implementing ScriptGraph coverage requires editing production code, Cargo metadata, generated files, scripts, docs, or any test file other than `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs`.
- The existing `crates/editor-ui/tests/node_graph_script_graph_smoke.rs` setup cannot be reused with the current public ScriptGraph API.
- `rge_script_graph` is not already available to the `rge-editor-ui` integration test target through existing dependencies.
- The verification gate fails for a reason not caused by the scoped combined-smoke edit, and the fix would require leaving the allowed edit scope.
- Any requested change would weaken or remove the existing MaterialGraph, AnimGraph, or OperatorGraph assertions.

## Planner Notes

The single-domain ScriptGraph smoke already proves the adapter path for ISSUE-69. This dispatch should copy that public setup style into the combined Phase 8 smoke so one consolidated test target exercises MaterialGraph, AnimGraph, OperatorGraph, and ScriptGraph through the same `NodeGraphWidget`/`VizAdapter` bridge. Keep the production editor-ui surface domain-agnostic.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-73
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
