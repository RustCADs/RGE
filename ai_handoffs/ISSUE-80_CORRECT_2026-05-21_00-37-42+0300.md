# Correction Packet

DISPATCH_ID: ISSUE-80
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_00-37-42+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-80_TASK_2026-05-21_00-32-07+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-80_EXEC_2026-05-21_00-37-01+0300.md`
- Latest Review Report: none; this correction is issued from the post-execution canonical verification gate failure reported by the orchestrator.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it
addresses.

1. **Normalize newline style in diff correctness test** - Post-execution verification gate failure:
   `cargo +nightly fmt --all -- --check` reported `Incorrect newline style in \\?\A:\RCAD\RGE\kernel\graph-foundation\tests\diff_correctness.rs`.
   Required change: convert `kernel/graph-foundation/tests/diff_correctness.rs`
   to the newline style accepted by repository rustfmt, preserving the existing
   test content and making no semantic, formatting, or scope changes beyond the
   newline normalization required for rustfmt to pass. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0 and the diff remains limited
   to the ISSUE-80 test addition plus newline normalization in the same file.

## Deferred Findings (NOT Approved for This Round)

None. No additional reviewer findings or latent risks are approved, deferred,
or created by this correction packet.

## Updated Acceptance Criteria

Unchanged from Task Packet, with one additional correction-specific bar:
the canonical verification gate must no longer fail on newline style in
`kernel/graph-foundation/tests/diff_correctness.rs`.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness -- --nocapture` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Additionally, halt and return control to the
Planner if passing `cargo +nightly fmt --all -- --check` requires any edit
outside `kernel/graph-foundation/tests/diff_correctness.rs` or any semantic
change to the regression test.

## Planner Notes

The execution report indicates the requested regression test passes, but the
canonical post-execution verification gate failed before later CI checks due
solely to newline style in `kernel/graph-foundation/tests/diff_correctness.rs`.
This correction authorizes only newline normalization for that file and the
minimum verification needed to prove the dispatch can pass the canonical gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-80
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
