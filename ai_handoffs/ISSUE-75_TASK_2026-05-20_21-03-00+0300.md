# Task Packet

DISPATCH_ID: ISSUE-75
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_21-03-00+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: OPEN

## Goal

Implement GitHub issue #75 by adding a narrow regression slice for `GraphDiff` direct edge mutations in `kernel/graph-foundation/tests/diff_correctness.rs`. The dispatch should prove that direct edge removal is reported as an edge-only removal when nodes are unchanged, and that mutating an edge payload through `Graph::edge_mut` is reported as an edge-only change with precise `removed_edges`, `changed_edges`, `node_change_count`, and `edge_change_count` results.

## Scope

### MAY edit
- `kernel/graph-foundation/tests/diff_correctness.rs`

### MUST NOT edit
- Any file other than `kernel/graph-foundation/tests/diff_correctness.rs`.
- Any production/runtime code in `kernel/graph-foundation/src/**`, including `Graph`, `GraphDiff`, `GraphSnapshot`, `EdgeRecord`, `NodeId`, or `EdgeId`.
- Any domain graph crates or graph consumers outside `kernel/graph-foundation`.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`, or existing handoff packets.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-75_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, new integration test files, fixtures, docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-75_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add a focused integration test for direct edge removal where the graph's node set and node payloads are unchanged between snapshots.
- Add a focused integration test for mutating an existing edge payload through `Graph::edge_mut`, changing only the edge record's `data` field between snapshots.
- In the direct removal test, assert `removed_edges` contains exactly the removed edge and that `added_edges`, `changed_edges`, `added_nodes`, `removed_nodes`, and `changed_nodes` remain empty.
- In the `edge_mut` payload test, assert `changed_edges` contains exactly the mutated edge with the expected old and new payloads, and that `added_edges`, `removed_edges`, `added_nodes`, `removed_nodes`, and `changed_nodes` remain empty.
- In both tests, assert `node_change_count()` and `edge_change_count()` stay precise.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `kernel/graph-foundation/tests/diff_correctness.rs`.
- The new direct edge removal coverage uses `Graph::remove_edge` or equivalent direct edge removal without deleting any node.
- The direct edge removal test asserts:
  - `diff.removed_edges.len() == 1`
  - `diff.removed_edges` contains the removed `EdgeId`
  - all node-change buckets are empty
  - `diff.added_edges.len() == 0`
  - `diff.changed_edges.len() == 0`
  - `diff.node_change_count() == 0`
  - `diff.edge_change_count() == 1`
- The edge payload mutation test mutates only the existing edge payload via `g.edge_mut(edge_id).unwrap().data = ...`.
- The edge payload mutation test asserts:
  - `diff.changed_edges.len() == 1`
  - `diff.changed_edges` contains the mutated `EdgeId`
  - the changed edge tuple preserves the expected old payload and new payload
  - `diff.removed_edges.len() == 0`
  - `diff.added_edges.len() == 0`
  - all node-change buckets are empty
  - `diff.node_change_count() == 0`
  - `diff.edge_change_count() == 1`
- No merge semantics, endpoint mutation semantics, runtime behavior, public API, dependency, Cargo configuration, documentation, or domain graph crate changes are introduced.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` passes.
- `cargo test -p rge-kernel-graph-foundation` passes.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify `GraphDiff`, graph storage, snapshotting, edge mutation, or edge removal runtime behavior to satisfy the new tests.
- Do not add merge semantics or tests for merge/reconciliation behavior.
- Do not mutate edge endpoints in the `edge_mut` regression; mutate only the payload field.
- Do not broaden the task into domain graph crates, graph consumers, serialization, traversal, invalidation, stable hashing, visualization, or architecture policy.
- Do not add dependencies, fixtures, helper modules, new test files, or Cargo manifest entries.
- Do not rewrite unrelated tests in `diff_correctness.rs` except for minimal local cleanup needed to keep the file readable.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` -> expected exit 0.
- `cargo test -p rge-kernel-graph-foundation` -> expected exit 0.
- `git diff -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected diff is limited to the requested regression tests.
- `git diff --check -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested tests fail against the existing production implementation and passing them would require changing `Graph`, `GraphDiff`, `GraphSnapshot`, `EdgeRecord`, or any other runtime code.
- Any required edit outside `kernel/graph-foundation/tests/diff_correctness.rs` appears necessary.
- Any new file other than the protocol-required execution report and its finalize-generated sidecar appears necessary.
- Implementing the requested coverage appears to require merge semantics, endpoint mutation semantics, or changes to domain graph crates.
- The `edge_mut` API shape differs from the expected mutable `EdgeRecord` access and the payload-only mutation cannot be expressed with a test-only edit.
- Formatting or tests fail for reasons unrelated to the requested regression tests and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #75. Revision number: 0. There is no prior Claude gate result. Keep the implementation narrow: add only direct edge removal and edge payload mutation regression coverage in the existing `diff_correctness` integration test.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-75
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
