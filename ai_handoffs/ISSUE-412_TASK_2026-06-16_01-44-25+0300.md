# Task Packet

DISPATCH_ID: ISSUE-412
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-16_01-44-25+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- ai_handoffs/ISSUE-411_TASK_2026-06-16_00-54-49+0300.md
- ai_handoffs/ISSUE-411_EXEC_2026-06-16_01-08-02+0300.md
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/tests.rs
- crates/editor-shell/src/lifecycle/mod.rs
- crates/cad-projection/src/lib.rs
- crates/cad-projection/src/render_adapter.rs
- crates/cad-projection/src/projection_structural/mod.rs
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
STATUS: OPEN

## Goal

Run GitHub issue #412 / task 157 as a gated source/docs read-only audit of the
post-stale-tracked-CAD-cleanup boundary. The executor must compare current
local source/tests/docs and the dispatcher-provided GitHub-state snapshot, then
pause for operator approval by recording exactly one `NEEDS_HUMAN_RECORDED`
marker plus an immediately following recommendation block in
`.ai/dispatch.tasks.md`. This dispatch must not append task 158, must not append
any feature or implementation task, and must carry the same gated-audit
instruction into the EXEC packet.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md` only for the final
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - source-grounded reason` marker line and
  the immediately following `Recommendation for human approval` block.
- `ai_handoffs/ISSUE-412_EXEC_*.md` only for the current-dispatch EXEC report.
- `ai_handoffs/ISSUE-412_EXEC_*.meta.json` only if packet finalization writes
  the current-dispatch EXEC sidecar.
- `ai_dispatch_logs/log_*` only if the queue runner writes the current-dispatch
  audit log.
- `.ai/dispatch-ISSUE-412/**` only for current-dispatch orchestrator scratch,
  control, and verification artifacts.

### MUST NOT edit
- `crates/**`
- `editor/**`
- `runtime/**`
- `kernel/**`
- `tools/**`
- `docs/**`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/**`
- `AI_DISPATCH_AUTOMATION.md`
- `AI_DISPATCH_PARALLEL.md`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-*.ps1`
- `Register-*.ps1`
- `Get-*.ps1`
- `Watch-*.ps1`
- `Wait-*.ps1`
- `new-handoff.ps1`
- unrelated existing `ai_handoffs/**`, `ai_dispatch_logs/**`, or
  `.ai/dispatch-*/**` artifacts from other dispatches

Do not change render architecture, render-path behavior, mesh-upload behavior,
GPU resource lifetime behavior, CAD-core behavior, cad-projection behavior,
CommandBus behavior, undo/redo behavior, dirty/save-mark behavior,
save/load/open/close behavior, camera/navigation behavior, camera math,
viewport hit testing, face-pick policy, UI/host behavior, plugin routing,
shortcut routing, command-palette behavior, or task-queue automation.

### MAY add new files
- `ai_handoffs/ISSUE-412_EXEC_*.md`
- `ai_handoffs/ISSUE-412_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*`
- `.ai/dispatch-ISSUE-412/**`

### MUST NOT add new files
- New Rust source or test files under `crates/**`, `editor/**`, `runtime/**`,
  `kernel/**`, or `tools/**`.
- New docs, ADRs, schemas, workflows, scripts, Cargo manifests, lockfiles,
  packet templates, architecture-lint rules/config, or unrelated handoff/log
  artifacts.
- Any task heading beyond task 157, including task 158.
- Any feature, implementation, or self-rearming follow-up task.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - ai_handoffs/ISSUE-412_EXEC_*.md
  - ai_handoffs/ISSUE-412_EXEC_*.meta.json
  - ai_dispatch_logs/log_*
  - .ai/dispatch-ISSUE-412/**
MUST_NOT_EDIT:
  - crates/**
  - editor/**
  - runtime/**
  - kernel/**
  - tools/**
  - docs/**
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/**
  - AI_DISPATCH_AUTOMATION.md
  - AI_DISPATCH_PARALLEL.md
  - ai_handoffs/AI_HANDOFF_PROTOCOL.md
  - ai_handoffs/templates/**
  - .ai/dispatch.verify.ps1
  - .ai/*.schema.json
  - Invoke-*.ps1
  - Register-*.ps1
  - Get-*.ps1
  - Watch-*.ps1
  - Wait-*.ps1
  - new-handoff.ps1
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A source/docs read-only audit recorded in the EXEC packet, including the
  source/test/docs reads, the dispatcher-snapshot citation, the candidate
  boundary comparison, and every falsifying search/read used for negative or
  unchanged claims.
- A `.ai/dispatch.tasks.md` final edit that records exactly one marker line:
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - source-grounded reason`, using the actual
  audit date and a source-grounded reason.
- A `Recommendation for human approval` block immediately after that marker,
  covering proposed next feature, exact edit surface, risks, verification, and
  why that is the smallest coherent next step.
- The normal current-dispatch EXEC packet/report, carrying this same gated-audit
  instruction: record `NEEDS_HUMAN_RECORDED`, do not append a feature task, and
  do not append task 158.

## Acceptance Criteria

- The executor uses only the dispatcher-provided GitHub-state snapshot copied
  into this task packet or an exact local issue-body artifact for queue-state
  and already-filed autonomous-task claims. No live `gh`, browser, network,
  GitHub API, or web lookup is run.
- The audit reads current local source/tests/docs before writing the
  recommendation. At minimum it inspects the boundary around
  `EditorShell::clear_stale_tracked_cad_entity`, `CadSceneInspection`, tracked
  CAD entity liveness, tracked render-mesh inspection, graph restore plus
  projection despawn cleanup tests, and adjacent status/task provenance.
- Every practical negative or unchanged source/task-state claim in the audit
  includes a falsifying `rg`/read command and observed result.
- The pre-edit task-heading check proves task 156 and task 157 exist and no
  task 158 heading exists before the marker edit.
- Exactly one new `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - source-grounded reason`
  marker is recorded with the actual audit date, immediately followed by the
  required recommendation block.
- No task 158, no feature task, no implementation task, and no source/docs/
  Cargo/workflow/script/schema changes are added.
- `git diff --name-only` for tracked changes shows only `.ai/dispatch.tasks.md`
  after the final task-brief edit, aside from generated current-dispatch
  handoff/audit/log artifacts visible in status.

## Dispatcher Snapshot Evidence

Use the following dispatcher-provided snapshot as the only GitHub evidence for
this dispatch. It was generated by `Invoke-AiDispatchAuto.ps1` at
`2026-06-16T01:43:55.6697824+03:00`, before ISSUE-412 was created.

- Repo: `RustCADs/RGE`.
- Open `ai-dispatch` issues before ISSUE-412 was created: none.
- Open failed autonomous issues labelled `ai-auto` plus
  `ai-dispatch-failed`: none.
- Autonomous issues already filed in the snapshot are the provided closed
  `ai-auto` issue list through #411, `Clear stale tracked-CAD entity ids`.
- The snapshot includes #410 closed, `Audit post-tracked-CAD-liveness feature
  boundary`, and #411 closed, `Clear stale tracked-CAD entity ids`.
- There is no dispatcher-snapshot evidence of an open queue item or failed
  autonomous issue blocking ISSUE-412 at snapshot time.

The executor must cite this section or an exact local issue-body artifact that
contains the same snapshot. It must not attempt to refresh GitHub state.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion this packet makes about current source or
task state lists its inverse-search/read below. The executor must repeat the
relevant checks immediately before editing because the worktree may have
changed after planning.

- Claim: `.ai/dispatch.tasks.md` currently has task 156 and task 157 headings
  and no task 158 heading.
  Falsifying search:
  `rg -n "^156\.|^157\.|^158\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
  -> matched task 156 at line 15526 and task 157 at line 15655 plus historical
  and instructional `NEEDS_HUMAN_RECORDED` text; no line beginning with `158.`
  was returned.
- Claim: current local source contains the post-cleanup lifecycle helper and
  tracked-CAD inspection surface that motivate this audit.
  Falsifying search:
  `rg -n "clear_stale_tracked_cad_entity|tracked_cad_entity_live|tracked_cad_entity_render_mesh_present|tracked_cad_entity_present|CadSceneInspection|cad_scene_inspection" crates/editor-shell/src/lifecycle`
  -> matched `CadSceneInspection`, the tracked-CAD presence/liveness/
  render-mesh fields, `clear_stale_tracked_cad_entity`, `cad_scene_inspection`,
  the `mod.rs` re-export, and focused lifecycle tests.
- Claim: current local source/tests expose restore/despawn and render-mesh
  evidence relevant to stale tracked-CAD cleanup.
  Falsifying search:
  `rg -n "restore_to|despawn_brep_entity|render_mesh_for|BRepHandle" crates/editor-shell/src/lifecycle crates/cad-projection/src`
  -> matched `CadProjection::despawn_brep_entity`, `render_mesh_for`,
  `BRepHandle`, lifecycle source, and focused lifecycle tests including
  restore/despawn stale-id cleanup.
- Claim: the human/status docs available at planning time primarily record the
  prior ISSUE-410/task-155 audit boundary and selected task 156; this task must
  re-read them before recommending the next feature.
  Falsifying search:
  `rg -n "ISSUE-409|ISSUE-410|ISSUE-411|task 154|task 155|task 156|task 157|tracked-CAD|tracked CAD|CadSceneInspection|cad_scene_inspection|clear_stale_tracked_cad_entity" Status.md HANDOFF.md plans/BASELINE.md change.md`
  -> matched ISSUE-410/task-155 records and the selected task-156 helper
  boundary in `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md`;
  the executor must repeat this read and report the current observed result.

## Constraints / Non-Goals

- This is not an implementation dispatch. Do not edit Rust source/tests,
  documentation, Cargo metadata, workflows, scripts, schemas, packet templates,
  architecture lints, automation, or unrelated generated artifacts.
- Do not append task 158. Do not append a feature task. Do not append any
  self-rearming task.
- Do not mark this as "no-op" without recording the required
  `NEEDS_HUMAN_RECORDED` marker and recommendation block.
- Do not use live GitHub/network evidence. The dispatcher snapshot is the only
  allowed GitHub-state evidence.
- Do not make or recommend a batch. The recommendation may name one smallest
  coherent next feature for human approval, but it must leave actual execution
  gated on the human decision.
- Do not broaden the post-cleanup audit into graph/projection cleanup policy,
  UI wiring, CommandBus/undo/dirty/save authority, render refresh, mesh upload,
  GPU state, persistence, camera/navigation, CAD-core, or cad-projection
  behavior changes.

## Verification Gates

The executor MUST run and document the result of each command in the
`EXECUTION_REPORT`:

- Before edits:
  `rg -n "^156\.|^157\.|^158\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
- Source audit grep:
  `rg -n "clear_stale_tracked_cad_entity|CadSceneInspection|cad_scene_inspection|tracked_cad_entity|add_cad_cuboid_to_empty_scene" crates/editor-shell/src/lifecycle`
- Projection/restore evidence grep:
  `rg -n "restore_to|despawn_brep_entity|render_mesh_for|BRepHandle" crates/editor-shell/src/lifecycle crates/cad-projection/src`
- Docs/task-provenance grep:
  `rg -n "ISSUE-409|ISSUE-410|ISSUE-411|task 154|task 155|task 156|task 157|tracked-CAD|tracked CAD|CadSceneInspection|cad_scene_inspection|clear_stale_tracked_cad_entity" Status.md HANDOFF.md plans/BASELINE.md change.md`
- Dispatcher-snapshot read from this TASK packet or an exact local issue-body
  artifact; expected result is the snapshot generated at
  `2026-06-16T01:43:55.6697824+03:00`, with no live GitHub/network query.
- After edits:
  `rg -n "^156\.|^157\.|^158\.|NEEDS_HUMAN_RECORDED|Recommendation for human approval" .ai/dispatch.tasks.md`
- `git diff --name-only`
- `git status --short --untracked-files=all`
- `git diff --check`
- Finalize dry-run for the EXEC packet:
  `$exec = Get-ChildItem ai_handoffs\ISSUE-412_EXEC_*.md | Sort-Object LastWriteTime -Descending | Select-Object -First 1; .\new-handoff.ps1 -Finalize -DryRun -PacketPath $exec.FullName`

Expected verification outcomes:

- The before-edit heading check proves task 156 and task 157 exist and no task
  158 heading exists before the marker edit.
- The after-edit heading check proves exactly one new
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - source-grounded reason` marker exists for
  this audit, immediately followed by `Recommendation for human approval`, and
  still no task 158 heading exists.
- `git diff --name-only` lists only `.ai/dispatch.tasks.md` for tracked
  changes after execution.
- `git status --short --untracked-files=all` shows only current-dispatch
  handoff/audit/log artifacts as allowed untracked additions.
- `git diff --check` exits 0, allowing only pre-existing line-ending warnings
  if Git prints them.
- The EXEC packet finalize dry-run exits 0.

## Halt Conditions

The executor MUST halt without trying to force the task if any of these occur:

- The dispatcher snapshot cannot be cited from this TASK packet or a local
  copied issue-body artifact without live `gh`, browser, network, GitHub API, or
  web access.
- The pre-edit heading check shows a task 158 heading or another live
  `NEEDS_HUMAN_RECORDED:` marker that would make exactly one final marker
  ambiguous.
- The audit would require editing any MUST-NOT path or performing
  implementation work to make the recommendation coherent.
- More than one next feature would be required to state a coherent
  recommendation.
- The smallest coherent recommendation is a product/architecture decision with
  no bounded feature surface to propose; in that case record the marker with
  the human decision needed and do not invent a feature.
- The canonical verification/correction loop asks the executor to fix an
  out-of-scope failure in source, Cargo, workflows, automation, schemas, docs,
  or another MUST-NOT surface. Preserve the audit scope and halt instead.
- Recording exactly one `NEEDS_HUMAN_RECORDED:` marker plus the required
  recommendation block would disturb existing task provenance.

## Planner Notes

This packet intentionally mirrors task 157 in `.ai/dispatch.tasks.md` while
making the dispatcher snapshot self-contained for the executor sandbox. The
preceding ISSUE-411/task 156 dispatch added
`EditorShell::clear_stale_tracked_cad_entity` and tests proving fresh no-op,
live first-cuboid no-op, and stale restore/despawn cleanup. ISSUE-412 must now
stop the autonomous chain at a recorded operator decision point: source-ground
one recommendation, record it in the task brief, and let the human approve or
reject the next feature before any implementation dispatch exists.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-412
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
