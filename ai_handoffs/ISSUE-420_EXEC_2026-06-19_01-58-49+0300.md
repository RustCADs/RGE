# Execution Report

DISPATCH_ID: ISSUE-420
AUTHOR: Executor / Codex
TIMESTAMP: 2026-06-19_01-58-49+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/commands.rs - corrected CAD add redo replay to restore the original add checkpoint identity before downstream delete replay.
- crates/editor-shell/src/lifecycle/tests.rs - added focused add/delete/undo/undo/redo/redo regression coverage and tightened add redo checkpoint assertion.
- .ai/dispatch.tasks.md - existing dispatch edit from the prior execution; left unchanged in this correction round.
- ai_handoffs/ISSUE-420_EXEC_2026-06-19_01-58-49+0300.md - current execution report.
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-420_TASK_2026-06-19_00-54-20+0300.md`

## Pre-Execution Review Consumed

No pre-execution review packet exists for this correction round. Consumed the
approved correction packet:
`ai_handoffs/ISSUE-420_CORRECT_2026-06-19_01-45-24+0300.md`.

## Current-State Claims: Falsification Re-run

- Claim: `rge-editor-actions` must remain generic and must not gain shell/CAD
  coupling.
  Re-run: `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> exit 1, no matches.

- Claim: `.ai/dispatch.tasks.md` should contain task 162 and exactly one task
  163, no task 164, and no direct task-162 `NEEDS_HUMAN_RECORDED` marker.
  Re-run: `rg -n "^162\.|^163\.|^164\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> exit 0. Observed task 162 at line 16246 and one task 163 at line 16379; no `^164.` heading. The command also prints existing instructional/historical `NEEDS_HUMAN_RECORDED` text in the brief; the task 163 diff contains no direct task-162 marker.

- Claim: tracked source/config diff remains inside the dispatch-authorized
  surface.
  Re-run: `git diff --name-only` -> exit 0 with `.ai/dispatch.tasks.md`,
  `crates/editor-shell/src/lifecycle/commands.rs`, and
  `crates/editor-shell/src/lifecycle/tests.rs`; Git also warned that
  `.ai/dispatch.tasks.md` will be normalized from LF to CRLF when touched.

## What I Changed

### Source

- `crates/editor-shell/src/lifecycle/commands.rs`: changed
  `AddCadCuboidToEmptyScene` to record its original committed checkpoint in a
  `OnceLock<CheckpointId>` on first apply. On redo, the add action now restores
  that original checkpoint and respawns/projection-ticks the cuboid entity
  instead of committing a fresh checkpoint. `DeleteCurrentCadCuboid` remains
  immutable and still validates its captured `from_head`.

### Tests

- `crates/editor-shell/src/lifecycle/tests.rs::delete_current_cad_cuboid_redo_after_redone_add_cad_cuboid_to_empty_scene_succeeds`: covers add cuboid, delete cuboid, undo delete, undo add, redo add, redo delete. It asserts the final redo succeeds, cursor returns to 2, the tracked CAD entity is absent, render lookups are cleared, and selection/face selection for the redone entity are pruned.
- `crates/editor-shell/src/lifecycle/tests.rs::bus_routed_cad_cuboid_add_undo_redo_keeps_scene_coherent_and_moves_dirty_mark`: tightened the redo assertion to require the original add checkpoint, proving add redo preserves checkpoint identity.

### Docs

- No documentation edits in this correction round. `.ai/dispatch.tasks.md` was already modified by the prior execution and was not edited during this correction.

## Per-File Summary

- `commands.rs`: add redo now reuses the originally committed CAD checkpoint
  identity, preserving replay compatibility with the immutable delete command
  stacked after it.
- `tests.rs`: new full-stack regression pins the exact failure reported by
  Codex control.

## Verification Results

- `cargo test -p rge-editor-shell --lib -- delete_current_cad_cuboid` -> exit 0; 4 passed, 0 failed, 323 filtered out.
- `cargo test -p rge-editor-shell --lib -- add_cad_cuboid_to_empty_scene` -> exit 0; 4 passed, 0 failed, 323 filtered out.
- `cargo test -p rge-editor-shell --lib -- clear_stale_tracked_cad_entity` -> exit 0; 4 passed, 0 failed, 323 filtered out.
- `cargo test -p rge-editor-shell --lib -- cad_scene_inspection` -> exit 0; 4 passed, 0 failed, 323 filtered out.
- `cargo check -p rge-editor-actions -p rge-editor-shell` -> exit 0.
- `cargo +nightly fmt --all -- --check` -> exit 0.
- `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> exit 1, no matches.
- `rg -n "^162\.|^163\.|^164\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> exit 0; task 162 and exactly one task 163 observed; no task 164 heading.
- `git diff --name-only` -> exit 0; tracked diff limited to `.ai/dispatch.tasks.md`, `crates/editor-shell/src/lifecycle/commands.rs`, and `crates/editor-shell/src/lifecycle/tests.rs`; warning: `.ai/dispatch.tasks.md` LF will be replaced by CRLF when Git touches it.
- `git diff --check` -> exit 0; same `.ai/dispatch.tasks.md` LF-to-CRLF warning only.
- `.ai/dispatch.verify.ps1` -> first attempt exited 1 at `cargo deny check` because cargo-deny tried to lock `C:\Users\halil/.cargo/advisory-db\db.lock`, which is read-only in this sandbox. Re-run with `USERPROFILE` and `HOME` redirected to ignored scratch `.ai/dispatch-ISSUE-420/home` exited 0. The full gate passed: fmt, architecture lints, architecture-lint tests, cargo deny, workspace all-target tests, workspace doctests, bench compile, and handoff advisory validation. Existing nonfatal warnings included cargo-deny duplicate-version/allowlist warnings and workspace missing-doc warnings.

## Deviations from Task Packet

None - execution stayed within the approved correction scope. No
`editor-actions` code, command-bus contracts, Cargo metadata, scripts, schemas,
or workflows were edited.

## Open Questions for Reviewer

- Please review the `OnceLock<CheckpointId>` replay path in
  `AddCadCuboidToEmptyScene`: first apply still commits the add checkpoint;
  redo restores that same checkpoint so downstream immutable actions can replay
  against their captured head.
- Please confirm the scratch-home redirection for `.ai/dispatch.verify.ps1` is
  acceptable as an environment workaround for this sandbox's read-only
  `C:\Users\halil` advisory lock path. It did not require tracked edits.

## Worktree State

- Tracked files: modified `.ai/dispatch.tasks.md`, `crates/editor-shell/src/lifecycle/commands.rs`, `crates/editor-shell/src/lifecycle/tests.rs`.
- Untracked items: current dispatch handoff artifacts under `ai_handoffs/` and `ai_handoffs/claims/`, including this EXEC packet; sidecar `.meta.json` to be generated by finalization.
- Branch: `ai-dispatch/ISSUE-420`
- Last commit: `518d181` Re-arm dispatch with approved task 162 (shell-only delete-current-cad-cuboid)

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-420
AUTHOR: Executor / Codex
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
