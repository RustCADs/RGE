# Correction Packet

DISPATCH_ID: ISSUE-50
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19T17:26:09+03:00
RELATED_FILES:
- crates/material-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-50_TASK_2026-05-19_17-18-44+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-50_EXEC_2026-05-19_17-25-17+0300.md`
- Latest Review Report: none; correction is from the post-execution verification gate.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction and nothing more. This
correction addresses the post-execution verification gate failure:
`cargo +nightly fmt --all -- --check` reported incorrect newline style in
`crates/material-graph/src/lib.rs`.

1. **Normalize MaterialGraph newline style** - Verification gate failure
   `cargo +nightly fmt --check`. Required change: rewrite
   `crates/material-graph/src/lib.rs` with rustfmt-accepted newline style only.
   Do not change source text, tests, docs, declarations, ordering, whitespace
   beyond newline encoding, or behavior. Acceptance: `cargo +nightly fmt --all
   -- --check` exits 0 and `git diff -- crates/material-graph/src/lib.rs`
   shows no semantic source change beyond newline normalization.

## Deferred Findings (NOT Approved for This Round)

None. No review findings are deferred because no review report was produced for
this correction round.

## Updated Acceptance Criteria

Unchanged from Task Packet, with one correction-specific addition: the
verification gate must no longer fail on newline style in
`crates/material-graph/src/lib.rs`.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-anim-graph` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

If `cargo` is not on `PATH`, use the repository-established toolchain path from
`.ai/dispatch.verify.ps1` when present.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, except this correction round explicitly permits
newline-style normalization of `crates/material-graph/src/lib.rs` only. Halt if
passing verification requires any content or behavior change in MaterialGraph or
any edit outside the files already changed by execution plus the newline-only
normalization approved here.

## Planner Notes

The dispatch implementation itself is not being broadened. The only blocker
reported by the canonical post-execution verification gate is newline style in
`crates/material-graph/src/lib.rs`, so the approved correction is limited to
making that file acceptable to rustfmt without changing its Rust source
content. Do not use this correction round to revisit the AnimGraph test design,
lockfile deviation, or any unrelated workspace issue.

---

HANDOFF_STATUS: COMPLETE
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0
