# Correction Packet

DISPATCH_ID: ISSUE-420
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-19_01-45-24+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/tests.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-420_TASK_2026-06-19_00-54-20+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-420_EXEC_2026-06-19_01-13-54+0300.md`
- Codex Control Review: `needs_changes` result supplied by the orchestrator for this correction round. Orchestrator dispatches do not create `_REVIEW_` packets.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific Codex control finding it
addresses.

1. **Preserve add/delete undo-redo replay**
   - Finding: Codex control review found that `crates/editor-shell/src/lifecycle/commands.rs:706-719` replays `AddCadCuboidToEmptyScene` by committing a new checkpoint on redo, while `crates/editor-shell/src/lifecycle/commands.rs:810` requires `DeleteCurrentCadCuboid` redo to see the original captured `from_head`. The sequence add cuboid, delete cuboid, undo delete, undo add, redo add, redo delete therefore fails on the final redo.
   - Required change: Fix command replay so redoing the add restores or reuses the original add checkpoint identity needed by the subsequent immutable delete command. Keep the delete action immutable, and do not edit `editor-actions` or command-bus contracts.
   - Acceptance: The full sequence add cuboid, delete cuboid, undo delete, undo add, redo add, redo delete succeeds without loosening `DeleteCurrentCadCuboid`'s captured-head validation.

2. **Cover the full stack replay sequence**
   - Finding: Existing tests cover new delete behavior but do not cover redo through the preceding add action.
   - Required change: Add focused coverage in `crates/editor-shell/src/lifecycle/tests.rs` for add cuboid, delete cuboid, undo delete, undo add, redo add, redo delete.
   - Acceptance: The test asserts the final redo succeeds, command cursor returns to 2, the tracked CAD entity is absent, render lookups are cleared, and relevant selections are pruned.

## Deferred Findings (NOT Approved for This Round)

None. Codex control reported one blocking issue, and both approved corrections
above address that issue directly.

## Updated Acceptance Criteria

Unchanged from Task Packet, with this additional correction-round acceptance
bar: the add -> delete -> undo -> undo -> redo -> redo stack replay sequence
must pass and retain immutable delete-command validation.

## Re-Verification Gates

The Executor MUST re-run and record these gates after the corrections:

- `cargo test -p rge-editor-shell --lib -- delete_current_cad_cuboid` -> expected exit 0, including the new full stack replay coverage.
- `cargo test -p rge-editor-shell --lib -- add_cad_cuboid_to_empty_scene` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib -- clear_stale_tracked_cad_entity` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib -- cad_scene_inspection` -> expected exit 0.
- `cargo check -p rge-editor-actions -p rge-editor-shell` -> expected exit 0.
- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> expected no matches, exit 1.
- `rg -n "^162\.|^163\.|^164\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> expected task 162 and exactly one task 163; no task 164; no direct task-162 `NEEDS_HUMAN_RECORDED` unless task 163 could not safely be appended.
- `git diff --name-only` -> expected only `crates/editor-shell/src/lifecycle/commands.rs`, `crates/editor-shell/src/lifecycle/tests.rs`, `.ai/dispatch.tasks.md`, and generated current-dispatch handoff/log artifacts.
- `git diff --check` -> expected exit 0; record any warning observed.
- `.ai/dispatch.verify.ps1` -> expected exit 0.
- `git status --short --branch` -> expected exit 0, with no unexpected files outside this dispatch's authorized work products.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. In addition, halt and report back if the fix would
require editing `editor-actions`, command-bus contracts, or any source, docs,
schema, or script file outside the correction scope above.

## Planner Notes

This correction round is intentionally narrow. The issue is not the delete
command's immutability check; it is the add command's redo behavior creating a
fresh checkpoint that breaks subsequent replay of an already captured delete.
Fix the replay semantics at the command-lifecycle layer and prove it with the
focused stack regression test before re-running the canonical gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-420
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
