# Task Packet

DISPATCH_ID: ISSUE-52
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_18-18-52+0300
RELATED_FILES:
- crates/anim-graph/src/lib.rs
- kernel/graph-foundation/src/diff.rs
STATUS: OPEN

## Goal

Add a narrow AnimGraph structural-diff smoke test for GitHub issue #52. The test should prove that an `AnimGraph` captured through `GraphSnapshot` participates in graph-foundation's `GraphDiff::between` path by snapshotting an old graph, mutating the same graph with exactly one added animation state and exactly one added transition, and asserting that the diff reports only those added node and edge records.

## Scope

### MAY edit
- `crates/anim-graph/src/lib.rs`, limited to the existing `#[cfg(test)] mod tests` and that test module's imports/helpers only.

### MUST NOT edit
- Any production code outside `crates/anim-graph/src/lib.rs`'s existing test module.
- `crates/anim-graph/Cargo.toml`.
- `Cargo.toml`, `Cargo.lock`, or any workspace manifest/lockfile.
- `kernel/graph-foundation/**`; it may be inspected only to understand `GraphDiff`.
- `crates/material-graph/**`; it may be inspected only as read-only precedent.
- Runtime, editor, renderer, asset, ECS, material, graph-viewer, tooling, or any other crate.
- Documentation, schemas, scripts, `.gitignore`, CI files, architecture-lint tooling, or any existing handoff packet.

### MAY add new files
- Protocol output only: the Executor's `ai_handoffs/ISSUE-52_EXEC_*.md` report and its `.meta.json` sidecar if finalized by `new-handoff.ps1`.

### MUST NOT add new files
- New source files, integration test files, fixtures, snapshots/goldens, crates, modules, docs, ADRs, schemas, scripts, Cargo entries, or task/correction/review/closeout packets.

## Deliverables

- Add exactly one new unit test in `crates/anim-graph/src/lib.rs`'s existing `#[cfg(test)] mod tests`; the required test name is `structural_diff_reports_added_anim_state_and_transition`.
- The test must build an initial `AnimGraph`, add at least two animation states, add at least one existing transition, and capture the old snapshot with `GraphSnapshot::from_graph(&g.graph)`.
- The test must mutate that same `AnimGraph` after the old snapshot by adding exactly one additional animation state and exactly one additional transition connected to that new state.
- The test must capture the new snapshot with `GraphSnapshot::from_graph(&g.graph)`, run `GraphDiff::between(&old_snapshot, &new_snapshot)`, and assert the structural diff results.
- The test must assert that `diff.added_nodes` contains exactly the newly added `NodeId` with the expected `AnimState` payload.
- The test must assert that `diff.added_edges` contains exactly the newly added `EdgeId` with the expected `EdgeRecord`: source node, destination node, and `AnimTransition` trigger payload.
- The test must assert that `removed_nodes`, `removed_edges`, `changed_nodes`, and `changed_edges` are all empty.

## Acceptance Criteria

- Only `crates/anim-graph/src/lib.rs` is changed for implementation, and changes are limited to the test module/imports/helpers needed by the new unit test.
- The new test uses `rge_kernel_graph_foundation::GraphDiff::between` on `GraphSnapshot` values captured from the private `AnimGraph::graph` field.
- The old snapshot is taken before adding the one new state and one new transition; the new snapshot is taken after those exact two structural additions.
- The assertions verify concrete added node and edge records rather than checking counts only.
- No public `AnimGraph` API, public inner-graph accessor, snapshot wrapper API, traversal helper, runtime behavior, editor behavior, or graph-foundation behavior is added.
- `cargo test -p rge-anim-graph structural_diff_reports_added_anim_state_and_transition` exits 0.

## Constraints / Non-Goals

- This dispatch is test coverage only.
- Do not add or change serde derives, serialization behavior, snapshot RON behavior, `VizAdapter` behavior, traversal, evaluation, blend trees, runtime state-machine logic, editor behavior, renderer integration, ECS integration, asset integration, material graph behavior, or graph-foundation behavior.
- Do not change `AnimGraph` semantics, state id derivation, transition id derivation, duplicate detection, dangling endpoint behavior, graph ordering, payload shapes, public method signatures, or error behavior.
- Do not add or change dependencies, feature flags, manifests, lockfiles, generated files, architecture-lint policy, or workspace configuration.
- Do not broaden the task into MaterialGraph, graph-foundation tests, runtime/editor work, documentation, or workspace-wide testing.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-anim-graph structural_diff_reports_added_anim_state_and_transition` -> expected exit 0.

If the local shell cannot find `cargo`, the Executor may use the repository's established Rust toolchain location (`A:\RustCache\cargo\bin\cargo.exe`) if present, matching `.ai/dispatch.verify.ps1`; otherwise halt and report the missing tool.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The change cannot be completed by editing only `crates/anim-graph/src/lib.rs` within the existing test module/imports/helpers.
- The implementation appears to require edits to `crates/anim-graph/Cargo.toml`, `Cargo.lock`, workspace manifests, graph-foundation, material-graph, or any crate other than anim-graph.
- The test cannot access the private substrate graph from the in-crate unit-test module without adding public API.
- The solution requires traversal, evaluation, blend trees, runtime state-machine logic, editor behavior, renderer integration, ECS integration, asset integration, serialization changes, `VizAdapter` changes, or broad public API.
- Cargo attempts to modify any manifest, lockfile, generated file, or file outside the MAY edit scope.
- `cargo test -p rge-anim-graph structural_diff_reports_added_anim_state_and_transition` fails after the scoped test change and the failure is not an obvious typo that can be fixed within the allowed test-only scope.
- A pre-existing environment problem, such as missing cargo after checking the established RustCache path, prevents running the required verification gate.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #52. Revision number: 0. There is no prior Claude gate result.

`crates/material-graph/src/lib.rs` contains a read-only precedent test named `structural_diff_reports_added_material_node_and_edge`. Mirror that coverage shape for `AnimGraph`: use `GraphSnapshot::from_graph(&g.graph)` before and after the mutation, import `GraphDiff` only in the test module as needed, and compare the exact `AnimState` and `AnimTransition` records reported by the graph-foundation diff. Keep the dispatch strictly test-only.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-52
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
