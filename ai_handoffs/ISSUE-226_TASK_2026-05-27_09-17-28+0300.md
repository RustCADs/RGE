# Task Packet

DISPATCH_ID: ISSUE-226
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_09-17-28+0300
RELATED_FILES:
- Invoke-AiDispatchLoop.ps1
- tools/dispatch-tests/**
- AI_DISPATCH_AUTOMATION.md
- new-handoff.ps1
- ai_handoffs/templates/TASK_PACKET.md
STATUS: OPEN

## Goal

Harden the `Invoke-PlanFill` planner prompt so a Codex planner can produce a
valid TASK packet even when it cannot read `ai_handoffs/templates/TASK_PACKET.md`,
and add focused regression coverage for the ISSUE-224 failure mode where TASK
finalization rejected a packet missing required top-level header fields.

## Scope

### MAY edit
- `Invoke-AiDispatchLoop.ps1`
- `tools/dispatch-tests/**`
- `AI_DISPATCH_AUTOMATION.md`

### MUST NOT edit
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchAuto.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/templates/TASK_PACKET.md`
- `ai_handoffs/templates/**`
- `.ai/handoff.schema.json`
- `.ai/codex_control.schema.json`
- `.ai/dispatch.verify.ps1`
- `.ai/dispatch.tasks.md`
- `.github/workflows/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `crates/**`
- `kernel/**`
- `runtime/**`
- `editor/**`
- `HANDOFF.md`
- `Status.md`
- `change.md`
- Roadmap or status documentation outside the optional planner-prompt excerpt
  in `AI_DISPATCH_AUTOMATION.md`
- Existing `ai_handoffs/**` packets or sidecars except this dispatch's own
  ISSUE-226 generated artifacts
- Existing `ai_dispatch_logs/**` logs except this dispatch's own queue log
- Sandbox or scratch worktrees
- Any file outside the MAY edit list and this dispatch's MAY add new files list

### MAY add new files
- `tools/dispatch-tests/**`
- `ai_handoffs/ISSUE-226_TASK_*.md`
- `ai_handoffs/ISSUE-226_TASK_*.meta.json`
- `ai_handoffs/ISSUE-226_EXEC_*.md`
- `ai_handoffs/ISSUE-226_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-226_CORRECT_*.md`
- `ai_handoffs/ISSUE-226_CORRECT_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files
- New production source, Rust source, Cargo metadata, workflows, schemas,
  queue or scheduler artifacts, documentation outside the optional
  `AI_DISPATCH_AUTOMATION.md` excerpt, or handoff/log artifacts for any dispatch
  other than ISSUE-226.

## Deliverables

- Update only the `Invoke-PlanFill` planner prompt Rules list in
  `Invoke-AiDispatchLoop.ps1`, or a narrowly extracted helper in the same file
  if static testing requires it.
- Make the planner prompt self-sufficient by explicitly requiring TASK header
  fields `DISPATCH_ID`, `AUTHOR`, `TIMESTAMP`, `RELATED_FILES`, and `STATUS`.
- Make the planner prompt explicitly require the complete machine-readable
  completion footer fields `HANDOFF_STATUS`, `DISPATCH_ID`, `AUTHOR`,
  `NEXT_ROLE`, and `EXIT_CODE`.
- Make the prompt state that every top-level header field and the full
  machine-readable completion footer must be preserved even if Codex cannot read
  `ai_handoffs/templates/TASK_PACKET.md`.
- Preserve the existing backtick-token rule for `### MAY edit` and
  `### MAY add new files`.
- Add focused Pester regression coverage under `tools/dispatch-tests/**` proving
  the production planner prompt text contains the required header/footer
  contract.
- Add focused Pester regression coverage using a synthetic temp TASK packet
  proving `new-handoff.ps1 -Finalize -DryRun` rejects missing top-level header
  fields and names the missing fields in the failure text.
- Update only the planner-prompt excerpt or gotcha text in
  `AI_DISPATCH_AUTOMATION.md` if needed to keep documentation synchronized with
  the implementation.

## Acceptance Criteria

- The production planner prompt text contains the required TASK header field
  names: `DISPATCH_ID`, `AUTHOR`, `TIMESTAMP`, `RELATED_FILES`, and `STATUS`.
- The production planner prompt text contains the complete footer field names:
  `HANDOFF_STATUS`, `DISPATCH_ID`, `AUTHOR`, `NEXT_ROLE`, and `EXIT_CODE`.
- The production planner prompt text says the TASK packet must preserve every
  top-level header field and the full machine-readable completion footer even if
  Codex cannot read `ai_handoffs/templates/TASK_PACKET.md`.
- The existing backtick-token rule for `### MAY edit` and
  `### MAY add new files` remains present in the planner prompt.
- The new Pester coverage statically inspects `Invoke-AiDispatchLoop.ps1` or its
  narrowly extracted prompt helper and does not invoke `codex`, `claude`, `gh`,
  `Invoke-AiDispatchQueue.ps1`, `Invoke-AiDispatchAuto.ps1`, a scheduler, the
  network, or a real dispatch loop.
- The finalizer regression uses a synthetic temp packet and `new-handoff.ps1
  -Finalize -DryRun`; it does not edit `new-handoff.ps1` or
  `ai_handoffs/templates/TASK_PACKET.md`.
- The finalizer regression asserts that missing top-level TASK header fields are
  named in the failure text.
- No queue scope-guard parsing, label behavior, publish behavior, retry behavior,
  failure taxonomy behavior, scheduler behavior, or autonomous selection behavior
  changes.
- Windows PowerShell 5.1 compatibility is preserved.
- Static inspection confirms the final diff stays within the allowed file
  surface and this dispatch's generated artifacts.

Verbatim review-gate strings:

```text
MUST harden only the Invoke-PlanFill planner prompt so the TASK header/footer contract is self-contained
MUST explicitly require DISPATCH_ID AUTHOR TIMESTAMP RELATED_FILES STATUS in the TASK header
MUST explicitly require HANDOFF_STATUS DISPATCH_ID AUTHOR NEXT_ROLE EXIT_CODE in the TASK footer
MUST preserve the existing backtick-token rule for ### MAY edit and ### MAY add new files
MUST add Pester regression coverage that does not run codex claude gh the queue or a real dispatch
MUST NOT edit Queue Auto scheduler new-handoff templates schemas workflows Rust Cargo task brief status docs publish behavior labels retries failure taxonomy or scope guard parsing
MUST run PowerShell parser validation, Pester dispatch tests, PSScriptAnalyzer, git diff --check, and canonical .ai/dispatch.verify.ps1 successfully
```

## Constraints / Non-Goals

- This dispatch is for the issue queue in branch mode with `-NoPublish`; do not
  use `Invoke-AiDispatchAuto.ps1`.
- The implementation must harden only the planner prompt contract and focused
  regression coverage for that contract.
- Do not change queue or auto behavior, publish behavior, labels, retry behavior,
  failure taxonomy, scheduler behavior, scope-guard parsing, or autonomous task
  selection.
- Do not change the finalizer implementation in `new-handoff.ps1`; the
  regression must prove the existing finalizer rejects malformed TASK packets.
- Do not change `ai_handoffs/templates/TASK_PACKET.md`; the prompt must become
  sufficient for the no-template-read fallback.
- Do not run `codex`, `claude`, `gh`, the real queue runner, the scheduler, a
  network call, or a real dispatch loop as part of the regression.
- Do not broaden the task into unrelated prompt, protocol, queue, scheduler,
  workflow, Rust, Cargo, status, or roadmap changes.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- PowerShell parser validation for `Invoke-AiDispatchLoop.ps1` reports zero
  parser errors, for example by calling
  `[System.Management.Automation.Language.Parser]::ParseFile(...)`.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Import-Module Pester -MinimumVersion 5.0 -ErrorAction Stop; Invoke-Pester -Path 'tools/dispatch-tests' -CI"`
  exits 0.
- PSScriptAnalyzer over tracked PowerShell files reports zero findings using
  `PSScriptAnalyzerSettings.psd1` and the same surface as
  `.github/workflows/powershell.yml`: tracked `*.ps1` and `*.psm1` files from
  `git ls-files`, excluding `(^|/)\.ai/dispatch-`.
- `git diff --check` exits 0.
- `.ai/dispatch.verify.ps1` exits 0.
- Static inspection of `git diff --name-only` confirms no changes outside
  `Invoke-AiDispatchLoop.ps1`, `tools/dispatch-tests/**`, optional
  `AI_DISPATCH_AUTOMATION.md`, and this dispatch's own generated artifacts.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- The fix requires editing anything outside `Invoke-AiDispatchLoop.ps1`,
  `tools/dispatch-tests/**`, optional `AI_DISPATCH_AUTOMATION.md` prompt
  documentation, or this dispatch's own generated artifacts.
- A correct regression requires running `codex`, `claude`, `gh`, the queue
  runner, the scheduler, a network call, or a real dispatch.
- The implementation would change queue or auto behavior, publish behavior,
  label behavior, retry behavior, failure taxonomy, scheduler behavior,
  autonomous selection behavior, or scope-guard parsing.
- The fix requires changing `new-handoff.ps1` or
  `ai_handoffs/templates/TASK_PACKET.md`.
- Windows PowerShell 5.1 compatibility cannot be preserved.
- The verification gates cannot be run as specified for reasons unrelated to
  missing local dependencies; if a dependency is missing, document the exact
  missing dependency and stop for guidance rather than replacing the gate.

## Planner Notes

ISSUE-224 exposed a deterministic planning-phase failure: Codex produced a TASK
packet body but omitted required top-level header/footer fields, and
`new-handoff.ps1 -Finalize -DryRun` correctly rejected it. The intended fix is
not to weaken validation or alter the packet template. The prompt itself must
carry enough of the header/footer contract that the planner can preserve the
required fields even when the template file is unavailable during planning.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-226
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
