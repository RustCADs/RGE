# Task Packet

DISPATCH_ID: ISSUE-7
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-17_23-21-38+0300
RELATED_FILES:
- AGENTS.md
- Invoke-AiDispatchAuto.ps1
- Register-AiDispatchSchedule.ps1
- .ai/dispatch.tasks.md
STATUS: OPEN

## Goal

Document the autonomous dispatch entry points in `AGENTS.md` so future agents
can find the unattended task selector and the Scheduled Task registration
script from the existing "Pointers" list without reconstructing those roles
from the repository tree.

## Scope

### MAY edit
- `AGENTS.md`
- `ai_handoffs/ISSUE-7_EXEC_*.md`, only as the required execution report
- `ai_handoffs/ISSUE-7_EXEC_*.meta.json`, only if created by finalizing the
  execution report

### MUST NOT edit
- Any repository content file other than `AGENTS.md`
- `Invoke-AiDispatchAuto.ps1`
- `Register-AiDispatchSchedule.ps1`
- Any other dispatch, queue, watcher, handoff, schema, source, build, status,
  or documentation file
- Any existing packet in `ai_handoffs/`, including this `TASK` packet

### MAY add new files
- `ai_handoffs/ISSUE-7_EXEC_*.md`, only as the required execution report
- `ai_handoffs/ISSUE-7_EXEC_*.meta.json`, only if created by finalizing the
  execution report

### MUST NOT add new files
- Any file outside the protocol artifacts explicitly allowed above

## Deliverables

- `AGENTS.md` has a concise "Pointers" bullet for
  `Invoke-AiDispatchAuto.ps1`.
- `AGENTS.md` has a concise "Pointers" bullet for
  `Register-AiDispatchSchedule.ps1`.
- The executor writes and finalizes the required `EXECUTION_REPORT` packet,
  unless finalization is blocked by the packet tooling.

## Acceptance Criteria

- The new `Invoke-AiDispatchAuto.ps1` bullet is in the existing "Pointers"
  list, follows the neighboring pointer style, and describes it as the
  autonomous driver that selects tasks from `.ai/dispatch.tasks.md`.
- The new `Register-AiDispatchSchedule.ps1` bullet is in the existing
  "Pointers" list, follows the neighboring pointer style, and describes it as
  registering the unattended Scheduled Task.
- The update is concise: no new section, no broad rewrite, and no explanatory
  paragraph outside the pointer bullets.
- No repository content file changes except `AGENTS.md`; only the allowed
  `ISSUE-7_EXEC` protocol artifacts may be added under `ai_handoffs/`.
- No commits or pushes are performed.

## Constraints / Non-Goals

- Do not change dispatch automation behavior.
- Do not edit, reformat, or audit either PowerShell script.
- Do not expand `AGENTS.md` beyond the two requested pointer bullets.
- Do not edit `AI_DISPATCH_AUTOMATION.md`, `AI_DISPATCH_PARALLEL.md`,
  `HANDOFF.md`, `Status.md`, `change.md`, `CLAUDE.md`, `.gitignore`, schemas,
  source files, or build files.
- Do not create `REVIEW`, `CORRECT`, or `CLOSEOUT` packets for this
  orchestrator dispatch.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `git diff -- AGENTS.md` and confirm the diff is limited to the two requested
  "Pointers" bullets.
- `git diff --name-only -- . ":(exclude)AGENTS.md" ":(exclude)ai_handoffs/*"`
  and confirm it prints no paths.
- `git status --short` and confirm any changed or untracked paths are limited
  to `AGENTS.md` plus the protocol packet artifacts for this dispatch.

No cargo, build, or test command is required for this documentation-only
change.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- The "Pointers" section cannot be found in `AGENTS.md`.
- Satisfying the request appears to require editing any repository content file
  other than `AGENTS.md`.
- Existing text already contains equivalent bullets and adding more would
  duplicate the pointers.
- Any verification gate shows repository content changes outside `AGENTS.md`.
- `new-handoff.ps1` cannot scaffold or finalize the required execution report.

## Planner Notes

This is GitHub issue #7, filed automatically by `Invoke-AiDispatchAuto.ps1`
from `.ai/dispatch.tasks.md`. The requested source change is intentionally
small: add only the two autonomous-dispatch pointers to the existing list and
preserve the current style.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-7
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
