# Task Packet

DISPATCH_ID: ISSUE-1
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-17_10-23-23+0300
RELATED_FILES:
- Status.md
- HANDOFF.md
- AI_DISPATCH_AUTOMATION.md
- AI_DISPATCH_PARALLEL.md
STATUS: OPEN

## Goal

Perform a docs-only smoke test for GitHub issue #1, "Reconcile dispatch docs
with automated queue state." Audit the current dispatch-status and automation
docs for stale wording introduced before the automated GitHub issue dispatcher
landed, then update only the allowed docs if needed so they accurately describe
the active queue model.

## Scope

### MAY edit
- Status.md
- HANDOFF.md
- AI_DISPATCH_AUTOMATION.md
- AI_DISPATCH_PARALLEL.md

### MUST NOT edit
- Any Rust source, tests, Cargo manifests, lockfiles, shaders, assets, or
  generated build outputs.
- Any PowerShell scripts, including Invoke-AiDispatchQueue.ps1,
  Invoke-AiDispatchLoop.ps1, Watch-AiDispatch.ps1, and new-handoff.ps1.
- Any schemas, templates, .gitignore, GitHub workflow files, ADRs, plans,
  README.md, change.md, or docs outside the four MAY-edit files.
- Any existing ai_handoffs packet, except for the new ISSUE-1 execution report
  required by the protocol.

### MAY add new files
- ai_handoffs/ISSUE-1_EXEC_*.md
- ai_handoffs/ISSUE-1_EXEC_*.meta.json, only if produced by new-handoff.ps1
  while finalizing the EXECUTION_REPORT.

### MUST NOT add new files
- New source, tests, docs, scripts, schemas, workflow files, ADRs, plans, or
  any additional handoff packets beyond the ISSUE-1 EXECUTION_REPORT and its
  optional generated sidecar.

## Deliverables

- A per-file audit of Status.md, HANDOFF.md, AI_DISPATCH_AUTOMATION.md, and
  AI_DISPATCH_PARALLEL.md for stale automated-dispatch wording.
- Minimal edits to the MAY-edit docs if stale wording is found.
- An EXECUTION_REPORT that lists each audited file as either changed or
  "audited, no change needed."
- No source-code, script, schema, template, workflow, or push-related changes.

## Acceptance Criteria

- The allowed docs, after any edits, reflect these current facts:
  - Claude gate and execute outputs are marker-based and active:
    GATE_VERDICT, EXEC_STATUS, and EXEC_PACKET markers drive control flow.
  - Invoke-AiDispatchQueue.ps1 is the GitHub issue queue runner.
  - Recurring /loop usage is semi-attended and bound to the current local
    authenticated CLI/session, not a headless cloud daemon.
  - Open GitHub issues labelled ai-dispatch are the queue work source.
  - Queue publish policy is local commit only on the per-issue branch; never
    push automatically.
- Historical snapshot text is not broadly rewritten. If a historical line is
  stale, prefer adding or adjusting current-state wording near the top of the
  file instead of mutating old chronology.
- If the audit finds the four docs already accurate, no doc edits are made and
  the EXECUTION_REPORT explicitly records the audit-only result.
- git status at completion shows only allowed doc edits plus the ISSUE-1 EXEC
  packet and optional generated sidecar.

## Constraints / Non-Goals

- This is docs-only. Do not change dispatcher behavior.
- Do not implement, refactor, or harden Invoke-AiDispatchQueue.ps1 or
  Invoke-AiDispatchLoop.ps1.
- Do not contact GitHub or require network access; inspect local files only.
- Do not add new policy documents, ADRs, schemas, templates, or queue protocol
  variants.
- Do not push. If this dispatch is later published by the queue runner, the
  intended policy is local commit only, never automatic push.

## Verification Gates

The Executor MUST run and document the result of each of these in their
EXECUTION_REPORT:

- `git status --short --branch` before editing and after editing -> expected
  changed files are limited to the MAY-edit docs plus the ISSUE-1 EXEC packet
  and optional generated sidecar.
- `git diff -- Status.md HANDOFF.md AI_DISPATCH_AUTOMATION.md AI_DISPATCH_PARALLEL.md`
  -> expected docs-only diff, or no diff if the audit is clean.
- `git diff --check -- Status.md HANDOFF.md AI_DISPATCH_AUTOMATION.md AI_DISPATCH_PARALLEL.md`
  -> expected exit 0 if any docs changed; also acceptable to run with no doc
  changes and record exit 0.
- `Select-String -Path Status.md,HANDOFF.md,AI_DISPATCH_AUTOMATION.md,AI_DISPATCH_PARALLEL.md -Pattern 'Invoke-AiDispatchQueue.ps1','ai-dispatch','GATE_VERDICT','EXEC_STATUS','EXEC_PACKET','/loop','local commit','push'`
  -> expected output sufficient to support the per-file audit conclusions.
- `$execPacket = (Get-ChildItem -LiteralPath ai_handoffs -Filter 'ISSUE-1_EXEC_*.md' | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName; .\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`
  -> expected exit 0.

## Halt Conditions

The Executor MUST halt without committing, pushing, or broadening scope and
write an EXECUTION_REPORT with STATUS: BLOCKED or NEEDS_HUMAN if any of the
following occur:

- Correcting the docs requires editing any file outside the MAY-edit list.
- The local scripts contradict the target facts above in a way that cannot be
  reconciled as documentation drift.
- Verification would require GitHub network access, gh authentication changes,
  or remote mutation.
- The work appears to require source-code, script, schema, template, workflow,
  ADR, or protocol changes.
- The working tree contains unrelated tracked edits in the MAY-edit files and
  the Executor cannot make a minimal non-destructive patch around them.

## Planner Notes

The target facts come from the landed automated queue runner and the current
single-dispatch automation model. Use Invoke-AiDispatchQueue.ps1 and
Invoke-AiDispatchLoop.ps1 as read-only references if needed, but do not edit
them. The likely stale wording is top-level status/handoff language that still
frames ai_handoffs as documentation-only with no runtime automation. Keep the
update small: reconcile current-state language, leave historical chronology
intact, and record no-change audit findings where a file is already current.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-1
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
