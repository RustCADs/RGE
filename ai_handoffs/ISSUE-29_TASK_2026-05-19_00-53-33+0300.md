# Task Packet

DISPATCH_ID: ISSUE-29
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_00-53-33+0300
RELATED_FILES:
- crates/cad-projection/tests/face_selection_partition_smoke.rs
- crates/cad-projection/tests/round_fillet_brep_face_id_lookup_smoke.rs
- crates/cad-core/src/operators/round_fillet/cuboid.rs
STATUS: OPEN

## Goal

Add the GitHub issue #29 cad-projection smoke proving that `FaceSelectionSet::partition` preserves upstream Cuboid face selections when the projected entity is bound to a `CuboidOp -> RoundFilletOp` root. This fills the editor-selection persistence coverage gap adjacent to the existing `FilletOp` partition smoke and the existing RoundFillet face-ID lookup smoke, without changing production BRep identity or projection resolver behavior.

## Scope

### MAY edit
- crates/cad-projection/tests/face_selection_partition_smoke.rs

Within that file, the Executor may add the test, update the file-level test inventory comment, and make tiny import/helper adjustments needed by the new test.

### MUST NOT edit
- crates/cad-projection/src/*
- crates/cad-core/src/*
- crates/editor-state/*
- crates/kernel-ecs/*
- crates/cad-projection/tests/round_fillet_brep_face_id_lookup_smoke.rs
- Any `Cargo.toml` or lockfile
- Any documentation, ADR, status, dispatch automation, schema, script, template, or gitignore file
- Any handoff packet other than the protocol-required execution report for this dispatch

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-29_EXEC_*.md`

### MUST NOT add new files
- Any new Rust source or test file
- Any new fixture, snapshot, generated artifact, documentation file, schema, script, or packet other than the protocol-required execution report

## Deliverables

- A new `#[test]` in `crates/cad-projection/tests/face_selection_partition_smoke.rs` covering `FaceSelectionSet::partition` against a projected `CuboidOp -> RoundFilletOp` root.
- The test constructs `RoundFilletOp` via `RoundFilletOp::new` using one or more real upstream Cuboid `BRepEdgeId`s obtained from `CuboidOp::brep_edge_ids(TEST_OWNER)`.
- The test builds and commits a graph where the Cuboid node is connected into the RoundFillet node and the graph root is the RoundFillet node.
- The projected entity is spawned or remapped against the RoundFillet node, not the upstream Cuboid node, and its `BRepHandle.brep_owner` is set to `TEST_OWNER` before projection assertions.
- The `FaceSelectionSet` is built from the upstream Cuboid's six `BRepProvider` face IDs for the same entity and owner.
- The test partitions the set through `CadProjection::face_resolves_in_projection` and asserts exactly 6 survivors and 0 invalidated selections.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- `cargo test -p rge-cad-projection --test face_selection_partition_smoke` exits 0.
- The new smoke is distinct from the existing `FilletOp` partition smoke and specifically exercises `OperatorNode::RoundFillet`.
- No `BRepEdgeId` is synthesized by hand; every edge passed into `RoundFilletOp::new` comes from the upstream Cuboid's `BRepEdgeProvider` surface.
- The entity under test is bound to the RoundFillet root, so the assertion proves selection preservation through the projected RoundFillet output rather than through the upstream Cuboid projection.
- The partition result is explicitly checked as `survivors.len() == 6` and `invalidated.len() == 0`.
- The implementation does not change production BRep identity derivation, RoundFillet tessellation, face-label emission, cad-projection resolver behavior, or `FaceSelectionSet::partition`.
- Tracked project-file changes are limited to `crates/cad-projection/tests/face_selection_partition_smoke.rs`, plus the protocol-required execution report packet.

## Constraints / Non-Goals

- Do not fix, relax, or redesign BRep identity rules.
- Do not modify `RoundFilletOp`, `CuboidOp`, topology resolution, projection lookup, `FaceSelectionSet`, ECS handles, or production source.
- Do not broaden this into new RoundFillet geometry, tessellation, picking, or degenerate-cap coverage; those surfaces are already covered elsewhere.
- Do not add a separate test file or move existing tests.
- Do not update GitHub issue metadata, labels, comments, branches, commits, or remotes.
- Do not run broad workspace gates unless needed for diagnosis after the targeted gate fails.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-cad-projection` -> expected exit 0.
- `cargo test -p rge-cad-projection --test face_selection_partition_smoke` -> expected exit 0, including the new RoundFillet partition smoke.
- `git diff -- crates/cad-projection/tests/face_selection_partition_smoke.rs` -> expected diff is limited to the new test, any file-level inventory note, and tiny import/helper changes inside that file.
- `git status --short` -> expected tracked project-file change is limited to `crates/cad-projection/tests/face_selection_partition_smoke.rs`, plus the protocol-required execution report packet.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The new test cannot be implemented without changing production BRep identity, RoundFillet, topology resolver, cad-projection, editor-state, or ECS code.
- `RoundFilletOp::new` cannot be constructed from real `CuboidOp::brep_edge_ids(TEST_OWNER)` edge IDs.
- The test would need to synthesize `BRepEdgeId`s, bind the entity to the Cuboid node instead of the RoundFillet root, or assert fewer than 6 surviving upstream Cuboid face selections.
- Any existing `face_selection_partition_smoke.rs` test has to be weakened, deleted, ignored, or have its expected counts changed.
- Verification shows tracked project-file changes outside `crates/cad-projection/tests/face_selection_partition_smoke.rs`.
- The targeted test failure appears to expose a production bug rather than a missing smoke; do not fix production code under this dispatch.

## Planner Notes

Use the existing `face_selection_partition_preserves_upstream_faces_on_filleted_output` test as the closest structural model, but add a separate RoundFillet-specific smoke rather than replacing the Fillet coverage. Use `crates/cad-projection/tests/round_fillet_brep_face_id_lookup_smoke.rs` only as read-only reference for constructing a `CuboidOp -> RoundFilletOp` graph from real upstream Cuboid edge IDs and spawning the projected entity on the RoundFillet root.

The expected core shape is: create a Cuboid, get `let edges = cuboid.brep_edge_ids(TEST_OWNER)`, call `RoundFilletOp::new(&cuboid, TEST_OWNER, vec![edges[0]], 0.1)`, connect Cuboid to RoundFillet, set the RoundFillet node as root, project that root, build the face-selection set from the upstream Cuboid faces, and assert partition yields 6 survivors / 0 invalidated.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-29
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
