# Task Packet

DISPATCH_ID: ISSUE-217
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-26_23-59-35+0300
REVISION: 0
RELATED_FILES:
- GitHub issue #217
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- plans/PLAN.md
- plans/IMPLEMENTATION.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.toml
- Cargo.lock
- kernel/job-system/
- kernel/io-scheduler/
- kernel/asset-streaming/
- kernel/asset-view/
- kernel/shared/
- kernel/asset/
- runtime/runtime-desktop/
- runtime/runtime-headless/
- runtime/runtime-mobile/
- runtime/runtime-web/
- editor/rge-editor/
- crates/editor-shell/
- crates/asset-store/
- crates/asset-pipeline/
- crates/io-gltf/
- crates/io-image/
- crates/rge-scene-loader/
- crates/script-host/
- crates/runtime-wasmtime/
- crates/runtime-wasmtime-engine/
- ai_handoffs/ISSUE-217_EXEC_*.md
- ai_handoffs/ISSUE-217_EXEC_*.meta.json
STATUS: OPEN

## Goal

Perform a read-only Phase 9 async-orchestration pressure audit for GitHub
issue #217. Determine whether `kernel/job-system`, `kernel/io-scheduler`,
`kernel/asset-streaming`, `kernel/asset-view`, or the `kernel/shared` cavity
has gained concrete consumer pressure from current runtime, editor, asset,
script, or loader paths. Use current code, docs, Cargo metadata, and dispatch
artifacts with line-cited evidence. Do not invent substrate work. The EXEC
report must classify the pressure state for each cavity, give one overall
async-orchestration verdict, and name exactly one smallest safe follow-up or
`NEEDS_HUMAN`. This dispatch must not change tracked source, tests, docs, Cargo
files, workflows, schemas, automation scripts, scheduler configuration, or
`.ai/dispatch.tasks.md`.

## Scope

### MAY edit
- None. This is an audit-only dispatch. Do not modify tracked source, tests,
  documentation, Cargo files, workflows, schemas, automation scripts,
  scheduler configuration, task brief files, existing handoff packets, existing
  logs, fixtures, or prior artifacts.

### MUST NOT edit
- `plans/PLAN.md`
- `plans/IMPLEMENTATION.md`
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.tasks.md`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchLoop.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- This task packet,
  `ai_handoffs/ISSUE-217_TASK_2026-05-26_23-59-35+0300.md`, after the
  executor starts.
- Any existing handoff packet or sidecar.
- Any existing `ai_dispatch_logs/**` artifact.
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `tests/**`
- `docs/**`
- `tasks/**`
- `plugins/**`
- `files/**`
- `golden-projects/**`
- `OLD/**`
- `.claude/worktrees/**`
- Any tracked source file, Rust test, documentation file, Cargo metadata file,
  workflow, schema, automation script, scheduler config, task brief, existing
  handoff artifact, existing dispatch log, generated fixture, sandbox
  worktree, or unrelated local scratch file.

### MAY add new files
- `ai_handoffs/ISSUE-217_EXEC_*.md`
- `ai_handoffs/ISSUE-217_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-217/**`

### MUST NOT add new files
- New source, test, documentation, schema, Cargo, workflow, task brief,
  automation, scheduler, fixture, generated asset, ADR, plan, status,
  architecture-lint, benchmark, screenshot, ZIP, copied evidence file, or
  persistent scratch file.
- New handoff packets or sidecars for any dispatch other than `ISSUE-217`.
- `REVIEW`, `CORRECT`, or `CLOSEOUT` packets for this orchestrator dispatch.
- Replacement or edited copies of prior `ISSUE-217` execution artifacts. If a
  prior failed EXEC exists, treat it as read-only context and write a new
  timestamped `ISSUE-217_EXEC_*.md` instead.
- Temporary source-layout inventories, extracted logs, generated reports, or
  measurement outputs inside the repository. If scratch is required, place it
  outside the repo and remove it before writing the EXEC report.

## Deliverables

- One new `ISSUE-217` EXEC report containing the read-only audit.
- A decision block with exactly these fields:
  `ASYNC_ORCHESTRATION_VERDICT` and `SMALLEST_SAFE_FOLLOW_UP`.
- `ASYNC_ORCHESTRATION_VERDICT` must use exactly one of:
  `pressure_deferred`, `pressure_triggered`,
  `needs_architecture_arbitration`.
- `SMALLEST_SAFE_FOLLOW_UP` must name exactly one action or be exactly
  `NEEDS_HUMAN`. If the follow-up touches production Rust, tests, scripts,
  Cargo metadata, workflows, schemas, scheduler configuration, or automation
  behavior, it must state `PublishMode branch only unless later authorized by a
  human`.
- A per-cavity classification table for `job-system`, `io-scheduler`,
  `asset-streaming`, `asset-view`, and `shared`. Each row must include:
  current owner path, current public surface, current external consumers,
  pressure classification, and line-cited evidence.
- A runtime/editor/asset consumer-pressure section covering current paths at
  minimum from `runtime/**`, `editor/rge-editor`,
  `crates/editor-shell`, `crates/rge-scene-loader`, `crates/io-gltf`,
  `crates/io-image`, `crates/asset-store`, `crates/asset-pipeline`,
  `kernel/asset`, `crates/script-host`, `crates/runtime-wasmtime`, and
  `crates/runtime-wasmtime-engine`.
- A required prior-control correction section titled
  `Job/Priority Standalone Search`. It must document a current read-only
  production/test usage search for the standalone identifiers `Job` and
  `Priority`, including the exact command, scope, and either the observed
  no-match result or line-cited/classified positive matches. Preserve any
  prior async-orchestration verdict unless this evidence changes it.
- A required identifier-search matrix covering at least:
  `rge-kernel-job-system`, `rge_kernel_job_system`, `Job`, `JobId`,
  `JobKind`, `JobPriority`, `JobScheduler`,
  `rge-kernel-io-scheduler`, `rge_kernel_io_scheduler`, `Priority`,
  `IoRequest`, `IoRequestId`, `IoRequestKind`, `IoScheduler`,
  `rge-kernel-asset-streaming`, `rge_kernel_asset_streaming`,
  `ResidencyRecord`, `ResidencyTracker`, `ResidencyState`,
  `rge-kernel-asset-view`, `rge_kernel_asset_view`, `AssetViewId`,
  `ViewDescriptor`, `ViewKind`, `AssetViewRegistry`,
  `rge-kernel-shared`, and `rge_kernel_shared`.
- A Phase 9 context section citing `plans/PLAN.md`,
  `plans/IMPLEMENTATION.md`, `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`,
  recent `change.md`, and relevant existing dispatch artifacts. It must
  distinguish current evidence from historical or doctrine-only deferrals.
- A candidate comparison section explaining why the selected follow-up is
  smaller or more urgent than other inspected async-orchestration candidates,
  or why `NEEDS_HUMAN` is required.
- A final verification section reporting `git diff --check`,
  `git status --short --untracked-files=all`, and static inspection proving no
  tracked production/doc/script/Cargo/workflow/schema/task-brief files changed.

## Acceptance Criteria

- The EXEC report is audit-only and does not modify tracked source, tests,
  docs, Cargo metadata, workflows, schemas, scripts, scheduler configuration,
  fixtures, or `.ai/dispatch.tasks.md`.
- The report classifies async-orchestration pressure using exactly one
  `ASYNC_ORCHESTRATION_VERDICT` value from the deliverables section.
- The report names exactly one smallest safe follow-up or exactly
  `NEEDS_HUMAN`; it does not produce a backlog list.
- The audit reads and line-cites `AI_DISPATCH_AUTOMATION.md` and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md` before judging dispatch state, and it
  does not treat missing orchestrator `_REVIEW_` or `_CLOSEOUT_` packets as
  evidence of an unfinished dispatch.
- The audit reads and line-cites `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, and recent `change.md`
  entries to establish Phase 9 async-orchestration context and current cavity
  posture.
- The audit inspects and line-cites current cavity surfaces at minimum from:
  `kernel/job-system/Cargo.toml`, `kernel/job-system/src/lib.rs`,
  `kernel/job-system/src/job.rs`, `kernel/job-system/src/priority.rs`,
  `kernel/job-system/src/scheduler.rs`,
  `kernel/io-scheduler/Cargo.toml`, `kernel/io-scheduler/src/lib.rs`,
  `kernel/io-scheduler/src/priority.rs`,
  `kernel/io-scheduler/src/request.rs`,
  `kernel/io-scheduler/src/scheduler.rs`,
  `kernel/asset-streaming/Cargo.toml`,
  `kernel/asset-streaming/src/lib.rs`,
  `kernel/asset-streaming/src/record.rs`,
  `kernel/asset-streaming/src/state.rs`,
  `kernel/asset-streaming/src/tracker.rs`,
  `kernel/asset-view/Cargo.toml`, `kernel/asset-view/src/lib.rs`,
  `kernel/asset-view/src/descriptor.rs`, `kernel/asset-view/src/id.rs`,
  `kernel/asset-view/src/registry.rs`, `kernel/shared/Cargo.toml`, and
  `kernel/shared/src/lib.rs`.
- The audit inspects current runtime/editor/asset paths read-only and
  line-cites any positive consumer evidence from `runtime/**`,
  `editor/rge-editor`, `crates/editor-shell`, `crates/rge-scene-loader`,
  `crates/io-gltf`, `crates/io-image`, `crates/asset-store`,
  `crates/asset-pipeline`, `kernel/asset`, `crates/script-host`,
  `crates/runtime-wasmtime`, or `crates/runtime-wasmtime-engine`.
- The audit searches Cargo dependency declarations and Rust production/test
  usage for each required cavity crate/API identifier. Positive findings must
  be line-cited and classified as self-test, external production consumer,
  external test consumer, docs/comment-only, Cargo metadata only, or unrelated
  lexical collision.
- The report explicitly documents the standalone identifier search for `Job`
  and `Priority`. A claim that all requested identifiers were searched is not
  acceptable unless both `Job` and `Priority` appear in the documented command
  or search matrix with current results.
- If a zero-use claim comes from search output rather than a line-bearing file,
  the report includes the exact read-only search command, scope, and observed
  no-match result.
- The report distinguishes concrete consumer pressure from crate names,
  workspace membership, self-tests, comments, historical plans, prior doctrine
  cavities, broad "eventual" roadmap language, and unrelated identifiers such
  as component camera `priority`.
- The report uses `pressure_deferred` only if current evidence shows no
  concrete production/editor/runtime/asset consumer has forced a cavity beyond
  its present v0/doctrine surface.
- The report uses `pressure_triggered` only if current evidence shows a real
  production/editor/runtime/asset consumer now needs one of the cavity
  behaviors, such as actual job execution, I/O driver dispatch, residency
  decisions, zero-copy asset views, or a non-suspect shared-kernel admission.
- The report uses `needs_architecture_arbitration` only if current evidence
  shows the next step depends on an unresolved architecture decision rather
  than a smallest branch-mode follow-up.
- The audit inspects relevant existing dispatch artifacts read-only, including
  prior kernel-cavity, asset-store, Phase 9 pressure, and recent related audit
  artifacts. It must cite current packet/log lines, not stale copied line
  numbers.
- The report does not run new benchmarks, profilers, full repository
  verification, the autonomous queue, or a nested dispatch as part of this
  audit.
- `git diff --check` exits 0.
- `git status --short --untracked-files=all` shows no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new files
  must be limited to this dispatch's own EXEC report, optional matching
  sidecar/log artifacts, and ignored `.ai/dispatch-ISSUE-217` scratch.

## Constraints / Non-Goals

- Do not implement a fix.
- Do not edit source, tests, docs, Cargo files, workflows, schemas, scripts,
  scheduler config, task brief files, fixtures, prior handoff packets, prior
  logs, or existing dispatch artifacts.
- Do not update `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, or any other
  documentation.
- Do not add or run new benchmarks, profilers, generated reports, screenshots,
  ZIPs, fixtures, or persistent scratch files in the repository.
- Do not run publish, push, merge, branch-delete, label-edit, GitHub issue
  mutation, scheduler registration, or scheduler modification commands.
- Do not create a branch, commit, PR, issue comment, label change, or close the
  GitHub issue.
- Do not run the autonomous queue or a nested dispatch as verification.
- Do not run full repository verification manually for this audit. The
  task-specific gates are read-only inspection plus diff/status checks; the
  orchestrator may still run its canonical gate automatically after execution.
- Do not infer async-orchestration pressure from file names, workspace
  membership, comments, or Cargo declarations alone. Use line-cited current
  dataflow and consumer evidence.
- Do not recommend filling a kernel cavity merely because it exists. The
  follow-up must be justified by current consumer pressure or must return
  `NEEDS_HUMAN`.
- Treat unrelated dirty worktree state as out of scope. Do not clean, revert,
  or modify changes you did not make; report them if they affect confidence.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` and
  `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` -> expected readable;
  use them to avoid mistaking orchestrator packet shape for dispatch
  incompleteness.
- `Get-Content -Raw plans/PLAN.md`,
  `Get-Content -Raw plans/IMPLEMENTATION.md`, and
  `Get-Content -Raw plans/BASELINE.md` -> expected readable; use them to
  establish Phase 9 async-orchestration pressure context, cavity deferrals, and
  revisit triggers.
- `Get-Content -Raw Status.md` and `Get-Content -Raw HANDOFF.md` -> expected
  readable; use them to establish current state, closed gates, known deferrals,
  and next-work posture.
- Inspect recent `change.md` entries with line numbers immediately before
  writing the final EXEC report. Use live line positions, not copied line
  numbers from prior packets or logs.
- Inspect `kernel/job-system` read-only:
  `kernel/job-system/Cargo.toml`, `kernel/job-system/src/lib.rs`,
  `kernel/job-system/src/job.rs`, `kernel/job-system/src/priority.rs`, and
  `kernel/job-system/src/scheduler.rs`.
- Inspect `kernel/io-scheduler` read-only:
  `kernel/io-scheduler/Cargo.toml`, `kernel/io-scheduler/src/lib.rs`,
  `kernel/io-scheduler/src/priority.rs`,
  `kernel/io-scheduler/src/request.rs`, and
  `kernel/io-scheduler/src/scheduler.rs`.
- Inspect `kernel/asset-streaming` read-only:
  `kernel/asset-streaming/Cargo.toml`,
  `kernel/asset-streaming/src/lib.rs`,
  `kernel/asset-streaming/src/record.rs`,
  `kernel/asset-streaming/src/state.rs`, and
  `kernel/asset-streaming/src/tracker.rs`.
- Inspect `kernel/asset-view` read-only:
  `kernel/asset-view/Cargo.toml`, `kernel/asset-view/src/lib.rs`,
  `kernel/asset-view/src/descriptor.rs`, `kernel/asset-view/src/id.rs`, and
  `kernel/asset-view/src/registry.rs`.
- Inspect `kernel/shared` read-only:
  `kernel/shared/Cargo.toml` and `kernel/shared/src/lib.rs`.
- Inspect current runtime/editor/asset/script paths read-only:
  `runtime/**`, `editor/rge-editor`, `crates/editor-shell`,
  `crates/rge-scene-loader`, `crates/io-gltf`, `crates/io-image`,
  `crates/asset-store`, `crates/asset-pipeline`, `kernel/asset`,
  `crates/script-host`, `crates/runtime-wasmtime`, and
  `crates/runtime-wasmtime-engine`.
- Search Cargo metadata read-only for dependency declarations involving:
  `rge-kernel-job-system`, `rge-kernel-io-scheduler`,
  `rge-kernel-asset-streaming`, `rge-kernel-asset-view`, and
  `rge-kernel-shared`. Use `git grep`, `Select-String`, `git ls-files`, or an
  equivalent read-only command, and report exact scope and results.
- Search Rust production/test usage read-only for cavity crate/API identifiers:
  `rge_kernel_job_system`, `Job`, `JobId`, `JobKind`, `JobPriority`,
  `JobScheduler`, `rge_kernel_io_scheduler`, `Priority`, `IoRequest`,
  `IoRequestId`, `IoRequestKind`, `IoScheduler`,
  `rge_kernel_asset_streaming`, `ResidencyRecord`, `ResidencyTracker`,
  `ResidencyState`, `rge_kernel_asset_view`, `AssetViewId`,
  `ViewDescriptor`, `ViewKind`, `AssetViewRegistry`, and
  `rge_kernel_shared`.
- Run and document a standalone `Job` / `Priority` search. Preferred command:
  `git grep -n -w -e Job -e Priority -- '*.rs' ':!OLD/**' ':!.ai/**' ':!.claude/**' ':!ai_handoffs/**'`.
  Also run the external-consumer variant excluding the two owning cavity
  crates:
  `git grep -n -w -e Job -e Priority -- '*.rs' ':!kernel/job-system/**' ':!kernel/io-scheduler/**' ':!OLD/**' ':!.ai/**' ':!.claude/**' ':!ai_handoffs/**'`.
  If `git grep` is unavailable, use an equivalent PowerShell
  `Get-ChildItem` plus `Select-String` command and record the exact fallback.
- Inspect relevant existing dispatch artifacts read-only, especially prior
  kernel-cavity audits, asset-store/asset-view audits, and Phase 9 pressure
  audits. Ignore `OLD/**`, `.ai/dispatch-*/**`, `.claude/worktrees/**`,
  `.git/**`, and build output when assessing repository state unless a
  specific artifact is cited as provenance.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new files
  must be limited to this dispatch's own handoff/log artifacts and optional
  ignored `.ai/dispatch-ISSUE-217` scratch.
- Static no-source-change inspection: run `git diff --name-only` and a
  targeted read-only inspection of protected tracked surfaces such as
  `crates`, `kernel`, `editor`, `runtime`, `tools`, `docs`, `plans`,
  `Status.md`, `HANDOFF.md`, `change.md`, `Cargo.toml`, `Cargo.lock`,
  `.github/workflows`, `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`,
  `.ai/*.schema.json`, and root `*.ps1` automation scripts. Expected result:
  no tracked production/doc/script/Cargo/workflow/schema/task-brief diff.

## Halt Conditions

The Executor MUST halt and return `NEEDS_HUMAN` without editing any source,
docs, tests, scripts, schemas, workflows, Cargo files, scheduler config, task
briefs, fixtures, prior packets, or prior logs if any of the following occur:

- Required evidence files are missing or unreadable.
- The required production/test usage search cannot be completed read-only.
- The required standalone `Job` and `Priority` search cannot be completed and
  documented with exact command/scope/results.
- The evidence cannot support exactly one `ASYNC_ORCHESTRATION_VERDICT` value
  from the allowed set.
- The smallest safe follow-up cannot be narrowed to exactly one action.
- The smallest safe follow-up would require code/script/Cargo/workflow/schema
  changes but cannot be constrained to branch-mode pending later human
  authorization.
- The audit needs an implementation, documentation edit, Cargo edit, workflow
  edit, schema edit, script edit, scheduler change, task-brief edit, prior
  artifact edit, benchmark run, profiler run, or GitHub mutation to be
  truthful.
- Required line citations cannot be produced for document-, code-, Cargo-,
  script-, or artifact-based claims.
- Current evidence shows a real consumer pressure signal, but the next step
  depends on an unresolved architecture decision about cavity ownership,
  runtime/editor dependency direction, asset streaming ownership, zero-copy
  view semantics, or `kernel/shared` admission.
- Any GitHub mutation, push, merge, branch deletion, label change, issue
  comment, scheduler command, benchmark run, profiler run, full verification
  run, or nested dispatch seems necessary.
- `git diff --check` fails for a reason caused by this dispatch.
- `git status --short --untracked-files=all` shows new or modified files
  outside this dispatch's own handoff/log artifacts and optional ignored
  `.ai/dispatch-ISSUE-217` scratch, unless they are clearly pre-existing
  unrelated user changes.

If a halt condition fires, the Executor must still write the `EXECUTION_REPORT`
if possible, set header `STATUS: NEEDS_HUMAN`, include the exact halt reason
and current status output, and route the footer to `NEXT_ROLE: HUMAN_ARBITER`.

## Planner Notes

This is delegated-main smoke batch task 5 of 10 from `.ai/dispatch.tasks.md`.
The desired outcome is not to fill any of the five kernel cavities by default;
it is to decide whether current runtime/editor/asset paths have created
concrete consumer pressure that justifies a next dispatch.

Prior Codex control feedback for an earlier attempt was narrow: the audit said
all requested identifiers were searched, but the documented identifier patterns
omitted the standalone terms `Job` and `Priority`. The EXEC report for this
task must include the `Job/Priority Standalone Search` section and may preserve
the prior verdict unless the newly documented evidence changes it.

Under the orchestrator protocol, no `REVIEW` or `CLOSEOUT` packet is expected
from this TASK. The queue's Codex control review is stored in the run
directory, not as a handoff packet.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-217
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
