# Task Packet

DISPATCH_ID: ISSUE-15
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_09-27-28+0300
RELATED_FILES:
- crates/gfx/Cargo.toml
- crates/gfx/tests/wgsl_parse_smoke.rs
- Cargo.lock
STATUS: OPEN

## Goal

Implement GitHub issue #15 by adding a focused test-only shader validation slice for Phase 6.3 WGSL plus naga coverage in `rge-gfx`. The dispatch should prove that the embedded `TRIANGLE_WGSL`, `MESH_WGSL`, and `LIT_MESH_WGSL` shader strings parse with `naga::front::wgsl::parse_str`, without changing any production shader text, render pipeline behavior, or runtime APIs.

## Scope

### MAY edit
- `crates/gfx/Cargo.toml`, only inside `[dev-dependencies]` to add the minimal direct `naga` test dependency needed for `naga::front::wgsl::parse_str`.
- `Cargo.lock`, only if Cargo updates the existing `rge-gfx` package dependency list because of the new direct dev-dependency; no dependency version, checksum, source, or unrelated package changes are allowed.

### MUST NOT edit
- Any production source file, including all files under `crates/gfx/src/**`.
- The embedded shader strings `TRIANGLE_WGSL`, `MESH_WGSL`, and `LIT_MESH_WGSL`.
- Any render pipeline construction, PSO cache behavior, material-intent behavior, public API, or shader hash logic.
- Workspace `Cargo.toml`, docs, schemas, scripts, CI files, `.gitignore`, architecture-lint code, or existing handoff packets.
- Any crate outside `crates/gfx`.

### MAY add new files
- `crates/gfx/tests/wgsl_parse_smoke.rs`.
- One protocol-required execution report matching `ai_handoffs/ISSUE-15_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new production source files, fixtures, shader files, snapshots, docs, schemas, scripts, CI files, crates, architecture-lint files, or generated artifacts.
- No handoff packets other than the protocol-required `ISSUE-15_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add a direct `naga` dev-dependency for `rge-gfx` using the existing workspace-pinned Naga version and enabling the WGSL parser feature required for `naga::front::wgsl`.
- Add `crates/gfx/tests/wgsl_parse_smoke.rs` as a focused integration test.
- The test MUST parse all three embedded shader constants with `naga::front::wgsl::parse_str`:
  - `rge_gfx::pipeline::TRIANGLE_WGSL`
  - `rge_gfx::mesh_pipeline::MESH_WGSL`
  - `rge_gfx::lit_mesh_pipeline::LIT_MESH_WGSL`
- The test MUST fail with a useful shader-name-specific panic or assertion message if any parse fails.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- `crates/gfx/tests/wgsl_parse_smoke.rs` exists and is the only new non-handoff file.
- The smoke test imports the shader constants from the public `rge_gfx` modules and parses the actual embedded strings, not copied shader text.
- The smoke test covers exactly `TRIANGLE_WGSL`, `MESH_WGSL`, and `LIT_MESH_WGSL`.
- The smoke test uses `naga::front::wgsl::parse_str` directly.
- `crates/gfx/Cargo.toml` changes are limited to the minimal `naga` dev-dependency needed for the test, preferably `naga = { workspace = true, features = ["wgsl-in"] }` unless the current Naga feature spelling requires an equivalent.
- `Cargo.lock`, if changed, is limited to the expected lockfile representation of the new direct `rge-gfx` dev-dependency and does not change resolved versions.
- No production shader strings, pipeline behavior, public APIs, docs, scripts, or non-gfx crates are changed.
- `cargo test -p rge-gfx --test wgsl_parse_smoke` exits 0.

## Constraints / Non-Goals

- This is a test-only validation dispatch plus the direct test dependency needed to compile it.
- Do not modify WGSL to make Naga accept it; if a shader fails to parse, halt and report the failure.
- Do not add GPU, adapter, device, headless render target, or wgpu pipeline creation requirements to this test.
- Do not add shader snapshots, external WGSL files, generated code, fixtures, or helper crates.
- Do not broaden this into shader linting, Naga IR validation beyond parsing, shader graph work, material graph work, or render-output testing.
- Do not bump dependency versions or alter workspace dependency policy.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-gfx` -> expected exit 0.
- `cargo test -p rge-gfx --test wgsl_parse_smoke` -> expected exit 0, with all three embedded WGSL shader strings parsing through Naga.
- `git diff -- crates/gfx/Cargo.toml Cargo.lock crates/gfx/tests/wgsl_parse_smoke.rs` -> expected diff is limited to the direct Naga dev-dependency, the focused smoke test, and any allowed lockfile side effect.
- `git diff --check -- crates/gfx/Cargo.toml Cargo.lock crates/gfx/tests/wgsl_parse_smoke.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Any of the three embedded WGSL shader strings fail to parse and passing the new test would require changing production shader text or pipeline behavior.
- The Naga dependency cannot be added as a direct `rge-gfx` dev-dependency using the existing workspace-pinned version and WGSL parser feature.
- Any dependency version bump, workspace dependency change, or non-minimal lockfile resolution change appears necessary.
- Any edit outside `crates/gfx/Cargo.toml`, `Cargo.lock` as narrowly allowed above, or `crates/gfx/tests/wgsl_parse_smoke.rs` appears necessary.
- Any GPU/device/pipeline execution is required to satisfy the test; this dispatch is parse-only.
- Formatting or the focused test fails for reasons unrelated to this dispatch and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #15. Revision number: 0. There is no prior Claude gate result. The workspace already pins `naga = "29"` alongside `wgpu = "29"` in the root workspace dependencies; use that existing pin rather than introducing a new version. Keep the test intentionally small: it should parse the three embedded shader constants and stop there.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-15
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
