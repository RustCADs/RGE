# Correction Packet

DISPATCH_ID: ISSUE-45
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_13-21-12+0300
RELATED_FILES:
- crates/cad-projection/tests/face_picking_smoke.rs
- crates/material-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-45_TASK_2026-05-19_13-10-08+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-45_EXEC_2026-05-19_13-20-12+0300.md`
- Latest Review Report: none; orchestrator verification failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.
- Verification failure source: post-execution `.ai/dispatch.verify.ps1` gate at `cargo +nightly fmt --all -- --check`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification-gate finding it
addresses.

1. **Normalize rustfmt newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` reported incorrect newline style in
   `crates/cad-projection/tests/face_picking_smoke.rs`. Required change:
   apply only the newline/style normalization required by rustfmt for that
   file. Do not make semantic edits. Acceptance: the fmt check no longer
   reports incorrect newline style for this file.
2. **Apply rustfmt output in material graph tests** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` reported diffs in
   `crates/material-graph/src/lib.rs` around the `#[cfg(test)]` module.
   Required change: apply rustfmt's exact mechanical formatting there,
   including the test-module import ordering and blank line near line 197 and
   the multi-line `assert_eq!` wrapping near line 235. Do not change behavior,
   public API, tests, or task scope. Acceptance: the fmt check no longer
   reports a diff for `crates/material-graph/src/lib.rs`.

## Deferred Findings (NOT Approved for This Round)

None. No review or control findings are deferred; the dispatch failed before
control review solely because the canonical verification gate stopped at
formatting.

Future trigger / dispatch: not applicable for this correction round.

## Updated Acceptance Criteria

Unchanged from Task Packet. In addition, queue publish readiness requires the
canonical dispatch verification gate to pass after the formatting corrections.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, with these correction-round clarifications:

- If applying rustfmt would modify any tracked file other than
  `crates/cad-projection/tests/face_picking_smoke.rs` and
  `crates/material-graph/src/lib.rs`, halt and report the additional proposed
  file before expanding scope.
- If `.ai/dispatch.verify.ps1` still fails after the approved formatting
  corrections, halt and document the exact failing command, exit code, and log
  tail in the next execution report.

## Planner Notes

This correction round is intentionally mechanical. The execution report claimed
successful crate tests and architecture lints, but the canonical post-execution
gate failed before reaching those later checks because formatting was not clean.
Do not revisit the material-graph design or add any new behavior in this round;
make the minimal rustfmt/newline fixes needed for the gate to proceed and pass.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-45
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
