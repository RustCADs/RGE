# Correction Packet

DISPATCH_ID: ISSUE-217
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_00-22-44+0300
RELATED_FILES:
- ai_handoffs/ISSUE-217_TASK_2026-05-26_23-59-35+0300.md
- ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-12-29+0300.md
- ai_handoffs/ISSUE-217_EXEC_*.md
- ai_handoffs/ISSUE-217_EXEC_*.meta.json
- .ai/dispatch-ISSUE-217/codex.control.round0.json
- .ai/dispatch-ISSUE-217/verification.round0.log
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-217_TASK_2026-05-26_23-59-35+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-217_EXEC_2026-05-27_00-12-29+0300.md`
- Codex Control Review Result: `.ai/dispatch-ISSUE-217/codex.control.round0.json`, verdict `needs_changes`
- Verification Log: `.ai/dispatch-ISSUE-217/verification.round0.log`
- Prior Correction Packets: none

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more. This is a
report-correction round only: write one new timestamped ISSUE-217 EXEC report
and its normal `.meta.json` sidecar if the completion tooling emits one. Do not
edit existing TASK or EXEC artifacts, source files, documentation files,
schemas, scripts, dispatch logs, or any other packet.

1. **Line-cite dispatch/protocol evidence before dispatch-state conclusions** -
   Control review `required_fixes[0]`. Required change: in the replacement EXEC
   report, before any dispatch-state conclusion, include live line citations
   from both `AI_DISPATCH_AUTOMATION.md` and
   `ai_handoffs/AI_HANDOFF_PROTOCOL.md`. The citations must support the
   orchestrator/control-review conclusion and the distinction from the manual
   `TASK -> EXEC -> REVIEW -> CORRECT -> CLOSEOUT` protocol. Do not rely only
   on section names, `RELATED_FILES`, `AGENTS.md`, or the prior EXEC's lines
   59, 107, and 622-626. Acceptance: the first dispatch-state conclusion in
   the replacement EXEC is preceded by, or contained in the same evidence block
   as, explicit `file:line` citations for both required documents.

2. **Make Identifier-Search Matrix coverage explicit for Job and Priority** -
   Control review `required_fixes[1]`. Required change: update the replacement
   EXEC report's Identifier-Search Matrix so the matrix itself covers standalone
   `Job` and standalone `Priority`. Either add explicit matrix rows for each
   term or add explicit matrix rows that point to the titled
   `Job/Priority Standalone Search` section. Each covered term must include the
   exact command, search scope, observed result, and classification. Acceptance:
   the Identifier-Search Matrix contains clear standalone `Job` and `Priority`
   coverage rather than leaving that evidence only in a separate prose section.

3. **Preserve audit-only state, pressure verdict, and verification reporting** -
   Control review `required_fixes[2]`. Required change: keep the existing
   `ASYNC_ORCHESTRATION_VERDICT: pressure_deferred` semantics unless refreshed
   evidence changes them, and preserve the no-source-change audit boundary.
   Re-report the same diff/status gates and canonical verification evidence in
   the replacement EXEC: branch/status, tracked diff, diff name list,
   whitespace check, and `.ai/dispatch-ISSUE-217/verification.round0.log`.
   Acceptance: the replacement EXEC documents that canonical verification
   passed, that no tracked source/doc/schema/script diff exists, and that any
   semantic change from `pressure_deferred` is backed by refreshed evidence.

## Deferred Findings (NOT Approved for This Round)

None. The control review did not approve any extra work beyond the three
report-correction fixes above, and no optional cleanup is approved for this
correction round.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-specific addition: the next
ISSUE-217 EXEC report must include the missing live line citations, explicit
Identifier-Search Matrix coverage for standalone `Job` and `Priority`, and the
same no-source-change verification/status reporting described above.

## Re-Verification Gates

The Executor MUST run or re-report these gates after writing the corrected EXEC
report:

- Read `AI_DISPATCH_AUTOMATION.md` with line numbers and use live `file:line`
  citations in the replacement EXEC before any dispatch-state conclusion.
- Read `ai_handoffs/AI_HANDOFF_PROTOCOL.md` with line numbers and use live
  `file:line` citations in the replacement EXEC before any dispatch-state
  conclusion.
- Inspect the replacement EXEC's Identifier-Search Matrix; expected: explicit
  standalone `Job` and `Priority` coverage with exact command, scope, observed
  result, and classification, or matrix rows that explicitly point to the titled
  standalone-search section containing those details.
- `git status --short --branch --untracked-files=all` -> expected: no tracked
  source, documentation, schema, script, or prior-packet edits; only
  dispatch-owned ISSUE-217 handoff artifacts may be new or changed.
- `git diff` -> expected empty output.
- `git diff --name-only` -> expected empty output.
- `git diff --check` -> expected exit 0.
- Read `.ai/dispatch-ISSUE-217/verification.round0.log` and report the
  canonical gate result; expected: `VERIFY OK` and all seven verification steps
  passed. If the orchestrator wrapper reruns `.ai/dispatch.verify.ps1`, it must
  also exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- Halt with `NEEDS_HUMAN` if the correction would require editing existing
  TASK/EXEC artifacts, source files, documentation files, schemas, scripts,
  dispatch logs, or any non-ISSUE-217 replacement EXEC sidecar artifact.
- Halt with `NEEDS_HUMAN` if live line citations from both
  `AI_DISPATCH_AUTOMATION.md` and `ai_handoffs/AI_HANDOFF_PROTOCOL.md` cannot
  be obtained before making any dispatch-state conclusion.
- Halt with `NEEDS_HUMAN` if standalone `Job` and `Priority` cannot be
  truthfully classified in the Identifier-Search Matrix without expanding
  scope.
- Halt rather than writing a `_REVIEW_` or `_CLOSEOUT_` packet for this
  orchestrator dispatch.

## Planner Notes

The canonical verification gate passed and the worktree had no tracked diff.
The blocker is EXEC report completeness: the prior report did not line-cite the
automation/protocol documents before its dispatch-state conclusion, and its
Identifier-Search Matrix did not explicitly cover standalone `Job` and
`Priority`. Keep the correction report-only and preserve the existing
`pressure_deferred` decision unless refreshed evidence requires a different
truthful report.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-217
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
