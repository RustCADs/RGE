# Execution Report

DISPATCH_ID: ISSUE-40
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-19_07-30-45+0300
RELATED_FILES:
- crates/cad-projection/tests/face_picking_smoke.rs — added Test 10 (`pick_resolves_revolve_start_cap_face`), file-level inventory comment, and `RevolveOp`/`RevolveFaceTag`/`PI` imports
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-40_TASK_2026-05-19_07-23-11+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## What I Changed

### Source

- None. No file under `crates/**/src/**` was touched.

### Tests

- `crates/cad-projection/tests/face_picking_smoke.rs::pick_resolves_revolve_start_cap_face`:
  one new additive `#[test]` (Test 10). It builds a `CadGraph` containing a
  directly rooted `OperatorNode::Revolve` from
  `RevolveOp::partial(Polygon2D::new([[1,0],[2,0],[2,1],[1,1]]), 8, PI)`,
  sets that Revolve node as the graph root, commits, spawns the projected
  entity for that exact Revolve node, sets `BRepHandle.brep_owner =
  Some(ENTITY_OWNER)` before `projection.tick`, and sanity-checks the
  fixture (`mesh.triangle_count() == 68`, `mesh.face_labels.is_some()`).
  It casts the deterministic ray `origin [1.25, 0.5, -5.0]` /
  `direction [0.0, 0.0, 1.0]` and asserts `CadProjection::pick_face`
  returns `Some(FacePick)` whose `entity` is the Revolve-bound entity,
  `owner` is `ENTITY_OWNER`, and `face_id` equals
  `BRepFaceId::for_revolve_face(ENTITY_OWNER, RevolveFaceTag::StartCap { profile_count: 4 })`.
  It also asserts `pick.t > 0.0` and the exact `t == 5.0` tied to the
  start cap at `z = 0`.

### Docs

- None. The only comment changes are inside the MAY-edit test file: a
  Test 10 entry appended to the file-level face-picking smoke inventory
  and the explanatory doc-comment on the new test.

## Per-File Summary

`crates/cad-projection/tests/face_picking_smoke.rs` — additive only:
+108 / -1 lines (the `-1` is the `use rge_cad_core::{…}` import block
being reflowed by rustfmt to absorb `RevolveFaceTag, RevolveOp`). Also
added `use std::f32::consts::PI;`. No existing test was weakened, deleted,
ignored, renamed, reordered, or had any assertion loosened.

## Verification Results

- `cargo test -p rge-cad-projection --test face_picking_smoke` → exit 0;
  10 passed / 0 failed / 0 ignored. The new
  `pick_resolves_revolve_start_cap_face` executed and passed alongside the
  9 pre-existing tests.
- `git diff -- crates/cad-projection/tests/face_picking_smoke.rs` → diff
  limited to the new test, the in-file smoke inventory comment, and the
  two import additions; no out-of-scope change.
- `git diff --check -- crates/cad-projection/tests/face_picking_smoke.rs`
  → exit 0 (no whitespace errors).
- `git status --short --untracked-files=all` → one tracked change
  (`crates/cad-projection/tests/face_picking_smoke.rs`); untracked items
  are the inbound `ISSUE-40_TASK` packet + sidecar and this `ISSUE-40_EXEC`
  report (+ its finalize sidecar). No unexpected tracked or untracked
  changes outside the MAY-edit scope.

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope.

## Open Questions for Reviewer

None. The ray `[1.25, 0.5, -5.0]` → `[0,0,1]` targets `[1.25, 0.5]` in XY:
interior to the `[1,2]×[0,1]` square profile, off the cap boundary, off
every cap vertex, and above the fan diagonal `y = x - 1` (`0.5 > 0.25`),
so the start-cap hit is unambiguous and the exact `t = 5.0` assertion is
tied to the `z = 0` cap plane.

## Worktree State

- Tracked files: `crates/cad-projection/tests/face_picking_smoke.rs` (modified, uncommitted)
- Untracked items: `ai_handoffs/ISSUE-40_TASK_2026-05-19_07-23-11+0300.md`,
  `ai_handoffs/ISSUE-40_TASK_2026-05-19_07-23-11+0300.meta.json`,
  `ai_handoffs/ISSUE-40_EXEC_2026-05-19_07-30-45+0300.md` (this report),
  and its finalize-generated `.meta.json` sidecar
- Branch: ai-dispatch/ISSUE-40
- Last commit: `b89e9f7` ai-dispatch ISSUE-39: Add ExtrudeOp face-picking smoke

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-40
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
