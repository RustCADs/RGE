# Correction Packet

DISPATCH_ID: ISSUE-69
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20T16:05:49+03:00
RELATED_FILES:
- crates/script-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-69_TASK_2026-05-20_15-54-16+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-69_EXEC_2026-05-20_16-03-47+0300.md`
- Latest Review Report: none; the orchestrator stopped before Codex control review because post-execution verification failed.
- Prior Correction Packets: none.
- Verification Failure: `.ai/dispatch.verify.ps1` exited 1 at `cargo +nightly fmt --all -- --check`; rustfmt reported incorrect newline style in `crates/script-graph/src/lib.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it addresses.

1. **Normalize Rust source newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with "Incorrect newline style"
   for `crates/script-graph/src/lib.rs`. Required change: normalize only
   `crates/script-graph/src/lib.rs` to the newline style accepted by rustfmt
   without changing Rust semantics or widening the task scope. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0, and the canonical
   `.ai/dispatch.verify.ps1` gate exits 0.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred. The only known blocker is the newline-style
formatting failure in `crates/script-graph/src/lib.rs`.

## Updated Acceptance Criteria

Unchanged from Task Packet. This correction round specifically requires the
post-execution verification gate to pass after the newline-style fix.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0
- `.ai/dispatch.verify.ps1` -> expected exit 0

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Additionally, if fixing the newline style would
require edits outside `crates/script-graph/src/lib.rs`, the Executor MUST halt
and report the unexpected extra formatting scope instead of making broader
changes.

## Planner Notes

The verification log identifies a single format blocker before any other gate
ran. Keep this round limited to newline normalization for
`crates/script-graph/src/lib.rs`, then prove the full canonical verification
gate passes.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-69
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
