# Task Packet

DISPATCH_ID: ISSUE-219
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_01-32-02+0300
REVISION: 0
RELATED_FILES:
- GitHub issue #219
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- .ai/dispatch.tasks.md
- plans/PLAN.md
- plans/IMPLEMENTATION.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.toml
- Cargo.lock
- editor/rge-editor/
- crates/editor-shell/
- crates/editor-ui/
- crates/editor-actions/
- crates/editor-state/
- crates/editor-egui-host/
- crates/components-editor/
- crates/anim-graph-editor/
- crates/material-graph-editor/
- runtime/runtime-headless/
- crates/rge-scene-loader/
- crates/rge-data/
- golden-projects/simple-scene/
- ai_handoffs/ISSUE-210_*.md
- ai_handoffs/ISSUE-212_*.md
- ai_handoffs/ISSUE-214_*.md
- ai_handoffs/ISSUE-216_*.md
- ai_handoffs/ISSUE-217_*.md
- ai_handoffs/ISSUE-218_*.md
- ai_handoffs/ISSUE-219_EXEC_*.md
- ai_handoffs/ISSUE-219_EXEC_*.meta.json
STATUS: OPEN

## Goal

Perform a read-only Phase 9 editor-usability pressure audit for GitHub issue
#219, corresponding to the current `.ai/dispatch.tasks.md` task #68. Inspect
the current editor substrate and consumer chain across `editor-shell`,
`editor-ui`, the command bus (`editor-actions`), `runtime-headless`,
`rge-scene-loader` consumers, and relevant current docs/artifacts. Distinguish
user-visible editor gaps from substrate prerequisites with line-cited evidence.
The EXEC report must name exactly one smallest safe follow-up or
`NEEDS_HUMAN`, with the common #62-#71 verification/status constraints
satisfied. This dispatch must not change tracked source, tests, docs, Cargo
files, workflows, schemas, automation scripts, scheduler configuration, or
`.ai/dispatch.tasks.md`.

## Scope

### MAY edit
- None. This is an audit-only dispatch. Do not modify tracked source, tests,
  documentation, Cargo files, workflows, schemas, automation scripts,
  scheduler configuration, task brief files, existing handoff packets, existing
  logs, fixtures, generated assets, or prior artifacts.

### MUST NOT edit
- `plans/PLAN.md`
- `plans/IMPLEMENTATION.md`
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.tasks.md`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchLoop.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- This task packet,
  `ai_handoffs/ISSUE-219_TASK_2026-05-27_01-32-02+0300.md`, after the
  executor starts.
- Any existing handoff packet or sidecar.
- Any existing `ai_dispatch_logs/**` artifact.
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `tests/**`
- `docs/**`
- `tasks/**`
- `plugins/**`
- `files/**`
- `golden-projects/**`
- `OLD/**`
- `.claude/worktrees/**`
- Any tracked source file, Rust test, documentation file, Cargo metadata file,
  workflow, schema, automation script, scheduler config, task brief, existing
  handoff artifact, existing dispatch log, generated fixture, sandbox
  worktree, or unrelated local scratch file.

### MAY add new files
- `ai_handoffs/ISSUE-219_TASK_*.meta.json`
- `ai_handoffs/ISSUE-219_EXEC_*.md`
- `ai_handoffs/ISSUE-219_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-219_CORRECT_*.md`
- `ai_handoffs/ISSUE-219_CORRECT_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-219/**`

### MUST NOT add new files
- New source, test, documentation, schema, Cargo, workflow, task brief,
  automation, scheduler, fixture, generated asset, ADR, plan, status,
  architecture-lint, benchmark, screenshot, ZIP, copied evidence file, or
  persistent scratch file.
- New handoff packets or sidecars for any dispatch other than `ISSUE-219`.
- `REVIEW` or `CLOSEOUT` packets for this orchestrator dispatch.
- Replacement or edited copies of prior `ISSUE-219` execution artifacts. If a
  prior failed EXEC exists, treat it as read-only context and write a new
  timestamped `ISSUE-219_EXEC_*.md` instead.
- Temporary source-layout inventories, extracted logs, generated reports,
  screenshots, or measurement outputs inside the repository. If scratch is
  required, place it outside the repo and remove it before writing the EXEC
  report.

## Deliverables

- One new `ISSUE-219` EXEC report containing the read-only audit.
- A decision block with exactly these fields:
  `EDITOR_USABILITY_PRESSURE_VERDICT`, `PRIMARY_USER_VISIBLE_GAP`,
  `PRIMARY_SUBSTRATE_PREREQUISITE`, `SMALLEST_SAFE_FOLLOW_UP`, and
  `FOLLOW_UP_BRANCH_MODE`.
- `EDITOR_USABILITY_PRESSURE_VERDICT` must use exactly one of:
  `pressure_deferred`, `user_visible_gap_triggered`,
  `substrate_prerequisite_triggered`, or `needs_human_arbitration`.
- `PRIMARY_USER_VISIBLE_GAP` must name exactly one current user-visible editor
  gap or be exactly `none` or `NEEDS_HUMAN`.
- `PRIMARY_SUBSTRATE_PREREQUISITE` must name exactly one substrate prerequisite
  that blocks or shapes the selected user-visible follow-up, or be exactly
  `none` or `NEEDS_HUMAN`.
- `SMALLEST_SAFE_FOLLOW_UP` must name exactly one action or be exactly
  `NEEDS_HUMAN`. It must not become a backlog list.
- `FOLLOW_UP_BRANCH_MODE` must be exactly
  `PublishMode branch only unless later authorized by a human` for any
  follow-up that would implement source, test, script, Cargo, workflow, schema,
  scheduler, automation-behavior, or production-doc changes. If the selected
  follow-up is audit-only, no-op/deferred, or `NEEDS_HUMAN`, state exactly
  `not_applicable`.
- A Phase 9 context section citing `plans/PLAN.md`,
  `plans/IMPLEMENTATION.md`, `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`,
  recent `change.md`, and relevant existing dispatch artifacts. It must
  distinguish current editor-usability pressure from historical roadmap,
  release-certification, and dispatch-doctrine evidence.
- An editor-shell section covering current user-facing or near-user-facing
  surfaces in `crates/editor-shell`, including lifecycle commands, command bus
  integration, play toolbar, inspector, viewport/camera/picking surfaces,
  snapshots, asset reload, visual smoke/perf harnesses, and integration tests.
- An editor-ui section covering current dock/layout/menu/workspace/widget
  surfaces, default workspaces, hot-reload layout behavior, inspector widget,
  node graph widgets, plugin adapter, and tests.
- A command-bus section covering `crates/editor-actions` as the command bus and
  undo/redo substrate, including action mediation, coalescing, compound
  actions, save/dirty state, audit ledger projection, and current consumers.
- A scene-loader consumer section covering `crates/rge-scene-loader`,
  `runtime/runtime-headless`, `crates/rge-data`, and
  `golden-projects/simple-scene`, plus a workspace search for production and
  test consumers of `rge_scene_loader` and `load_scene_into_world`.
- A consumer-chain matrix with one row per inspected usability candidate. Each
  row must classify the candidate as `user_visible_gap`,
  `substrate_prerequisite`, `already_covered`, `historical_only`, or
  `NEEDS_HUMAN`, and must include line-cited evidence or an exact read-only
  search command for zero-use claims.
- A candidate comparison section explaining why the selected follow-up is the
  smallest safe editor-usability follow-up, why substrate work must come first,
  why pressure should remain deferred, or why `NEEDS_HUMAN` is required.
- A final verification section reporting `git diff --check`,
  `git status --short --untracked-files=all`, and static inspection proving no
  tracked production/doc/script/Cargo/workflow/schema/task-brief files changed.

## Acceptance Criteria

- The EXEC report is audit-only and does not modify tracked source, tests,
  docs, Cargo metadata, workflows, schemas, automation scripts, scheduler
  configuration, fixtures, existing artifacts, or `.ai/dispatch.tasks.md`.
- The report classifies editor-usability pressure using exactly one
  `EDITOR_USABILITY_PRESSURE_VERDICT` value from the deliverables section.
- The report names exactly one smallest safe follow-up or exactly
  `NEEDS_HUMAN`; it does not produce a backlog list.
- The report includes the branch-mode requirement for any implementation
  follow-up exactly as specified in `FOLLOW_UP_BRANCH_MODE`.
- The audit reads and line-cites `AI_DISPATCH_AUTOMATION.md` and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md` before judging dispatch state, and it
  does not treat missing orchestrator `_REVIEW_` or `_CLOSEOUT_` packets as
  evidence of an unfinished dispatch.
- The audit reads and line-cites `.ai/dispatch.tasks.md` task #68 and the
  common #62-#71 constraints before applying this dispatch's audit shape.
- The audit reads and line-cites `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, and recent `change.md`
  entries to establish Phase 9 editor-usability context and current editor
  posture.
- The audit inspects and line-cites `crates/editor-shell/Cargo.toml`,
  `crates/editor-shell/src/lib.rs`,
  `crates/editor-shell/src/lifecycle/commands.rs`,
  `crates/editor-shell/src/lifecycle/asset_reload.rs`,
  `crates/editor-shell/src/lifecycle/playback.rs`,
  `crates/editor-shell/src/inspector.rs`,
  `crates/editor-shell/src/play_toolbar.rs`,
  `crates/editor-shell/src/viewport.rs`,
  `crates/editor-shell/src/camera.rs`,
  `crates/editor-shell/src/pick_path.rs`,
  `crates/editor-shell/src/snapshot.rs`,
  `crates/editor-shell/src/audit.rs`, and relevant
  `crates/editor-shell/tests/**` surfaces.
- The audit inspects and line-cites `crates/editor-ui/Cargo.toml`,
  `crates/editor-ui/src/lib.rs`, `crates/editor-ui/src/dock/**`,
  `crates/editor-ui/src/layout/**`, `crates/editor-ui/src/menus/**`,
  `crates/editor-ui/src/widgets/**`, `crates/editor-ui/src/workspace/**`,
  `crates/editor-ui/src/plugin_adapter.rs`,
  `crates/editor-ui/assets/defaults/**`, and relevant
  `crates/editor-ui/tests/**` surfaces.
- The audit inspects and line-cites `crates/editor-actions/Cargo.toml`,
  `crates/editor-actions/src/lib.rs`, `crates/editor-actions/src/action.rs`,
  `crates/editor-actions/src/bus.rs`,
  `crates/editor-actions/src/coalesce.rs`,
  `crates/editor-actions/src/compound.rs`,
  `crates/editor-actions/src/undo_stack.rs`, and relevant
  `crates/editor-actions/tests/**` surfaces.
- The audit inspects and line-cites `editor/rge-editor/Cargo.toml`,
  `editor/rge-editor/src/main.rs`, and
  `editor/rge-editor/src/glb_watcher.rs` to determine which substrate paths
  are actually reachable from the user-facing editor binary.
- The audit inspects and line-cites `runtime/runtime-headless/Cargo.toml`,
  `runtime/runtime-headless/src/main.rs`,
  `runtime/runtime-headless/tests/loads_golden_simple_scene.rs`,
  `crates/rge-scene-loader/Cargo.toml`,
  `crates/rge-scene-loader/src/lib.rs`,
  `crates/rge-scene-loader/tests/simple_scene.rs`,
  `crates/rge-data/src/scene.rs`, and
  `golden-projects/simple-scene/scenes/main.rge-scene` for scene-loader
  consumer pressure.
- The audit searches current production and test usage read-only for
  `EditorShell`, `EditorKeyCommand`, `CommandBus`, `UndoStack`,
  `handle_key_command`, `handle_asset_reload`, `EditorUi`, `Dock`,
  `Workspace`, `Menu`, `Inspector`, `NodeGraph`, `rge_scene_loader`, and
  `load_scene_into_world`. Positive findings must be line-cited. If a
  zero-use claim comes from search output rather than a line-bearing file, the
  report must include the exact read-only command, scope, and observed
  no-match result.
- The report distinguishes user-visible editor gaps from substrate
  prerequisites. A user-visible gap must be tied to a current editor workflow,
  key/menu/control/screen, or scene-load/edit/reload experience. A substrate
  prerequisite must be tied to a code path or crate surface that must exist
  before a truthful user-visible editor fix can land.
- The report uses `user_visible_gap_triggered` only if current evidence shows
  one concrete user-facing editor workflow gap that can become a bounded
  follow-up.
- The report uses `substrate_prerequisite_triggered` only if current evidence
  shows the next safe work is substrate or integration support required before
  a user-visible editor workflow can be finished.
- The report uses `pressure_deferred` only if current evidence does not show a
  concrete user-visible editor gap or substrate prerequisite that deserves the
  next dispatch.
- The report uses `needs_human_arbitration` only if current evidence is
  contradictory, unavailable, or points to an unbounded product/design or
  architecture decision rather than one safe follow-up.
- The audit inspects relevant existing dispatch artifacts read-only, including
  prior Phase 9 pressure audits (`ISSUE-210`, `ISSUE-212`, `ISSUE-214`,
  `ISSUE-216`, `ISSUE-217`, `ISSUE-218`) and recent editor/command-bus/
  scene-loader artifacts. It must cite current packet/log lines, not stale
  copied line numbers.
- Every substantive claim about current code, docs, scripts, Cargo metadata,
  fixtures, tests, logs, or dispatch artifacts is backed by `path:line`
  evidence. If a source-layout or zero-search claim comes from inventory
  rather than a line-bearing file, the report says so explicitly.
- The report does not run new benchmarks, profilers, cargo builds, cargo
  tests, cargo fmt, cargo deny, full repository verification, the autonomous
  queue, or a nested dispatch as part of this audit.
- `git diff --check` exits 0.
- `git status --short --untracked-files=all` shows no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new
  files must be limited to this dispatch's own handoff/log artifacts and
  optional ignored `.ai/dispatch-ISSUE-219` scratch.

## Constraints / Non-Goals

- Do not implement a fix.
- Do not edit source, tests, docs, Cargo files, workflows, schemas, scripts,
  scheduler config, task brief files, fixtures, prior handoff packets, prior
  logs, or existing dispatch artifacts.
- Do not update `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, or any other
  documentation.
- Do not add or run new benchmarks, profilers, generated reports,
  screenshots, ZIPs, fixtures, or persistent scratch files in the repository.
- Do not run publish, push, merge, branch-delete, label-edit, GitHub issue
  mutation, scheduler registration, or scheduler modification commands.
- Do not create a branch, commit, PR, issue comment, label change, or close the
  GitHub issue.
- Do not run the autonomous queue or a nested dispatch as verification.
- Do not run full repository verification manually for this audit. The
  task-specific gates are read-only inspection plus diff/status checks; the
  orchestrator may still run its canonical gate automatically after execution.
- Do not infer editor-usability pressure from crate names, comments, roadmap
  entries, or test existence alone. Tie the conclusion to current reachable
  editor workflows, command paths, UI surfaces, scene-loader consumers, and
  line-cited evidence.
- Do not recommend broad UI redesign, product strategy, workspace-manager
  rewrite, scene-loader rewrite, command-bus rewrite, or runtime integration
  project unless the report returns `NEEDS_HUMAN`.
- Treat unrelated dirty worktree state as out of scope. Do not clean, revert,
  or modify changes you did not make; report them if they affect confidence.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` and
  `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` -> expected readable;
  use them to avoid mistaking orchestrator packet shape for dispatch
  incompleteness.
- Inspect `.ai/dispatch.tasks.md` read-only around tasks #62-#71, including
  task #68 and the common constraints. Use line citations in the EXEC report.
- `Get-Content -Raw plans/PLAN.md`,
  `Get-Content -Raw plans/IMPLEMENTATION.md`, and
  `Get-Content -Raw plans/BASELINE.md` -> expected readable; use them to
  establish Phase 9 editor-usability pressure context and revisit triggers.
- `Get-Content -Raw Status.md` and `Get-Content -Raw HANDOFF.md` -> expected
  readable; use them to establish current state, closed gates, known
  deferrals, and next-work posture.
- Inspect recent `change.md` entries with live line numbers immediately before
  writing the final EXEC report. Do not reuse copied line numbers from prior
  packets or logs.
- Inspect `crates/editor-shell` read-only, including Cargo metadata,
  lifecycle command/asset-reload/playback modules, inspector/play-toolbar/
  viewport/camera/pick-path/snapshot/audit modules, and relevant tests.
- Inspect `crates/editor-ui` read-only, including Cargo metadata, dock,
  layout, menus, widgets, workspace, plugin adapter, default workspace assets,
  and relevant tests.
- Inspect `crates/editor-actions` read-only, including Cargo metadata,
  command bus, actions, coalescing, compound actions, undo stack, and relevant
  tests.
- Inspect `editor/rge-editor` read-only to identify user-facing wiring from
  keyboard/input, GLB watch/reload, editor shell, UI host, and render loop into
  the editor binary.
- Inspect scene-loader and consumer paths read-only:
  `crates/rge-scene-loader`, `runtime/runtime-headless`, `crates/rge-data`,
  and `golden-projects/simple-scene`.
- Search Cargo metadata read-only for dependency declarations involving
  `rge-editor-shell`, `rge-editor-ui`, `rge-editor-actions`,
  `rge-scene-loader`, and `runtime-headless`. Use `git grep`,
  `Select-String`, `git ls-files`, or an equivalent read-only command, and
  report exact scope and results.
- Search production and test usage read-only for `EditorShell`,
  `EditorKeyCommand`, `CommandBus`, `UndoStack`, `handle_key_command`,
  `handle_asset_reload`, `EditorUi`, `Dock`, `Workspace`, `Menu`,
  `Inspector`, `NodeGraph`, `rge_scene_loader`, and
  `load_scene_into_world`. Report positive matches with line citations and
  zero-use claims with the exact command and scope.
- Inspect relevant existing dispatch artifacts read-only, especially prior
  Phase 9 audits and recent editor/command-bus/scene-loader work. Ignore
  `OLD/**`, `.ai/dispatch-*/**`, `.claude/worktrees/**`, `.git/**`, and build
  output when assessing repository state unless a specific artifact is cited
  as provenance.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new
  files must be limited to this dispatch's own handoff/log artifacts and
  optional ignored `.ai/dispatch-ISSUE-219` scratch.
- Static no-source-change inspection: run `git diff --name-only` and a
  targeted read-only inspection of protected tracked surfaces such as
  `crates`, `kernel`, `editor`, `runtime`, `tools`, `docs`, `plans`,
  `Status.md`, `HANDOFF.md`, `change.md`, `Cargo.toml`, `Cargo.lock`,
  `.github/workflows`, `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`,
  `.ai/*.schema.json`, and root `*.ps1` automation scripts. Expected result:
  no tracked production/doc/script/Cargo/workflow/schema/task-brief diff.

## Halt Conditions

The Executor MUST halt and return `NEEDS_HUMAN` without editing any source,
docs, tests, scripts, schemas, workflows, Cargo files, scheduler config, task
briefs, fixtures, prior packets, or prior logs if any of the following occur:

- Required evidence files are missing or unreadable.
- The required production/test usage search cannot be completed read-only.
- The evidence cannot support exactly one
  `EDITOR_USABILITY_PRESSURE_VERDICT` value from the allowed set.
- The audit cannot distinguish the primary user-visible editor gap from the
  primary substrate prerequisite.
- The smallest safe follow-up cannot be narrowed to exactly one action.
- The smallest safe follow-up would require source/test/script/Cargo/workflow/
  schema/scheduler/automation changes but cannot be constrained to branch-mode
  pending later human authorization.
- The audit needs an implementation, documentation edit, Cargo edit, workflow
  edit, schema edit, script edit, scheduler change, task-brief edit, prior
  artifact edit, benchmark run, profiler run, cargo build/test/fmt/deny run, or
  GitHub mutation to be truthful.
- Required line citations cannot be produced for document-, code-, Cargo-,
  fixture-, script-, log-, or artifact-based claims.
- Current evidence shows a real editor-usability pressure signal, but the next
  step depends on an unresolved product/design or architecture decision about
  editor workflow priority, command ownership, scene-loading ownership, UI host
  direction, or runtime/editor dependency direction.
- Any GitHub mutation, push, merge, branch deletion, label change, issue
  comment, scheduler command, benchmark run, profiler run, full verification
  run, or nested dispatch seems necessary.
- `git diff --check` fails for a reason caused by this dispatch.
- `git status --short --untracked-files=all` shows new or modified files
  outside this dispatch's own handoff/log artifacts and optional ignored
  `.ai/dispatch-ISSUE-219` scratch, unless they are clearly pre-existing
  unrelated user changes.

If a halt condition fires, the Executor must still write the
`EXECUTION_REPORT` if possible, set header `STATUS: NEEDS_HUMAN`, include the
exact halt reason and current status output, and route the footer to
`NEXT_ROLE: HUMAN_ARBITER`.

## Planner Notes

This is delegated-main smoke batch task 7 of 10 from `.ai/dispatch.tasks.md`;
the task number in that brief is #68, while the filed GitHub issue and dispatch
ID are `ISSUE-219`. The desired result is not to create editor work by default.
It is to decide whether current editor-shell/editor-ui/command-bus/
scene-loader evidence shows one bounded editor-usability follow-up, whether a
substrate prerequisite must come first, whether pressure remains deferred, or
whether human product/architecture arbitration is required.

Use current evidence over prior summaries. Prior dispatch artifacts can be used
as provenance, but the report must label them as historical unless the cited
artifact itself proves current applicability. Under the orchestrator protocol,
no `REVIEW` or `CLOSEOUT` packet is expected from this TASK. The queue's Codex
control review is stored in the run directory, not as a handoff packet.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-219
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
