# Task Packet

DISPATCH_ID: ISSUE-11
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_05-48-02+0300
RELATED_FILES:
- AGENTS.md
STATUS: OPEN

## Goal

Implement GitHub issue #11 by adding one concise pointer bullet for
`Get-AiDispatchHealth.ps1` to the "Pointers" list in `AGENTS.md`. The bullet
should describe it as the dispatch-health readout covering pass rate,
correction rounds, and retries across recorded `.ai/dispatch-*/` runs.

## Scope

### MAY edit
- AGENTS.md

### MUST NOT edit
- Get-AiDispatchHealth.ps1
- AI_DISPATCH_AUTOMATION.md
- AI_DISPATCH_PARALLEL.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- ai_handoffs/templates/*
- .ai/*
- .gitignore
- CLAUDE.md
- HANDOFF.md
- Status.md
- change.md
- Any source, schema, script, packet, log, status, handoff, or documentation
  file other than `AGENTS.md`

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-11_EXEC_*.md`
- The matching execution-report `.meta.json` sidecar if generated by
  `new-handoff.ps1 -Finalize`

### MUST NOT add new files
- Any new source, docs, schema, script, test, log, or packet file other than
  the protocol-required execution report and its finalize-generated sidecar

## Deliverables

- One new bullet in the existing `AGENTS.md` "Pointers" list for
  `Get-AiDispatchHealth.ps1`.
- The bullet is concise and matches the brevity/style of the surrounding
  pointer bullets.
- The bullet describes `Get-AiDispatchHealth.ps1` as the dispatch-health readout
  covering pass rate, correction rounds, and retries across recorded
  `.ai/dispatch-*/` runs.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- `AGENTS.md` is the only tracked project file changed by the implementation.
- The `AGENTS.md` "Pointers" list contains exactly one bullet naming
  `Get-AiDispatchHealth.ps1`.
- The new bullet explicitly covers pass rate, correction rounds, and retries
  across recorded `.ai/dispatch-*/` runs.
- The diff to `AGENTS.md` is limited to adding that one bullet; no existing
  prose, headings, or other pointer bullets are rewritten.
- No scripts, schemas, source files, templates, status files, handoff protocol
  docs, or unrelated documentation are changed.

## Constraints / Non-Goals

- This is a documentation-only dispatch.
- Do not change runtime behavior, PowerShell scripts, schemas, tests, dispatch
  automation, issue metadata, branches, commits, or remotes.
- Do not inspect `.ai/dispatch-*/` run contents unless needed only to verify the
  wording target; do not modify anything under `.ai/`.
- Do not add a new section, reorganize `AGENTS.md`, or rewrite the surrounding
  pointer list.
- Do not run broad Rust build, lint, or test suites for this docs-only change
  unless required by local dispatch tooling outside this packet.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Select-String -Path AGENTS.md -Pattern 'Get-AiDispatchHealth\.ps1','pass rate','correction rounds','retries','\.ai/dispatch-\*/'`
  -> expected: matches for the script name and each required wording element.
- `git diff -- AGENTS.md` -> expected: diff shows only one new pointer bullet in
  the "Pointers" list.
- `git diff --check -- AGENTS.md` -> expected exit 0.
- `git status --short` -> expected tracked project-file change is limited to
  `AGENTS.md`, plus protocol-required handoff output for this dispatch.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- The "Pointers" list in `AGENTS.md` cannot be identified clearly.
- Matching the requested wording appears to require rewriting existing
  `AGENTS.md` content instead of adding one bullet.
- Accurate wording appears to require changing `Get-AiDispatchHealth.ps1` or any
  other script, schema, source file, template, or documentation file.
- Verification shows any tracked project-file change outside `AGENTS.md`.
- The executor believes the requested pointer should live somewhere other than
  the existing "Pointers" list.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from
`.ai/dispatch.tasks.md` for GitHub issue #11. Keep the implementation to one
AGENTS.md pointer bullet; `Get-AiDispatchHealth.ps1` is relevant context by
name only and is not in edit scope.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-11
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
