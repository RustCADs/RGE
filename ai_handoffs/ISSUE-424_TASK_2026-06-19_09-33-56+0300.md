# Task Packet

DISPATCH_ID: ISSUE-424
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-19_09-33-56+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- crates/editor-shell/src/render_path.rs
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/tests.rs
- crates/editor-shell/src/lifecycle/mod.rs
- crates/editor-actions/src
- crates/editor-actions/Cargo.toml
- crates/editor-ui/**
- crates/editor-egui-host/**
STATUS: OPEN

## Goal

Perform the gated audit requested by GitHub issue #424 for task 165: audit task
164's menu Delete CAD routing boundary without changing production source,
tests, Cargo metadata, docs, schemas, scripts, command/menu UI surfaces, or CAD
implementation code. Confirm that `Command::Delete` routes to the CAD
CommandBus delete path only for the exact single tracked-CAD selection, while
all other selections preserve the wrapper-world Delete/Cut boundary. Record the
required human pause in `.ai/dispatch.tasks.md` as the final marker and
recommendation block, and do not append task 166 or any feature task.

## Scope

This is a source/docs/test/Cargo read-only audit with one narrow task-brief
write permitted for the final human-pause marker.

### MAY edit

- `.ai/dispatch.tasks.md` only for one final marker line beginning
  `NEEDS_HUMAN_RECORDED: 2026-06-19 - ` followed by a source-grounded reason,
  and the immediately following `Recommendation for human approval` block
  required by task 165.

### MUST NOT edit

- `crates/editor-shell/src/render_path.rs`
- `crates/editor-shell/src/lifecycle/commands.rs`
- `crates/editor-shell/src/lifecycle/tests.rs`
- `crates/editor-shell/src/lifecycle/mod.rs`
- `crates/editor-actions/**`
- `crates/editor-ui/**`
- `crates/editor-egui-host/**`
- `crates/**/Cargo.toml`
- `Cargo.toml`
- `Cargo.lock`
- `docs/**`
- `AI_DISPATCH_AUTOMATION.md`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- `.github/workflows/**`
- `.ai/*.schema.json`
- `.ai/dispatch.verify.ps1`
- `*.ps1`
- Any production source, tests, Cargo metadata, docs, schemas, workflows,
  automation scripts, UI/menu registries, command enums, shortcuts, labels,
  CAD/action/render/persistence implementation, or existing handoff/log
  artifact not generated for this current dispatch.

### MAY add new files

- `ai_handoffs/ISSUE-424_EXEC_*.md`
- `ai_handoffs/ISSUE-424_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-424/**`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files

- `crates/**`
- `docs/**`
- `ai_handoffs/ISSUE-424_REVIEW_*.md`
- `ai_handoffs/ISSUE-424_CLOSEOUT_*.md`
- Any task 166, feature task, ADR, schema, workflow, source file, test file,
  Cargo file, script, or manual review/closeout packet.

## Deliverables

- A source-grounded audit in the EXEC packet covering the task 164 menu Delete
  CAD routing boundary and all required unchanged/generic-boundary checks.
- Exactly one new `.ai/dispatch.tasks.md` marker line beginning
  `NEEDS_HUMAN_RECORDED: 2026-06-19 - ` followed by a source-grounded reason,
  followed by a `Recommendation for human approval` block.
- The recommendation block MUST contain these five parts: proposed next
  feature, exact edit surface, risks, verification, and why it is the smallest
  coherent next step.
- No task 166 and no feature task appended.
- No production source, test, Cargo, docs, schema, workflow, script, UI/menu,
  CAD/action/render/persistence, or existing packet/log edits.

## Acceptance Criteria

- `Command::Delete` routing in `crates/editor-shell/src/render_path.rs` is
  verified as a direct tracked-entity ID comparison: selection length is exactly
  one, `self.cad_entity` is `Some(e)`, and the selected entity is `e`.
- The exact single tracked-CAD selection route calls
  `self.delete_current_cad_cuboid()` and logs/swallows rejection with no
  fallback to `delete_selected_entities`, no selection clearing from the wrapper
  path, and no CommandBus stack growth when stale/invalid CAD preflight rejects.
- Empty selection, non-CAD selection, and mixed tracked-CAD plus non-CAD
  selection preserve the wrapper-world `delete_selected_entities` route and do
  not mutate CAD graph/projection/CAD world state through the CAD bus route.
- `Command::Cut` and `cut_selected_entities` remain wrapper-world only.
- `delete_current_cad_cuboid`, `DeleteCurrentCadCuboid`,
  `ShellActionContext`, the `Action` contract, and `lifecycle/mod.rs` public
  exports are unchanged by task 164.
- `crates/editor-actions` remains generic and has no CAD/editor-shell
  references or dependency coupling.
- No UI/menu/enum/shortcut/label surface was touched by task 164.
- The final `.ai/dispatch.tasks.md` edit contains exactly one new current
  human-pause marker and recommendation block, and no `^166.` heading.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion above must be verified again by the
Executor before relying on it. Planner pre-read observations for orientation:

- Claim: The current task brief has task 164 and task 165 headings and no task
  166 heading at the tail; many older `NEEDS_HUMAN_RECORDED` strings are
  historical provenance, not current task 165 completion markers.
  Falsifying search:
  `rg -n "^164\.|^165\.|^166\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
  -> observed `164.` at line 16455 and `165.` at line 16590, no `^166.` line;
  historical marker/template references appear throughout older task text.
- Claim: Task 164's implementation range is the adjacent commit range
  `90afb96..0402a6a` for this audit.
  Source read: `git log --oneline -8` -> observed `0402a6a ai-dispatch
  ISSUE-423: Route menu Delete to tracked CAD cuboid delete` immediately after
  `90afb96 Re-arm dispatch with approved task 164 (route menu Delete to
  headless CAD-cuboid delete)`.
- Claim: The planner has not observed pending diffs in the named audit
  surfaces before this TASK revision.
  Falsifying search:
  `git diff --name-only -- crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/tests.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-actions crates/editor-ui crates/editor-egui-host Cargo.toml Cargo.lock docs .github/workflows .ai/dispatch.verify.ps1 *.ps1`
  -> no output. Untracked current-dispatch artifacts may still exist and must
  be treated as generated artifacts, not source diffs.
- Claim: `Command::Delete` is currently guarded by the exact single
  tracked-CAD predicate, the CAD route logs/swallows a rejected CAD delete, and
  the wrapper Delete route remains only in the non-exact-selection `else`.
  Falsifying search:
  `rg -n "fn delete_menu_selection_is_exact_tracked_cad_entity|selection\.len\(\) == 1|selection\.contains\(cad_entity\)|if let Err\(e\) = self\.delete_current_cad_cuboid\(\)|self\.delete_selected_entities\(\)" crates/editor-shell/src/render_path.rs`
  -> observed `delete_current_cad_cuboid()` at line 455,
  `delete_selected_entities()` at line 463, predicate definition at line 491,
  and the `selection.len() == 1 && selection.contains(cad_entity)` check at
  line 495.
- Claim: stale/invalid exact tracked-CAD Delete rejection has no fallback, no
  wrapper selection clearing/pruning, and no CommandBus stack growth.
  Falsifying search:
  `rg -n "route_menu_command_delete_current_cad_cuboid_stale_selection_swallow_no_fallback|rejected routed CAD delete leaves stale|does not move the bus stack|does not fall back and clear selection|does not prune face selection" crates/editor-shell/src/lifecycle/tests.rs`
  -> observed the stale-selection test at line 6481 and assertions/messages at
  lines 6501, 6507, 6512, and 6517.
- Claim: empty, non-CAD, and mixed Delete selections preserve the wrapper-world
  route and do not move the CAD command bus.
  Falsifying search:
  `rg -n "route_menu_command_delete_selected_non_cad_preserves_tracked_cad_scene|route_menu_command_delete_selected_mixed_selection_preserves_cad_scene|route_menu_command_delete_selected_empty_selection_preserves_tracked_cad_scene|wrapper-world delete does not move the CAD command bus|mixed wrapper-world delete does not move the CAD command bus|empty wrapper-world delete does not move the CAD command bus" crates/editor-shell/src/lifecycle/tests.rs`
  -> observed tests at lines 6374, 6413, and 6455, with no-bus-move assertion
  messages at lines 6407, 6446, and 6475.
- Claim: `Command::Cut` remains wrapper-world only, including exact tracked-CAD
  selection.
  Falsifying searches:
  `rg -n "Command::Cut =>|cut_selected_entities\(\)" crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/mod.rs`
  -> observed `Command::Cut` dispatching `cut_selected_entities()` in
  `render_path.rs` lines 444-445 and `cut_selected_entities()` in
  `lifecycle/mod.rs` line 1835.
  `rg -n "route_menu_command_cut_selected_exact_tracked_cad_stays_wrapper_world_only|Cut remains wrapper-world only and does not route to CAD delete|wrapper-world Cut clears the selected CAD id without deleting CAD|does not create wrapper-world clipboard contents" crates/editor-shell/src/lifecycle/tests.rs`
  -> observed the exact tracked-CAD Cut test at line 6720 and wrapper-only
  assertions/messages at lines 6741, 6745, and 6749.
- Claim: `delete_current_cad_cuboid`, `DeleteCurrentCadCuboid`,
  `ShellActionContext`, the `Action` contract, and `lifecycle/mod.rs` exports
  were not changed by task 164.
  Falsifying search:
  `git diff 90afb96..0402a6a -- crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs | rg -n "Command::Cut|cut_selected_entities|delete_current_cad_cuboid|DeleteCurrentCadCuboid|ShellActionContext|impl Action|pub use"`
  -> no output (negative grep exit 1). Current source locations remain:
  `ShellActionContext` at `commands.rs` line 184, `DeleteCurrentCadCuboid` at
  line 511, `delete_current_cad_cuboid` at line 1218, `pub use commands` at
  `lifecycle/mod.rs` line 259, and the generic `Action` contract in
  `crates/editor-actions/src/action.rs` line 164.
- Claim: Task 164 did not touch UI/menu/enum/shortcut/label surfaces, Cargo
  metadata, docs, workflows, scripts, editor-actions, or lifecycle
  action/export surfaces.
  Falsifying searches:
  `git diff --name-only 90afb96..0402a6a -- crates/editor-ui crates/editor-egui-host crates/editor-actions crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs Cargo.toml Cargo.lock docs .github/workflows .ai/dispatch.verify.ps1 *.ps1`
  -> no output.
  `git diff 90afb96..0402a6a -- crates/editor-ui crates/editor-egui-host | rg -n "Command::Delete|Command::Cut|Delete|Cut|shortcut|menu|label|registry"`
  -> no output (negative grep exit 1).
- Claim: `crates/editor-actions` currently has no CAD/editor-shell reference
  surface.
  Falsifying search:
  `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml`
  -> no matches (negative grep exit 1).

## Constraints / Non-Goals

- Do not implement a feature, do not repair source, and do not broaden the
  routing behavior. This dispatch audits the task 164 result.
- Do not call `gh` or the network. For any queue-state or already-filed
  autonomous-task confirmation, use the dispatcher-provided GitHub snapshot in
  the issue body as the GitHub evidence.
- Do not append task 166 or any feature task.
- Do not modify the command enum, menu registry, menu-enable predicates,
  shortcuts, labels, editor-ui, egui host, editor-actions, CAD crates,
  projection, render pipeline, persistence, Cargo metadata, docs, schemas,
  scripts, workflows, or tests.
- Do not change `Command::Cut`, `cut_selected_entities`,
  `delete_current_cad_cuboid`, `DeleteCurrentCadCuboid`,
  `ShellActionContext`, `Action`, or `lifecycle/mod.rs` exports.
- Do not treat missing orchestrator `_REVIEW_` or `_CLOSEOUT_` packets as an
  unfinished dispatch; this repository's orchestrator normally writes TASK and
  EXEC packets only.

## Verification Gates

Run and record every command result in the EXEC packet. If a command exits
non-zero where the expected result says exit 0, halt without source edits.

- `git diff --name-only`
  - Before the marker edit, expect no production source/test/Cargo/doc diffs.
  - After the marker edit, expect only `.ai/dispatch.tasks.md` plus generated
    current-dispatch artifacts.
- `git diff --name-only -- crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/tests.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-actions crates/editor-ui crates/editor-egui-host Cargo.toml Cargo.lock docs .github/workflows .ai/dispatch.verify.ps1 *.ps1`
  - Before the marker edit, expect no output. After the marker edit, this
    path-scoped command may show only `.ai/dispatch.tasks.md` if that path is
    included in a separate diff command; it must not show source/test/Cargo/doc
    or automation paths.
- `git diff -- crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/tests.rs .ai/dispatch.tasks.md`
  - Expect source files read-only in this dispatch; `.ai/dispatch.tasks.md`
    may show only the final marker/recommendation block.
- `git diff --name-only 90afb96..0402a6a -- crates/editor-ui crates/editor-egui-host crates/editor-actions crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs Cargo.toml Cargo.lock docs .github/workflows .ai/dispatch.verify.ps1 *.ps1`
  - Expect no output; this is the task-164 no-touch check for UI/menu,
    editor-actions, lifecycle action/export, Cargo/docs/workflow/script, and
    verification-gate surfaces.
- `git diff 90afb96..0402a6a -- crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs | rg -n "Command::Cut|cut_selected_entities|delete_current_cad_cuboid|DeleteCurrentCadCuboid|ShellActionContext|impl Action|pub use"`
  - Expect no matches and grep exit 1; record this as a successful negative
    grep, not a task failure.
- `git diff 90afb96..0402a6a -- crates/editor-ui crates/editor-egui-host | rg -n "Command::Delete|Command::Cut|Delete|Cut|shortcut|menu|label|registry"`
  - Expect no matches and grep exit 1; record this as a successful negative
    grep, not a task failure.
- `rg -n "Command::Delete =>|delete_menu_selection_is_exact_tracked_cad_entity|delete_current_cad_cuboid\\(\\)|delete_selected_entities\\(\\)" crates/editor-shell/src/render_path.rs`
- `rg -n "struct DeleteCurrentCadCuboid|fn delete_current_cad_cuboid|fn cut_selected_entities|ShellActionContext|impl Action|pub use" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs`
- `rg -n "route_menu_command|delete_selected|cut_selected|stale_selection|mixed_selection|empty_selection|non_cad" crates/editor-shell/src/lifecycle/tests.rs`
- `rg -n "route_menu_command_delete_current_cad_cuboid_stale_selection_swallow_no_fallback|rejected routed CAD delete leaves stale|does not move the bus stack|does not fall back and clear selection|does not prune face selection" crates/editor-shell/src/lifecycle/tests.rs`
- `rg -n "route_menu_command_delete_selected_non_cad_preserves_tracked_cad_scene|route_menu_command_delete_selected_mixed_selection_preserves_cad_scene|route_menu_command_delete_selected_empty_selection_preserves_tracked_cad_scene|wrapper-world delete does not move the CAD command bus|mixed wrapper-world delete does not move the CAD command bus|empty wrapper-world delete does not move the CAD command bus" crates/editor-shell/src/lifecycle/tests.rs`
- `rg -n "route_menu_command_cut_selected_exact_tracked_cad_stays_wrapper_world_only|Cut remains wrapper-world only and does not route to CAD delete|wrapper-world Cut clears the selected CAD id without deleting CAD|does not create wrapper-world clipboard contents" crates/editor-shell/src/lifecycle/tests.rs`
- `cargo test -p rge-editor-shell --lib -- route_menu_command`
- `cargo test -p rge-editor-shell --lib -- delete_selected`
- `cargo test -p rge-editor-shell --lib -- cut_selected`
- `cargo test -p rge-editor-shell --lib -- delete_current_cad_cuboid`
- `cargo check -p rge-editor-actions -p rge-editor-shell`
- `cargo +nightly fmt --all -- --check`
- `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml`
  - Expect no matches. Record this as a successful negative grep; do not treat
    grep exit 1 as a task failure for this specific command.
- `rg -n "^164\\.|^165\\.|^166\\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
  - Expect exactly one task 164 heading, exactly one task 165 heading, no task
    166 heading, and exactly one new current marker from this dispatch.
    Historical marker references in older tasks are provenance.
- `git diff --check`

## Halt Conditions

The Executor MUST halt, write the EXEC packet with the failure/deviation, and
request human guidance without editing forbidden files if any of the following
occur:

- Any audit finding would require a production source, test, Cargo, docs,
  schema, workflow, script, UI/menu, CAD/action/render/persistence, or existing
  handoff/log edit to make the boundary true.
- `Command::Delete` is not limited to the exact single tracked-CAD selection,
  or stale/invalid exact CAD selection falls back to wrapper Delete, clears
  selection through the wrapper path, or grows the CommandBus stack.
- Empty, non-CAD, or mixed selections do not preserve wrapper-world Delete
  behavior.
- `Command::Cut`, `cut_selected_entities`, `delete_current_cad_cuboid`,
  `DeleteCurrentCadCuboid`, `ShellActionContext`, `Action`, or
  `lifecycle/mod.rs` exports changed in a way not already authorized by task
  164's committed surface.
- `crates/editor-actions` has CAD/editor-shell coupling.
- The audit would require appending task 166 or a feature task.
- `.ai/dispatch.tasks.md` already contains a current task 165 completion marker
  before the Executor's final edit, or the final marker/recommendation cannot
  be recorded as exactly one new marker plus one recommendation block.
- A verification failure is outside the permitted audit surface and any
  correction would require source/test/Cargo/docs/schema/workflow/script edits.

## Planner Notes

This dispatch is task 165 from `.ai/dispatch.tasks.md`, filed as GitHub issue
#424. Task 164 already implemented the narrow menu Delete route; this task is
the gated read-only audit and human-pause record that task 164 required. The
executor should inspect `crates/editor-ui/**` and `crates/editor-egui-host/**`
only as needed to confirm no UI/menu/enum/shortcut/label surface was touched;
the primary source evidence is in `render_path.rs`, `lifecycle/commands.rs`,
`lifecycle/tests.rs`, `lifecycle/mod.rs`, and `crates/editor-actions`.

The recommendation block should not create work by itself. It records the next
human decision point after the audit; the human decides whether any future task
is filed.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-424
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
