# Correction Packet

DISPATCH_ID: ISSUE-37
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_05-32-55+0300
RELATED_FILES:
- crates/cad-projection/tests/render_adapter_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-37_TASK_2026-05-19_05-24-51+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-37_EXEC_2026-05-19_05-32-02+0300.md`
- Latest Verification Log: `.ai/dispatch-ISSUE-37/verification.round0.log`
- Latest Review Report: none. The orchestrator stopped before Codex control review because canonical verification failed.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction - nothing more, nothing less.
The correction references the post-execution verification finding that blocked
the dispatch before control review.

1. **Apply rustfmt wrapping to the Boolean graph commit** - Canonical
   verification failed in `cargo +nightly fmt --all -- --check` at
   `crates/cad-projection/tests/render_adapter_smoke.rs:1407`. Required
   change: edit only `crates/cad-projection/tests/render_adapter_smoke.rs`
   so the `graph.commit("cuboid + cuboid -> boolean union").expect("commit");`
   statement is formatted exactly as rustfmt expects:
   `graph` on its own line, then the chained `.commit(...)` and `.expect(...)`
   calls on following indented lines. Do not change test behavior, assertions,
   imports, documentation, production code, Cargo metadata, scripts, schemas,
   or any other file. Acceptance: `cargo +nightly fmt --all -- --check` exits
   0, and the canonical dispatch verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control review findings exist for this run because verification
failed before control review. The only approved correction is the mechanical
rustfmt change reported by the canonical verification gate.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with the additional correction-round
requirement that the canonical post-execution verification gate
`.\.ai\dispatch.verify.ps1` exits 0 after the formatting correction.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-cad-projection --test render_adapter_smoke` -> expected
  exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0. This is the dispatch-passing
  gate; the dispatch cannot pass until it succeeds.
- `git status --short --untracked-files=all` -> expected no unexpected changes
  outside `crates/cad-projection/tests/render_adapter_smoke.rs` and the
  required ISSUE-37 execution-report packet for the correction round.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- Halt and route back to Planner if satisfying rustfmt requires any source
  change beyond the displayed formatting-only rewrite in
  `crates/cad-projection/tests/render_adapter_smoke.rs`.
- Halt and preserve the failing output if the canonical verification gate
  exposes a new, distinct blocking failure after the formatting correction.

## Planner Notes

The focused execution test already passed in the execution report. The
post-execution gate failed at the first canonical CI step only because the new
Boolean smoke left one method chain in non-rustfmt layout. This packet approves
only that mechanical formatting correction and the required re-verification;
it does not approve semantic test changes, production changes, new files,
dependency changes, or edits to any other packet.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-37
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
