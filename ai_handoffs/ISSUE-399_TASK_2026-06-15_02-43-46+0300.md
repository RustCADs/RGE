# Task Packet

DISPATCH_ID: ISSUE-399
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_02-43-46+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/mod.rs
- crates/editor-shell/src/lifecycle/tests.rs
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- ai_handoffs/ISSUE-399_EXEC_*.md
- ai_handoffs/ISSUE-399_EXEC_*.meta.json
- ai_dispatch_logs/log_*.md
STATUS: OPEN

## Goal

Implement `.ai/dispatch.tasks.md` task 145 exactly: when
`EditorShell::reset_camera` runs through the existing View -> Reset Camera /
Frame Scene or Home accelerator `Command::ResetCamera` path, clear any pending
`ViewportLeftDoubleClick` state so an earlier viewport left press cannot pair
with a later in-threshold left press after reset-camera framing. Keep the
behavioral change confined to the editor-shell lifecycle surface and preserve
existing reset-camera framing, valid viewport double-click framing, selected-CAD
double-click framing, and prior stale-reset behavior.

## Scope

This is a bounded editor-shell lifecycle implementation plus focused regression
coverage and the required self-re-arm task-bookkeeping update. Do not widen the
work into command routing, menu definitions, shortcut policy, camera math,
viewport hit testing, CAD mutation, or any non-lifecycle crate.

### MAY edit
- `crates/editor-shell/src/lifecycle/mod.rs`
- `crates/editor-shell/src/lifecycle/tests.rs`
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-399_EXEC_*.md`
- `ai_handoffs/ISSUE-399_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT edit
- `crates/editor-ui/**`
- `crates/editor-egui-host/**`
- `crates/editor-shell/src/render_path.rs`
- `crates/editor-shell/src/camera.rs`
- `crates/editor-shell/src/lifecycle/accelerator.rs`
- `crates/editor-shell/src/lifecycle/commands.rs`
- `crates/editor-shell/src/lifecycle/extension_command.rs`
- `crates/editor-shell/src/lifecycle/save_request.rs`
- `crates/editor-shell/src/lifecycle/viewport_navigation.rs`
- `crates/editor-actions/**`
- `crates/cad-projection/**`
- `crates/plugin-discovery/**`
- `runtime/**`
- Cargo manifests or `Cargo.lock`
- `.github/workflows/**`
- `.ai/*.schema.json`
- `.ai/dispatch.verify.ps1`
- dispatch automation, guard, queue, scheduler, watcher, verification, health,
  trend, or handoff scripts
- schemas, ADR files, architecture-lint rules/config, packet templates, or
  unrelated existing handoff/log artifacts
- command routing, command enum/menu definitions, shortcut execution,
  remapping/persistence/fatal policy, plugin runtime/discovery/loading, OS
  clipboard behavior, CAD/projection/CommandBus mutation, undo/dirty,
  save/load authority, camera math, viewport hit testing, face-pick policy,
  wheel zoom, right-button orbit math, middle-button pan math, focus-loss
  behavior, cursor-left behavior, or mouse-wheel behavior

### MAY add new files
- `ai_handoffs/ISSUE-399_EXEC_*.md`
- `ai_handoffs/ISSUE-399_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files
- Any new Rust source file, test fixture, Cargo file, workflow, schema, ADR,
  automation script, packet template, architecture-lint file, or unrelated
  documentation file
- Any handoff/log artifact not generated for ISSUE-399

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - crates/editor-shell/src/lifecycle/mod.rs
  - crates/editor-shell/src/lifecycle/tests.rs
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-399_EXEC_*.md
  - ai_handoffs/ISSUE-399_EXEC_*.meta.json
  - ai_dispatch_logs/log_*.md
MUST_NOT_EDIT:
  - crates/editor-ui/**
  - crates/editor-egui-host/**
  - crates/editor-shell/src/render_path.rs
  - crates/editor-shell/src/camera.rs
  - crates/editor-shell/src/lifecycle/accelerator.rs
  - crates/editor-shell/src/lifecycle/commands.rs
  - crates/editor-shell/src/lifecycle/extension_command.rs
  - crates/editor-shell/src/lifecycle/save_request.rs
  - crates/editor-shell/src/lifecycle/viewport_navigation.rs
  - crates/editor-actions/**
  - crates/cad-projection/**
  - crates/plugin-discovery/**
  - runtime/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/workflows/**
  - .ai/*.schema.json
  - .ai/dispatch.verify.ps1
  - Invoke-*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-*.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
  - docs/adr/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A lifecycle implementation that resets pending
  `ViewportLeftDoubleClick` state when `EditorShell::reset_camera` performs
  reset-camera framing through the existing View/Home command path.
- Focused lifecycle tests proving a first viewport left press, reset-camera
  framing, and a second in-threshold viewport left press does not complete a
  stale double-click for scene-wide framing or selected-CAD framing.
- Existing lifecycle coverage for reset-camera, viewport left double-click,
  wheel stale reset, focus-loss stale reset, and cursor-left stale reset remains
  passing.
- `.ai/dispatch.tasks.md` is updated per the required Self-re-arm final step:
  append exactly one bounded docs/source-read-only audit task 146, or append a
  single `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line if no bounded
  in-policy next task exists.
- A completed ISSUE-399 EXEC packet recording the implementation, verification
  results, and the final task-146-or-human outcome.

## Acceptance Criteria

- `EditorShell::reset_camera` or its existing lifecycle caller clears pending
  `ViewportLeftDoubleClick` state before a following viewport left press can
  pair with a pre-reset-camera left press.
- A first viewport left press, then View/Home reset-camera framing, then a
  second in-threshold viewport left press does not frame the scene as a stale
  double-click.
- A first viewport left press, then View/Home reset-camera framing with a
  selected CAD entity present, then a second in-threshold viewport left press
  does not frame selected CAD bounds as a stale double-click.
- Direct reset-camera behavior still frames prebuilt render meshes, CAD
  projection scenes, and empty/no-scene fallback as before except for clearing
  pending left-double-click state.
- Existing ordinary viewport left-double-click scene framing, selected-CAD
  double-click framing, focus-loss/cursor-left/mouse-wheel stale reset
  behavior, right-button orbit, and middle-button pan remain unchanged.
- `.ai/dispatch.tasks.md` ends with task 145 completed or otherwise annotated
  according to local task-list convention, and exactly one bounded next-task
  source audit task 146 is appended with explicit `MAY edit`, `MUST NOT edit`,
  `Done criteria`, `Verification`, and `Halt conditions` sections. If no bounded
  audit can be specified, append exactly one
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line instead. Do not add
  both.
- The task 146 audit must copy this Self-re-arm final-step requirement into its
  body: after the audit is complete and verified, append exactly one bounded
  FEATURE task 147 carrying the copied Self-re-arm requirement, or append a
  single `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line.

## Current-State Claims: Falsification Required

Every negative or "unchanged" assertion the executor makes in the EXEC packet
must include the inverse search/read that would falsify it, plus the observed
result.

- Claim: Current source does not clear `ViewportLeftDoubleClick` from the
  `reset_camera` or `frame_cad_selection_or_reset_camera` path.
  Falsifying search:
  `rg -n "reset_camera.*reset_viewport_left_double_click|reset_viewport_left_double_click.*reset_camera|Command::ResetCamera.*reset_viewport_left_double_click|frame_cad_selection_or_reset_camera.*reset_viewport_left_double_click" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> exit 1, no matches.
- Claim: Task 145 is present and task 146 has not yet been appended.
  Falsifying search:
  `rg -n "^143\.|^144\.|^145\.|^146\." .ai/dispatch.tasks.md` -> observed
  result: task 143 done at line 13788, task 144 done at line 13898, task 145
  open at line 14101, and no `146.` match.
- Claim: The user-provided dispatcher snapshot is the only GitHub queue state
  evidence for this dispatch.
  Falsifying search: not applicable to repository source. Executor must cite
  the snapshot embedded in this ISSUE-399 task context and must not run `gh`,
  browser, network, or GitHub API commands for queue/already-filed evidence.

## Constraints / Non-Goals

- Do not change command routing, command enums, menu definitions, or accelerator
  mapping. This task relies on the existing `Command::ResetCamera` path reaching
  `EditorShell::reset_camera`.
- Do not change camera math, viewport hit testing, face-pick policy, CAD
  projection, selection authority, CommandBus mutation, undo/dirty state, or
  save/load authority.
- Do not change wheel zoom, right-button orbit, middle-button pan, focus-loss
  behavior, cursor-left behavior, or mouse-wheel behavior except by preserving
  their existing tests.
- Do not introduce dependencies, Cargo metadata changes, schemas, workflows,
  ADRs, architecture-lint changes, automation changes, packet-template changes,
  or new source files.
- Do not use live GitHub state. Use the dispatcher-provided snapshot in the
  issue body for queue/already-filed-task evidence.
- Do not append more than one task 146, do not append task 147 during this
  implementation, and do not both append task 146 and a `NEEDS_HUMAN_RECORDED`
  line.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `cargo test -p rge-editor-shell --lib reset_camera`
- `cargo test -p rge-editor-shell --lib viewport_left_double_click`
- `cargo test -p rge-editor-shell --lib viewport_mouse_wheel`
- `cargo test -p rge-editor-shell --lib viewport_focus_loss`
- `cargo test -p rge-editor-shell --lib viewport_cursor_left`
- `rg -n "^143\.|^144\.|^145\.|^146\." .ai/dispatch.tasks.md`
- `git diff --name-only`
- `git diff --check`

## Halt Conditions

The Executor MUST halt and request fresh guidance if any of the following occur:

- The implementation requires edits outside `MAY edit` or new files outside
  `MAY add new files`.
- The fix cannot be made in `crates/editor-shell/src/lifecycle/mod.rs` without
  changing command routing, command enum/menu definitions, shortcut execution,
  remapping/persistence/fatal policy, plugin runtime/discovery/loading, OS
  clipboard behavior, CAD/projection/CommandBus mutation, undo/dirty,
  save/load authority, camera math, viewport hit testing, face-pick policy,
  wheel zoom, right-button orbit math, middle-button pan math, focus-loss
  behavior, cursor-left behavior, or mouse-wheel behavior.
- The required stale-reset tests cannot be written in
  `crates/editor-shell/src/lifecycle/tests.rs` without touching another test
  module, fixture, or crate.
- More than one behavioral follow-up is required to make reset-camera stale
  double-click handling coherent.
- The self-re-arm step cannot append exactly one bounded docs/source-read-only
  audit task 146, and cannot honestly record a single
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line instead.
- Any verification/correction round asks the executor to fix a source, test,
  Cargo, workflow, schema, automation, packet-template, or unrelated artifact
  failure outside this task's declared scope.

## Planner Notes

Task 144's source audit selected this exact follow-up because the existing
wheel, focus-loss, and cursor-left stale double-click resets already converge
on `reset_viewport_left_double_click`, while reset-camera framing currently
does not. The narrow implementation target is therefore the lifecycle reset path
that all View/Home reset-camera callers already share.

Use current local source for implementation and tests. The dispatcher GitHub
state snapshot in the issue body reports no open `ai-dispatch` issue before
ISSUE-399 was created, no open failed autonomous issue, and autonomous issues
already filed through closed ISSUE-398. Treat that snapshot as the only queue
state evidence source.

Self-re-arm final step, required: after the implementation is complete and
verified, follow the Self-re-arm protocol in `.ai/dispatch.tasks.md` by
appending exactly one bounded next-task-source AUDIT task as task 146 named
`Post-reset-camera-double-click-reset Phase 9 next-task source audit`
(docs/source-read-only), or append a single
`NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line if no bounded
in-policy next task exists. Mirror task 144's audit structure and scope
discipline, but task 146's done criteria must require appending exactly one
bounded FEATURE task 147 carrying this copied Self-re-arm requirement, or a
single `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-399
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
