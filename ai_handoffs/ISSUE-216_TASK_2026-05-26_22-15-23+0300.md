# Task Packet

DISPATCH_ID: ISSUE-216
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-26_22-15-23+0300
REVISION: 0
RELATED_FILES:
- GitHub issue #216
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- plans/PLAN.md
- plans/IMPLEMENTATION.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.toml
- Cargo.lock
- kernel/types/
- crates/macros-reflect/
- crates/rge-data/
- crates/rge-scene-loader/
- runtime/runtime-headless/
- golden-projects/simple-scene/
- crates/components-spatial/
- crates/components-render/
- crates/components-visibility/
- ai_handoffs/ISSUE-216_EXEC_*.md
- ai_handoffs/ISSUE-216_EXEC_*.meta.json
STATUS: OPEN

## Goal

Perform a read-only Phase 9 reflection-scale pressure audit for GitHub issue
#216. Re-audit reflection adoption after the recent typed scene-loader work by
inspecting current production and test usage of `kernel/types`,
`rge-macros-reflect`, typed `ComponentValue`, schema/loader bridge references,
and `rge-scene-loader`. Decide whether reflection-scale pressure is still
untriggered, newly triggered, or needs architecture arbitration. The EXEC
report must use current code, docs, Cargo metadata, and dispatch artifacts with
line-cited evidence, then name exactly one smallest safe follow-up or
`NEEDS_HUMAN`. This dispatch must not change tracked source, tests, docs, Cargo
files, workflows, schemas, automation scripts, scheduler configuration, or
`.ai/dispatch.tasks.md`.

## Scope

### MAY edit
- None. This is an audit-only dispatch. Do not modify tracked source, tests,
  documentation, Cargo files, workflows, schemas, automation scripts,
  scheduler configuration, task brief files, existing handoff packets, existing
  logs, fixtures, or prior artifacts.

### MUST NOT edit
- `plans/PLAN.md`
- `plans/IMPLEMENTATION.md`
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.tasks.md`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchLoop.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- This task packet,
  `ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md`, after the
  executor starts.
- Any existing handoff packet or sidecar.
- Any existing `ai_dispatch_logs/**` artifact.
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `tests/**`
- `docs/**`
- `tasks/**`
- `plugins/**`
- `files/**`
- `golden-projects/**`
- `OLD/**`
- `.claude/worktrees/**`
- Any tracked source file, Rust test, documentation file, Cargo metadata file,
  workflow, schema, automation script, scheduler config, task brief, existing
  handoff artifact, existing dispatch log, generated fixture, sandbox
  worktree, or unrelated local scratch file.

### MAY add new files
- `ai_handoffs/ISSUE-216_EXEC_*.md`
- `ai_handoffs/ISSUE-216_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-216/**`

### MUST NOT add new files
- New source, test, documentation, schema, Cargo, workflow, task brief,
  automation, scheduler, fixture, generated asset, ADR, plan, status,
  architecture-lint, benchmark, screenshot, ZIP, copied evidence file, or
  persistent scratch file.
- New handoff packets or sidecars for any dispatch other than `ISSUE-216`.
- `REVIEW`, `CORRECT`, or `CLOSEOUT` packets for this orchestrator dispatch.
- Temporary source-layout inventories, extracted logs, generated reports, or
  measurement outputs inside the repository. If scratch is required, place it
  outside the repo and remove it before writing the EXEC report.

## Deliverables

- One new `ISSUE-216` EXEC report containing the read-only audit.
- A decision block with exactly these fields:
  `REFLECTION_SCALE_VERDICT` and `SMALLEST_SAFE_FOLLOW_UP`.
- `REFLECTION_SCALE_VERDICT` must use exactly one of:
  `still_untriggered`, `newly_triggered`,
  `needs_architecture_arbitration`.
- `SMALLEST_SAFE_FOLLOW_UP` must name exactly one action or be exactly
  `NEEDS_HUMAN`. If the follow-up touches source, tests, scripts, Cargo
  metadata, workflows, schemas, scheduler configuration, or automation
  behavior, it must state `PublishMode branch only unless later authorized by a
  human`.
- A line-cited evidence table covering every required surface:
  `kernel/types`, `crates/macros-reflect`, production uses of
  `rge_kernel_types` / `rge-macros-reflect`, test-only reflection pilots,
  typed `ComponentValue` in `crates/rge-data`, golden simple-scene typed
  payloads, schema/loader bridge references, `crates/rge-scene-loader`, and
  `runtime/runtime-headless` as the current scene-loader consumer.
- A production-vs-test adoption section that separates:
  production imports/usages, Cargo dependency declarations, tests/examples,
  fixtures, comments/docs, and historical dispatch evidence.
- A typed scene-loader impact section explaining whether the recent
  `ComponentValue` and `rge-scene-loader` work actually adopts reflection
  machinery, remains an explicit match-table / typed-RON bridge, or creates an
  unresolved architecture question.
- A trigger-analysis section applying the current Phase 9 reflection-scale
  thresholds and revisit triggers from `plans/PLAN.md`, `plans/BASELINE.md`,
  `kernel/types/BUDGET.md`, and recent history.
- A candidate comparison section explaining why the selected follow-up is
  smaller or more urgent than other inspected reflection-scale candidates, or
  why `NEEDS_HUMAN` is required.
- A final verification section reporting `git diff --check`,
  `git status --short --untracked-files=all`, and static inspection proving no
  tracked production/doc/script/Cargo/workflow/schema/task-brief files changed.

## Acceptance Criteria

- The EXEC report is audit-only and does not modify tracked source, tests,
  docs, Cargo metadata, workflows, schemas, scripts, scheduler configuration,
  fixtures, or `.ai/dispatch.tasks.md`.
- The report classifies reflection-scale pressure using exactly one
  `REFLECTION_SCALE_VERDICT` value from the deliverables section.
- The report names exactly one smallest safe follow-up or exactly
  `NEEDS_HUMAN`; it does not produce a backlog list.
- The audit reads and line-cites `AI_DISPATCH_AUTOMATION.md` and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md` before judging dispatch state, and it
  does not treat missing orchestrator `_REVIEW_` or `_CLOSEOUT_` packets as
  evidence of an unfinished dispatch.
- The audit reads and line-cites `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `kernel/types/BUDGET.md`, `Status.md`, `HANDOFF.md`,
  and recent `change.md` entries to establish Phase 9 context, reflection
  thresholds, and typed scene-loader history.
- The audit inspects and line-cites `kernel/types/Cargo.toml`,
  `kernel/types/src/lib.rs`, `kernel/types/src/reflect.rs`,
  `kernel/types/src/type_id.rs`, `kernel/types/src/serde_bridge.rs`, and
  `kernel/types/tests/reflect_round_trip.rs`.
- The audit inspects and line-cites `crates/macros-reflect/Cargo.toml`,
  `crates/macros-reflect/src/lib.rs`,
  `crates/macros-reflect/src/codegen.rs`,
  `crates/macros-reflect/src/derive.rs`, and the current
  `crates/macros-reflect/tests/**` reflection-pilot surfaces.
- The audit inspects and line-cites `crates/rge-data/src/lib.rs`,
  `crates/rge-data/src/scene.rs`,
  `crates/rge-data/tests/golden_simple_scene_schema.rs`,
  `crates/rge-data/tests/golden_simple_scene_load_tick.rs`, and relevant
  `crates/rge-data/tests/**` or `crates/rge-data/examples/**` uses of
  `ComponentValue`.
- The audit inspects and line-cites
  `golden-projects/simple-scene/scenes/main.rge-scene` for the current typed
  `ComponentValue.type_id` and payload strings.
- The audit inspects and line-cites `crates/rge-scene-loader/Cargo.toml`,
  `crates/rge-scene-loader/src/lib.rs`, and
  `crates/rge-scene-loader/tests/simple_scene.rs` to determine whether the
  loader uses reflection, an explicit match table, typed RON parsing, a global
  registry, or another bridge mechanism.
- The audit inspects and line-cites `runtime/runtime-headless/Cargo.toml`,
  `runtime/runtime-headless/src/main.rs`, and
  `runtime/runtime-headless/tests/loads_golden_simple_scene.rs` to classify the
  current non-test scene-loader consumer.
- The audit inspects and line-cites component crate surfaces relevant to the
  typed bridge, at minimum current `Component` impl and serde payload evidence
  in `crates/components-spatial`, `crates/components-render`, and
  `crates/components-visibility`.
- The audit searches current production and test usage of `rge_kernel_types`,
  `rge_macros_reflect`, `#[derive(Reflect)]`, `ReflectValue`, `TypeId`,
  `ComponentValue`, `load_scene_into_world`, and `rge_scene_loader`. Positive
  findings must be line-cited. If a zero-use claim comes from search output
  rather than a line-bearing file, the report must include the exact read-only
  search command, scope, and observed no-match result.
- The audit inspects relevant existing dispatch artifacts for the typed
  scene-loader chain and Phase 9 pressure audits read-only. It must cite
  current packet/log lines, not stale copied line numbers.
- The report uses `still_untriggered` only if current evidence shows no
  production reflection-scale adoption trigger has fired, even if typed
  `ComponentValue` and scene-loader work exists.
- The report uses `newly_triggered` only if current evidence shows a real
  production reflection adoption trigger, such as production component types
  deriving `Reflect`, production bridge code depending on `kernel/types` /
  `rge-macros-reflect`, or the typed scene-loader path requiring
  reflection-scale measurement now.
- The report uses `needs_architecture_arbitration` only if current evidence
  shows the next step depends on an unresolved architecture decision, such as
  choosing between explicit match tables, reflection-driven lookup, a registry,
  derive-emitted type names, or a new bridge owner.
- Every substantive claim about current code, docs, scripts, Cargo metadata,
  fixtures, tests, or dispatch artifacts is backed by `path:line` evidence.
  If a source-layout or zero-search claim comes from inventory rather than a
  line-bearing file, the report says so explicitly.
- The report does not run new benchmarks, profilers, full repository
  verification, the autonomous queue, or a nested dispatch as part of this
  audit.
- `git diff --check` exits 0.
- `git status --short --untracked-files=all` shows no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new files
  must be limited to this dispatch's own EXEC report, optional matching
  sidecar/log artifacts, and ignored `.ai/dispatch-ISSUE-216` scratch.

## Constraints / Non-Goals

- Do not implement a fix.
- Do not edit source, tests, docs, Cargo files, workflows, schemas, scripts,
  scheduler config, task brief files, fixtures, prior handoff packets, prior
  logs, or existing dispatch artifacts.
- Do not update `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, or any other
  documentation.
- Do not add or run new benchmarks, profilers, generated reports, screenshots,
  ZIPs, fixtures, or persistent scratch files in the repository.
- Do not run publish, push, merge, branch-delete, label-edit, GitHub issue
  mutation, scheduler registration, or scheduler modification commands.
- Do not create a branch, commit, PR, issue comment, label change, or close the
  GitHub issue.
- Do not run the autonomous queue or a nested dispatch as verification.
- Do not run full repository verification manually for this audit. The
  task-specific gates are read-only inspection plus diff/status checks; the
  orchestrator may still run its canonical gate automatically after execution.
- Do not infer reflection-scale pressure from crate names, comments, or Cargo
  dependency declarations alone. Separate declared dependencies from actual
  production use, test-only use, fixtures, and historical intent.
- Do not treat typed `ComponentValue` payloads as reflection adoption unless
  current code actually routes them through `kernel/types`, `Reflect`,
  `ReflectValue`, `TypeId`, `rge-macros-reflect`, or a reflection registry.
- Do not recommend adding new synthetic reflection pilots merely to create a
  scale signal. A follow-up must be justified by current production adoption
  or must return `NEEDS_HUMAN`.
- Treat unrelated dirty worktree state as out of scope. Do not clean, revert,
  or modify changes you did not make; report them if they affect confidence.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` and
  `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` -> expected readable;
  use them to avoid mistaking orchestrator packet shape for dispatch
  incompleteness.
- `Get-Content -Raw plans/PLAN.md`,
  `Get-Content -Raw plans/IMPLEMENTATION.md`,
  `Get-Content -Raw plans/BASELINE.md`, and
  `Get-Content -Raw kernel/types/BUDGET.md` -> expected readable; use them to
  establish reflection-scale thresholds, budget history, and current revisit
  triggers.
- `Get-Content -Raw Status.md` and `Get-Content -Raw HANDOFF.md` -> expected
  readable; use them to establish current state, closed gates, known deferrals,
  and next-work posture.
- Inspect recent `change.md` entries with live line numbers immediately before
  writing the final EXEC report. Do not reuse copied line numbers from prior
  packets or logs.
- Inspect `kernel/types` read-only:
  `kernel/types/Cargo.toml`, `kernel/types/src/lib.rs`,
  `kernel/types/src/field_descriptor.rs`, `kernel/types/src/reflect.rs`,
  `kernel/types/src/schema_version.rs`, `kernel/types/src/serde_bridge.rs`,
  `kernel/types/src/type_id.rs`, `kernel/types/src/ui_hint.rs`, and
  `kernel/types/tests/reflect_round_trip.rs`.
- Inspect `crates/macros-reflect` read-only:
  `crates/macros-reflect/Cargo.toml`,
  `crates/macros-reflect/src/attrs.rs`,
  `crates/macros-reflect/src/codegen.rs`,
  `crates/macros-reflect/src/derive.rs`,
  `crates/macros-reflect/src/lib.rs`, and
  `crates/macros-reflect/tests/**`.
- Inspect typed schema surfaces read-only:
  `crates/rge-data/src/lib.rs`, `crates/rge-data/src/scene.rs`,
  `crates/rge-data/src/prefab.rs`, `crates/rge-data/tests/**`,
  `crates/rge-data/examples/**`, `golden-projects/simple-scene/.rge-project`,
  and `golden-projects/simple-scene/scenes/main.rge-scene`.
- Inspect bridge and consumer surfaces read-only:
  `crates/rge-scene-loader/Cargo.toml`,
  `crates/rge-scene-loader/src/lib.rs`,
  `crates/rge-scene-loader/tests/simple_scene.rs`,
  `runtime/runtime-headless/Cargo.toml`,
  `runtime/runtime-headless/src/main.rs`, and
  `runtime/runtime-headless/tests/loads_golden_simple_scene.rs`.
- Inspect component surfaces read-only:
  `crates/components-spatial`, `crates/components-render`, and
  `crates/components-visibility` for `Component` impls, serde derives,
  payload-compatible field shapes, and any reflection adoption.
- Inspect workspace Cargo metadata read-only with `git grep`, `Select-String`,
  `git ls-files`, or equivalent commands for dependency declarations involving
  `rge-kernel-types`, `rge-macros-reflect`, `rge-data`,
  `rge-scene-loader`, and `runtime-headless`.
- Inspect production and test usage read-only with `git grep`, `Select-String`,
  `git ls-files`, or equivalent commands for `rge_kernel_types`,
  `rge_macros_reflect`, `#[derive(Reflect)]`, `ReflectValue`, `TypeId`,
  `ComponentValue`, `load_scene_into_world`, and `rge_scene_loader`. Report
  positive matches with line citations and zero-use claims with the exact
  command and scope.
- Inspect relevant existing dispatch artifacts read-only, especially the
  typed `ComponentValue`, typed bridge, `rge-scene-loader`, and
  `runtime-headless` consumer chain. Ignore `OLD/**`, `.ai/dispatch-*/**`,
  `.claude/worktrees/**`, `.git/**`, and build output when assessing
  repository state unless a specific artifact is cited as provenance.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new files
  must be limited to this dispatch's own handoff/log artifacts and optional
  ignored `.ai/dispatch-ISSUE-216` scratch.
- Static no-source-change inspection: run `git diff --name-only` and a
  targeted read-only inspection of protected tracked surfaces such as
  `crates`, `kernel`, `editor`, `runtime`, `tools`, `docs`, `plans`,
  `Status.md`, `HANDOFF.md`, `change.md`, `Cargo.toml`, `Cargo.lock`,
  `.github/workflows`, `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`,
  `.ai/*.schema.json`, and root `*.ps1` automation scripts. Expected result:
  no tracked production/doc/script/Cargo/workflow/schema/task-brief diff.

## Halt Conditions

The Executor MUST halt and return `NEEDS_HUMAN` without editing any source,
docs, tests, scripts, schemas, workflows, Cargo files, scheduler config, task
briefs, fixtures, prior packets, or prior logs if any of the following occur:

- Required evidence files are missing or unreadable.
- The required production/test usage search cannot be completed read-only.
- The evidence cannot support exactly one `REFLECTION_SCALE_VERDICT` value from
  the allowed set.
- The smallest safe follow-up cannot be narrowed to exactly one action.
- The smallest safe follow-up would require code/script/Cargo/workflow/schema
  changes but cannot be constrained to branch-mode pending later human
  authorization.
- The audit needs an implementation, documentation edit, Cargo edit, workflow
  edit, schema edit, script edit, scheduler change, task-brief edit, prior
  artifact edit, benchmark run, profiler run, or GitHub mutation to be
  truthful.
- Required line citations cannot be produced for document-, code-, Cargo-,
  fixture-, script-, or artifact-based claims.
- Current code shows an unresolved architecture decision about explicit match
  tables versus reflection-driven loading, registry/inventory/linkme,
  derive-emitted type-name lookup, bridge ownership, or dependency direction
  that cannot be reduced to one safe follow-up.
- Any GitHub mutation, push, merge, branch deletion, label change, issue
  comment, scheduler command, benchmark run, profiler run, full verification
  run, or nested dispatch seems necessary.
- `git diff --check` fails for a reason caused by this dispatch.
- `git status --short --untracked-files=all` shows new or modified files
  outside this dispatch's own handoff/log artifacts and optional ignored
  `.ai/dispatch-ISSUE-216` scratch, unless they are clearly pre-existing
  unrelated user changes.

If a halt condition fires, the Executor must still write the `EXECUTION_REPORT`
if possible, set header `STATUS: NEEDS_HUMAN`, include the exact halt reason
and current status output, and route the footer to `NEXT_ROLE: HUMAN_ARBITER`.

## Planner Notes

This is delegated-main smoke batch task 4 of 10 from `.ai/dispatch.tasks.md`.
It follows the freeze-validity, abstraction-pain, and invalidation-economics
audits. The desired result is not to force a reflection task; it is to decide
whether recent typed scene-loader work changed the reflection adoption
baseline enough to trip Phase 9 reflection-scale pressure.

The key distinction for this audit is typed payload adoption versus reflection
machinery adoption. `ComponentValue` payloads and `rge-scene-loader` may be
real production progress without necessarily using `kernel/types`,
`rge-macros-reflect`, `ReflectValue`, `TypeId`, or a registry. The EXEC report
must prove which is true from current lines.

Under the orchestrator protocol, no `REVIEW` or `CLOSEOUT` packet is expected
from this TASK. The queue's Codex control review is stored in the run
directory, not as a handoff packet.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-216
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
