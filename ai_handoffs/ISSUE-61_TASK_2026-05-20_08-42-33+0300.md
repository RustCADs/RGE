# Task Packet

DISPATCH_ID: ISSUE-61
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_08-42-33+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_anim_graph_smoke.rs
- crates/editor-ui/Cargo.toml
- Cargo.lock
- crates/editor-ui/src/widgets/node_graph.rs
- crates/anim-graph/src/lib.rs
STATUS: OPEN

## Goal

Add the focused editor-ui integration smoke for GitHub issue #61 proving that `NodeGraphWidget` can consume a real `rge_anim_graph::AnimGraph` through the existing `rge_kernel_graph_foundation::VizAdapter` bridge. This is a test-only cross-crate smoke for the generic node-graph model added in issue #58 and already exercised for material graphs in issue #60; production `editor-ui` must remain domain-agnostic and should not gain animation-domain coupling.

## Scope

### MAY edit
- `crates/editor-ui/Cargo.toml`, only inside `[dev-dependencies]`, and only to add the test-only dependency required for the new integration test to name `rge-anim-graph`.
- `Cargo.lock`, only for the minimal dependency-list metadata Cargo writes as a consequence of the approved `rge-editor-ui` dev-dependency on the existing workspace `rge-anim-graph` package. No external package changes are allowed.

### MUST NOT edit
- Any production source file, including `crates/editor-ui/src/widgets/node_graph.rs`, unless the test cannot compile through the existing public `NodeGraphWidget::model_from` and `VizAdapter` APIs. If such a production edit appears necessary, halt first rather than making it.
- `crates/anim-graph/**`, `crates/material-graph/**`, `crates/cad-core/**`, `crates/script-graph/**`, `kernel/graph-foundation/**`, renderer/gfx crates, runtime crates, editor-shell crates, or any graph-domain crate.
- Workspace-root `Cargo.toml`, any crate manifest other than `crates/editor-ui/Cargo.toml`, architecture-lint files, schemas, scripts, CI files, documentation, `.gitignore`, generated artifacts other than the narrow `Cargo.lock` update allowed above, or any existing handoff packet.

### MAY add new files
- `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs`

### MUST NOT add new files
- Any file other than `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs`.
- New crates, fixtures, snapshots, assets, docs, schemas, scripts, architecture-lint rules, CI files, or generated files.

## Deliverables

- Add `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs` as an integration test.
- Add exactly one editor-ui dev-dependency if needed for the test to compile: `rge-anim-graph = { path = "../anim-graph" }`. Do not add a production dependency and do not modify workspace dependencies.
- Let Cargo update `Cargo.lock` only as needed to reflect that editor-ui dev-dependency. The expected lockfile shape is a dependency-list update involving `rge-editor-ui` and `rge-anim-graph`, with no external crate version changes.
- In the smoke test, build a small real `AnimGraph` using public anim-graph APIs such as `AnimGraph::new`, `add_state`, `add_transition`, and `AnimTransition::new`.
- Build a node-graph model through the existing editor-ui API, for example `NodeGraphWidget::new().model_from(&graph)` if `model_from` remains an instance method.
- Assert the model's node count and edge count match the animation graph.
- Assert animation node records expose the expected `display_name` values from the animation state keys and the expected node `kind` value, `AnimState`.
- Assert animation edge records expose the expected endpoints and exact edge labels derived by the anim-graph adapter, such as the transition trigger string `start_run`.
- Make assertions by stable `NodeId` and `EdgeId` values returned from graph construction, or otherwise avoid depending on insertion order because the underlying adapter order is deterministic but sorted by ids.

## Acceptance Criteria

- `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs` exists and is the only new file.
- The new test uses a real `rge_anim_graph::AnimGraph`; it does not use a fake adapter or standalone graph-foundation test double.
- The test proves `NodeGraphWidget` consumes `AnimGraph` through `VizAdapter` by compiling and calling the existing `model_from` bridge without changing production APIs.
- The test builds at least two animation states and at least one transition between them.
- The test asserts exact node count, edge count, at least two animation state display names, the animation node kind string `AnimState`, at least one transition label, and the corresponding transition endpoints.
- `crates/editor-ui/Cargo.toml` is changed only inside `[dev-dependencies]`, and only by adding the editor-ui dev-dependency on `rge-anim-graph`.
- `Cargo.lock` is changed only if Cargo requires it for that dev-dependency, with no registry dependency upgrades, no new external packages, and no unrelated lockfile churn.
- No production `editor-ui` code mentions `rge-anim-graph`, `rge-material-graph`, `rge-cad-core`, renderer crates, runtime crates, or other graph-domain crates as part of this dispatch.
- No source changes are made to animation graph, material graph, CAD, script, graph-foundation, renderer, runtime, architecture-lint, schema, script, documentation, or CI files.
- `cargo test -p rge-editor-ui --test node_graph_anim_graph_smoke` exits 0.

## Constraints / Non-Goals

- This dispatch is test-only except for the editor-ui `[dev-dependencies]` entry and its narrow lockfile consequence.
- Do not change `NodeGraphWidget`, `NodeGraphModel`, `VizAdapter`, `AnimGraph`, animation graph labels, animation graph ordering, graph-foundation ids, or any production API to make the test easier.
- Do not add rendering, egui UI, layout, pan/zoom, selection, ports UI, persistence, animation editor behavior, graph traversal, evaluation, validation, runtime state-machine behavior, blend trees, transition conditions, clip sampling, or mutation behavior.
- Do not promote `rge-anim-graph` to a production dependency of editor-ui.
- Do not add workspace dependency entries, feature flags, dependency version bumps, or architecture-lint exemptions.
- Do not fix unrelated warnings, formatting churn, failing tests, or dependency metadata outside the listed scope.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-ui --test node_graph_anim_graph_smoke` -> expected exit 0.

If time permits, the Executor MAY also run and document:

- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The smoke cannot be implemented entirely within `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs`, the `crates/editor-ui/Cargo.toml` `[dev-dependencies]` entry, and the narrow `Cargo.lock` metadata update allowed in this packet.
- The test appears to require any production source edit, production dependency, workspace manifest edit, feature flag, architecture-lint exemption, or change to `NodeGraphWidget`, `VizAdapter`, `AnimGraph`, graph-foundation, animation graph, material graph, CAD, script, renderer, runtime, schema, script, documentation, or CI files.
- Cargo wants to change `Cargo.lock` beyond the minimal editor-ui-to-anim-graph dev-dependency metadata described above, including external package additions, removals, or version changes.
- The architecture lint gate, if run, rejects the editor-ui dev-dependency on `rge-anim-graph` and resolving that rejection would require any change outside this packet's MAY-edit scope.
- The existing public `NodeGraphWidget` model does not expose node display names, node kinds, edge labels, edge endpoints, or counts strongly enough for the smoke to assert them without changing production APIs.
- The required verification gate fails for reasons unrelated to this task and the fix would require edits outside the MAY-edit or MAY-add scope.
- Existing uncommitted changes in any in-scope file make it unclear how to proceed without overwriting someone else's work.

## Planner Notes

Use `crates/editor-ui/tests/node_graph_material_smoke.rs` as the closest local pattern, but do not copy material-domain assertions verbatim. The intended animation smoke is smaller: create states such as `idle` and `run`, add a transition such as `start_run`, pass `&graph` to `NodeGraphWidget::model_from`, and assert the adapter-provided counts, state labels, static kind string `AnimState`, transition label, and endpoints survive in the editor-ui model.

The production dependency direction remains clean because only the integration test target names `rge-anim-graph`. The existing `crates/editor-ui/src/widgets/node_graph.rs` design is already generic over `&dyn VizAdapter`; this task should validate that bridge, not specialize it.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-61
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
