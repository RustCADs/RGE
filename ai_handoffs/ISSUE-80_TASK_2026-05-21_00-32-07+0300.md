# Task Packet

DISPATCH_ID: ISSUE-80
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_00-32-07+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: OPEN

## Goal

Implement GitHub issue #80 by adding one focused Phase 8 graph-foundation
regression test for removed-edge diff payloads. The test must prove that
`GraphDiff::between` records a removed edge as the full old `EdgeRecord`,
including `src`, `dst`, and `data`, without added-edge, changed-edge, or node
change noise.

## Scope

### MAY edit
- `kernel/graph-foundation/tests/diff_correctness.rs`

### MUST NOT edit
- Any file other than `kernel/graph-foundation/tests/diff_correctness.rs`,
  except for the protocol-required execution report packet described below.
- Any production/runtime code in `kernel/graph-foundation/src/**`, including
  `Graph`, `GraphDiff`, `GraphSnapshot`, `NodeId`, `EdgeId`, or `EdgeRecord`.
- Any domain graph crate or graph consumer outside `kernel/graph-foundation`.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`,
  generated files, or any existing handoff packet.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-80_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by
  `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, new integration test files, fixtures, snapshots,
  docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-80_EXEC` report
  and its finalize-generated sidecar.

## Deliverables

- Add exactly one focused integration test in
  `kernel/graph-foundation/tests/diff_correctness.rs` covering removed-edge
  `EdgeRecord` payload preservation.
- The test must snapshot a graph containing an edge, remove only that edge,
  snapshot again, and compute `GraphDiff::between(&snap1, &snap2)`.
- The test must assert the removed edge entry preserves the old `EdgeRecord`
  `src`, `dst`, and `data` values.
- The test must assert no added-edge, changed-edge, added-node, removed-node,
  or changed-node noise is reported.
- The test must assert precise `node_change_count()` and `edge_change_count()`
  results.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is
  `kernel/graph-foundation/tests/diff_correctness.rs`.
- The new regression is one new `#[test]` function; it may reuse the existing
  local `n(...)` and `e(...)` helpers.
- The test removes an edge without removing any node and without mutating edge
  endpoints or edge payloads after the old snapshot is captured.
- The test asserts:
  - `diff.removed_edges.len() == 1`
  - `diff.removed_edges` contains the removed `EdgeId`
  - the removed `EdgeRecord` has the expected old `src`
  - the removed `EdgeRecord` has the expected old `dst`
  - the removed `EdgeRecord` has the expected old `data`
  - `diff.added_edges.len() == 0`
  - `diff.changed_edges.len() == 0`
  - `diff.added_nodes.len() == 0`
  - `diff.removed_nodes.len() == 0`
  - `diff.changed_nodes.len() == 0`
  - `diff.node_change_count() == 0`
  - `diff.edge_change_count() == 1`
- No graph-foundation API, production diff implementation, domain graph crate,
  dependency, Cargo configuration, documentation, or architecture policy is
  changed.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness -- --nocapture`
  exits 0.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify `Graph`, `GraphDiff`,
  `GraphSnapshot`, graph storage, edge removal, snapshotting, diffing, or any
  other runtime behavior to satisfy the new test.
- Do not add or change public APIs.
- Do not add coverage for added edges, changed edges, node payload changes,
  node removals, serialization, traversal, stable hashing, visualization, or
  domain graph behavior.
- Do not add dependencies, fixtures, helper modules, new test files, Cargo
  manifest entries, docs, scripts, or snapshots.
- Do not rewrite unrelated tests in `diff_correctness.rs` except for minimal
  local cleanup needed to place the requested regression cleanly.
- Do not commit, stage, push, or create any packet other than the
  protocol-required execution report and optional EXEC sidecar.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `cargo test -p rge-kernel-graph-foundation --test diff_correctness -- --nocapture` -> expected exit 0.
- `git diff -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected diff is limited to the requested removed-edge payload preservation regression test and any minimal local cleanup needed for that test.
- `git diff --check -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST
document the exact command, the failure reason, and any partial verification
that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh
guidance if any of the following occur:

- The requested removed-edge payload regression fails against the existing
  production implementation and passing it would require changing `Graph`,
  `GraphDiff`, `GraphSnapshot`, `NodeId`, `EdgeId`, `EdgeRecord`, or any other
  runtime source.
- Any required edit outside `kernel/graph-foundation/tests/diff_correctness.rs`
  appears necessary.
- Any new file other than the protocol-required execution report and its
  finalize-generated sidecar appears necessary.
- The current public integration-test API cannot express the removed-edge
  payload assertion without adding or changing a public API.
- Implementing the requested coverage appears to require changing added-edge,
  changed-edge, node-removal, domain graph, or architecture behavior.
- Formatting or tests fail for reasons unrelated to the requested regression
  and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from
`.ai/dispatch.tasks.md` for GitHub issue #80. Revision number: 0. There is no
prior Claude gate result. Keep the implementation narrow: add only the removed
edge `EdgeRecord` payload preservation regression in the existing
`diff_correctness` integration test file.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-80
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
