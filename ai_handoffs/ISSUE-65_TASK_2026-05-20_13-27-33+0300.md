# Task Packet

DISPATCH_ID: ISSUE-65
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_13-27-33+0300
RELATED_FILES:
- crates/editor-ui/tests/phase8_graph_diff_smoke.rs
- crates/editor-ui/Cargo.toml
- crates/material-graph/src/lib.rs
- crates/anim-graph/src/lib.rs
- crates/cad-core/src/graph/operator_graph.rs
- kernel/graph-foundation/src/graph.rs
- kernel/graph-foundation/src/snapshot.rs
- kernel/graph-foundation/src/diff.rs
- kernel/graph-foundation/src/viz_adapter.rs
- plans/PLAN.md
STATUS: OPEN

## Goal

Add the GitHub issue #65 Phase 8 integration-test slice for PLAN 13.14's
combined material + CAD + animation graph-foundation checkpoint gate. The test
must build `MaterialGraph`, `AnimGraph`, and `OperatorGraph` together, capture
one old combined checkpoint through the existing `VizAdapter` projections,
mutate each domain by exactly one node and one edge, capture one new combined
checkpoint, and prove one shared `GraphDiff::between` reports exactly three
added nodes and three added edges with no removals or changed records.

## Scope

### MAY edit
- `crates/editor-ui/tests/phase8_graph_diff_smoke.rs`

### MUST NOT edit
- Any production source file, including `crates/editor-ui/src/**`,
  `crates/material-graph/**`, `crates/anim-graph/**`, `crates/cad-core/**`,
  `kernel/graph-foundation/**`, renderer/gfx crates, runtime crates,
  editor-shell crates, or any other graph-domain crate.
- `crates/editor-ui/Cargo.toml`, workspace-root `Cargo.toml`, `Cargo.lock`, any
  crate manifest, architecture-lint files, schemas, scripts, CI files,
  generated artifacts, documentation, `.gitignore`, or any existing handoff
  packet.
- Any existing integration test file other than
  `crates/editor-ui/tests/phase8_graph_diff_smoke.rs`.

### MAY add new files
- None.

### MUST NOT add new files
- Any Rust source/test file, crate, fixture, snapshot, asset, documentation
  file, schema, script, CI file, generated file, Cargo manifest, lockfile,
  architecture-lint rule, feature flag, workspace dependency entry, renderer
  dependency, runtime dependency, or game-domain dependency.

## Deliverables

- Add one focused integration test to
  `crates/editor-ui/tests/phase8_graph_diff_smoke.rs`; keep the existing issue
  #64 per-domain diff smoke intact.
- The new test must build one live `MaterialGraph`, one live `AnimGraph`, and
  one live `OperatorGraph` using their existing public APIs.
- The old combined checkpoint must contain all three old domain projections in
  one `GraphSnapshot<DiffNode, String>` built through the public
  `rge_kernel_graph_foundation::VizAdapter` surface.
- After the old combined checkpoint is captured, mutate each live domain graph
  by exactly one node and one edge:
  - material: add one material node and one material edge;
  - animation: add one animation state and one transition;
  - CAD operator: add one operator node and one operator edge.
- The new combined checkpoint must contain all three mutated domain projections
  in one `GraphSnapshot<DiffNode, String>` built through the same combined
  helper path as the old checkpoint.
- Compute exactly one combined diff with
  `GraphDiff::between(&old_combined_checkpoint, &new_combined_checkpoint)`.
- Assert the combined diff has exactly three `added_nodes` and exactly three
  `added_edges`.
- Assert `removed_nodes`, `removed_edges`, `changed_nodes`, and `changed_edges`
  are all empty on the combined diff.

## Acceptance Criteria

- `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` is the only file changed.
- No new files are added.
- The new smoke is a combined-checkpoint test, not three independent
  per-domain diffs. There must be one old combined snapshot, one new combined
  snapshot, and one `GraphDiff::between` call for the new issue #65 assertion.
- The combined checkpoint is built only from existing public `VizAdapter`
  projections and graph-foundation primitives such as `Graph`,
  `GraphSnapshot`, `GraphDiff`, `EdgeRecord`, `NodeId`, `EdgeId`, and
  `VizAdapter`.
- The test does not access domain-private fields and does not add or require
  any new production accessor such as `inner()` on `MaterialGraph` or
  `AnimGraph`.
- The old combined checkpoint includes one node+edge baseline in each domain.
  The new combined checkpoint adds exactly one node and one edge in each domain
  without removing or mutating any pre-existing projected node or edge record.
- The combined diff asserts:
  - `added_nodes.len() == 3`;
  - `added_edges.len() == 3`;
  - `removed_nodes.is_empty()`;
  - `removed_edges.is_empty()`;
  - `changed_nodes.is_empty()`;
  - `changed_edges.is_empty()`;
  - `node_change_count() == 3`;
  - `edge_change_count() == 3`;
  - `!diff.is_empty()`.
- The CAD fixture uses distinct operator payloads where needed so
  content-derived `NodeId`s do not collide.
- If the combined helper inserts multiple domain projections into one
  graph-foundation `Graph<DiffNode, String>`, it must preserve stable structure
  from the adapter views: node identity, edge identity, edge source, edge
  destination, display name, kind, and edge label. If test-local namespacing is
  needed to avoid cross-domain id collisions, it must be deterministic, local to
  this integration test, and derived only from the adapter ids plus an explicit
  domain label.
- No production code, public API, graph-domain implementation,
  graph-foundation implementation, renderer/runtime/game-domain code,
  architecture lint, schema, script, CI file, documentation, manifest, or
  lockfile is changed.
- `cargo test -p rge-editor-ui --test phase8_graph_diff_smoke` exits 0.

## Constraints / Non-Goals

- This dispatch is test-only and must be implemented entirely in
  `crates/editor-ui/tests/phase8_graph_diff_smoke.rs`.
- Use only dependencies already available to `rge-editor-ui`; do not edit
  `Cargo.toml` or `Cargo.lock`.
- Do not change `GraphDiff`, `GraphSnapshot`, `Graph`, `VizAdapter`,
  `MaterialGraph`, `AnimGraph`, `OperatorGraph`, node id derivation, edge id
  derivation, labels, ordering, or any production API to make the test easier.
- Do not add graph-foundation API, domain-internal exposure, new adapters, new
  domain wrappers, new checkpoint types, or new serialization behavior.
- Do not add rendering, egui UI, `NodeGraphWidget` coverage, layout, pan/zoom,
  selection, ports UI, persistence, material editor behavior, animation editor
  behavior, CAD editor behavior, graph traversal, evaluation, validation,
  runtime behavior, or renderer-tier behavior.
- Do not add renderer-tier dependencies, game-domain dependencies, runtime
  dependencies, workspace dependency entries, feature flags, dependency version
  bumps, lockfile updates, architecture-lint exemptions, scripts, schemas, or
  documentation.
- Do not delete, rename, rewrite, or weaken the existing issue #64 per-domain
  assertions in `phase8_graph_diff_smoke.rs`.
- Do not fix unrelated warnings, formatting churn, failing tests, dependency
  metadata, docs, scripts, or other handoff packets.

## Verification Gates

The Executor MUST run and document the result of this command in their
`EXECUTION_REPORT`:

- `cargo test -p rge-editor-ui --test phase8_graph_diff_smoke` -> expected exit 0.

## Halt Conditions

The Executor MUST halt without committing and request fresh Planner guidance if
any of the following occur:

- The combined checkpoint smoke cannot be implemented entirely within
  `crates/editor-ui/tests/phase8_graph_diff_smoke.rs`.
- The test appears to require any production source edit, public API edit,
  private-field access, new domain accessor, manifest edit, lockfile edit,
  workspace dependency edit, feature flag, renderer-tier dependency,
  game-domain dependency, runtime dependency, architecture-lint exemption,
  schema edit, script edit, documentation edit, generated artifact, or CI edit.
- `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` is missing or does not
  contain the issue #64 per-domain graph-diff smoke this task is intended to
  extend.
- A required dependency is not already available to `rge-editor-ui` and making
  it available would require editing any manifest or lockfile.
- The public `VizAdapter` projection does not expose enough stable structure to
  build one old combined checkpoint and one new combined checkpoint without
  changing production code.
- Cross-domain `NodeId` or `EdgeId` collisions prevent insertion into the
  combined test graph and cannot be resolved by a deterministic test-local
  namespacing helper derived from adapter ids plus explicit domain labels.
- A domain graph construction API refuses the tiny old/new shape and resolving
  it would require changing production behavior or expanding scope.
- The required verification gate fails for reasons unrelated to this task and
  the fix would require edits outside the MAY-edit scope.
- Existing uncommitted changes in
  `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` make it unclear how to
  proceed without overwriting someone else's work.

## Planner Notes

This is the next slice after issue #64. Issue #64 proved the shared
graph-foundation diff path independently for each Phase 8 graph domain. Issue
#65 should now exercise PLAN 13.14's cross-graph checkpoint wording by combining
the material, animation, and CAD operator projections into one checkpoint pair
and diffing that pair once.

Keep the implementation small and local. Reuse or extend the existing
test-local `DiffNode` and adapter snapshot helper if that is the cleanest path,
but the important invariant is that the new smoke proves a single combined
checkpoint diff reports exactly three node additions and three edge additions
across the material + CAD + animation set.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-65
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
