# Task Packet

DISPATCH_ID: ISSUE-76
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_21-26-58+0300
RELATED_FILES:
- kernel/graph-foundation/tests/round_trip_ron.rs
STATUS: OPEN

## Goal

Implement GitHub issue #76 by adding one narrow integration test in `kernel/graph-foundation/tests/round_trip_ron.rs` proving that `GraphSnapshot::to_graph()` rebuilds Tier-B fanout metrics from restored edge records after a RON round trip. The test should exercise nontrivial incoming and outgoing fanout so restored `Graph` metrics cannot pass merely from node and edge counts.

## Scope

### MAY edit
- `kernel/graph-foundation/tests/round_trip_ron.rs`

### MUST NOT edit
- Any file other than `kernel/graph-foundation/tests/round_trip_ron.rs`.
- Any production/runtime code in `kernel/graph-foundation/src/**`, including `Graph`, `GraphSnapshot`, `EdgeRecord`, `NodeId`, or `EdgeId`.
- Any domain graph crates or graph consumers outside `kernel/graph-foundation`.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`, or existing handoff packets.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-76_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, new integration test files, fixtures, docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-76_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add exactly one focused test in `kernel/graph-foundation/tests/round_trip_ron.rs` for fanout metrics after `GraphSnapshot::to_graph()`.
- Build a graph with six nodes and five directed edges before snapshotting so restored metrics include:
  - at least one node with out-degree `3`
  - at least one node with in-degree `3`
  - at least one node with both in-degree and out-degree `1`
  - at least one isolated node with in-degree and out-degree `0`
- Round-trip the graph through `GraphSnapshot::from_graph`, `to_ron`, `GraphSnapshot::from_ron`, and `to_graph`.
- Assert restored graph counts and Tier-B fanout metrics on the graph returned by `to_graph()`.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `kernel/graph-foundation/tests/round_trip_ron.rs`.
- The new test is test-only and does not require changing the existing `GraphSnapshot::to_graph()` implementation.
- The new test uses a nontrivial directed shape equivalent to:
  - nodes `1..=6`
  - edges `1 -> 2`, `1 -> 3`, `1 -> 4`, `5 -> 2`, and `3 -> 2`
- After RON restore and `to_graph()`, the test asserts:
  - `node_count() == 6`
  - `edge_count() == 5`
  - `node_out_degree(NodeId::from_raw(1)) == 3`
  - `node_in_degree(NodeId::from_raw(2)) == 3`
  - `node_in_degree(NodeId::from_raw(3)) == 1`
  - `node_out_degree(NodeId::from_raw(3)) == 1`
  - `node_in_degree(NodeId::from_raw(6)) == 0`
  - `node_out_degree(NodeId::from_raw(6)) == 0`
  - `max_out_fanout() == 3`
  - `max_in_fanout() == 3`
  - `average_fanout()` equals `5.0 / 6.0` within an appropriate floating-point tolerance such as `f64::EPSILON`
- The test may add small local helper code inside `round_trip_ron.rs` only if it keeps the file readable and remains scoped to this test.
- No runtime behavior, public API, dependency, Cargo configuration, documentation, domain graph crate, or source file changes are introduced.
- `cargo test -p rge-kernel-graph-foundation --test round_trip_ron` passes.
- `cargo test -p rge-kernel-graph-foundation` passes.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify `Graph`, `GraphSnapshot`, graph storage, adjacency caches, Tier-B metric computation, serialization, or deserialization runtime behavior.
- Do not broaden the task into diffing, mutation semantics, traversal, domain graph crates, graph consumers, architecture policy, documentation, or CI changes.
- Do not add dependencies, fixtures, helper modules, new test files, Cargo manifest entries, or snapshots.
- Do not rewrite existing tests in `round_trip_ron.rs` except for minimal local cleanup needed to add the requested regression cleanly.
- Do not commit, stage, push, or create any packet other than the protocol-required execution report and optional EXEC sidecar.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-kernel-graph-foundation --test round_trip_ron` -> expected exit 0.
- `cargo test -p rge-kernel-graph-foundation` -> expected exit 0.
- `git diff -- kernel/graph-foundation/tests/round_trip_ron.rs` -> expected diff is limited to one narrow fanout-restore test and any minimal local helper needed for that test.
- `git diff --check -- kernel/graph-foundation/tests/round_trip_ron.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested fanout-restore test fails against the existing production implementation and passing it would require changing `Graph`, `GraphSnapshot`, `EdgeRecord`, adjacency cache maintenance, Tier-B metric code, or any other runtime source.
- Any required edit outside `kernel/graph-foundation/tests/round_trip_ron.rs` appears necessary.
- Any new file other than the protocol-required execution report and its finalize-generated sidecar appears necessary.
- The RON round-trip APIs differ from the expected `GraphSnapshot::from_graph`, `to_ron`, `GraphSnapshot::from_ron`, and `to_graph` flow and the requested coverage cannot be expressed with a test-only edit.
- The fanout metric APIs differ from the expected `node_in_degree`, `node_out_degree`, `max_in_fanout`, `max_out_fanout`, and `average_fanout` methods.
- Formatting or tests fail for reasons unrelated to the requested regression test and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #76. Revision number: 0. There is no prior Claude gate result. Keep the implementation narrow: the issue is specifically about proving `GraphSnapshot::to_graph()` rebuilds Tier-B fanout metrics from restored edges after RON deserialization, not changing the graph implementation.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-76
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
