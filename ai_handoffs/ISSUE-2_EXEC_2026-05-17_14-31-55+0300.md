# Execution Report

DISPATCH_ID: ISSUE-2
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-17_14-31-55+0300
RELATED_FILES:
- crates/script-bench/BASELINE.md — added one dated additive section recording the 2026-05-17 metrics-enabled one-hour memory soak
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-2_TASK_2026-05-17_13-21-45+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch. (The TASK Planner Notes
confirm: "No prior Claude gate exists for this revision.")

## What I Changed

### Source

- None. This is a run-and-document task; no source file was edited.

### Tests

- None added or modified. The existing ignored test
  `crates/script-bench/src/script_host.rs::script_host::tests::phase_3_memory_soak_one_hour`
  was run as-is via the exact formal soak target; no test code changed.

### Docs

- `crates/script-bench/BASELINE.md`: added one new dated section,
  "Formal 1-hour memory soak — RUN 2026-05-17 (process-memory metrics
  enabled)", recording this run's exact stdout evidence, a per-metric table,
  the honest Windows platform mapping (working set / commit charge), and a
  scope-limitation note. The historical 2026-05-12 soak section is preserved
  byte-for-byte; the change is purely additive (73 insertions, 0 deletions).

## Per-File Summary

`crates/script-bench/BASELINE.md` — a new `## Formal 1-hour memory soak — RUN
2026-05-17 (process-memory metrics enabled)` section was inserted immediately
before the existing `## Formal W04 raw WasmtimeCranelift gates — RUN 2026-05-12`
section, keeping the two memory-soak sections adjacent. It records the exact
`phase3_memory_soak:` and `phase3_memory_soak_memory:` stdout lines verbatim,
tabulates each metric, states that all byte counts are recorded as printed
(none normalized / rounded / re-derived), and documents that on the Windows
recorder host resident memory is the process working set
(`GetProcessMemoryInfo` → `WorkingSetSize`) and virtual memory is commit charge
(`PagefileUsage`), so `vss_delta_bytes` is a commit-charge delta. No other file
was touched.

## Verification Results

Toolchain on the recorder host: `cargo 1.92.0 (344c4567c 2025-10-21)` /
`rustc 1.92.0 (ded5c06cf 2025-12-08)`, `wasmtime 44.0.1`, Windows 11 / x86_64.
(Earlier BASELINE.md rows were recorded under cargo 1.94.1; the formal soak
target has no pinned-version requirement, and the actual version used is
recorded honestly in the new BASELINE.md section.)

### Gate: `git status --short --branch` (pre-execution)

```
## ai-dispatch/ISSUE-2
?? ai_handoffs/ISSUE-2_TASK_2026-05-17_13-21-45+0300.md
?? ai_handoffs/ISSUE-2_TASK_2026-05-17_13-21-45+0300.meta.json
```

### Gate: `git diff -- crates/script-bench/src/script_host.rs crates/script-bench/METHODOLOGY.md crates/script-bench/Cargo.toml Cargo.lock`

- Before execution: no diff.
- After execution: no diff. (Confirmed — these MUST-NOT-edit files are untouched.)

### Gate: formal one-hour ignored soak

Command run exactly as specified by the Acceptance Criteria:

```
cargo test -p rge-script-bench --release --lib script_host::tests::phase_3_memory_soak_one_hour -- --ignored --nocapture
```

- Pre-build (`... --no-run`) → exit 0, release profile compiled in 2m 32s.
- Soak run started 2026-05-17 13:30:05 +03:00, ended 2026-05-17 14:30:06 +03:00.
- Recorder wall-clock: 3600.46 s (`WALL_CLOCK_SECONDS=3600.46`).
- Process exit code: **0** (`EXITCODE=0`).
- Test harness summary:
  `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 28 filtered out; finished in 3600.00s`
- The ignored one-hour test reported `... ok`.

Captured stdout evidence (exact lines, verbatim from the `--nocapture` run):

```
phase3_memory_soak: entities=1000 cycles=3736934 elapsed_s=3600.00 restored_components=3736934000 expected_restored_components=3736934000 final_counter_sum=1000499500
phase3_memory_soak_memory: samples=3736936 start_rss_bytes=8859648 end_rss_bytes=10346496 peak_rss_bytes=10510336 start_vss_bytes=1327104 end_vss_bytes=2695168 vss_delta_bytes=1368064
```

Acceptance-criteria checks against the captured output:

- `elapsed_s` = 3600.00 ≥ 3600 → PASS.
- `cycles` = 3736934 > 0 → PASS.
- `restored_components` (3736934000) == `expected_restored_components` (3736934000) → PASS.
- `peak_rss_bytes` = 10510336 — numeric and > 0 on the Windows recorder host → PASS.
- `vss_delta_bytes` = 1368064 — numeric, captured exactly as printed (positive
  here; commit charge grew) → PASS.
- `samples` = 3736936 — equals cycles + 2 (soak start + one per cycle + end) → consistent.
- The process-memory line did NOT report
  `process-memory sampling unavailable on this platform`; numeric metrics were printed.

A temporary log of the one-hour run output was kept outside the repository at
`A:\tmp\issue2_soak.log` (plus the background-task output file); no new repo
file was created for it.

### Gate: `git diff --check -- crates/script-bench/BASELINE.md`

No whitespace errors. (Git emitted only the informational
"LF will be replaced by CRLF" notice; the file remains LF-terminated, matching
its prior state — that notice is not a `diff --check` whitespace error.)

### Gate: `git diff --stat -- crates/script-bench/BASELINE.md`

```
 crates/script-bench/BASELINE.md | 73 +++++++++++++++++++++++++++++++++++++++++
 1 file changed, 73 insertions(+)
```

73 insertions, 0 deletions — additive only; the 2026-05-12 historical rows are
preserved.

### Gate: `git status --short --branch` (post-execution)

```
## ai-dispatch/ISSUE-2
 M crates/script-bench/BASELINE.md
?? ai_handoffs/ISSUE-2_TASK_2026-05-17_13-21-45+0300.md
?? ai_handoffs/ISSUE-2_TASK_2026-05-17_13-21-45+0300.meta.json
```

(The untracked `ISSUE-2_EXEC_*` packet and its `.meta.json` sidecar produced by
this report are added after this snapshot.)

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope. Only
`crates/script-bench/BASELINE.md` was edited (additively), and only the single
EXEC packet plus its sidecar were added. No commit, stage, or push was
performed; no MUST-NOT file was modified.

One non-deviation worth noting for the Reviewer: the recorder host's toolchain
is `cargo 1.92.0` / `rustc 1.92.0`, whereas earlier BASELINE.md rows were taken
under `cargo 1.94.1`. The TASK fixes the soak *target* and forbids changing the
toolchain/dependencies, not the cargo version; the actual version used is
recorded honestly in the new BASELINE.md section, so this is transparency, not
a scope deviation.

## Open Questions for Reviewer

- Confirm the BASELINE.md placement is acceptable: the new 2026-05-17 section
  was inserted before the `## Formal W04 raw WasmtimeCranelift gates — RUN
  2026-05-12` section so the two memory-soak sections sit adjacent. The
  pre-existing 2026-05-16 "Memory-soak process-memory metrics" subsection still
  states no one-hour soak was re-run *for that revision* — that remains
  historically accurate for the 2026-05-16 revision, so it was left untouched;
  the new dated section is the explicit answer to that subsection's
  "future release-readiness one-hour soak" forward pointer. Flag if the
  Reviewer would prefer that forward-pointer paragraph also be cross-referenced.

## Worktree State

- Tracked files: `crates/script-bench/BASELINE.md` modified (uncommitted, +73/-0).
- Untracked items:
  - `ai_handoffs/ISSUE-2_TASK_2026-05-17_13-21-45+0300.md` (the TASK packet)
  - `ai_handoffs/ISSUE-2_TASK_2026-05-17_13-21-45+0300.meta.json` (TASK sidecar)
  - `ai_handoffs/ISSUE-2_EXEC_2026-05-17_14-31-55+0300.md` (this report)
  - `ai_handoffs/ISSUE-2_EXEC_2026-05-17_14-31-55+0300.meta.json` (this report's sidecar)
- Branch: `ai-dispatch/ISSUE-2`
- Last commit: `9977eed` ai-dispatch ISSUE-1: Reconcile dispatch docs with automated queue state

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-2
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
