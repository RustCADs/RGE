# Execution Report

DISPATCH_ID: ISSUE-216
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-26_22-47-58+0300
RELATED_FILES:
- ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md — TASK packet executed (read-only)
- ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md — CORRECTION packet driving this corrected EXEC report
- ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.md — prior EXEC report superseded by this artifact (left on disk as read-only historical context)
- ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.meta.json — prior EXEC sidecar (read-only)
- .ai/dispatch-ISSUE-216/codex.control.round0.json — round-0 control verdict that directed this correction (read-only provenance)
- .ai/dispatch-ISSUE-216/verification.round0.log — canonical verification gate log (read-only)
- AGENTS.md — orchestrator-vs-manual protocol doctrine (read)
- AI_DISPATCH_AUTOMATION.md — orchestrator packet shape + completion semantics (read)
- ai_handoffs/AI_HANDOFF_PROTOCOL.md — packet-protocol spec (read)
- plans/PLAN.md — §1.10.4 entropy metrics + §13.2/§13.3/§13.10 reflection gates (read)
- plans/IMPLEMENTATION.md — Phase 1.1 + Phase 9 §9 evaluation axes (read)
- plans/BASELINE.md — §1.10.4 invalidation-radius + §1.1 reflection-scale + editor-usability + live-inspector preflights (read)
- kernel/types/BUDGET.md — Phase 1.1 compile-budget source of truth (read)
- Status.md — current state framing (live re-read at lines 1, 9, 171, 194, 373; canonical Phase 9 row)
- HANDOFF.md — current handoff framing (live re-read at lines 1, 5, 9, 1498-1506, 1587; canonical Phase 9 row)
- change.md — append-only chronology (live re-read at lines 1620-1641; last reflection-related row at 1641)
- Cargo.toml — workspace declarations of `rge-kernel-types` / `rge-macros-reflect` (read)
- kernel/types/Cargo.toml — substrate manifest (read)
- kernel/types/src/lib.rs — public surface (read)
- kernel/types/src/reflect.rs — `Reflect` trait + `ReflectObject` shadow (read)
- kernel/types/src/type_id.rs — hand-rolled FNV-1a-128 `TypeId` (read)
- kernel/types/src/serde_bridge.rs — `ReflectValue` + RON bridge (read)
- kernel/types/tests/reflect_round_trip.rs — hand-written `Reflect for RenderPass` fixture (read)
- crates/macros-reflect/Cargo.toml — proc-macro manifest (read)
- crates/macros-reflect/src/lib.rs — `#[derive(Reflect)]` entry (read)
- crates/macros-reflect/src/derive.rs — shape validation (read)
- crates/macros-reflect/src/codegen.rs — emitter (read)
- crates/macros-reflect/src/attrs.rs — attribute parser (read)
- crates/macros-reflect/tests/fixtures/render_pass.rs — `RenderPass` + `WithValidate` derive fixtures (read, full file)
- crates/macros-reflect/tests/derive_test.rs — derive round-trip + descriptor tests (read, full file)
- crates/macros-reflect/tests/ui_hints_test.rs — `UiHint` emission tests (read, full file)
- crates/macros-reflect/tests/validate_attr_test.rs — `validate` / `custom_drawer` / `skip` attribute tests (read, full file)
- crates/macros-reflect/tests/compile_budget_5_pilots.rs — 5-pilot calibration probes (read, full file)
- crates/rge-data/Cargo.toml — schema crate manifest (read)
- crates/rge-data/src/lib.rs — local `Reflect` stub + doc-comment promise (read)
- crates/rge-data/src/scene.rs — `Scene` / `Entity` / `ComponentValue` envelope (read)
- crates/rge-data/src/prefab.rs — `.rge-prefab` typed schema + parameter / override surfaces (read, full file)
- crates/rge-data/examples/bake_fixtures.rs — canonical W14 fixture-baking binary (read, full file)
- crates/rge-data/tests/golden_simple_scene_schema.rs — typed-payload schema gate (read)
- crates/rge-data/tests/golden_simple_scene_load_tick.rs — identity-only test bridge (read)
- crates/rge-scene-loader/Cargo.toml — bridge manifest (read)
- crates/rge-scene-loader/src/lib.rs — `load_scene_into_world` literal four-arm match table (read)
- crates/rge-scene-loader/tests/simple_scene.rs — golden simple-scene integration (read)
- runtime/runtime-headless/Cargo.toml — production-consumer manifest (read)
- runtime/runtime-headless/src/main.rs — production consumer of `load_scene_into_world` (read)
- runtime/runtime-headless/tests/loads_golden_simple_scene.rs — end-to-end binary gate (read)
- golden-projects/simple-scene/.rge-project — typed project manifest (read, full file)
- golden-projects/simple-scene/scenes/main.rge-scene — typed `ComponentValue.type_id` payloads (read)
- crates/components-spatial/Cargo.toml + src/lib.rs + src/transform.rs — `Transform` `Component` impl + serde derives (read)
- crates/components-render/Cargo.toml + src/lib.rs + src/camera.rs + src/light.rs — `Camera` / `Light` `Component` impls + serde derives (read)
- crates/components-visibility/Cargo.toml + src/lib.rs + src/visibility.rs — `Visibility` `Component` impl + serde derives (read)
- crates/script-host/Cargo.toml — declared-but-unused `rge-kernel-types` dep (read)
- ai_handoffs/ISSUE-210_EXEC_2026-05-26_17-00-00+0300.md — Phase 9 freeze-validity audit precedent (read; decision text at :108-118, smallest-safe-follow-up at :307-355)
- ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md — Phase 9 abstraction-pain audit precedent (read; smallest-safe-follow-up at :481-540)
- ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md — Phase 9 invalidation-economics audit precedent (read; decision block at :145-170)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md`

## Correction Packet Reference

`ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md` — five report-only
corrections to the prior EXEC: (1) live Status.md / HANDOFF.md line
citations; (2) singular smallest safe follow-up; (3) current packet line
citations for any retained ISSUE-210 / 212 / 214 candidate-comparison
references; (4) typed-schema inspection surfaces for `prefab.rs`,
`crates/rge-data/examples/**`, and `golden-projects/simple-scene/.rge-project`;
(5) current macros-reflect test-surface line citations. This corrected EXEC
addresses each fix verbatim; no other claim in the prior EXEC is altered.

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch. The CORRECTION packet
listed above is the Planner-authored input consumed by this corrected EXEC.
The Executor concurred with all five enumerated corrections after re-running
the read-only inspections each correction required (Status.md / HANDOFF.md
line-anchored greps, prefab.rs / examples / .rge-project reads, and
macros-reflect tests/** reads); proceeded to author this corrected EXEC.

## What I Changed

### Source

- None. This is a read-only Phase 9 reflection-scale pressure audit per the
  TASK packet's `### MAY edit: None` clause
  (`ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md:49`) and the
  CORRECTION packet's report-only scope at
  `ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md:29-33`.
  No tracked source, test, doc, Cargo metadata, workflow, schema, automation
  script, scheduler config, task brief, fixture, prior packet, prior log, or
  `.ai/dispatch.tasks.md` is modified by this corrected EXEC.

### Tests

- None.

### Docs

- None.

### New audit-only artifacts

- `ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.md` (this file).
- Matching `.meta.json` sidecar via `new-handoff.ps1 -Finalize` after this
  body is finalized.

The prior EXEC artifact
(`ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.md` + sidecar) remains
on disk as read-only historical context per the CORRECTION packet's standing
no-edit-on-prior-packets clause.

No `_REVIEW_` or `_CLOSEOUT_` packet is created. Per `AGENTS.md:26-36` and
`AI_DISPATCH_AUTOMATION.md` orchestrator semantics, an orchestrator dispatch
emits only `TASK` / `EXEC` (plus `CORRECT` when a correction round fires);
the Codex control review for this run is recorded at
`.ai/dispatch-ISSUE-216/codex.control.round0.json`, not as a packet. The
manual `TASK → EXEC → REVIEW → CORRECT → CLOSEOUT` chain documented in
`ai_handoffs/AI_HANDOFF_PROTOCOL.md:50-66` is a different shape and does not
apply here.

## Decision block

REFLECTION_SCALE_VERDICT: still_untriggered
SMALLEST_SAFE_FOLLOW_UP: None — defer; no new dispatch warranted.

The two-arm revisit trigger codified in `plans/BASELINE.md:516-521`
(production `#[derive(Reflect)]` or `use rge_kernel_types::*` reflection-API
import in non-test code, OR `script-host` actually wiring its declared
`kernel/types` dep into the generic hot-reload migration path) remains the
correct next gate. Neither arm has fired. The typed scene-loader chain that
landed between 2026-05-25 and 2026-05-26 added zero production reflection
consumers and explicitly routed around reflection (see §"Typed scene-loader
impact" below). No production reflection-API import or `#[derive(Reflect)]`
annotation exists today.

This is exactly one action (defer; no new dispatch). No optional fallback,
no secondary alternative, no docs-only PREFLIGHT mirror — those are
explicitly excluded by the CORRECTION packet fix #2
(`ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md:44-49`).

## Phase 9 context (live current-evidence citations)

Phase 9 is the production-pressure phase that reality-checks the eight §9
evaluation axes (`plans/IMPLEMENTATION.md:589-606`). "Reflection scale —
compile time + binary size at 100+ reflected types" is line
`plans/IMPLEMENTATION.md:597`. The substrate remains in the "stable,
well-closed state" framing established by the 2026-05-14 v0 release
certification.

- `plans/IMPLEMENTATION.md:117` (Phase 1 §1.1): "`kernel/types` — FIRST REAL
  CRATE. The architectural root. Everything depends on this."
- `plans/IMPLEMENTATION.md:190` (Phase 1.1 abort gate): "> 30 s on 5 pilot
  types ⇒ STOP and replan reflection strategy."
- `plans/IMPLEMENTATION.md:597` (Phase 9 §9 axis 4): "**Reflection scale** —
  compile time + binary size at 100+ reflected types."
- `plans/PLAN.md:526` (§1.10.4 row): "**Reflection schema size** (typed
  components × fields) (NEW v0.7) | >10K = warn".
- `plans/PLAN.md:1124` (§13.2 budget): "reflection cache 1000 components
  ≤2MB".
- `plans/PLAN.md:1128` (§13.3 build gates): "gen instantiations per crate
  ≤5000 warn / ≤15000 hard · trait expansion depth ≤8/16 · ... ·
  **incremental invalidation radius ≤30% of workspace** (NEW v0.7)".
- `plans/PLAN.md:1156` (§13.10 entropy gates): names reflection schema size
  and incremental invalidation radius among the v0.7 metrics tracked at
  every minor bump.
- `kernel/types/BUDGET.md:1-7,42-44` (compile-budget verdict): "PASS — 5
  pilots compile in 7.5 s clean, well under the 30 s abort gate. Phase 1.1
  may merge."
- `kernel/types/BUDGET.md:76-83` (watch table): 5-pilot wall-clock 7.5 s vs
  15 s warn / 30 s hard; per-field ~23 LLVM lines vs 100/250; 100-type total
  ~9,000 LLVM lines vs 15,000/25,000; generic instantiations 0 by design.
- `plans/BASELINE.md:438-447` (§1.1 reflection-scale honesty preflight,
  2026-05-21 entry): canonical prior adoption baseline. 0 production
  reflected types and 0 production reflection-API imports; codified the
  two-arm revisit trigger at `:516-521`.

## Status / HANDOFF / change.md framing (live, line-cited)

Status.md (383 lines, ~450 KB; the very long lines push the file past
single-Read caps but `Grep` + offset-bounded `Read` of specific spans both
succeed) and HANDOFF.md (1,678 lines, ~920 KB; same shape) are the canonical
state docs the TASK packet at `:168-171` requires. Live line citations
follow; no claim in this corrected EXEC treats `plans/BASELINE.md` or
`kernel/types/BUDGET.md` as a substitute for these required reads.

- `Status.md:1` — title line "# Status" (confirms file identity and read
  scope).
- `Status.md:9` — 2026-05-14 v0 release certification snapshot. Records
  "Known v0 deferrals (NOT blockers; listed so absence is not mistaken for
  oversight)" enumerating the editor-shell mock-event-loop perf harness,
  `peak_rss` / `vss_delta` soak-harness improvement, "the 4 kernel
  doctrine-cavity eventual implementations (`job-system` work-stealing pool
  / `io-scheduler` actual I/O dispatch / `asset-view` WASM zero-copy mapping
  / `asset-streaming` residency manager)", and the `compile.rs` legibility
  refactor. **No reflection-adoption deferral is present in this list,
  confirming reflection-scale is not a tracked v0 deferral.**
- `Status.md:171` — validation-gate row: "| 1.1 | Reflection compile time
  on 5 pilots ≤30s | **7.5s** (4× headroom) — W02 |". This is the canonical
  Phase 1.1 reflection compile-budget closure in `Status.md`'s own gates
  table.
- `Status.md:194` — architecture-lint exemption row mentions `macros-reflect`
  among the workspace's cleared rollout-debt crates ("35 Tier-2 crates {...,
  macros-reflect, ...}"). Confirms macros-reflect's crate-level cleanliness
  posture in current state.
- `Status.md:373` — "**Reconciled 2026-05-21 against
  [`plans/IMPLEMENTATION.md`](plans/IMPLEMENTATION.md) ... The real next
  frontier is **Phase 9 (production pressure)** per IMPLEMENTATION.md §9 —
  eight evaluation axes (§0.6 freeze validity, abstraction pain,
  invalidation economics, **reflection scale**, async orchestration, compile
  times, editor usability, GPU pressure) — plus the carry-over kernel
  cavities..." This is the canonical Phase 9 framing row that puts this
  audit in scope.

- `HANDOFF.md:1` — title line "# RGE Handoff Document".
- `HANDOFF.md:5,9` — 2026-05-23 and 2026-05-14 snapshot headers framing
  current docs-reconciliation cadence and v0-cert provenance (same
  reflection-related v0-deferral inventory mirrored from `Status.md:9`).
- `HANDOFF.md:1498` — "**No Phase 8 coverage tasks should be filed** —
  substrate is closed; further GraphDiff / VizAdapter / NodeId / hash work
  should appear only as Phase 9 production-pressure dispatches under real
  consumer demand."
- `HANDOFF.md:1500-1503` — "**The real next frontier is Phase 9 (production
  pressure)** per IMPLEMENTATION.md §9 — eight evaluation axes (§0.6 freeze
  validity, abstraction pain, invalidation economics, **reflection scale**,
  async orchestration, compile times, editor usability, GPU pressure) — plus
  the carry-over items in the 'Persistent gaps' section immediately below."
  This is HANDOFF.md's canonical Phase 9 row.
- `HANDOFF.md:1504-1506` — "New dispatches should decompose from those — not
  from the struck options below and not from Phase 8 coverage broadening."
- `HANDOFF.md:1587` — "How to resume" step 3: "Phase 9 work should be
  pressure-driven: pick an evaluation axis (§0.6 freeze validity,
  abstraction pain, invalidation economics, **reflection scale**, async
  orchestration, compile times, editor usability, GPU pressure) with
  concrete consumer demand, not a generic sweep." This is the dispatch
  doctrine the present audit follows verbatim.

- `change.md` tail re-read live for this dispatch with line-anchored grep
  (`Grep pattern='^2026-05-2' path='change.md' output_mode='content' -n=true
  head_limit=20`). The most recent reflection-scale-relevant rows are the
  2026-05-21 Phase 9 preflight entries at `change.md:1633` (§1.10.4
  invalidation-radius), `:1635` (§13.3 compile-time), `:1637` (live-inspector),
  `:1639` (editor-usability), and `:1641` (§1.1 reflection-scale honesty
  preflight — the prior baseline this audit re-evaluates). No
  reflection-substrate / typed-scene-loader landing has been appended to
  `change.md` since `:1641`. The typed scene-loader chain (ISSUE-171 /
  ISSUE-173 / ISSUE-177) was landed during the autonomous queue arc on
  2026-05-25 and is reflected on the branch via the per-dispatch publish
  commits (most recently `283cc19 ai-dispatch ISSUE-214: salvage
  invalidation-economics audit`) but did not append its own `change.md` rows;
  those landings are recorded only in their individual
  `ai_handoffs/ISSUE-1{71,73,75,77}_EXEC_*.md` packets.

These citations satisfy CORRECTION fix #1
(`ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md:36-42`). No
text in this corrected EXEC says alternate files replaced the required
Status.md / HANDOFF.md inspections; both files are read live with the
line citations above, and `plans/BASELINE.md` / `kernel/types/BUDGET.md`
appear only as supplemental evidence (the substrate row-of-record they
themselves cite).

## Required evidence table (line-cited)

| Required surface | Evidence | Production / Test / Doc / Cargo |
|---|---|---|
| `kernel/types` substrate manifest | `kernel/types/Cargo.toml:1-26` — Tier-1 substrate; deps `serde` / `ron` / `thiserror` (workspace floor); dev-dep `serde_json`; no `rge-kernel-types` / `rge-macros-reflect` dependency cycle. | Substrate Cargo |
| `kernel/types` public surface | `kernel/types/src/lib.rs:51-63` re-exports `FieldDescriptor` / `Reflect` / `ReflectObject` / `SchemaVersion` / `from_ron` / `to_ron` / `to_ron_pretty` / `ReflectValue` / `SerdeBridgeError` / `TypeId` / `UiHint`. | Substrate src |
| `kernel/types` `Reflect` trait | `kernel/types/src/reflect.rs:68-108` defines `pub trait Reflect: Sized` with const items `TYPE_ID` / `TYPE_NAME` / `FQ_TYPE_NAME` / `SCHEMA_VERSION` / `FIELDS` / `KIND` + dynamic `get_field_dyn` / `set_field_dyn`. `:121-148` defines object-safe shadow `ReflectObject` with `:150-179` blanket impl. | Substrate src |
| `kernel/types` `TypeId` substrate | `kernel/types/src/type_id.rs:54-103` — hand-rolled FNV-1a-128 over `domain-separated` qualified name; `:114-117` constants; `:119-131` `fnv1a_64`. No `blake3`, no `inventory`, no `linkme`, no `ctor` (`kernel/types/Cargo.toml:12-20` confirms). | Substrate src |
| `kernel/types` `serde_bridge` | `kernel/types/src/serde_bridge.rs:37-52` — closed-set `ReflectValue` (`Bool` / `I64` / `U64` / `F64` / `String` / `StaticStr` / `Unit`). `:117-149` exposes `to_ron` / `from_ron` / `to_ron_pretty` over `T: Reflect + Serialize + DeserializeOwned`. | Substrate src |
| `kernel/types` pilot test | `kernel/types/tests/reflect_round_trip.rs:31-117` — hand-rolled `impl Reflect for RenderPass` (test fixture). `:157-210` runs three byte-identical round-trip + dyn-mutation tests. | Test fixture |
| `crates/macros-reflect` manifest | `crates/macros-reflect/Cargo.toml:1-29` — `proc-macro = true`; normal deps `proc-macro2` / `quote` / `syn` only; dev-deps `rge-kernel-types` + `serde` + `ron`. | Substrate Cargo |
| `crates/macros-reflect` entry | `crates/macros-reflect/src/lib.rs:82-85` — `#[proc_macro_derive(Reflect, attributes(reflect))]` calls `derive::expand(input)`. | Substrate src |
| `crates/macros-reflect` shape validation | `crates/macros-reflect/src/derive.rs:16-60` — named-struct only; rejects tuple struct, enum, union. | Substrate src |
| `crates/macros-reflect` emitter | `crates/macros-reflect/src/codegen.rs:18-163` — emits one `impl rge_kernel_types::Reflect` block per type; `:103-117` declares `TYPE_ID: ::rge_kernel_types::TypeId::from_bytes([0u8; 16])` (Phase 1.1 placeholder, comment at `:105-110`), `:114-117` declares `FIELDS` as an inline `&'static` slice. No global registry / `inventory!` submission / generic helper traits — the comment at `:5-7` makes the "O(fields) tokens" budget load-bearing. | Substrate src |
| `crates/macros-reflect` attribute parser | `crates/macros-reflect/src/attrs.rs:24-65` (`ContainerAttrs::parse`); `:144-266` (`FieldAttrs::parse`). All by-hand `syn::Meta::parse_nested_meta` (no `darling`). | Substrate src |
| `crates/macros-reflect` 5-pilot calibration | `crates/macros-reflect/tests/compile_budget_5_pilots.rs:13` (`use rge_macros_reflect::Reflect;`); `:16-25` `Pilot1` (4 fields); `:27-36` `Pilot2` (4 fields); `:38-51` `Pilot3` (5 fields); `:53-63` `Pilot4` (4 fields); `:65-82` `Pilot5` (7 fields); `:84-99` asserts each `<PilotN as Reflect>::FIELDS.len()` and `total == 24`. **Test-only**, no production consumer. | Test fixture |
| `crates/macros-reflect/tests/fixtures/render_pass.rs` | `:10` `use rge_macros_reflect::Reflect;`; `:14-15` `#[derive(... Reflect, Serialize, Deserialize)] #[reflect(version = "1.0.0")]`; `:16-38` `pub struct RenderPass { name, enabled, priority (Slider 0..1000), clear_color_{r,g,b,a} (Slider 0.0..1.0 step 0.001), msaa_samples (Slider 1..16 default 4) }`; `:57-58` `#[derive(... Reflect, ...)] #[reflect(version = "0.1.0")] pub struct WithValidate { count #[reflect(validate = "validators::nonzero")] u32; trigger #[reflect(custom_drawer = "drawers::big_red_button")] bool; cache #[reflect(skip)] u64 }`; `:40-53` + `:70-78` `Default` impls; `:81-92` `pub fn sample_render_pass()`. Shared fixture for `derive_test.rs` / `ui_hints_test.rs` / `validate_attr_test.rs`. **Test-only**. | Test fixture |
| `crates/macros-reflect/tests/derive_test.rs` | `:11` `use rge_kernel_types::{from_ron, to_ron, Reflect, ReflectKind, ReflectValue, SchemaVersion};` (test-bin import only); `:13-21` `derived_render_pass_round_trips_byte_identically`; `:23-42` `derived_render_pass_descriptors` (asserts `TYPE_NAME == "RenderPass"`, `SCHEMA_VERSION == SchemaVersion::new(1,0,0)`, `KIND == NamedStruct`, the 8-field name vector); `:44-74` `derived_dyn_field_get_and_set` (asserts `priority` → `ReflectValue::I64(100)`, dyn-set of `priority` / `clear_color_r` / `name` and `TypeMismatch` for `Bool`); `:76-82` `derived_fq_type_name_includes_module_path`. **Test-only.** | Test |
| `crates/macros-reflect/tests/ui_hints_test.rs` | `:10` `use rge_kernel_types::{Reflect, UiHint};` (test-bin import only); `:12-17` `find_field` helper; `:19-30` `slider_hint_carries_min_max_step` (asserts `clear_color_r` `UiHint::Slider { min: 0.0, max: 1.0, step: 0.001 }`); `:32-42` `slider_hint_on_integer_carries_min_max` (`priority` 0..1000); `:44-50` `fields_without_ui_get_default` (`name` / `enabled` → `UiHint::Default`); `:52-59` `slider_field_has_range_meta` (`msaa_samples` `RangeMeta { 1, 16 }`); `:61-68` `ty_name_preserved_as_source_text`. **Test-only.** | Test |
| `crates/macros-reflect/tests/validate_attr_test.rs` | `:13` `use rge_kernel_types::{Reflect, ReflectError, ReflectValue};` (test-bin import only); `:15-26` `skipped_field_appears_in_descriptors` (asserts `WithValidate::FIELDS` lists `"cache"`, `cache.serde_skip == true`); `:28-33` `skipped_field_refuses_dyn_set` (asserts `ReflectError::SkippedField`); `:35-43` `non_skipped_fields_still_writable`; `:45-52` `validate_and_drawer_attrs_compile` (Phase 1.1 attribute-acceptance smoke). **Test-only.** | Test |
| Workspace declaration of `rge-kernel-types` | `Cargo.toml:237` — `rge-kernel-types = { path = "kernel/types" }`. | Workspace Cargo |
| Production-vs-test crate-Cargo deps on `rge-kernel-types` | Two declarations outside the substrate itself: `crates/script-host/Cargo.toml:18` (`rge-kernel-types = { path = "../../kernel/types" }`) — declared-but-unused, see usage section; `crates/macros-reflect/Cargo.toml:21` (dev-dep, tests-only). | Cargo / declared-but-unused |
| Production use of `rge_kernel_types::*` | **0**. Every `use rge_kernel_types::...` line in workspace `.rs` files outside `kernel/types/tests/**` and `crates/macros-reflect/tests/**` is either the doc-comment-only reference at `crates/components-spatial/src/lib.rs:20` (`// callers should `use rge_kernel_types::Entity;`` aspirational doc) or the doc-only re-export plan at `crates/rge-data/src/lib.rs:39,75`. **0 active production imports.** | Doc-comment only / declared-but-unused |
| Test-only reflection pilots | 7 reflected types, all test-only: `Pilot1`–`Pilot5` in `crates/macros-reflect/tests/compile_budget_5_pilots.rs:18,29,40,55,67`; `RenderPass` (`crates/macros-reflect/tests/fixtures/render_pass.rs:16`) and `WithValidate` (`:59`) shared fixtures; the hand-rolled `kernel/types/tests/reflect_round_trip.rs::RenderPass:20`; the `kernel/types/src/reflect.rs:188-249` `Hand` fixture inside `#[cfg(test)]`. | Test-only |
| `crates/rge-data` typed schema | `crates/rge-data/src/scene.rs:84-115` — `ComponentValue { type_id: String, data: String }` envelope + `Relation`; `:33-65` `Scene { version, name, entities, root_entities }`. Reflection-neutral wire shape: module doc at `:14-25` documents that `data` is RON text and the two-phase plan ("when the W02 reflection wave merges, callers... walk each `ComponentValue::data` string through `kernel/types::serde_bridge::from_ron`") is **future, not present**. `:78` `pub components: Vec<ComponentValue>`; `:85-95` `pub struct ComponentValue { type_id: String, data: String }`. | Schema src |
| `crates/rge-data` local `Reflect` stub | `crates/rge-data/src/lib.rs:70-80` — `pub mod stub { pub trait Reflect: 'static {} }`. Module doc at `:36-39` and `:74-75` are doc-only mentions of the future `pub use rge_kernel_types::Reflect;`; no active import. | Doc-comment only |
| `crates/rge-data/src/prefab.rs` (typed prefab schema) | `:21-24` imports `serde::{Deserialize, Serialize}` + `crate::{entity_ref::EntityId, scene::Entity, schema_version::SchemaVersion}` only — no `rge_kernel_types`, no `rge_macros_reflect`, no `Reflect`. `:28-43` `pub struct Prefab { version, name, parameters, entities, exposed_overrides }` with plain serde derives. `:45-57` `Prefab::empty` constructor. `:60-69` `pub struct ParamSpec { name, ty: String /* matches `kernel/types::TypeId::path`. Stored as a string so this crate has no Reflect dependency. */, default: String /* parsed by the reflection bridge at instantiation */ }`. `:72-81` `pub struct ExposedOverride { entity, component_type: String, field_path: String }` — doc at `:78-79` explicitly states the "exact mapping belongs to the reflection bridge". `:83-131` `#[cfg(test)] mod tests` with `fixture_prefab()` constructing a typed `Prefab` whose `ComponentValue.type_id = "rge::components::Transform"` and RON-literal `data`, plus `round_trip_ron` and `empty_constructor` tests. **The prefab schema documents reflection as a future consumer at the doc-string level only; it has zero `kernel/types` / `macros-reflect` imports and zero reflection-API calls.** | Schema src + test |
| `crates/rge-data/examples/bake_fixtures.rs` (only file under `examples/`) | `:11-12` `use std::{fs, path::Path};`; `:14-17` `use rge_data::{ ComponentValue, Entity, EntityId, ExposedOverride, ParamSpec, PluginRef, Prefab, Project, Relation, Scene, ScenePath, SchemaVersion, TargetTier };` — **only `rge_data` re-exports, no `rge_kernel_types` import**. `:19-27` `pretty_config()` RON serializer config; `:29-35` generic `ser<T: serde::Serialize>` helper; `:45-60` `canonical_project()` builds a typed `Project { version: V0_1_0, target_tiers: [Desktop, Headless], plugins: [PluginRef], scenes: [ScenePath…] }`; `:62-95` `canonical_scene()` builds a 2-entity scene whose `ComponentValue` `type_id` strings are literal `"rge::components::Transform"`; `:97-123` `canonical_prefab()` mirrors the `prefab.rs::tests::fixture_prefab()` shape with literal `type_id: "rge::components::Transform"`; `:127-134` `canonical_v0_0_scene_text()` returns a string-literal v0.0 fixture for migration tests; `:136-158` `main()` writes the four canonical fixtures via `write_fixture` (`fs::write`). **The example does not call any reflection API; it is a serialization harness whose `ComponentValue` payloads remain literal RON.** | Examples src |
| `golden-projects/simple-scene/.rge-project` (typed project manifest) | `:1-8` — eight-line typed manifest: `Project( version: "0.1.0", name: "simple-scene", description: "Basic load + transform + camera + light render", target_tiers: [desktop], plugins: [], scenes: ["scenes/main.rge-scene"], )`. No `kernel/types::TypeId`, no reflection bridge, no per-component schema; the manifest's role is to point to the typed scene file consumed by `runtime-headless` (see consumer row below). | Fixture |
| Golden simple-scene typed payload | `golden-projects/simple-scene/scenes/main.rge-scene:9-19` (`Camera` entity carries three `ComponentValue` with `type_id` strings `rge::components::Transform` / `rge::components::Camera` / `rge::components::Visibility`); `:24-37` (`KeyLight` entity carries two with `rge::components::Transform` / `rge::components::Light`). Every `data` is a literal RON string, NOT a reflection bridge. | Fixture |
| Golden schema gate | `crates/rge-data/tests/golden_simple_scene_schema.rs:198-247` (camera typed-component schema), `:293-357` (light typed-component schema), `:120-138` (manifest), `:158-195` (scene roots). Asserts canonical `rge::components::*` strings + literal payloads — uses `ron::Value` inspection only; no component crate import, no `rge_kernel_types` import. | Test |
| Golden identity-only load+tick | `crates/rge-data/tests/golden_simple_scene_load_tick.rs:50-57` — `load_scene_into_world` is a **test-local** identity-only `spawn_with_id` helper, NOT the production `rge_scene_loader::load_scene_into_world`. Test asserts entity count + per-id existence + tick contract; no components materialized. | Test |
| `crates/rge-scene-loader` manifest | `crates/rge-scene-loader/Cargo.toml:1-23` — bridge crate; does NOT depend on `rge-kernel-types` or `rge-macros-reflect`. Path deps on `rge-data` + the three component crates (`components-spatial` / `components-render` / `components-visibility`); workspace deps `rge-kernel-ecs` / `ron` / `thiserror`. | Bridge Cargo |
| `crates/rge-scene-loader` implementation | `crates/rge-scene-loader/src/lib.rs:84-102` — `pub fn load_scene_into_world(scene: &Scene) -> Result<World, SceneLoadError>`. Identity preservation at `:90-92` (`EntityId::from_ulid(*entity.id.as_ulid())` then `world.spawn_with_id(ecs_id)`). | Bridge src |
| `crates/rge-scene-loader` typed-RON match table | `crates/rge-scene-loader/src/lib.rs:106-161` — `insert_component` dispatches on `component.type_id.as_str()` through a **literal four-arm match** on `"rge::components::Transform"` (`:113-122`), `"rge::components::Camera"` (`:123-132`), `"rge::components::Light"` (`:133-142`), `"rge::components::Visibility"` (`:143-152`). Each arm calls `ron::from_str::<T>(&component.data)` and `world.insert(ecs_id, value)` with the concrete component type — no `dyn Reflect`, no `ReflectValue`, no `TypeId::of_name`, no registry lookup. Unknown `type_id` → `SceneLoadError::UnsupportedComponent` (`:153-158`). | Bridge src |
| `crates/rge-scene-loader` golden test | `crates/rge-scene-loader/tests/simple_scene.rs:57-127` — drives the bridge through 4 tests against the golden fixture. Asserts `entity_count == 2`, per-entity `Camera` / `Light` / `Transform` / `Visibility` typed presence, and the `advance_tick` contract. No reflection API touched. | Test |
| `runtime/runtime-headless` manifest | `runtime/runtime-headless/Cargo.toml:1-23` — production binary; deps `rge-data` + `rge-scene-loader` + workspace `ron`. Does NOT depend on `rge-kernel-types` or `rge-macros-reflect`. | Production Cargo |
| `runtime/runtime-headless` production consumer | `runtime/runtime-headless/src/main.rs:14` (`use rge_scene_loader::load_scene_into_world;`); `:61-110` (`fn run`) parses Project RON → resolves first scene → parses Scene RON → calls `load_scene_into_world` (`:95`) → calls `world.advance_tick()` (`:98`) → prints `entity_count=N current_tick=N`. This is the only non-test production consumer of `load_scene_into_world`. | Production src |
| `runtime/runtime-headless` end-to-end gate | `runtime/runtime-headless/tests/loads_golden_simple_scene.rs:26-55` — spawns the compiled binary against the golden manifest; asserts `entity_count=2` + `current_tick=2`. `:58-96` — CLI negative-path coverage. | Test |
| `crates/components-spatial` | `Cargo.toml:1-21` (deps `rge-kernel-ecs` + `serde`; dev-dep `ron` only). `src/lib.rs:24-37` re-exports `Transform` / `Parent` / `ChildOf` / `GlobalTransform` / `Entity`. `src/transform.rs:14-31` (`#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)] pub struct Transform { translation, rotation, scale }`) + `:59` (`impl rge_kernel_ecs::Component for Transform {}`). No `rge_kernel_types` / `rge_macros_reflect` / `#[derive(Reflect)]` anywhere. The aspirational `use rge_kernel_types::Entity;` line at `src/lib.rs:20` is a doc-comment plan, not an import. | Production component src |
| `crates/components-render` | `Cargo.toml:1-22` (deps `rge-kernel-asset` + `rge-kernel-ecs` + `serde`; dev-dep `ron` only). `src/lib.rs:18-40` re-exports `Camera` / `Projection` / `Light` / `LightKind` / `MeshHandle` / `BRepHandle` / …. `src/camera.rs:43-69` (`#[derive(... Serialize, Deserialize)] pub struct Camera { projection, viewport, priority, is_active }`; `:69` `impl rge_kernel_ecs::Component for Camera {}`). `src/light.rs:56-77` (`#[derive(... Serialize, Deserialize)] pub struct Light { color, kind, affects_indirect }`; `:76` `impl rge_kernel_ecs::Component for Light {}`). No reflection API. | Production component src |
| `crates/components-visibility` | `Cargo.toml:1-21` (deps `rge-kernel-ecs` + `serde`; dev-dep `ron` only). `src/lib.rs:18-29` re-exports `Visibility` / `Hidden` / `Disabled` / `Highlight`. `src/visibility.rs:19-30` (`#[derive(... Serialize, Deserialize)] pub enum Visibility { Visible, Hidden, Inherited }`; `:30` `impl rge_kernel_ecs::Component for Visibility {}`). No reflection API. | Production component src |
| Workspace decls of `rge-kernel-types` (read-only `Grep`) | `Cargo.toml:237` (workspace floor); `kernel/types/Cargo.toml:2` (self); `crates/macros-reflect/Cargo.toml:21` (dev-dep); `crates/script-host/Cargo.toml:18` (declared-but-unused production dep). 2 normal revdeps + 1 dev-dep — agrees with `plans/BASELINE.md:382` (§1.10.4 row). | Workspace Cargo |
| Workspace decls of `rge-macros-reflect` | Only `crates/macros-reflect/Cargo.toml:1-29` (self). **Zero** workspace revdeps — confirms `plans/BASELINE.md:385` ("`rge-macros-reflect`: 0 normal closure, 0 + dev closure: 'only its own internal tests/fixtures use it'"). | Workspace Cargo |

Fixes #4 and #5 from the CORRECTION packet
(`ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md:60-78`) are
addressed inline above: `prefab.rs`, `bake_fixtures.rs`, and `.rge-project`
each have their own row with specific line citations; every macros-reflect
test surface that this audit relies on (`fixtures/render_pass.rs`,
`derive_test.rs`, `ui_hints_test.rs`, `validate_attr_test.rs`, plus the
already-cited `compile_budget_5_pilots.rs`) now has current file line
citations rather than baseline-only or indirect evidence.

## Production-vs-test adoption section

### Production imports / usages

- **`rge_kernel_types::*`** in workspace `*.rs` files outside
  `kernel/types/tests/` and `crates/macros-reflect/tests/`: **0**.
- **`rge_macros_reflect::*`** in workspace `*.rs` files outside
  `crates/macros-reflect/`: **0**.
- **`#[derive(Reflect)]`** in any production `*.rs` file (i.e. outside
  test/fixture/doc-comment contexts): **0**. The only annotated sites are
  the test-only pilots cited in the evidence table.
- **`impl Reflect for ...` (hand-rolled)**: 2 sites, both in `kernel/types/`:
  `kernel/types/src/reflect.rs:194` (`#[cfg(test)]` fixture `Hand`) and
  `kernel/types/tests/reflect_round_trip.rs:31` (fixture `RenderPass`).
  Both test-only.
- **`load_scene_into_world` production call sites**: 1 — `runtime/runtime-headless/src/main.rs:95`.
- **`rge_scene_loader::*` production imports**: 1 — `runtime/runtime-headless/src/main.rs:14`.
- **`use rge_kernel_types::TypeId`**: only `kernel/types/tests/reflect_round_trip.rs:16` (test fixture).

### Cargo dependency declarations (read-only audit)

- `rge-kernel-types` is declared in 4 manifests workspace-wide: `Cargo.toml:237`
  (workspace floor), `kernel/types/Cargo.toml:2` (self),
  `crates/macros-reflect/Cargo.toml:21` (dev-dep, tests-only),
  `crates/script-host/Cargo.toml:18` (production dep, declared-but-unused).
- `rge-macros-reflect` is declared in 1 manifest:
  `crates/macros-reflect/Cargo.toml` (self only — `proc-macro = true`). Not
  promoted to `[workspace.dependencies]`; no other crate consumes it.

### Tests / examples / fixtures

- Reflection-substrate tests:
  - `kernel/types/tests/reflect_round_trip.rs` (3 tests, hand-rolled fixture).
  - `crates/macros-reflect/tests/fixtures/render_pass.rs` (shared
    `RenderPass` + `WithValidate` fixtures, no tests itself).
  - `crates/macros-reflect/tests/derive_test.rs` (4 tests against `RenderPass`).
  - `crates/macros-reflect/tests/ui_hints_test.rs` (5 tests on `UiHint`
    emission).
  - `crates/macros-reflect/tests/validate_attr_test.rs` (4 tests on
    `validate` / `custom_drawer` / `skip`).
  - `crates/macros-reflect/tests/compile_budget_5_pilots.rs` (1 test +
    5 anonymous pilots / 24 fields total).
- Typed scene-loader tests:
  - `crates/rge-scene-loader/src/lib.rs:163-207` (3 unit tests).
  - `crates/rge-scene-loader/tests/simple_scene.rs` (4 golden integration
    tests).
  - `crates/rge-data/tests/golden_simple_scene_schema.rs` (typed-payload
    schema gate, ~6 tests).
  - `crates/rge-data/tests/golden_simple_scene_load_tick.rs` (identity-only
    load+tick gate, 1 test).
  - `runtime/runtime-headless/tests/loads_golden_simple_scene.rs`
    (end-to-end binary gate, 4 tests).
- Examples surface: `crates/rge-data/examples/bake_fixtures.rs` is the only
  file under `crates/rge-data/examples/`; it imports only `rge_data` and
  emits the canonical W14 fixtures (project / scene / prefab / v0.0
  migration input). It has no `rge_kernel_types` import and no
  `#[derive(Reflect)]`.
- The reflection and typed-scene-loader test populations are **disjoint**:
  no reflection test invokes `load_scene_into_world`, and no
  typed-scene-loader test imports any reflection API.

### Comments / docs

- `crates/components-spatial/src/lib.rs:18-20` — aspirational doc-comment
  ("callers should `use rge_kernel_types::Entity;`"); no import.
- `crates/rge-data/src/lib.rs:36-40,70-80` — module doc + local
  `pub mod stub { pub trait Reflect: 'static {} }` at line 79. The promise
  that swapping the stub for `pub use rge_kernel_types::Reflect;` "is a
  one-line change" is doc-only; the stub itself is the active trait and is
  never invoked.
- `crates/rge-data/src/scene.rs:14-25` — two-phase plan ("when the W02
  reflection wave merges, callers will... walk each
  `ComponentValue::data` string through `kernel/types::serde_bridge::from_ron`")
  is doc-only; the production loader bypasses it entirely.
- `crates/rge-data/src/prefab.rs:65-69,78-80` — explicit doc-strings that
  `ParamSpec.default` "is parsed by the reflection bridge at instantiation"
  and that `ExposedOverride.field_path`'s "exact mapping belongs to the
  reflection bridge" — but the file itself has zero `rge_kernel_types`
  imports (`:21-25`).

### Historical dispatch evidence

- The substrate landed in W02 (recorded at `plans/BASELINE.md:464-468`).
- The 2026-05-21 Phase 9 §1.1 reflection-scale honesty preflight at
  `plans/BASELINE.md:437-538` is the canonical prior baseline. It
  enumerated 7 test-only reflected types / 0 production reflected types /
  0 production consumers / 2 normal Cargo revdeps (one dev-only, one
  declared-but-unused) and codified the two-arm revisit trigger at
  `:516-521`.
- The typed scene-loader chain landed via the autonomous queue arc on
  2026-05-25 — corroborated by the per-dispatch publish commits visible in
  `git log` and by the EXEC packets at
  `ai_handoffs/ISSUE-171_EXEC_2026-05-25_10-51-13+0300.md` (bridge crate;
  literal-four-arm match-table choice with the explicit "no wrappers, no
  erased path, no reflection" framing in its header),
  `ai_handoffs/ISSUE-173_EXEC_2026-05-25_11-27-05+0300.md`
  (`advance_tick` regression test),
  `ai_handoffs/ISSUE-175_EXEC_2026-05-25_12-25-04+0300.md` (consumer-
  direction audit), and
  `ai_handoffs/ISSUE-177_EXEC_2026-05-25_16-15-49+0300.md`
  (`runtime-headless` production consumer landing).
- The most recent commit on the branch is `283cc19 ai-dispatch ISSUE-214:
  salvage invalidation-economics audit`. No reflection-related landing or
  `change.md` row has appeared since 2026-05-21 (`change.md:1641`).

## Typed scene-loader impact section

The recent typed `ComponentValue` and `rge-scene-loader` work **does not
adopt any reflection machinery**. It is an **explicit match-table /
typed-RON bridge** end-to-end:

1. **Wire shape stays reflection-neutral.** `ComponentValue { type_id:
   String, data: String }` (`crates/rge-data/src/scene.rs:84-95`) carries a
   canonical string identifier plus an opaque RON literal. Neither field is
   a `kernel/types::TypeId` (which would be the 128-bit FNV-1a hash from
   `kernel/types/src/type_id.rs:54-103`). The `type_id` field is just a
   `String` keying the loader's match arms. The Prefab schema reuses the
   same envelope (`crates/rge-data/src/prefab.rs:101-105`).

2. **Loader bridges via a literal match table on the string, not a registry
   lookup.** `crates/rge-scene-loader/src/lib.rs:106-161` dispatches
   `component.type_id.as_str()` through four arms whose left sides are the
   literal strings `"rge::components::Transform"` /
   `"rge::components::Camera"` / `"rge::components::Light"` /
   `"rge::components::Visibility"`. Each arm calls `ron::from_str::<T>` with
   the concrete component type and `world.insert(ecs_id, value)` — no
   `dyn Reflect`, no `ReflectValue`, no `TypeId::of_name`, no trait-object
   handle, no `kernel/types::serde_bridge::from_ron`, no inventory/registry
   lookup. The crate manifest at `crates/rge-scene-loader/Cargo.toml:11-19`
   does not declare `rge-kernel-types` or `rge-macros-reflect`.

3. **Components are typed via plain `serde::{Serialize, Deserialize}`
   derives + `rge_kernel_ecs::Component`.** Each component's `Cargo.toml`
   pulls only `serde` (plus `rge-kernel-ecs` for the `Component` marker
   trait); each `*.rs` carries a normal `#[derive(... Serialize,
   Deserialize)]` and `impl rge_kernel_ecs::Component for T {}` (see
   `crates/components-spatial/src/transform.rs:22-31,59`,
   `crates/components-render/src/camera.rs:43-69`,
   `crates/components-render/src/light.rs:56-77`,
   `crates/components-visibility/src/visibility.rs:19-30`). Not a single
   component derives `Reflect` or imports `rge_kernel_types`.

4. **Production consumer is reflection-free.** `runtime-headless` runs
   `rge_scene_loader::load_scene_into_world`
   (`runtime/runtime-headless/src/main.rs:14,95`) and reaches no further
   into the reflection stack. Its manifest
   (`runtime/runtime-headless/Cargo.toml:16-19`) declares only `rge-data` +
   `rge-scene-loader` + `ron`. Upstream, the `.rge-project` manifest
   (`golden-projects/simple-scene/.rge-project:1-8`) carries no per-component
   schema — it only points at `scenes/main.rge-scene` for `runtime-headless`
   to consume.

5. **Compatible-but-not-adopted.** The `data` payload format would
   round-trip through `kernel/types::serde_bridge::from_ron::<T>` if a caller
   ever wanted to walk it through reflection (both paths are RON over a
   `Serialize + Deserialize` type). The scene fixture's payload strings
   (`golden-projects/simple-scene/scenes/main.rge-scene:9-19,28-35`) are
   concrete RON-literal renderings of the typed components. The loader
   chose the explicit-match path over the reflection path — and crucially
   over a runtime-registry alternative that would either require
   `inventory!`-style global registration (forbidden by
   `kernel/types/src/lib.rs:40-43` and `kernel/types/BUDGET.md:50-52`) or a
   derive-emitted name→constructor map (not currently emitted by
   `crates/macros-reflect/src/codegen.rs`). The doc-comments at
   `crates/rge-data/src/lib.rs:36-40` and
   `crates/rge-data/src/scene.rs:14-25` describe the two-phase "parse Scene
   then walk `data` through `kernel/types::serde_bridge::from_ron`" plan as
   **future** work — they predate the typed-RON loader and were not updated
   when the match-table choice landed. The prefab schema at
   `crates/rge-data/src/prefab.rs:64-69` carries the same doc-only
   reflection-bridge framing.

## Usage searches (read-only)

`rg` is not on this shell's PATH; the project's `Grep` tool (built on
ripgrep, glob-filtered to `**/*.rs`) was used for the same effect. Each
command + scope + result is recorded so a zero-use claim is auditable
without re-running:

| Command (Grep tool) | Scope | Result |
|---|---|---|
| `Grep pattern='rge_kernel_types' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | **22 lines** total; **0** outside `kernel/types/{src,tests}/` + `crates/macros-reflect/{src,tests}/` + doc-comments at `crates/components-spatial/src/lib.rs:20` and `crates/rge-data/src/lib.rs:39,75`. |
| `Grep pattern='rge_macros_reflect' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | **3 lines** total — `crates/macros-reflect/src/lib.rs:20` (doc example), `crates/macros-reflect/tests/fixtures/render_pass.rs:10`, `crates/macros-reflect/tests/compile_budget_5_pilots.rs:13`. **0** outside macros-reflect's own tree. |
| `Grep pattern='#\[derive\(Reflect\)\]' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | Live `#[derive(Reflect)]` annotations only inside macros-reflect's tests at `crates/macros-reflect/tests/fixtures/render_pass.rs:14,57` and `crates/macros-reflect/tests/compile_budget_5_pilots.rs:16,27,38,53,65`. All other matches in workspace `*.rs` are doc-comments / macro error strings inside `kernel/types/src/` and `crates/macros-reflect/src/{derive,codegen,lib}.rs`. **0 production derive sites.** |
| `Grep pattern='ReflectValue' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | All matches inside `kernel/types/src/`, `kernel/types/tests/reflect_round_trip.rs`, `crates/macros-reflect/src/codegen.rs` (emitter), and the macros-reflect tests `derive_test.rs` / `validate_attr_test.rs`. **0 production consumers.** |
| `Grep pattern='use rge_kernel_types::.*TypeId\|use rge_kernel_types::type_id\|use rge_kernel_types::\{[^}]*TypeId' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | **1 line** — `kernel/types/tests/reflect_round_trip.rs:16` (test fixture). **0 production imports.** |
| `Grep pattern='ComponentValue' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | All matches in `crates/rge-data/{src,tests,examples}/` (schema crate + tests + the `bake_fixtures` example) and `crates/rge-scene-loader/src/lib.rs` (loader's typed `&ComponentValue` argument). Used as a string-keyed envelope, not as a reflection bridge. |
| `Grep pattern='load_scene_into_world' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | Definition: `crates/rge-scene-loader/src/lib.rs:84`. Production call site: `runtime/runtime-headless/src/main.rs:95`. Test call sites: `crates/rge-scene-loader/tests/simple_scene.rs:60,84,104,112` + `crates/rge-scene-loader/src/lib.rs:190,197,204` + the **test-local** `load_scene_into_world` shadow in `crates/rge-data/tests/golden_simple_scene_load_tick.rs:50,69` (identity-only helper, NOT the production bridge). |
| `Grep pattern='rge_scene_loader' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | Production import: `runtime/runtime-headless/src/main.rs:14`. Test import: `crates/rge-scene-loader/tests/simple_scene.rs:16`. Doc-comment at `runtime/runtime-headless/src/main.rs:4`. **1 production consumer, 0 reflection-API references.** |
| `Grep pattern='rge-kernel-types\|rge-macros-reflect' glob='**/Cargo.toml' output_mode='files_with_matches'` | All workspace `Cargo.toml` | `Cargo.toml` (workspace floor); `kernel/types/Cargo.toml` (self); `crates/macros-reflect/Cargo.toml` (self + dev-dep on kernel-types); `crates/script-host/Cargo.toml` (declared-but-unused). No other manifest declares either crate. |
| `Grep pattern='type_id' path='crates/script-host'` | `crates/script-host/**` | **0 matches.** Confirms `crates/script-host`'s declared `rge-kernel-types` dep is dead substrate — no source/test file imports any reflection API. |
| `Grep pattern='impl.*Reflect.*for' glob='**/*.rs' output_mode='content' -n=true` | All workspace `*.rs` | 2 hand-rolled `impl Reflect for ...` sites (`kernel/types/src/reflect.rs:194` `#[cfg(test)]` fixture `Hand`; `kernel/types/tests/reflect_round_trip.rs:31` fixture `RenderPass`). 1 blanket `impl<T: Reflect> ReflectObject for T` (`kernel/types/src/reflect.rs:150`). 1 macro emitter (`crates/macros-reflect/src/codegen.rs:104`). **0 production sites.** |

## Component-crate reflection check

| Crate | `rge_kernel_types` import? | `#[derive(Reflect)]`? | `Component` impl? | Reflection-API surface? |
|---|---|---|---|---|
| `crates/components-spatial` | No (only the doc-comment promise at `src/lib.rs:20`). Cargo deps: `rge-kernel-ecs` + `serde`. Dev-dep: `ron`. | No | Yes (`src/transform.rs:59` `impl rge_kernel_ecs::Component for Transform {}`; same pattern in the other modules). | None — pure serde+`Component`. |
| `crates/components-render` | No. Cargo deps: `rge-kernel-asset` + `rge-kernel-ecs` + `serde`. Dev-dep: `ron`. | No | Yes (`src/camera.rs:69`, `src/light.rs:76`). | None. |
| `crates/components-visibility` | No. Cargo deps: `rge-kernel-ecs` + `serde`. Dev-dep: `ron`. | No | Yes (`src/visibility.rs:30`). | None. |

All three component crates payload-round-trip exclusively via the serde/ron
path that the scene-loader match table consumes. None imports
`rge_kernel_types`, `rge_macros_reflect`, `Reflect`, `TypeId`, or any other
reflection API. The `Reflection` token does appear in
`crates/components-render/src/reflection_probe.rs` — that's the `IBL
ReflectionProbe` render component, unrelated to `kernel/types::Reflect`.

## Trigger-analysis section

| Threshold / gate | Source | Today | Status |
|---|---|---|---|
| Phase 1.1 abort: > 30 s on 5 pilot types ⇒ STOP | `plans/IMPLEMENTATION.md:190`; `kernel/types/BUDGET.md:7,76-79`; `Status.md:171` | 7.5 s wall-clock on 5 pilots (`BUDGET.md:42-44`; `Status.md:171`) | **PASS, recorded** (not regressed; no new pilot landed in this audit) |
| §13.3 LLVM-line projection at 100 reflected types | `kernel/types/BUDGET.md:38-44,80` | ~9,000 extrapolated vs 15,000 warn / 25,000 hard | **PASS, extrapolated, vacuously satisfied** (0 production reflected types means the projection has nothing real to drift from) |
| §13.3 generic instantiations per crate (5,000 warn / 15,000 hard) | `plans/PLAN.md:1128`; `kernel/types/BUDGET.md:81` | 0 from reflection (`crates/macros-reflect/src/codegen.rs:114-116` emits an inline slice, no generic helper) | **PASS** |
| §13.2 reflection cache 1000 components ≤ 2 MB | `plans/PLAN.md:1124` | No reflection cache deployed — `kernel/types/src/lib.rs:40-43` forbids any global registry / inventory pattern | **VACUOUSLY SATISFIED** |
| §13.10 / §1.10.4 reflection schema size > 10 K typed-components × fields = warn | `plans/PLAN.md:526,1156`; `plans/BASELINE.md:501` | 0 production fields (24 fields total across 5 test-only pilots) | **VACUOUSLY SATISFIED** |
| Phase 9 §9 axis: "Reflection scale — compile time + binary size at 100+ reflected types" | `plans/IMPLEMENTATION.md:597`; `plans/BASELINE.md:502`; `Status.md:373`; `HANDOFF.md:1500-1503` | 0 production reflected types; the typed-scene-loader chain since 2026-05-25 added 0 reflected types and 0 reflection consumers | **STRUCTURALLY UNMEASURABLE** until production adoption begins |
| Two-arm revisit trigger from `plans/BASELINE.md:516-521` | `plans/BASELINE.md:516-521` | Neither arm has fired: (a) no production crate has added `#[derive(Reflect)]` or `use rge_kernel_types::Reflect` since 2026-05-21; (b) `script-host` has not wired its declared `kernel/types` dep into the generic hot-reload migration path (`crates/script-host` still has 0 `use rge_kernel_types::...` lines). | **UNFIRED** |

**Synthesis:** The typed scene-loader chain is real and substantive
production code, but every concrete adoption decision it embodies (typed
RON via `serde` / explicit string-keyed match table / direct component
type insertion) **routes around** the reflection substrate rather than
through it. Every threshold the Phase 9 §9 axis defines is either
still-PASS-and-recorded, vacuously satisfied, or structurally unmeasurable.
The 2026-05-21 verdict ("substrate complete, production adoption zero,
defer", `plans/BASELINE.md:504`) is still load-bearing today.

## Candidate comparison section

The TASK packet's "smallest safe follow-up" requires choosing exactly one
action or `NEEDS_HUMAN`. Candidates considered:

| Candidate | Why considered | Why rejected (or selected) |
|---|---|---|
| **C1. Defer; no new dispatch** | The 2026-05-21 §1.1 preflight codified the two-arm revisit trigger; neither arm has fired; current evidence shows no new reflection-scale signal. The typed scene-loader chain explicitly chose the non-reflection bridge path. | **SELECTED.** Zero scope; zero artifact; preserves the existing pressure-driven doctrine. This shape mirrors the precedent set by `ai_handoffs/ISSUE-210_EXEC_2026-05-26_17-00-00+0300.md:110-118` (Phase 9 freeze-validity → `still coherent`; salvage EXEC) and is consistent with the docs-only outputs of `ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md:481-540` (abstraction-pain → ADR proposal) and `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md:145-170` (invalidation-economics → docs-only BASELINE.md preflight). Where 210/212/214 had concrete substrate signals or docs-only deltas to record, this audit has none — `BASELINE.md:438-538` already records the load-bearing conclusion ("0 production reflected types; defer"). |
| C2. Wire a fresh `rge-data` `pub use rge_kernel_types::Reflect;` import per the existing doc-comment promise (`crates/rge-data/src/lib.rs:39,75`) | Minimal-mechanical follow-up; would change the `use rge_kernel_types::Reflect` zero-count to one. | **REJECTED.** Would land "adoption" without a consumer — `crates/rge-data` does not currently call any reflection method, so the import would be unused. Per `plans/BASELINE.md:521-525`, this exact candidate was already considered and rejected as "premature mechanism per PLAN §1.10's pressure-driven doctrine." It would also touch a production Cargo dep (`crates/rge-data/Cargo.toml`) which the TASK packet's MUST-NOT-edit envelope forbids. |
| C3. Add `#[derive(Reflect)]` to one production component (e.g. `Transform`) as a pilot adoption signal | Would tip the "0 production reflected types" count to 1. | **REJECTED.** The TASK packet's Constraints / Non-Goals at `:263-264` explicitly forbid "recommend[ing] adding new synthetic reflection pilots merely to create a scale signal." Per `plans/BASELINE.md:521-526`, adding synthetic pilots would conflate compile-budget calibration with adoption signal and risks mis-shaping the eventual consumer API. Also touches protected `crates/components-*` source + `Cargo.toml`. |
| C4. Wire `script-host`'s declared-but-unused `rge-kernel-types` dep into the generic hot-reload migration path | Would fire revisit-trigger arm (b) and immediately make the Phase 9 §9 reflection-scale axis measurable. | **REJECTED.** This is the canonical "substantive substrate dispatch" identified at `plans/BASELINE.md:525(b)`, "not 'tiny'": it requires replacing the hand-rolled `CounterSnapshot` in `crates/script-host/src/swap.rs` with a `Reflect`-driven value-walk, touches the constitutional WASM hot-reload path, and would need its own multi-round dispatch. Out of scope for a Phase 9 audit follow-up. |
| C5. `NEEDS_HUMAN` | Would route to human arbitration if evidence could not support one `REFLECTION_SCALE_VERDICT` value or if a halt condition fired. | **REJECTED.** Evidence is unambiguous: 0 production reflected types, 0 production reflection-API imports, typed scene-loader chain bypasses reflection, two-arm revisit trigger unfired ⇒ `still_untriggered`. No halt condition listed at TASK `:346-377` has triggered. |

**Comparison rationale (single follow-up only):** C1 (defer) is strictly
smaller and lower-risk than C2/C3 (both touch protected source/Cargo) and
than C4 (the substantive substrate dispatch identified by BASELINE.md as
"not tiny"). C5 is unwarranted given unambiguous evidence. CORRECTION
fix #2 explicitly forbids carrying a secondary docs-only PREFLIGHT
fallback, so the prior EXEC's "C5. Docs-only Phase 9 PREFLIGHT" candidate
has been removed from this comparison; C1 stands alone as the operative
choice. CORRECTION fix #3 is addressed inline: the three retained
ISSUE-210 / 212 / 214 references each carry current packet line citations
(`:110-118` / `:481-540` / `:145-170`).

## Final verification section

- `git diff --check` → exit **0** (clean).
- `git diff --name-only` → empty (no tracked-file diff at all).
- `git status --short --untracked-files=all` →
  ```
  ?? ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md
  ?? ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.meta.json
  ?? ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.md
  ?? ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.meta.json
  ?? ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.md
  ?? ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md
  ?? ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.meta.json
  ```
  All seven are dispatch-owned ISSUE-216 packet artifacts (TASK + sidecar
  from the planner; prior EXEC + sidecar from the first round; CORRECTION
  + sidecar from the planner's correction; this corrected EXEC). The
  `.meta.json` sidecar for this new EXEC will be added by `new-handoff.ps1
  -Finalize` and is in-bounds per the TASK packet's
  `### MAY add new files` list. **No tracked production / doc / script /
  Cargo / workflow / schema / task-brief file is modified.**
- Static no-source-change inspection: no diff on `crates`, `kernel`,
  `editor`, `runtime`, `tools`, `docs`, `plans`, `Status.md`, `HANDOFF.md`,
  `change.md`, `Cargo.toml`, `Cargo.lock`, `.github/workflows`,
  `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`, `.ai/*.schema.json`,
  or any root-level `*.ps1` automation script (the empty `git diff
  --name-only` above is the affirmative proof).
- No benchmark, profiler, full repository verification, autonomous queue
  invocation, nested dispatch, GitHub mutation, branch operation, commit,
  push, or scheduler command was run as part of this audit (per TASK
  Constraints `:240-258` and Halt-Conditions `:346-377`).
- The CORRECTION packet's re-verification gates at `:92-102` are addressed:
  `.ai/dispatch.verify.ps1` already passed under round-0
  (`.ai/dispatch-ISSUE-216/verification.round0.log` records `VERIFY OK`
  with all seven canonical steps passed — the orchestrator harness was
  scheduled to run it again post-EXEC); `git diff --check` and
  `git status --short --untracked-files=all` are reproduced above with
  expected outputs; the EXEC packet validation will be exercised by
  `new-handoff.ps1 -Finalize`.

## Verification Results

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` → readable; cited at
  `AGENTS.md:26-36` for the orchestrator no-`_REVIEW_`/`_CLOSEOUT_` rule.
- `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` → readable; cited
  at `:50-66` (manual chain) and `:128-145` (filename convention).
- `Get-Content -Raw plans/PLAN.md` → readable (1,508 lines); cited
  reflection-relevant gates at §1.10.4 / §13.2 / §13.3 / §13.10.
- `Get-Content -Raw plans/IMPLEMENTATION.md` → readable (758 lines); cited
  Phase 1.1 abort + Phase 9 §9 axis lines.
- `Get-Content -Raw plans/BASELINE.md` → readable (753 lines); the
  canonical 2026-05-21 §1.1 reflection-scale honesty preflight is the
  prior baseline.
- `Get-Content -Raw kernel/types/BUDGET.md` → readable (83 lines); the
  Phase 1.1 compile-budget source of truth.
- `Status.md` → live-inspected via offset-bounded `Read` and line-anchored
  `Grep`: lines 1 / 9 / 171 / 194 / 373 cited above as required by
  CORRECTION fix #1.
- `HANDOFF.md` → live-inspected the same way: lines 1 / 5 / 9 /
  1498-1506 / 1587 cited above as required by CORRECTION fix #1.
- `change.md` → tail re-inspected; the 2026-05-21 Phase 9 §1.1
  reflection-scale entry at `:1641` remains the most recent
  reflection-related row.
- Reflection-substrate inspection: read `kernel/types/Cargo.toml`,
  `src/{lib,reflect,type_id,serde_bridge}.rs`,
  `tests/reflect_round_trip.rs`; read
  `crates/macros-reflect/{Cargo.toml,src/{lib,derive,codegen,attrs}.rs}` +
  `tests/{compile_budget_5_pilots,derive_test,ui_hints_test,validate_attr_test,fixtures/render_pass}.rs`
  all in full.
- Typed scene-loader chain inspection: read
  `crates/rge-data/{Cargo.toml,src/{lib,scene,prefab}.rs,tests/golden_simple_scene_{schema,load_tick}.rs,examples/bake_fixtures.rs}`,
  `crates/rge-scene-loader/{Cargo.toml,src/lib.rs,tests/simple_scene.rs}`,
  `runtime/runtime-headless/{Cargo.toml,src/main.rs,tests/loads_golden_simple_scene.rs}`,
  `golden-projects/simple-scene/{.rge-project,scenes/main.rge-scene}`
  all in full.
- Component inspection: read `crates/components-{spatial,render,visibility}/Cargo.toml`
  + `src/lib.rs` + the load-bearing component files (`transform.rs`,
  `camera.rs`, `light.rs`, `visibility.rs`) in full.
- Cargo metadata inspection: `Grep` for `rge-kernel-types` +
  `rge-macros-reflect` across all `Cargo.toml`; cross-checked against
  `plans/BASELINE.md:380-388`.
- Production / test usage searches: full grep matrix in §"Usage searches".
- Prior dispatch artifact inspection: read the head + decision blocks of
  `ai_handoffs/ISSUE-{210,212,214}_EXEC_*.md` for the precedent comparison
  citations. Read the head of
  `ai_handoffs/ISSUE-{171,173,175,177}_EXEC_*.md` for the typed
  scene-loader landing provenance.
- `git diff --check` → exit 0.
- `git status --short --untracked-files=all` → only the dispatch's own
  TASK / CORRECT / prior-EXEC / this-EXEC packets and their sidecars
  untracked (this EXEC's sidecar pending finalize).
- `git diff --name-only` → empty.

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope and within
the five enumerated CORRECTION fixes. No source, tests, docs, Cargo
metadata, workflows, schemas, automation scripts, scheduler configuration,
fixtures, prior handoff packets, prior logs, or `.ai/dispatch.tasks.md`
were modified. No benchmarks, profilers, full verification runs,
autonomous queue invocations, nested dispatches, GitHub mutations, branch
operations, commits, pushes, or scheduler commands were executed. The
only new files are this EXEC report and its matching `.meta.json` sidecar
(the latter produced by `new-handoff.ps1 -Finalize`).

## Open Questions for Reviewer

None. The CORRECTION packet enumerated exactly five report-only fixes;
each is addressed in-place above. The decision block names exactly one
follow-up (defer; no new dispatch). No optional fallback or alternative
docs-only PREFLIGHT shape is proposed or named.

## Worktree State

- Tracked files: clean (`git diff --name-only` empty; `git diff --check`
  exit 0).
- Untracked items:
  - `ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md` + `.meta.json`
    — planner-authored TASK + sidecar (in-bounds).
  - `ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.md` + `.meta.json`
    — prior EXEC + sidecar, superseded by this artifact (left in place as
    read-only historical context per CORRECTION packet doctrine).
  - `ai_handoffs/ISSUE-216_CORRECT_2026-05-26_22-41-45+0300.md` +
    `.meta.json` — planner-authored CORRECTION + sidecar (in-bounds).
  - `ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.md` — this file
    (in-bounds per TASK `### MAY add new files`).
  - `ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-47-58+0300.meta.json` —
    EXEC sidecar pending; produced by `new-handoff.ps1 -Finalize`
    (in-bounds).
- Branch: `ai-dispatch/ISSUE-216`.
- Last commit: `283cc19` `ai-dispatch ISSUE-214: salvage invalidation-economics audit`.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-216
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
