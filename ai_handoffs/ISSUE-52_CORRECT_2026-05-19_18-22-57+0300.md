# Correction Packet

DISPATCH_ID: ISSUE-52
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_18-22-57+0300
RELATED_FILES:
- crates/material-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-52_TASK_2026-05-19_18-18-52+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-52_EXEC_2026-05-19_18-22-24+0300.md`
- Verification Gate Failure: post-execution `.ai/dispatch.verify.ps1` run started 2026-05-19T18:22:56.2653575+03:00 failed at `cargo +nightly fmt --all -- --check` with `Incorrect newline style in \\?\A:\RCAD\RGE\crates\material-graph\src\lib.rs`.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more.

1. **Normalize material-graph newline style** - Verification gate failure in `cargo +nightly fmt --all -- --check`. Required change: normalize `crates/material-graph/src/lib.rs` to the newline style expected by rustfmt for this repository, without changing Rust code semantics, declarations, test assertions, public API, or comments. Acceptance: `cargo +nightly fmt --all -- --check` exits 0, and the only material-graph change is newline-style normalization needed for rustfmt.

## Deferred Findings (NOT Approved for This Round)

None. The verification failure supplied for this correction round contains only the newline-style issue above.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this additional correction-round requirement: the canonical post-execution verification gate must no longer fail on newline style in `crates/material-graph/src/lib.rs`.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

If `cargo` is not on `PATH`, the Executor may use the repository's established Rust toolchain location `A:\RustCache\cargo\bin\cargo.exe`, matching `.ai/dispatch.verify.ps1`.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus these correction-specific halt conditions:

- Halt if fixing the newline-style failure requires semantic Rust changes, API changes, manifest changes, lockfile changes, generated-file changes, or edits outside `crates/material-graph/src/lib.rs`.
- Halt if `cargo +nightly fmt --all -- --check` reports additional required changes outside the originally modified `crates/anim-graph/src/lib.rs` test-module work and this newline-style correction.
- Halt if `.\.ai\dispatch.verify.ps1` fails after the newline-style correction for any reason not directly caused by this correction.

## Planner Notes

This correction is limited to unblocking the canonical verification gate. The original ISSUE-52 implementation scope remains test-only for `crates/anim-graph/src/lib.rs`; this packet authorizes only the minimal `crates/material-graph/src/lib.rs` newline-style normalization required by rustfmt. Do not broaden the dispatch into material-graph behavior, graph-foundation behavior, architecture policy, dependency, manifest, or documentation work.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-52
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
