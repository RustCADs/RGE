# Execution Report

DISPATCH_ID: ISSUE-412
AUTHOR: Executor / Codex
TIMESTAMP: 2026-06-16_01-55-23+0300
RELATED_FILES:
- .ai/dispatch.tasks.md - recorded the task-157 human-decision marker and immediately following recommendation block
- ai_handoffs/ISSUE-412_TASK_2026-06-16_01-44-25+0300.md - read-only source for dispatcher snapshot evidence and task scope
- crates/editor-shell/src/lifecycle/commands.rs - read-only source audit of stale tracked-CAD cleanup and inspection boundaries
- crates/editor-shell/src/lifecycle/tests.rs - read-only source audit of focused lifecycle coverage
- crates/editor-shell/src/lifecycle/mod.rs - read-only source audit of CAD shell fields/re-export/render-bound helpers
- crates/cad-projection/src/lib.rs - read-only source audit of projection despawn and BRep entity behavior
- crates/cad-projection/src/render_adapter.rs - read-only source audit of render_mesh_for gates
- crates/cad-projection/src/projection_structural/mod.rs - read-only source audit of BRepHandle semantics
- Status.md - read-only provenance audit
- HANDOFF.md - read-only provenance audit
- plans/BASELINE.md - read-only provenance audit
- change.md - read-only provenance audit
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-412_TASK_2026-06-16_01-44-25+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## Gated-Audit Instruction Carried Forward

This EXEC report preserves the task-157 gated-audit instruction: record exactly
one `NEEDS_HUMAN_RECORDED` marker in `.ai/dispatch.tasks.md`, do not append task
158, do not append a feature or implementation task, and leave any next feature
behind a human approval decision.

## Current-State Claims: Falsification Re-run

- Claim: `.ai/dispatch.tasks.md` had task 156 and task 157 headings and no task
  158 heading before the marker edit.
  Re-run:
  `rg -n "^156\.|^157\.|^158\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
  -> exit 0; matched task 156 at line 15526 and task 157 at line 15655, plus
  historical/instructional `NEEDS_HUMAN_RECORDED` references. No line beginning
  with `158.` was returned before edits.

- Claim: the dispatcher snapshot can be cited without live GitHub/network
  access.
  Re-run:
  `Select-String -Path ai_handoffs\ISSUE-412_TASK_2026-06-16_01-44-25+0300.md -Pattern "2026-06-16T01:43:55.6697824\+03:00|Open `ai-dispatch` issues before ISSUE-412 was created|Open failed autonomous issues|Autonomous issues already filed|no dispatcher-snapshot evidence"`
  -> exit 0; matched the snapshot timestamp `2026-06-16T01:43:55.6697824+03:00`,
  the open failed/autonomous issue lines, the already-filed autonomous issue
  line, and the "no dispatcher-snapshot evidence" line. No `gh`, browser,
  network, GitHub API, or web lookup was run.

- Claim: current lifecycle source contains the post-cleanup helper and tracked
  CAD inspection fields that motivate the audit.
  Re-run:
  `rg -n "clear_stale_tracked_cad_entity|CadSceneInspection|cad_scene_inspection|tracked_cad_entity|add_cad_cuboid_to_empty_scene" crates/editor-shell/src/lifecycle`
  -> exit 0; matched `CadSceneInspection`, the tracked-CAD presence/liveness/
  render-mesh fields, `clear_stale_tracked_cad_entity`, `cad_scene_inspection`,
  `add_cad_cuboid_to_empty_scene`, the lifecycle re-export, and focused tests.

- Claim: current source/tests expose restore/despawn and render-mesh evidence
  relevant to stale tracked-CAD cleanup.
  Re-run:
  `rg -n "restore_to|despawn_brep_entity|render_mesh_for|BRepHandle" crates/editor-shell/src/lifecycle crates/cad-projection/src`
  -> exit 0; matched `CadProjection::despawn_brep_entity`,
  `CadProjection::render_mesh_for`, `BRepHandle`, lifecycle render-bound helpers,
  and focused restore/despawn cleanup tests.

- Claim: adjacent status/provenance docs primarily record the prior
  ISSUE-410/task-155 audit boundary and selected task 156, so this audit needed
  a fresh source read before recommending the next feature.
  Re-run:
  `rg -n "ISSUE-409|ISSUE-410|ISSUE-411|task 154|task 155|task 156|task 157|tracked-CAD|tracked CAD|CadSceneInspection|cad_scene_inspection|clear_stale_tracked_cad_entity" Status.md HANDOFF.md plans/BASELINE.md change.md`
  -> exit 0; matched ISSUE-410/task-155 records and the selected task-156
  helper boundary in `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and
  `change.md`. These docs did not provide the post-ISSUE-411 source result;
  the recommendation was based on the current source/test reads above.

- Claim: the cleanup helper currently has no production caller.
  Falsifying search:
  `rg -n "clear_stale_tracked_cad_entity" crates/editor-shell/src/lifecycle`
  -> exit 0; matched only the helper definition in `commands.rs` and focused
  lifecycle tests in `tests.rs`.

- Claim: the recommendation does not depend on changing CommandBus/undo/dirty/
  save authority.
  Falsifying read/search:
  `rg -n "CommandBus|Action|submit\(|undo_command|redo_command|mark_saved_command" crates/editor-actions/src crates/editor-shell/src/lifecycle/commands.rs`
  -> exit 0; confirmed the existing CommandBus surface remains separate from
  `add_cad_cuboid_to_empty_scene`, which is documented as a headless shell
  entry point rather than a CommandBus action.

## Source/Test/Docs Audit Summary

- `EditorShell::clear_stale_tracked_cad_entity` is present and narrow. It
  returns `false` when no tracked id exists or when the tracked id resolves to a
  live CAD-world entity carrying `BRepHandle`; it clears only `self.cad_entity`
  when the stored id is stale.
- `CadSceneInspection` separately reports stored tracked-id presence,
  CAD-world liveness, and `CadProjection::render_mesh_for` availability.
  Render-mesh absence is inspection data; the cleanup predicate remains
  `BRepHandle` liveness.
- Focused lifecycle tests cover fresh no-op, live first-cuboid no-op, restore to
  `pre_add_head` plus `despawn_brep_entity`, stale tracked-id inspection, and
  helper cleanup. Adjacent add/restore tests also prove projection cleanup can
  leave no render mesh or BRep entity without turning that into a broader
  graph/projection reset policy.
- `CadProjection::render_mesh_for` gates on `BRepHandle` presence and projected
  mesh cache presence, not `brep_owner`; `BRepHandle` remains projection
  bookkeeping, while the entity-to-CAD-node mapping remains in `EntityCadMap`.
- The adjacent docs read as older provenance through ISSUE-410/task 155 and the
  selected task-156 helper; they are not a substitute for the current source
  read and were not edited.
- The dispatcher snapshot used for queue/already-filed-task state is the
  self-contained snapshot in the ISSUE-412 task packet, generated
  `2026-06-16T01:43:55.6697824+03:00`, with no open queue/failed autonomous
  evidence at snapshot time.

## What I Changed

### Source

- No source files were edited.

### Tests

- No test files were edited.

### Docs / Task Brief

- `.ai/dispatch.tasks.md`: appended exactly one new
  `NEEDS_HUMAN_RECORDED: 2026-06-16 - ...` marker at the end of task 157.
- `.ai/dispatch.tasks.md`: immediately followed that marker with a
  `Recommendation for human approval` block covering the proposed next feature,
  exact edit surface, risks, verification, and why it is the smallest coherent
  next step.
- No task 158, feature task, implementation task, or self-rearming follow-up was
  appended.

## Recommendation Recorded

The recorded recommendation asks a human to approve or reject one bounded next
feature: wire `EditorShell::clear_stale_tracked_cad_entity` into the existing
`add_cad_cuboid_to_empty_scene` empty-scene preflight so a stale stored
`cad_entity` alone can be normalized before the current first-cuboid add guard
runs. The recommendation explicitly preserves existing rejection for installed
CAD graph/projection/world state, render-only content, and other partial or
non-empty scenes.

## Per-File Summary

- `.ai/dispatch.tasks.md`: the only tracked edit. Added the task-157
  human-decision marker and recommendation block at the end of the existing
  task-157 audit section.
- `ai_handoffs/ISSUE-412_EXEC_2026-06-16_01-55-23+0300.md`: this current
  dispatch EXEC report.

## Verification Results

- Before edits:
  `rg -n "^156\.|^157\.|^158\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
  -> exit 0; task 156 and task 157 were present, no task 158 heading was
  returned, and the other `NEEDS_HUMAN_RECORDED` hits were historical or
  instructional references.
- Source audit grep:
  `rg -n "clear_stale_tracked_cad_entity|CadSceneInspection|cad_scene_inspection|tracked_cad_entity|add_cad_cuboid_to_empty_scene" crates/editor-shell/src/lifecycle`
  -> exit 0; matched the helper, inspection struct/fields, add entry point,
  re-export, and focused lifecycle tests.
- Projection/restore evidence grep:
  `rg -n "restore_to|despawn_brep_entity|render_mesh_for|BRepHandle" crates/editor-shell/src/lifecycle crates/cad-projection/src`
  -> exit 0; matched the projection despawn/render APIs, BRepHandle source,
  lifecycle render-bound helpers, and restore/despawn tests.
- Docs/task-provenance grep:
  `rg -n "ISSUE-409|ISSUE-410|ISSUE-411|task 154|task 155|task 156|task 157|tracked-CAD|tracked CAD|CadSceneInspection|cad_scene_inspection|clear_stale_tracked_cad_entity" Status.md HANDOFF.md plans/BASELINE.md change.md`
  -> exit 0; matched the prior ISSUE-410/task-155 records and task-156 helper
  boundary in the adjacent docs/provenance files.
- Dispatcher-snapshot read:
  `Select-String -Path ai_handoffs\ISSUE-412_TASK_2026-06-16_01-44-25+0300.md -Pattern "2026-06-16T01:43:55.6697824\+03:00|Open `ai-dispatch` issues before ISSUE-412 was created|Open failed autonomous issues|Autonomous issues already filed|no dispatcher-snapshot evidence"`
  -> exit 0; cited the task-packet snapshot. No live GitHub/network query was
  run.
- After edits:
  `rg -n "^156\.|^157\.|^158\.|NEEDS_HUMAN_RECORDED|Recommendation for human approval" .ai/dispatch.tasks.md`
  -> exit 0; task 156 and task 157 remained present, no task 158 heading was
  returned, and the new marker was immediately followed by
  `Recommendation for human approval`.
- Supplemental marker-count check:
  `rg -n "NEEDS_HUMAN_RECORDED: 2026-|^158\." .ai/dispatch.tasks.md`
  -> exit 0; returned exactly one dated marker, line 15756, and no `158.`
  heading.
- `git diff --name-only`
  -> exit 0; listed only `.ai/dispatch.tasks.md` for tracked changes. Git
  emitted the existing LF-to-CRLF warning for `.ai/dispatch.tasks.md`.
- `git status --short --untracked-files=all`
  -> exit 0; showed `M .ai/dispatch.tasks.md` plus current-dispatch untracked
  artifacts: this EXEC packet, the ISSUE-412 task packet/sidecar, and the
  ISSUE-412 claim JSON. Git also warned that the user-level ignore file was not
  accessible.
- `git diff --check`
  -> exit 0; emitted only the existing LF-to-CRLF warning for
  `.ai/dispatch.tasks.md`.
- Finalize dry-run for this EXEC packet:
  `$exec = Get-ChildItem ai_handoffs\ISSUE-412_EXEC_*.md | Sort-Object LastWriteTime -Descending | Select-Object -First 1; .\new-handoff.ps1 -Finalize -DryRun -PacketPath $exec.FullName`
  -> exit 0; dry-run derived
  `ai_handoffs/ISSUE-412_EXEC_2026-06-16_01-55-23+0300.meta.json` with
  `schema_version: handoff-sidecar-v1`, `packet_type: EXEC`,
  `handoff_status: COMPLETE`, `next_role: REVIEWER_AI`, and `exit_code: 0`.

## Deviations from Task Packet

None - execution stayed strictly within the Task Packet scope. No live GitHub,
browser, network, GitHub API, or web lookup was used. No source, docs, Cargo,
workflow, script, schema, packet-template, or unrelated generated artifact was
edited.

## Open Questions for Reviewer

- Confirm the recorded recommendation is appropriately human-gated and does not
  read as an appended feature task.
- Confirm the proposed next feature is bounded tightly enough: one possible
  caller for the existing helper, with no graph/projection cleanup policy,
  CommandBus/undo/dirty/save authority, UI routing, render refresh, mesh upload,
  GPU work, CAD-core, or cad-projection changes implied.

## Worktree State

- Tracked files:
  - `.ai/dispatch.tasks.md`
- Untracked items observed before EXEC finalization:
  - `ai_handoffs/ISSUE-412_EXEC_2026-06-16_01-55-23+0300.md`
  - `ai_handoffs/ISSUE-412_TASK_2026-06-16_01-44-25+0300.md`
  - `ai_handoffs/ISSUE-412_TASK_2026-06-16_01-44-25+0300.meta.json`
  - `ai_handoffs/claims/ISSUE-412_2026-06-16_01-44-23-9280567+0300_claim.json`
  - `ai_handoffs/ISSUE-412_EXEC_2026-06-16_01-55-23+0300.meta.json` will be
    created if finalization succeeds; the TASK packet allows this current-
    dispatch sidecar.
- Branch: `ai-dispatch/ISSUE-412`
- Last commit: `b94c2ef` Issue 411: clear stale tracked-CAD entity ids

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-412
AUTHOR: Executor / Codex
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
