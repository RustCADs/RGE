# Correction Packet

DISPATCH_ID: ISSUE-62
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_09-23-02+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_anim_graph_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-62_TASK_2026-05-20_09-13-30+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-62_EXEC_2026-05-20_09-22-07+0300.md`
- Latest Review Report: none; post-execution verification failed before a review packet was written.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification finding it
addresses.

1. **Normalize rustfmt newline style** - Post-execution verification failure,
   `cargo +nightly fmt --check`, started 2026-05-20T09:23:01.0705611+03:00:
   "Incorrect newline style in \\?\A:\RCAD\RGE\crates\editor-ui\tests\node_graph_anim_graph_smoke.rs".
   Required change: normalize only the newline style of
   `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs` so rustfmt accepts
   the file. Do not change Rust tokens, assertions, names, comments, or any
   other file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and
   the diff for this file is line-ending-only.

## Deferred Findings (NOT Approved for This Round)

The following findings from the Review Report are NOT approved for this
correction round. They are recorded here as latent risks to be carried
forward in `FINAL_CLOSEOUT`.

1. None. No review findings exist yet; the only blocker is the canonical
   verification failure above.

## Updated Acceptance Criteria

Unchanged from Task Packet, with the additional requirement that the
post-execution canonical verification gate must pass after the newline-style
normalization.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.
- `cargo test -p rge-editor-ui --test node_graph_operator_smoke` -> expected
  exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Additionally, halt if making `cargo +nightly fmt
--all -- --check` pass requires any content change beyond line-ending
normalization of `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs`.

## Planner Notes

The full verification gate stopped at formatting before architecture lints,
supply-chain checks, workspace tests, or doctests could run. This packet
approves only the minimal line-ending repair identified by rustfmt. Do not
reformat unrelated files, do not modify the new ISSUE-62 operator smoke unless
verification reports a direct failure in that file, and do not broaden the
dispatch beyond passing the failed verification gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-62
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
