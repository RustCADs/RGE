# Task Packet

DISPATCH_ID: ISSUE-8
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_00-17-32+0300
RELATED_FILES:
- AI_DISPATCH_AUTOMATION.md
- Invoke-AiDispatchAuto.ps1
- Register-AiDispatchSchedule.ps1
- .ai/dispatch.verify.ps1
- .ai/dispatch.tasks.md
- Get-AiDispatchHealth.ps1
STATUS: OPEN

## Goal

Update GitHub issue #8, "Add missing dispatch component rows." The section 4
"Files & components" table in `AI_DISPATCH_AUTOMATION.md` predates several
dispatch automation components, so add concise rows for the missing scripts and
support files without changing any other section.

## Scope

This is a documentation-only task. It must not change dispatch behavior,
scripts, schemas, source code, workflows, or packet protocol semantics.

### MAY edit

- `AI_DISPATCH_AUTOMATION.md`, only the Markdown table under
  `## 4. Files & components`, and only to add or update rows for these five
  paths:
  - `Invoke-AiDispatchAuto.ps1`
  - `Register-AiDispatchSchedule.ps1`
  - `.ai/dispatch.verify.ps1`
  - `.ai/dispatch.tasks.md`
  - `Get-AiDispatchHealth.ps1`
- `ai_handoffs/ISSUE-8_EXEC_*.md`, only as the required execution report.
- `ai_handoffs/ISSUE-8_EXEC_*.meta.json`, only if produced by finalizing the
  execution report with `new-handoff.ps1`.

### MUST NOT edit

- Any section of `AI_DISPATCH_AUTOMATION.md` outside the section 4
  "Files & components" table.
- Any existing text in `AI_DISPATCH_AUTOMATION.md` except the target table rows
  listed in `MAY edit`; do not reflow unrelated rows, prose, headings, table of
  contents, or porting instructions.
- `Invoke-AiDispatchAuto.ps1`, `Register-AiDispatchSchedule.ps1`,
  `.ai/dispatch.verify.ps1`, `.ai/dispatch.tasks.md`,
  `Get-AiDispatchHealth.ps1`, or any other script/configuration file.
- `AI_DISPATCH_PARALLEL.md`, `AGENTS.md`, `CLAUDE.md`, `HANDOFF.md`,
  `Status.md`, `change.md`, `README.md`, ADRs, schemas, templates, workflows,
  `.gitignore`, `.mcp.json`, Cargo files, Rust source, tests, assets, or
  generated outputs.
- Any existing handoff packet, sidecar, queue log, or `.ai/dispatch-*` run
  directory.

### MAY add new files

- Exactly one execution report matching `ai_handoffs/ISSUE-8_EXEC_*.md`.
- The matching `ai_handoffs/ISSUE-8_EXEC_*.meta.json` sidecar, only if produced
  by finalizing the execution report.

### MUST NOT add new files

- Any file outside the two protocol artifacts explicitly allowed above.

### MUST NOT commit

- Do not stage, commit, merge, push, mutate GitHub issues, or contact the
  network. Leave the documentation edit and execution report uncommitted for
  the queue or human reviewer.

## Deliverables

- The section 4 "Files & components" table in `AI_DISPATCH_AUTOMATION.md`
  contains a concise row for `Invoke-AiDispatchAuto.ps1` describing it as the
  autonomous driver that has Codex select tasks from `.ai/dispatch.tasks.md`
  and run them through the dispatch queue.
- The table contains a concise row for `Register-AiDispatchSchedule.ps1`
  describing it as the Windows Scheduled Task registration/status/removal entry
  point for unattended dispatch automation.
- The table contains a concise row for `.ai/dispatch.verify.ps1` describing it
  as the canonical post-execution verification gate that mirrors CI before
  Codex control and publish.
- The table contains a concise row for `.ai/dispatch.tasks.md` describing it as
  the autonomous task-selection brief/backlog consumed by the autonomous
  driver.
- The table contains a concise row for `Get-AiDispatchHealth.ps1` describing it
  as the read-only dispatch-health readout over retained `.ai/dispatch-*` run
  directories.
- Each new or revised row has a clear Role and a sensible `Port?` value using
  the table's existing vocabulary. Prefer `**Copy/adapt**` for these five
  repo-specific automation components unless local inspection shows a stronger
  fit among the existing values.
- An `EXECUTION_REPORT` documents the exact table change, verification results,
  and final working-tree status.

## Acceptance Criteria

- `AI_DISPATCH_AUTOMATION.md` has exactly one table row for each of the five
  target paths listed in this packet.
- The rows are placed in section 4's existing table and preserve the current
  `| Path | Role | Port? |` format.
- Role text is concise and specific to each component; no row uses vague text
  such as "script" or "file" as the whole role.
- `Port?` values are consistent with the existing table style and do not
  introduce a new legend or explanatory paragraph.
- No other section of `AI_DISPATCH_AUTOMATION.md` changes.
- No repository content file changes except `AI_DISPATCH_AUTOMATION.md`; only
  the allowed ISSUE-8 EXEC protocol artifacts may be added under
  `ai_handoffs/`.
- No commits, pushes, GitHub issue mutations, scheduler registrations, queue
  runs, or autonomous dispatch runs are performed.

## Constraints / Non-Goals

- Do not edit, run, refactor, or audit the five components being documented
  beyond read-only inspection needed to write accurate row text.
- Do not add a new section, subsection, note, command example, parameter table,
  or porting guide text.
- Do not update the table of contents or cross-references.
- Do not change dispatch automation behavior, defaults, locks, scheduling,
  publish policy, verification commands, labels, markers, schemas, templates,
  or packet protocol.
- Do not create `REVIEW`, `CORRECT`, or `CLOSEOUT` packets for this
  orchestrator dispatch.

## Verification Gates

The Executor MUST run and document the result of each gate in the
`EXECUTION_REPORT`:

- `git status --short --branch`
- `Select-String -Path AI_DISPATCH_AUTOMATION.md -Pattern 'Invoke-AiDispatchAuto.ps1','Register-AiDispatchSchedule.ps1','.ai/dispatch.verify.ps1','.ai/dispatch.tasks.md','Get-AiDispatchHealth.ps1'`
  - Expected: each target path appears in the section 4 table.
- `git diff -- AI_DISPATCH_AUTOMATION.md`
  - Expected: the diff is limited to adding or updating rows in the section 4
    "Files & components" table for the five target paths.
- `git diff --check -- AI_DISPATCH_AUTOMATION.md`
  - Expected: exit 0.
- `git diff --name-only -- . ":(exclude)AI_DISPATCH_AUTOMATION.md" ":(exclude)ai_handoffs/ISSUE-8_EXEC_*.md" ":(exclude)ai_handoffs/ISSUE-8_EXEC_*.meta.json"`
  - Expected: no paths introduced by this dispatch outside the allowed docs
    file and protocol artifacts. If pre-existing unrelated paths appear, note
    them separately and do not modify them.
- After writing the EXEC packet:
  `$execPacket = (Get-ChildItem -LiteralPath ai_handoffs -Filter 'ISSUE-8_EXEC_*.md' | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName; .\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`
  - Expected: exit 0.

No cargo, build, test, scheduler, queue, autonomous-driver, GitHub, or network
gate is required for this documentation-only change.

## Halt Conditions

The Executor MUST halt without committing, pushing, or broadening scope and
write an `EXECUTION_REPORT` with `STATUS: BLOCKED` or `STATUS: NEEDS_HUMAN` if
any of the following occur:

- The section 4 "Files & components" table cannot be found in
  `AI_DISPATCH_AUTOMATION.md`.
- Accurate completion appears to require editing any section outside the
  section 4 table or any file other than `AI_DISPATCH_AUTOMATION.md`.
- Existing tracked edits in `AI_DISPATCH_AUTOMATION.md` make a minimal,
  non-destructive table-only patch unsafe or ambiguous.
- Any target row already exists with conflicting meaning that cannot be fixed
  by editing only that target row in the section 4 table.
- Verification would require a real dispatch run, scheduler registration,
  GitHub/network access, CI, cargo commands, staging, committing, or pushing.
- Any verification gate fails after the allowed table edit and cannot be fixed
  within the allowed scope.
- `new-handoff.ps1` cannot scaffold or finalize the required execution report.

## Planner Notes

- Revision 0 has no prior Claude gate.
- This issue was filed automatically by `Invoke-AiDispatchAuto.ps1` from
  `.ai/dispatch.tasks.md`.
- Keep the source diff smaller than the issue text: only the missing section 4
  table rows should change.
- The suggested `Port?` value for all five rows is `**Copy/adapt**` because the
  components are repo-specific automation entry points, gates, or briefs that
  should be reviewed when ported.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-8
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
