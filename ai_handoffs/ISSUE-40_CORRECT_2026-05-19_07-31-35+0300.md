# Correction Packet

DISPATCH_ID: ISSUE-40
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_07-31-35+0300
RELATED_FILES:
- crates/cad-projection/tests/face_picking_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-40_TASK_2026-05-19_07-23-11+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-40_EXEC_2026-05-19_07-30-45+0300.md`
- Latest Review Report: none; this correction is issued from the failed post-execution canonical verification gate.
- Verification failure: `cargo +nightly fmt --all -- --check` reported incorrect newline style in `crates/cad-projection/tests/face_picking_smoke.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections: nothing more, nothing less.

1. **Normalize newline style** - Post-execution verification failure. Required change: rewrite `crates/cad-projection/tests/face_picking_smoke.rs` with the newline style accepted by `cargo +nightly fmt --all -- --check`, without semantic edits, assertion changes, test weakening, import churn beyond formatter-stable output, or edits to any other project file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and the canonical dispatch verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. No review findings are approved for deferral in this correction round. If re-verification reveals a failure unrelated to the newline-style issue above, the Executor must report it rather than expanding scope silently.

## Updated Acceptance Criteria

Unchanged from the Task Packet. This correction only restores formatting and newline compliance for the already scoped test file.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0, covering the canonical format, architecture lint, supply chain, workspace test, and doctest checks.
- `cargo test -p rge-cad-projection --test face_picking_smoke` -> expected exit 0, confirming the ISSUE-40 smoke still passes after newline normalization.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus this correction-specific condition: halt and report if making `cargo +nightly fmt --all -- --check` pass requires any edit outside `crates/cad-projection/tests/face_picking_smoke.rs` or any semantic change to the ISSUE-40 smoke.

## Planner Notes

The failed gate points to file newline style only. Keep the correction mechanical and limited to `crates/cad-projection/tests/face_picking_smoke.rs`; do not change Rust behavior, broaden the test, or touch packet history beyond the Executor's required follow-up execution report.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-40
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
