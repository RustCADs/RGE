# Task Packet

DISPATCH_ID: ISSUE-220
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_02-34-42+0300
REVISION: 0
RELATED_FILES:
- GitHub issue #220
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- .ai/dispatch.tasks.md
- plans/PLAN.md
- plans/IMPLEMENTATION.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.toml
- Cargo.lock
- crates/gfx/
- crates/gfx-ir/
- crates/brep-render/
- crates/components-render/
- crates/cad-projection/src/render_adapter.rs
- crates/editor-shell/
- crates/editor-egui-host/
- editor/rge-editor/
- docs/**/GFX_RENDER_TIER.md
- ai_handoffs/MAIN-RENDER-*.md
- ai_handoffs/MAIN-ORDERED-JOB-00*-FRAMEGRAPH-*.md
- ai_handoffs/POSTV0-EDITOR-SHELL-PERF-*.md
- ai_handoffs/ISSUE-20_*.md
- ai_handoffs/ISSUE-21_*.md
- ai_handoffs/ISSUE-92_*.md
- ai_handoffs/ISSUE-110_*.md
- ai_handoffs/ISSUE-210_*.md
- ai_handoffs/ISSUE-212_*.md
- ai_handoffs/ISSUE-214_*.md
- ai_handoffs/ISSUE-216_*.md
- ai_handoffs/ISSUE-217_*.md
- ai_handoffs/ISSUE-218_*.md
- ai_handoffs/ISSUE-219_*.md
- ai_handoffs/ISSUE-220_EXEC_*.md
- ai_handoffs/ISSUE-220_EXEC_*.meta.json
STATUS: OPEN

## Goal

Perform a read-only Phase 9 GPU/render pressure audit for GitHub issue #220,
corresponding to the current `.ai/dispatch.tasks.md` task #69. Inspect current
GPU/render pressure across `crates/gfx`, frame-graph substrate, render-handoff
and editor-shell render path, user-facing editor render integration, and
baseline/status docs. Determine whether the next GPU task should be exactly
one of: measurement, integration, docs reconciliation, no-op, or
`NEEDS_HUMAN`. The EXEC report must cite line evidence, name exactly one
smallest safe follow-up or `NEEDS_HUMAN`, and verify no tracked production,
documentation, script, Cargo, workflow, schema, or task-brief changes.

## Scope

### MAY edit
- None. This is an audit-only dispatch. Do not modify tracked source, tests,
  documentation, Cargo files, workflows, schemas, automation scripts,
  scheduler configuration, task brief files, existing handoff packets, existing
  logs, fixtures, generated assets, or prior artifacts.

### MUST NOT edit
- `plans/PLAN.md`
- `plans/IMPLEMENTATION.md`
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.tasks.md`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchLoop.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- This task packet,
  `ai_handoffs/ISSUE-220_TASK_2026-05-27_02-34-42+0300.md`, after the
  executor starts.
- Any existing handoff packet or sidecar.
- Any existing `ai_dispatch_logs/**` artifact.
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `tests/**`
- `docs/**`
- `tasks/**`
- `plugins/**`
- `files/**`
- `golden-projects/**`
- `OLD/**`
- `.claude/worktrees/**`
- Any tracked source file, Rust test, documentation file, Cargo metadata file,
  workflow, schema, automation script, scheduler config, task brief, existing
  handoff artifact, existing dispatch log, generated fixture, sandbox
  worktree, or unrelated local scratch file.

### MAY add new files
- `ai_handoffs/ISSUE-220_TASK_*.meta.json`
- `ai_handoffs/ISSUE-220_EXEC_*.md`
- `ai_handoffs/ISSUE-220_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-220_CORRECT_*.md`
- `ai_handoffs/ISSUE-220_CORRECT_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-220/**`

### MUST NOT add new files
- New source, test, documentation, schema, Cargo, workflow, task brief,
  automation, scheduler, fixture, generated asset, ADR, plan, status,
  architecture-lint, benchmark, screenshot, ZIP, copied evidence file, or
  persistent scratch file.
- New handoff packets or sidecars for any dispatch other than `ISSUE-220`.
- `REVIEW` or `CLOSEOUT` packets for this orchestrator dispatch.
- Replacement or edited copies of prior `ISSUE-220` execution artifacts. If a
  prior failed EXEC exists, treat it as read-only context and write a new
  timestamped `ISSUE-220_EXEC_*.md` instead.
- Temporary source-layout inventories, extracted logs, generated reports,
  screenshots, frame captures, profiler output, benchmark output, copied CI
  logs, or measurement outputs inside the repository. If scratch is required
  to inspect command output, place it outside the repo and remove it before
  writing the EXEC report.

## Deliverables

- One new `ISSUE-220` EXEC report containing the read-only audit.
- A decision block with exactly these fields:
  `GPU_RENDER_PRESSURE_VERDICT`, `NEXT_GPU_TASK_CLASS`,
  `PRIMARY_PRESSURE_SIGNAL`, `SMALLEST_SAFE_FOLLOW_UP`, and
  `FOLLOW_UP_BRANCH_MODE`.
- `GPU_RENDER_PRESSURE_VERDICT` must use exactly one of:
  `measurement`, `integration`, `docs_reconciliation`, `no_op`, or
  `NEEDS_HUMAN`.
- `NEXT_GPU_TASK_CLASS` must use the same exact value as
  `GPU_RENDER_PRESSURE_VERDICT`.
- `PRIMARY_PRESSURE_SIGNAL` must name exactly one current pressure signal, or
  be exactly `none` or `NEEDS_HUMAN`.
- `SMALLEST_SAFE_FOLLOW_UP` must name exactly one action. If the verdict is
  `no_op`, it must be exactly
  `no-op: do not file a GPU follow-up from current evidence`. If the verdict
  is `NEEDS_HUMAN`, it must be exactly `NEEDS_HUMAN`.
- `FOLLOW_UP_BRANCH_MODE` must be exactly
  `PublishMode branch only unless later authorized by a human` for any
  follow-up that would implement source, test, documentation, script, Cargo,
  workflow, schema, scheduler, automation-behavior, benchmark, harness, or
  measurement-output changes. If the selected follow-up is `no_op` or
  `NEEDS_HUMAN`, state exactly `not_applicable`.
- A Phase 9 context section citing `plans/PLAN.md`,
  `plans/IMPLEMENTATION.md`, `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`,
  recent `change.md`, `.ai/dispatch.tasks.md` task #69, and the common #62-#71
  constraints. It must distinguish current GPU/render pressure from historical
  roadmap, release-certification, benchmark, and dispatch-doctrine evidence.
- A gfx substrate section covering `crates/gfx` current public surface,
  `GfxContext`, GPU/wgpu init and adapter/device behavior, frame recorder and
  readback, headless targets, surface/target ownership, mesh and lit-mesh
  pipelines, material/pipeline/PSO cache behavior, plugin/intent adapters, and
  current tests.
- A frame-graph section covering `crates/gfx/src/frame_graph/**`, including
  descriptor/resource/pass types, compile algorithm, resource lifetime,
  aliasing groups, resource maps, texture/buffer pools, structural hashes,
  and frame-graph smoke/umbrella tests.
- A render-handoff section covering `crates/editor-shell/src/render_input.rs`,
  `RenderInput`, `RenderInputOwned`, `RenderHandoff`, snapshot ownership,
  generation/acquire/publish semantics, and the relationship to ADR-117 and
  Phase 6 render-snapshot separation.
- An editor-shell render-path section covering
  `crates/editor-shell/src/render_path.rs`,
  `crates/editor-shell/src/render_frame_e2e_perf.rs`,
  `crates/editor-shell/src/visual_smoke.rs`,
  `crates/editor-shell/src/visual_test_harness.rs`,
  `crates/editor-shell/src/lifecycle/**`, viewport/camera/picking surfaces,
  asset reload, play toolbar/playback, snapshots, and relevant
  `crates/editor-shell/tests/**` render or timing tests.
- An editor integration section covering `editor/rge-editor/src/main.rs`,
  `editor/rge-editor/src/glb_watcher.rs`,
  `crates/editor-egui-host/src/handoff.rs`, relevant
  `crates/editor-egui-host/tests/**` viewport/handoff tests, and any current
  dependency path from editor UI/runtime surfaces into render inputs.
- A render consumer section covering current render-adjacent crates and
  components: `crates/gfx-ir`, `crates/brep-render`,
  `crates/components-render`, and `crates/cad-projection/src/render_adapter.rs`.
  The section must classify whether each is current GPU pressure, CPU-side
  data/model pressure, historical-only context, or unrelated to the selected
  follow-up.
- A baseline/status reconciliation section covering current baseline and status
  claims from `plans/BASELINE.md`, `plans/IMPLEMENTATION.md`, `Status.md`,
  `HANDOFF.md`, `change.md`, and any discovered `docs/**/GFX_RENDER_TIER.md`.
  It must identify whether claims are current, stale, narrower than current
  source, broader than current source, or historical-only.
- A GPU pressure candidate matrix with one row per inspected candidate. Each
  row must classify the candidate as `measurement`, `integration`,
  `docs_reconciliation`, `already_covered`, `no_op`, `historical_only`, or
  `NEEDS_HUMAN`, and must include line-cited evidence or an exact read-only
  search command for zero-use claims.
- A candidate comparison section explaining why the selected follow-up is the
  smallest safe GPU/render follow-up, why another class is not selected, or
  why `NEEDS_HUMAN` is required. It must compare measurement, integration,
  docs reconciliation, and no-op rather than defaulting to the first visible
  signal.
- A final verification section reporting `git diff --check`,
  `git status --short --untracked-files=all`, and static inspection proving no
  tracked production/doc/script/Cargo/workflow/schema/task-brief files changed.

## Acceptance Criteria

- The EXEC report is audit-only and does not modify tracked source, tests,
  docs, Cargo metadata, workflows, schemas, scripts, scheduler configuration,
  fixtures, generated assets, or `.ai/dispatch.tasks.md`.
- The report classifies GPU/render pressure using exactly one
  `GPU_RENDER_PRESSURE_VERDICT` value and exactly one matching
  `NEXT_GPU_TASK_CLASS` value from the deliverables section.
- The report names exactly one smallest safe follow-up, the exact no-op string,
  or exactly `NEEDS_HUMAN`; it does not produce a backlog list.
- The report includes the branch-mode requirement for any implementation,
  measurement, benchmark, harness, test, documentation, script, Cargo,
  workflow, schema, scheduler, or automation-behavior follow-up exactly as
  specified in `FOLLOW_UP_BRANCH_MODE`.
- Every substantive claim about current code, docs, scripts, Cargo metadata,
  tests, workflows, logs, or dispatch artifacts is backed by `path:line`
  evidence when the evidence lives in a line-bearing file. If a claim comes
  from command output, git history, source inventory, or zero-use search rather
  than a line-bearing file, the report includes the exact read-only command,
  scope, and observed result.
- The audit reads and line-cites `AI_DISPATCH_AUTOMATION.md` and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md` before judging dispatch state, and it
  does not treat missing orchestrator `_REVIEW_` or `_CLOSEOUT_` packets as
  evidence of an unfinished dispatch.
- The audit reads and line-cites `.ai/dispatch.tasks.md` task #69 and the
  common #62-#71 constraints before applying this dispatch's audit shape.
  If it recommends a measurement follow-up that would run GPU/wgpu tests, it
  must also line-cite the repository's GPU test serialization guidance.
- The audit reads and line-cites `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, and recent `change.md`
  entries to establish Phase 9 GPU/render context, current gates, closed
  milestones, known deferrals, and current next-work posture.
- The audit inspects and line-cites `crates/gfx/Cargo.toml`,
  `crates/gfx/src/lib.rs`, `crates/gfx/src/context.rs`,
  `crates/gfx/src/frame.rs`, `crates/gfx/src/target.rs`,
  `crates/gfx/src/surface.rs`, `crates/gfx/src/pso_cache.rs`,
  `crates/gfx/src/pipeline.rs`, `crates/gfx/src/mesh_pipeline.rs`,
  `crates/gfx/src/lit_mesh_pipeline.rs`, `crates/gfx/src/material.rs`,
  `crates/gfx/src/mesh.rs`, `crates/gfx/src/intent_adapter.rs`,
  `crates/gfx/src/plugin_adapter.rs`, and relevant `crates/gfx/tests/**`
  surfaces.
- The audit inspects and line-cites `crates/gfx/src/frame_graph/mod.rs`,
  `crates/gfx/src/frame_graph/descriptor.rs`,
  `crates/gfx/src/frame_graph/pass.rs`,
  `crates/gfx/src/frame_graph/resource.rs`,
  `crates/gfx/src/frame_graph/resource_map.rs`,
  `crates/gfx/src/frame_graph/texture_pool.rs`,
  `crates/gfx/src/frame_graph/buffer_pool.rs`,
  `crates/gfx/src/frame_graph/compile/mod.rs`,
  `crates/gfx/src/frame_graph/compile/algorithm.rs`,
  `crates/gfx/src/frame_graph/compile/types.rs`,
  `crates/gfx/src/frame_graph/compile/error.rs`,
  `crates/gfx/tests/frame_graph_smoke.rs`, and
  `crates/gfx/tests/frame_graph_umbrella_smoke.rs`.
- The audit inspects and line-cites `crates/editor-shell/Cargo.toml`,
  `crates/editor-shell/src/lib.rs`,
  `crates/editor-shell/src/render_input.rs`,
  `crates/editor-shell/src/render_path.rs`,
  `crates/editor-shell/src/render_frame_e2e_perf.rs`,
  `crates/editor-shell/src/visual_smoke.rs`,
  `crates/editor-shell/src/visual_test_harness.rs`,
  `crates/editor-shell/src/lifecycle/mod.rs`,
  `crates/editor-shell/src/lifecycle/asset_reload.rs`,
  `crates/editor-shell/src/lifecycle/commands.rs`,
  `crates/editor-shell/src/lifecycle/playback.rs`,
  `crates/editor-shell/src/viewport.rs`,
  `crates/editor-shell/src/camera.rs`,
  `crates/editor-shell/src/pick_path.rs`,
  `crates/editor-shell/src/snapshot.rs`, and relevant
  `crates/editor-shell/tests/**` render/timing surfaces.
- The audit inspects and line-cites `editor/rge-editor/Cargo.toml`,
  `editor/rge-editor/src/main.rs`, `editor/rge-editor/src/glb_watcher.rs`,
  `crates/editor-egui-host/Cargo.toml`,
  `crates/editor-egui-host/src/handoff.rs`, and relevant
  `crates/editor-egui-host/tests/**` viewport/handoff surfaces.
- The audit inspects and line-cites render-adjacent consumer/data surfaces:
  `crates/gfx-ir/Cargo.toml`, `crates/gfx-ir/src/lib.rs`,
  `crates/brep-render/Cargo.toml`, `crates/brep-render/src/lib.rs`,
  `crates/brep-render/tests/render_mesh_smoke.rs`,
  `crates/components-render/Cargo.toml`, `crates/components-render/src/lib.rs`,
  relevant `crates/components-render/src/**`, and
  `crates/cad-projection/src/render_adapter.rs`.
- The audit searches current production and test usage read-only for
  `GfxContext`, `FrameRecorder`, `ReadbackBuffer`, `HeadlessTarget`,
  `FrameGraph`, `CompiledFrameGraph`, `ResourceLifetime`, `AliasingGroup`,
  `RenderInput`, `RenderInputOwned`, `RenderHandoff`, `render_frame`,
  `render_frame_to_target`, `acquire_depth_view`, `VisualTestHarness`,
  `MeshPipeline`, `LitMeshPipeline`, `PipelineCache`, `wgpu`, and
  `frame_graph`. Positive findings must be line-cited. If a zero-use claim
  comes from search output rather than a line-bearing file, the report must
  include the exact read-only command, scope, and observed no-match result.
- The report distinguishes GPU/render pressure from editor usability pressure,
  compile-time pressure, invalidation-economics pressure, historical roadmap
  ambition, completed Gate A/Gate C milestones, stale baseline text, and
  ordinary expected cost of owning a wgpu substrate.
- The report uses `measurement` only if current evidence shows one concrete
  GPU/render metric gap or stale measurement that can be closed by a bounded
  follow-up without changing runtime/editor semantics.
- The report uses `integration` only if current evidence shows one concrete
  missing wiring path between gfx/frame-graph/render-handoff/editor-shell or
  editor binary surfaces that blocks a truthful current workflow.
- The report uses `docs_reconciliation` only if current evidence shows current
  source and current baseline/status docs disagree and a docs-only follow-up is
  the smallest safe task.
- The report uses `no_op` only if current evidence shows no concrete GPU/render
  measurement, integration, or docs reconciliation follow-up is justified now.
- The report uses `NEEDS_HUMAN` only if current evidence is unavailable,
  contradictory, or points to an unbounded product, architecture, performance,
  renderer-roadmap, benchmark-policy, or GPU-backend decision rather than one
  safe follow-up.
- The audit inspects relevant existing dispatch artifacts read-only, including
  render Gate A / visual fidelity packets, frame-graph audit/follow-up packets,
  editor-shell perf-harness packets, prior Phase 9 pressure audits
  (`ISSUE-210`, `ISSUE-212`, `ISSUE-214`, `ISSUE-216`, `ISSUE-217`,
  `ISSUE-218`, `ISSUE-219`), and recent render/editor/gfx artifacts. It must
  cite current packet/log lines, not stale copied line numbers.
- The report does not run new benchmarks, profilers, cargo builds, cargo
  tests, cargo fmt, cargo deny, full repository verification, the autonomous
  queue, a nested dispatch, GPU captures, external profilers, or GitHub
  mutations as part of this audit.
- `git diff --check` exits 0.
- `git status --short --untracked-files=all` shows no tracked production,
  documentation, script, Cargo, workflow, schema, or task-brief changes. Any
  new files must be limited to this dispatch's own handoff/log artifacts and
  optional ignored `.ai/dispatch-ISSUE-220` scratch.

## Constraints / Non-Goals

- Do not implement a fix.
- Do not edit source, tests, docs, Cargo files, workflows, schemas, scripts,
  scheduler config, task brief files, fixtures, generated assets, prior
  handoff packets, prior logs, or existing dispatch artifacts.
- Do not update `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, or any other
  documentation.
- Do not add or run new benchmarks, profilers, generated reports,
  screenshots, frame captures, ZIPs, fixtures, or persistent scratch files in
  the repository.
- Do not run publish, push, merge, branch-delete, label-edit, GitHub issue
  mutation, scheduler registration, or scheduler modification commands.
- Do not create a branch, commit, PR, issue comment, label change, or close the
  GitHub issue.
- Do not run the autonomous queue or a nested dispatch as verification.
- Do not run full repository verification manually for this audit. The
  task-specific gates are read-only inspection plus diff/status checks; the
  orchestrator may still run its canonical gate automatically after execution.
- Do not infer GPU/render pressure from roadmap language, crate names, old
  Gate A closure, old perf-harness closure, or test existence alone. Tie the
  conclusion to current reachable render code, current editor render paths,
  current measurements or baselines, and line-cited evidence.
- Do not recommend broad renderer architecture, frame-graph rewrite, GPU
  backend policy, material system, shader system, render-thread, profiling
  infrastructure, or benchmark suite changes unless the report returns
  `NEEDS_HUMAN`.
- Treat unrelated dirty worktree state as out of scope. Do not clean, revert,
  or modify changes you did not make; report them if they affect confidence.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` and
  `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` -> expected readable;
  use them to avoid mistaking orchestrator packet shape for dispatch
  incompleteness.
- Inspect `.ai/dispatch.tasks.md` read-only around tasks #62-#71, including
  task #69 and the common constraints. If recommending GPU/wgpu measurement or
  test work, also inspect the GPU test serialization guidance. Use line
  citations in the EXEC report.
- `Get-Content -Raw plans/PLAN.md`,
  `Get-Content -Raw plans/IMPLEMENTATION.md`, and
  `Get-Content -Raw plans/BASELINE.md` -> expected readable; use them to
  establish Phase 9 GPU/render pressure context, rendering milestones,
  budgets, and revisit triggers.
- `Get-Content -Raw Status.md` and `Get-Content -Raw HANDOFF.md` -> expected
  readable; use them to establish current state, closed gates, known
  deferrals, and next-work posture.
- Inspect recent `change.md` entries with live line numbers immediately before
  writing the final EXEC report. Do not reuse copied line numbers from prior
  packets or logs.
- Inspect `crates/gfx` read-only, including Cargo metadata, public lib surface,
  context/frame/target/surface/pipeline/material/mesh/PSO cache modules,
  plugin/intent adapters, and relevant tests.
- Inspect `crates/gfx/src/frame_graph/**` read-only, including compile,
  descriptor, pass, resource, resource map, buffer/texture pool, and frame
  graph tests.
- Inspect `crates/editor-shell` render paths read-only, including
  render-input/handoff, render path, render-frame perf harness, visual smoke
  harness, lifecycle modules, viewport/camera/picking/snapshot modules, and
  relevant render/timing tests.
- Inspect `editor/rge-editor` and `crates/editor-egui-host` read-only to
  identify current user-facing editor render wiring and viewport/handoff
  integration.
- Inspect render-adjacent consumer/data crates read-only:
  `crates/gfx-ir`, `crates/brep-render`, `crates/components-render`, and
  `crates/cad-projection/src/render_adapter.rs`.
- Search Cargo metadata read-only for dependency declarations involving
  `rge-gfx`, `rge-gfx-ir`, `rge-brep-render`, `rge-components-render`,
  `rge-editor-shell`, and `rge-editor-egui-host`. Use `git grep`,
  `Select-String`, `git ls-files`, or an equivalent read-only command, and
  report exact scope and results.
- Search production and test usage read-only for `GfxContext`,
  `FrameRecorder`, `ReadbackBuffer`, `HeadlessTarget`, `FrameGraph`,
  `CompiledFrameGraph`, `ResourceLifetime`, `AliasingGroup`, `RenderInput`,
  `RenderInputOwned`, `RenderHandoff`, `render_frame`,
  `render_frame_to_target`, `acquire_depth_view`, `VisualTestHarness`,
  `MeshPipeline`, `LitMeshPipeline`, `PipelineCache`, `wgpu`, and
  `frame_graph`. Report positive matches with line citations and zero-use
  claims with exact command/scope/results.
- Inspect baseline/status docs read-only, including `plans/BASELINE.md`,
  `plans/IMPLEMENTATION.md`, `Status.md`, `HANDOFF.md`, recent `change.md`,
  and any discovered `docs/**/GFX_RENDER_TIER.md`. Use live line citations.
- Inspect relevant existing dispatch artifacts read-only, especially prior
  render Gate A, visual fidelity, frame-graph, editor-shell perf-harness, and
  Phase 9 pressure-audit artifacts. Ignore `OLD/**`, `.ai/dispatch-*/**`,
  `.claude/worktrees/**`, `.git/**`, and build output when assessing
  repository state unless a specific artifact is cited as provenance.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new
  files must be limited to this dispatch's own handoff/log artifacts and
  optional ignored `.ai/dispatch-ISSUE-220` scratch.
- Static no-source-change inspection: run `git diff --name-only` and a
  targeted read-only inspection of protected tracked surfaces such as
  `crates`, `kernel`, `editor`, `runtime`, `tools`, `docs`, `plans`,
  `Status.md`, `HANDOFF.md`, `change.md`, `Cargo.toml`, `Cargo.lock`,
  `.github/workflows`, `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`,
  `.ai/*.schema.json`, and root `*.ps1` automation scripts. Expected result:
  no tracked production/doc/script/Cargo/workflow/schema/task-brief diff.

## Halt Conditions

The Executor MUST halt and return `NEEDS_HUMAN` without editing any source,
docs, tests, scripts, schemas, workflows, Cargo files, scheduler config, task
briefs, fixtures, generated assets, prior packets, or prior logs if any of the
following occur:

- Required evidence files are missing or unreadable.
- The required production/test usage search cannot be completed read-only.
- The evidence cannot support exactly one `GPU_RENDER_PRESSURE_VERDICT` value
  from the allowed set.
- `NEXT_GPU_TASK_CLASS` cannot be made identical to
  `GPU_RENDER_PRESSURE_VERDICT`.
- The smallest safe follow-up cannot be narrowed to exactly one action, the
  exact no-op string, or exactly `NEEDS_HUMAN`.
- The smallest safe follow-up would require source/test/doc/script/Cargo/
  workflow/schema/scheduler/automation/benchmark/harness changes but cannot be
  constrained to `PublishMode branch only unless later authorized by a human`.
- The audit needs an implementation, documentation edit, Cargo edit, workflow
  edit, schema edit, script edit, scheduler change, task-brief edit, prior
  artifact edit, benchmark run, profiler run, cargo build/test/fmt/deny run,
  GPU capture, external profiler, or GitHub mutation to be truthful.
- Required line citations cannot be produced for document-, code-, Cargo-,
  fixture-, script-, log-, or artifact-based claims.
- Current evidence shows a real GPU/render pressure signal, but the next step
  depends on an unresolved product/design or architecture decision about
  renderer roadmap priority, GPU backend policy, frame-graph ownership,
  render-thread direction, measurement policy, material/shader ownership, or
  editor/runtime dependency direction.
- Any GitHub mutation, push, merge, branch deletion, label change, issue
  comment, scheduler command, benchmark run, profiler run, full verification
  run, GPU capture, external profiler, or nested dispatch seems necessary.
- `git diff --check` fails for a reason caused by this dispatch.
- `git status --short --untracked-files=all` shows new or modified files
  outside this dispatch's own handoff/log artifacts and optional ignored
  `.ai/dispatch-ISSUE-220` scratch, unless they are clearly pre-existing
  unrelated user changes.

If a halt condition fires, the Executor must still write the
`EXECUTION_REPORT` if possible, set header `STATUS: NEEDS_HUMAN`, include the
exact halt reason and current status output, and route the footer to
`NEXT_ROLE: HUMAN_ARBITER`.

## Planner Notes

This is delegated-main smoke batch task 8 of 10 from `.ai/dispatch.tasks.md`;
the task number in that brief is #69, while the filed GitHub issue and
dispatch ID are `ISSUE-220`. The desired result is not to create GPU work by
default. It is to decide whether current gfx/frame-graph/render-handoff/
editor-render evidence shows one bounded measurement follow-up, one bounded
integration follow-up, one bounded docs-reconciliation follow-up, no current
GPU task, or a need for human product/architecture arbitration.

Use current evidence over prior summaries. Prior dispatch artifacts can be
used as provenance, but the report must label them as historical unless the
cited artifact itself proves current applicability. Measurement means a
follow-up to collect or refresh one bounded render/GPU measurement using
existing or narrowly scoped harness surfaces; it does not authorize this audit
to run the measurement. Integration means a follow-up to wire one current
render substrate into one current consumer path; it does not authorize source
changes in this audit. Docs reconciliation means a future docs-only task
because current source and baseline/status docs disagree; it does not
authorize docs edits in this audit.

Under the orchestrator protocol, no `REVIEW` or `CLOSEOUT` packet is expected
from this TASK. The queue's Codex control review is stored in the run
directory, not as a handoff packet.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-220
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
