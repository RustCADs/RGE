# Task Packet

DISPATCH_ID: ISSUE-60
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_08-13-22+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_material_smoke.rs
- crates/editor-ui/Cargo.toml
- Cargo.lock
- crates/editor-ui/src/widgets/node_graph.rs
- crates/material-graph/src/lib.rs
STATUS: OPEN

## Goal

Add the focused editor-ui integration smoke for GitHub issue #60 proving that `NodeGraphWidget` can consume a real `rge-material-graph::MaterialGraph` through the existing `rge_kernel_graph_foundation::VizAdapter` bridge. This is a test-only cross-crate smoke for the generic node-graph model added in issue #58; production `editor-ui` must remain domain-agnostic and must not gain material, animation, CAD, renderer, or graph-foundation changes.

## Scope

### MAY edit
- `crates/editor-ui/Cargo.toml`, only to add the test-only dev-dependency required for the integration test to name `rge-material-graph`.
- `Cargo.lock`, only for the minimal lockfile dependency-list metadata Cargo writes as a consequence of the approved `rge-editor-ui` dev-dependency on the existing workspace `rge-material-graph` package. This explicit allowance resolves the prior control block for this issue.

### MUST NOT edit
- Any production source file, including `crates/editor-ui/src/**`.
- `crates/material-graph/**`, `crates/anim-graph/**`, `crates/cad-core/**`, `crates/script-graph/**`, `kernel/graph-foundation/**`, renderer/gfx crates, runtime crates, editor-shell crates, or any graph-domain crate.
- Workspace-root `Cargo.toml`, any crate manifest other than `crates/editor-ui/Cargo.toml`, architecture-lint files, schemas, scripts, CI files, documentation, `.gitignore`, or generated artifacts other than the narrow Cargo.lock update allowed above.
- Any existing handoff packet.

### MAY add new files
- `crates/editor-ui/tests/node_graph_material_smoke.rs`

### MUST NOT add new files
- Any file other than `crates/editor-ui/tests/node_graph_material_smoke.rs`.
- New crates, fixtures, snapshots, assets, docs, schemas, scripts, architecture-lint rules, CI files, or generated files.

## Deliverables

- Add `crates/editor-ui/tests/node_graph_material_smoke.rs` as an integration test.
- If the test cannot compile without naming the material graph crate, add exactly one editor-ui dev-dependency in `crates/editor-ui/Cargo.toml`: `rge-material-graph = { path = "../material-graph" }`. Do not add a production dependency and do not modify workspace dependencies.
- Let Cargo update `Cargo.lock` only as needed to reflect that editor-ui dev-dependency. The expected lockfile shape is a dependency-list update involving `rge-editor-ui` and `rge-material-graph`, with no external crate version changes.
- In the smoke test, build a small real `MaterialGraph` using public material-graph APIs such as `MaterialGraph::new`, `add_node`, `connect`, `MaterialEdge`, and `PortType`.
- Build a node-graph model through the existing `NodeGraphWidget` API, for example `NodeGraphWidget::new().model_from(&graph)` if `model_from` remains an instance method.
- Assert the model's node count and edge count match the material graph.
- Assert material node records expose the expected `display_name` values from the material node keys and the expected node `kind` value, `MaterialNode`.
- Assert material edge records expose the expected endpoints and exact edge labels derived by the material graph adapter, such as `color->color` or `vector->texture`.
- Make assertions by stable `NodeId` and `EdgeId` returned from graph construction, or otherwise avoid depending on insertion order if the underlying adapter order is deterministic but sorted by ids.

## Acceptance Criteria

- `crates/editor-ui/tests/node_graph_material_smoke.rs` exists and is the only new file.
- The new test uses a real `rge_material_graph::MaterialGraph`; it does not use a fake adapter or standalone graph-foundation test double.
- The test proves `NodeGraphWidget` consumes `MaterialGraph` through `VizAdapter` by compiling and calling the existing `model_from` bridge without changing production APIs.
- The test asserts exact node count, edge count, at least two material node display names, material node kind strings, at least one edge label, and the corresponding edge endpoints.
- `crates/editor-ui/Cargo.toml` is changed only if required, and only by adding the editor-ui dev-dependency on `rge-material-graph`.
- `Cargo.lock` is changed only if Cargo requires it for that dev-dependency, with no registry dependency upgrades, no new external packages, and no unrelated lockfile churn.
- No production `editor-ui` code mentions `rge-material-graph`, `rge-anim-graph`, `rge-cad-core`, renderer crates, runtime crates, or other graph-domain crates.
- No source changes are made to material, animation, CAD, script, graph-foundation, renderer, runtime, architecture-lint, schema, script, documentation, or CI files.
- `cargo test -p rge-editor-ui --test node_graph_material_smoke` exits 0.
- `cargo run -q -p rge-tool-architecture-lints -- all` exits 0.

## Constraints / Non-Goals

- This dispatch is test-only except for the optional editor-ui dev-dependency and its narrow lockfile consequence.
- Do not change `NodeGraphWidget`, `NodeGraphModel`, `VizAdapter`, `MaterialGraph`, material graph labels, material graph ordering, graph-foundation ids, or any production API to make the test easier.
- Do not add rendering, egui UI, layout, pan/zoom, selection, ports UI, persistence, material editor behavior, graph traversal, evaluation, validation, or mutation behavior.
- Do not promote `rge-material-graph` to a production dependency of editor-ui.
- Do not add workspace dependency entries, feature flags, dependency version bumps, or architecture-lint exemptions.
- Do not fix unrelated warnings, formatting churn, failing tests, or dependency metadata outside the listed scope.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-ui --test node_graph_material_smoke` -> expected exit 0.
- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0.

If formatting changed and time permits, the Executor MAY also run and document:

- `cargo fmt --check -p rge-editor-ui` -> expected exit 0.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The smoke cannot be implemented entirely within `crates/editor-ui/tests/node_graph_material_smoke.rs`, the optional `crates/editor-ui/Cargo.toml` dev-dependency, and the narrow Cargo.lock metadata update allowed in this packet.
- The test appears to require any production source edit, production dependency, workspace manifest edit, feature flag, architecture-lint exemption, or change to `NodeGraphWidget`, `VizAdapter`, `MaterialGraph`, graph-foundation, material, animation, CAD, script, renderer, runtime, schema, script, documentation, or CI files.
- Cargo wants to change `Cargo.lock` beyond the minimal editor-ui-to-material-graph dev-dependency metadata described above, including external package additions, removals, or version changes.
- The architecture lint gate rejects the editor-ui dev-dependency on `rge-material-graph` and resolving that rejection would require any change outside this packet's MAY-edit scope.
- The existing public `NodeGraphWidget` model does not expose node display names, node kinds, edge labels, edge endpoints, or counts strongly enough for the smoke to assert them without changing production APIs.
- The required verification gates fail for reasons unrelated to this task and the fix would require edits outside the MAY-edit or MAY-add scope.
- Existing uncommitted changes in any in-scope file make it unclear how to proceed without overwriting someone else's work.

## Planner Notes

Prior execution for this issue reached a technically correct implementation and passed verification, but Codex control blocked publication because the previous task packet forbade all `Cargo.lock` edits while the approved editor-ui dev-dependency caused Cargo to update the lockfile. This revised packet explicitly authorizes only that single lockfile consequence. Do not treat this as permission for broader dependency churn.

The intended smoke is deliberately small: build a real material graph with a few named nodes and typed-port edges, ask `NodeGraphWidget` for a model, and assert the material adapter's counts, node labels/kinds, and edge labels/endpoints are visible through the generic editor-ui surface. The production dependency direction remains clean because only the integration test names `rge-material-graph`.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-60
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
