# Correction Packet

DISPATCH_ID: ISSUE-29
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_00-59-04+0300
RELATED_FILES:
- crates/cad-core/tests/fillet_brep_chain_resolution_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-29_TASK_2026-05-19_00-53-33+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-29_EXEC_2026-05-19_00-58-14+0300.md`
- Latest Review Report: none; the canonical post-execution verification gate failed before Codex control review.
- Verification failure: `.ai/dispatch.verify.ps1` step `[1] cargo +nightly fmt --check`, specifically `Incorrect newline style in \\?\A:\RCAD\RGE\crates\cad-core\tests\fillet_brep_chain_resolution_smoke.rs`.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the verification failure it addresses.

1. **Normalize newline style in cad-core smoke test** - Post-execution verification gate failure from `cargo +nightly fmt --all -- --check`. Required change: convert `crates/cad-core/tests/fillet_brep_chain_resolution_smoke.rs` to the newline style expected by `rustfmt` for this repository, preserving the file's exact Rust tokens and behavior. Do not rename, reorder, reformat semantically, add tests, delete tests, or otherwise modify the file beyond the line-ending normalization required for `rustfmt` to accept it. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and `git diff -- crates/cad-core/tests/fillet_brep_chain_resolution_smoke.rs` shows no semantic Rust changes beyond newline-style normalization.

## Deferred Findings (NOT Approved for This Round)

None. There are no Codex control-review findings to defer because the
canonical verification gate failed before a control review was run.

## Updated Acceptance Criteria

Unchanged from the Task Packet, except this correction round additionally
permits one mechanical project-file touch:

- `crates/cad-core/tests/fillet_brep_chain_resolution_smoke.rs` may be changed only to normalize newline style so the canonical workspace fmt gate passes.

All ISSUE-29 functional changes remain limited to the previously edited
`crates/cad-projection/tests/face_selection_partition_smoke.rs`; this
correction does not authorize any production-code, test-logic, manifest,
schema, script, documentation, or additional packet changes.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.
- `git diff -- crates/cad-core/tests/fillet_brep_chain_resolution_smoke.rs` -> expected no semantic Rust changes beyond newline-style normalization.
- `git status --short` -> expected tracked project-file changes limited to the original ISSUE-29 test change and this newline-style normalization, plus protocol-required handoff packets.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with these additions for the correction round:

- Halt if `crates/cad-core/tests/fillet_brep_chain_resolution_smoke.rs` requires any semantic Rust change to satisfy formatting.
- Halt if fixing the newline style would require changing production code, manifests, scripts, schemas, docs, or additional test logic.
- Halt if `.ai/dispatch.verify.ps1` still fails after the newline-style normalization and report the exact failing step in the next execution report.

## Planner Notes

The failed gate is a repository-format hygiene issue, not a functional issue
with the ISSUE-29 smoke test. Keep the correction mechanical and minimal:
normalize the named file's line endings, then re-run the workspace fmt gate and
the canonical dispatch verification. Do not broaden this into cleanup of other
files or any behavior change.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-29
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
