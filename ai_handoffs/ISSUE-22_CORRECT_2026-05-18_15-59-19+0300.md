# Correction Packet

DISPATCH_ID: ISSUE-22
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18T15:59:19+03:00
RELATED_FILES:
- crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-22_TASK_2026-05-18_15-50-49+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-22_EXEC_2026-05-18_15-58-16+0300.md`
- Latest Review Report: none; post-execution canonical verification failed before control review.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it
addresses.

1. **Normalize rustfmt newline style** - Verification gate failure:
   `cargo +nightly fmt --all -- --check` failed with `Incorrect newline style`
   in `crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs`.
   Required change: edit only
   `crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs` to normalize
   its line endings/newline style to the rustfmt-accepted repository style.
   Do not change Rust code, test assertions, imports, comments, file content
   semantics, formatting rules, Cargo metadata, scripts, architecture lints, or
   any other file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0
   and the canonical dispatch verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. The post-execution verification log reported one blocking failure: the
newline-style mismatch in
`crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs`. No other
correction is approved for this round.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this verification-only addition: the
repository rustfmt check must pass after normalizing the newline style in
`crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs`.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0
- `.\.ai\dispatch.verify.ps1` -> expected exit 0

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- If passing rustfmt requires editing any file other than
  `crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs`, halt and
  route back to Planner.
- If the correction requires semantic source or test changes rather than the
  newline-style normalization above, halt and route back to Planner.
- If the canonical verification gate exposes a new, distinct blocking failure
  after this newline-style fix, halt and preserve the failing output.

## Planner Notes

The canonical verification gate failed at step 1, before any architecture
lints, supply-chain checks, workspace tests, or doctests could run. This packet
approves only the mechanical newline-style repair needed for rustfmt to accept
the existing test file. It does not approve changes to Transform label
preservation behavior, Sweep BRep face lookup semantics, test expectations, or
any broader cleanup.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-22
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
