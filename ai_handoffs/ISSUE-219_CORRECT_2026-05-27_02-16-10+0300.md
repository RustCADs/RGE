# Correction Packet

DISPATCH_ID: ISSUE-219
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_02-16-10+0300
RELATED_FILES:
- ai_handoffs/ISSUE-219_TASK_2026-05-27_01-32-02+0300.md
- ai_handoffs/ISSUE-219_EXEC_2026-05-27_02-05-41+0300.md
- crates/editor-shell/src/lifecycle/mod.rs
- crates/editor-shell/tests/time_scale_test.rs
- editor/rge-editor/Cargo.toml
- editor/rge-editor/src/main.rs
- New correction-round ISSUE-219 EXEC packet generated by the Executor
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-219_TASK_2026-05-27_01-32-02+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-219_EXEC_2026-05-27_02-05-41+0300.md`
- Latest Control Review: `.ai/dispatch-ISSUE-219/codex.control.round1.json`
- Prior CORRECT packets in this dispatch:
  - `ai_handoffs/ISSUE-219_CORRECT_2026-05-27_02-03-06+0300.md`

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more. All work
for this round is limited to writing a new ISSUE-219 EXEC packet and its normal
sidecar if finalization is run.

1. **Write a new EXEC packet only** - Control review required fix. Required
   change: write a new ISSUE-219 EXEC packet; do not edit prior TASK, EXEC, or
   CORRECT packets. Acceptance: a later-timestamped ISSUE-219 EXEC packet
   exists, the prior packets remain unchanged, and no source, docs, Cargo,
   schema, script, workflow, task-brief, dispatch-log, or scheduler file is
   edited.

2. **Correct the `EditorShell::with_world` premise** - Control review found the
   latest EXEC rests on a false current-code claim. Required change: re-evaluate
   the decision block and candidate comparison against the current evidence that
   `crates/editor-shell/src/lifecycle/mod.rs:567` defines
   `pub fn with_world(mut world: World) -> Self`, and
   `crates/editor-shell/tests/time_scale_test.rs:277` and `:296` already use
   `EditorShell::with_world(world)`. The new EXEC must not claim that
   `EditorShell` lacks `with_world(world)`. Acceptance: the new EXEC cites the
   existing constructor evidence and removes the false missing-constructor
   prerequisite from `PRIMARY_SUBSTRATE_PREREQUISITE`, the candidate comparison,
   and any explanatory prose.

3. **Select exactly one current smallest safe follow-up** - Control review
   required the follow-up to be re-derived from current code. Required change:
   choose exactly one smallest safe follow-up. If the selected follow-up still
   targets `rge-editor -> rge-scene-loader`, identify the remaining current
   blocker with live citations, using the current facts that
   `editor/rge-editor/Cargo.toml` has no `rge-scene-loader` dependency,
   `editor/rge-editor/src/main.rs` has no `load_scene_into_world` call site,
   and `editor/rge-editor/src/main.rs` exposes the existing `--glb` path.
   Whenever the selected follow-up would touch source, tests, Cargo, docs, or
   another protected surface, set the decision block to exactly
   `FOLLOW_UP_BRANCH_MODE: PublishMode branch only unless later authorized by a human`.
   Acceptance: the new EXEC contains one and only one selected follow-up, its
   blocker claims are true against the current tree, and the branch-mode string
   matches the Task Packet rule for the selected surface.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred. This correction packet does not approve implementing
any selected follow-up; it approves only a corrected EXEC packet.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with these correction-round additions:

- The new EXEC must not repeat the false claim that `EditorShell` lacks
  `with_world(world)`.
- The new EXEC must re-derive the selected follow-up from current code evidence.
- The correction round must leave tracked source, docs, Cargo, schema, scripts,
  workflows, task brief, dispatch logs, and prior handoff packets untouched.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `git status --short --branch` -> expected exit 0; branch
  `ai-dispatch/ISSUE-219`, with no tracked source, docs, Cargo, schema, script,
  workflow, task-brief, dispatch-log, or scheduler changes.
- `git diff --name-only` -> expected exit 0; empty for tracked files.
- `git diff --check` -> expected exit 0.
- `Get-Content -LiteralPath .ai/dispatch-ISSUE-219/verification.round1.log -Tail 30`
  -> expected exit 0; log ends with `VERIFY OK: all 7 verification step(s) passed.`
- `findstr /n /c:"pub fn with_world" crates\editor-shell\src\lifecycle\mod.rs`
  -> expected exit 0; current tree shows the constructor at line 567.
- `findstr /n /c:"EditorShell::with_world(world)" crates\editor-shell\tests\time_scale_test.rs`
  -> expected exit 0; current tests show uses at lines 277 and 296.
- `$newExec = Get-ChildItem -LiteralPath ai_handoffs -Filter 'ISSUE-219_EXEC_*.md' | Sort-Object Name | Select-Object -Last 1; findstr /n /c:"EditorShell lacks" $newExec.FullName`
  -> expected exit 1; the new EXEC must not contain the false premise.
- If the new EXEC selects an `rge-editor -> rge-scene-loader` follow-up:
  - `findstr /n /c:"rge-scene-loader" editor\rge-editor\Cargo.toml` -> expected exit 1.
  - `findstr /n /c:"load_scene_into_world" editor\rge-editor\src\main.rs` -> expected exit 1.
  - `findstr /n /c:"--glb" editor\rge-editor\src\main.rs` -> expected exit 0.
  - `$newExec = Get-ChildItem -LiteralPath ai_handoffs -Filter 'ISSUE-219_EXEC_*.md' | Sort-Object Name | Select-Object -Last 1; findstr /n /c:"FOLLOW_UP_BRANCH_MODE: PublishMode branch only unless later authorized by a human" $newExec.FullName` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, with this correction-round clarification: halt
rather than edit source, docs, Cargo, schemas, scripts, workflows, task brief,
dispatch logs, prior handoff packets, or any file other than the new
correction-round ISSUE-219 EXEC packet and its normal sidecar.

## Planner Notes

The previous correction fixed the branch-mode string and canonical verification
round1 passed with no tracked diff. The remaining review blocker is factual:
the latest EXEC's substrate prerequisite says `EditorShell` lacks
`with_world(world)`, but the current tree defines and tests that constructor.
Repair that premise first, then choose the smallest safe follow-up from the
remaining true blockers only. Do not implement the follow-up in this dispatch.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-219
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
