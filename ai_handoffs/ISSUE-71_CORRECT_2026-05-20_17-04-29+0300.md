# Correction Packet

DISPATCH_ID: ISSUE-71
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_17-04-29+0300
RELATED_FILES:
- crates/script-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-71_TASK_2026-05-20_16-58-26+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-71_EXEC_2026-05-20_17-03-45+0300.md`
- Latest Review Report: none; the canonical post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.
- Verification failure: `.ai/dispatch.verify.ps1` step 1, `cargo +nightly fmt --check`, reported incorrect newline style in `crates/script-graph/src/lib.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections: nothing more, nothing less.

1. **Normalize ScriptGraph test file newline style** - Verification gate finding `cargo +nightly fmt --check` failed with `Incorrect newline style in \\?\A:\RCAD\RGE\crates\script-graph\src\lib.rs`. Required change: normalize `crates/script-graph/src/lib.rs` to the newline style expected by `cargo +nightly fmt --all -- --check`, using `cargo +nightly fmt --all` or an equivalent formatting-only rewrite. Do not change test behavior, production API, manifests, docs, scripts, schemas, packets, or any file other than `crates/script-graph/src/lib.rs`. Acceptance: `cargo +nightly fmt --all -- --check` exits 0.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control review findings exist because the canonical verification gate failed before control review.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-round gate added: the canonical dispatch verification gate must pass after the newline-style normalization.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-script-graph invalidation_propagates_through_script_outgoing_edges` -> expected exit 0, with `invalidation_propagates_through_script_outgoing_edges` passing.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet. Additionally, halt and report `BLOCKED` if making `cargo +nightly fmt --all -- --check` pass requires any semantic source change, production API change, manifest edit, documentation edit, script edit, schema edit, packet edit other than the Executor's own execution report, or any file edit outside `crates/script-graph/src/lib.rs`.

## Planner Notes

The prior execution satisfied the scoped ScriptGraph test work, but the dispatch cannot proceed while the canonical post-execution verification gate fails. This correction approves only a formatting/newline-style repair for `crates/script-graph/src/lib.rs`. Do not broaden the work beyond making the failed fmt gate and the canonical verification gate pass.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-71
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
