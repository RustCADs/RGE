# Task Packet

DISPATCH_ID: ISSUE-421
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-19_02-13-08+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/tests.rs
- crates/editor-actions/src
- crates/editor-actions/Cargo.toml
- .ai/dispatch.tasks.md
STATUS: OPEN

## Goal

Perform GitHub issue #421 as a gated, source-read-only audit of task 162's
post-delete CAD mutation boundary. Verify from the allowed source surfaces that
the current CAD cuboid delete is routed through the editor-shell CommandBus
context, `rge-editor-actions` remains generic with no CAD/editor-shell
coupling, and the shell-owned CAD lifecycle is coherent across
add/delete/undo/redo. End by recording exactly one new task-163
`NEEDS_HUMAN_RECORDED:` marker plus an immediately following `Recommendation for
human approval` block in `.ai/dispatch.tasks.md`; do not append task 164 and do
not implement the recommendation.

This dispatch is the task 163 audit that task 162 appended. The task-162
self-rearm paragraph was carried into task 163 for provenance; it is not
authorization to append another task 163 or any task 164.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md`
- `ai_handoffs/ISSUE-421_*.md`
- `ai_handoffs/ISSUE-421_*.meta.json`
- `ai_handoffs/claims/ISSUE-421_*.json`
- `ai_dispatch_logs/log_*ISSUE-421*.md`

The `.ai/dispatch.tasks.md` edit is limited to the task 163 audit conclusion:
exactly one new line beginning `NEEDS_HUMAN_RECORDED: ` with the actual local
ISO execution date, a dash, and a source-grounded reason, immediately followed
by one `Recommendation for human approval` block covering the five required
parts. The generated handoff/claim/log paths are limited to current-dispatch
ISSUE-421 artifacts.

### MUST NOT edit
- `crates/**`
- `editor/**`
- `kernel/**`
- `runtime/**`
- `tools/**`
- `docs/**`
- `plans/**`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `AI_DISPATCH_AUTOMATION.md`
- `AI_DISPATCH_PARALLEL.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/**`
- `.ai/*.schema.json`
- `.ai/dispatch.verify.ps1`
- `Invoke-*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-*.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/templates/**`
- `ai_handoffs/ISSUE-420_*`
- any handoff, sidecar, claim, or dispatch-log artifact not generated for
  current dispatch ISSUE-421

### MAY add new files
- `ai_handoffs/ISSUE-421_EXEC_*.md`
- `ai_handoffs/ISSUE-421_EXEC_*.meta.json`
- `ai_handoffs/claims/ISSUE-421_*.json`
- `ai_dispatch_logs/log_*ISSUE-421*.md`

### MUST NOT add new files
- `crates/**`
- `editor/**`
- `kernel/**`
- `runtime/**`
- `tools/**`
- `docs/**`
- `plans/**`
- `.github/**`
- any Cargo manifest, `Cargo.lock`, schema, script, ADR, packet template,
  non-current-dispatch artifact, task-164 artifact, or feature-task artifact

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - ai_handoffs/ISSUE-421_*.md
  - ai_handoffs/ISSUE-421_*.meta.json
  - ai_handoffs/claims/ISSUE-421_*.json
  - ai_dispatch_logs/log_*ISSUE-421*.md
MUST_NOT_EDIT:
  - crates/**
  - editor/**
  - kernel/**
  - runtime/**
  - tools/**
  - docs/**
  - plans/**
  - Status.md
  - HANDOFF.md
  - change.md
  - AI_DISPATCH_AUTOMATION.md
  - AI_DISPATCH_PARALLEL.md
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/**
  - .ai/*.schema.json
  - .ai/dispatch.verify.ps1
  - Invoke-*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-*.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
  - ai_handoffs/ISSUE-420_*
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A source-grounded audit report in the EXEC packet covering the task 162
  post-delete CAD mutation boundary.
- Explicit answers for whether delete is CommandBus-routed, whether
  `rge-editor-actions` remains generic, and whether add/delete/undo/redo
  preserve graph head, tracked entity, CAD-world liveness, projection cache,
  stale render-mesh lookups, selection pruning, dirty state, and bus cursor
  movement.
- A `.ai/dispatch.tasks.md` task 163 conclusion containing exactly one new
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - source-grounded reason` marker.
- An immediately following `Recommendation for human approval` block covering:
  proposed next feature, exact edit surface, risks, verification, and why the
  proposal is the smallest coherent next step.
- Verification output proving no source/test/Cargo/docs edit occurred, no task
  164 was appended, and only allowed current-dispatch artifacts changed.

## Acceptance Criteria

- The audit inspects only `crates/editor-shell/src/lifecycle/commands.rs`,
  `crates/editor-shell/src/lifecycle/tests.rs`, `crates/editor-actions/src`,
  `crates/editor-actions/Cargo.toml`, `.ai/dispatch.tasks.md`, and the local
  current-dispatch issue/task artifacts needed to record the dispatcher
  snapshot rule. Do not inspect other source/docs surfaces to build audit
  evidence.
- The audit cites source for the delete route:
  `EditorShell::delete_current_cad_cuboid` captures a
  `DeleteCurrentCadCuboid` before submit and applies it through
  `submit_shell_action` / `command_bus.submit` using the existing
  `EditorShellActionContext` / `ShellActionContext` path.
- The audit cites source and greps for the generic `rge-editor-actions`
  boundary. `crates/editor-actions/src` and `crates/editor-actions/Cargo.toml`
  must have no editor-shell, cad-core, cad-projection, CAD graph/projection,
  CAD-world, B-Rep, render, or shell-specific action/context coupling.
- The audit cites tests/source for coherent shell-owned CAD lifecycle across
  add/delete/undo/redo, including fresh tracked entity on undo, no stale
  `render_mesh_for` lookup, entity/face selection pruning, rejected invalid
  states without stack growth, and CommandBus dirty/save-mark/cursor movement.
- The audit classifies any risk as an audit finding only. It must not fix,
  refactor, add tests, edit Cargo, update docs, or implement a recommendation.
- `.ai/dispatch.tasks.md` receives exactly one new task-163
  `NEEDS_HUMAN_RECORDED:` marker with the actual local ISO execution date and a
  source-grounded reason, immediately followed by a `Recommendation for human
  approval` block with the five required parts.
- No task 164, feature task, implementation approval, source edit, test edit,
  Cargo edit, docs edit, workflow edit, script edit, schema edit, template edit,
  or unrelated handoff/log edit is made.
- Queue/already-filed claims use only the dispatcher-provided GitHub snapshot
  from this TASK packet or an exact local issue-body artifact. The executor
  must not run `gh`, browser, network, GitHub API, or live remote lookup
  commands.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion in this packet is backed by an inverse
search or source read below. The Executor must re-run relevant searches after
the `.ai/dispatch.tasks.md` marker edit before making final negative claims in
the EXEC packet.

- Claim: `.ai/dispatch.tasks.md` currently contains task 162 and task 163
  headings, contains no task 164 heading, and has no completed task-163 marker
  yet. Existing `NEEDS_HUMAN_RECORDED` / `Recommendation for human approval`
  matches are historical or instructional and must be classified, not counted
  as the new task-163 conclusion.
  Falsifying search:
  `rg -n "^162\.|^163\.|^164\.|NEEDS_HUMAN_RECORDED|Recommendation for human approval" .ai/dispatch.tasks.md`
  -> observed task 162 at line 16246, task 163 at line 16379, no `^164.`
  heading, and many historical/instructional marker references including prior
  recommendation blocks.
- Claim: the generic-context no-CAD-reference guard currently finds no
  CAD/editor-shell/render/shell-specific coupling in `rge-editor-actions`
  source or manifest.
  Falsifying search:
  `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world|B-?Rep|BRep|render|Render|EditorShell|ShellActionContext|shell-specific|shell" crates/editor-actions/src crates/editor-actions/Cargo.toml`
  -> no matches, exit 1.
- Claim: task 162 added a shell-owned delete surface in the allowed lifecycle
  files, and the source includes the expected CommandBus route and undo/redo
  helpers.
  Source/search evidence:
  `rg -n "delete_current_cad_cuboid|DeleteCurrentCadCuboid|CadCuboidDelete|submit_shell_action|command_bus\.submit|undo_command|redo_command|mark_saved_command|AddCadCuboidToEmptyScene" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> observed `DeleteCurrentCadCuboid`, `CadCuboidDeleteResult`,
  `CadCuboidDeleteError`, `EditorShell::delete_current_cad_cuboid`,
  `submit_shell_action`, `command_bus.submit`, add/delete tests, and bus
  undo/redo/save-mark calls in the two allowed files.
- Claim: task 162 tests include delete/add/undo/redo lifecycle evidence in the
  allowed lifecycle test file.
  Source/search evidence:
  `rg -n "delete_current_cad_cuboid|DeleteCurrentCadCuboid|CadCuboidDelete|bus_routed_cad_cuboid_delete|render_mesh_for|face_selection|command_bus\(\)\.stack\(\)|is_dirty\(|undo_command|redo_command" crates/editor-shell/src/lifecycle/tests.rs`
  -> observed delete tests at lines 2633, 2691, 2780, the shared rejected-state
  helper at line 2865, invalid-state rejection test at line 2907, and many
  assertions covering render-mesh lookup, face selection, bus stack/cursor,
  dirty state, undo, and redo.
- Claim: the audit must not use live GitHub state.
  Falsifying source: this TASK packet records the dispatcher GitHub snapshot
  supplied with issue #421. The snapshot was generated by
  `Invoke-AiDispatchAuto.ps1` at `2026-06-19T02:12:43.0356692+03:00` before
  issue #421 was created. It states repo `RustCADs/RGE`, no open ai-dispatch
  issues before issue creation, no open failed autonomous issues, and prior
  autonomous issue #420 closed as "Delete current CAD cuboid through
  CommandBus". Use this snapshot or an exact local issue-body artifact; do not
  query live GitHub.
- Claim: the audit can be completed without source/test/Cargo/docs edits
  beyond the final task marker/recommendation.
  Falsifying search to run before final EXEC:
  `git diff --name-only` -> all changed paths must be `.ai/dispatch.tasks.md`
  plus current-dispatch ISSUE-421 handoff/sidecar/claim/log artifacts only.

## Constraints / Non-Goals

- This is a gated audit only. It is not a feature, fix, refactor, cleanup,
  test-addition, docs-update, dependency update, or task-selection dispatch.
- Do not edit Rust source, tests, Cargo manifests, `Cargo.lock`, docs, ADRs,
  plans, status/history files, automation scripts, workflows, schemas, packet
  templates, or unrelated handoff/log artifacts.
- Do not append task 164 or any feature/implementation task.
- Do not approve, implement, or partially implement the recommendation.
- Do not inspect source/docs outside the explicit allowed audit surfaces:
  `crates/editor-shell/src/lifecycle/commands.rs`,
  `crates/editor-shell/src/lifecycle/tests.rs`,
  `crates/editor-actions/src`, and `crates/editor-actions/Cargo.toml`.
- Do not broaden the CAD mutation boundary into UI/menu/shortcut routing,
  render-path refresh, CAD-core/cad-projection changes, save/load persistence,
  multi-root CAD composition, duplication, transforms, parameter editing,
  camera/viewport behavior, plugin execution, OS clipboard behavior, or
  Cargo/dependency work.
- Do not use `gh`, browser, network, GitHub API, or live remote lookup for
  GitHub queue or already-filed claims.

## Verification Gates

The Executor MUST run and document the command, exit code, and relevant output
summary for each of these in the EXECUTION_REPORT. If any gate fails, classify
the failure as an audit finding and do not fix it without a fresh Planner
packet.

- `cargo test -p rge-editor-shell --lib -- delete_current_cad_cuboid` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib -- add_cad_cuboid_to_empty_scene` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib -- clear_stale_tracked_cad_entity` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib -- cad_scene_inspection` -> expected exit 0.
- `cargo check -p rge-editor-actions -p rge-editor-shell` -> expected exit 0.
- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world|B-?Rep|BRep|render|Render|EditorShell|ShellActionContext|shell-specific|shell" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> expected no matches, exit 1.
- `rg -n "delete_current_cad_cuboid|DeleteCurrentCadCuboid|CadCuboidDelete|submit_shell_action|command_bus\.submit|undo_command|redo_command|mark_saved_command|AddCadCuboidToEmptyScene" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/tests.rs` -> expected matches in the shell-owned add/delete action and bus route surface.
- `rg -n "delete_current_cad_cuboid|DeleteCurrentCadCuboid|CadCuboidDelete|render_mesh_for|face_selection|command_bus\(\)\.stack\(\)|is_dirty\(|undo_command|redo_command" crates/editor-shell/src/lifecycle/tests.rs` -> expected matches in focused add/delete/undo/redo lifecycle tests.
- `rg -n "^162\.|^163\.|^164\.|NEEDS_HUMAN_RECORDED|Recommendation for human approval" .ai/dispatch.tasks.md` -> expected task 162 and exactly one task 163; no task 164; exactly one new task-163 `NEEDS_HUMAN_RECORDED:` marker and one immediately following recommendation block, with older marker/recommendation hits classified as historical or instructional.
- Read the dispatcher snapshot facts recorded in this TASK packet or an exact
  local issue-body artifact and record that no live GitHub lookup was used.
- `git diff --name-only` -> expected only `.ai/dispatch.tasks.md` plus
  current-dispatch ISSUE-421 handoff/sidecar/claim/log artifacts.
- `git diff --check` -> expected exit 0.

The orchestrator's canonical `.ai/dispatch.verify.ps1` gate may run after this
read-only audit. If it fails on an out-of-scope target and a correction would
require forbidden edits, halt per the halt conditions rather than expanding
scope.

## Halt Conditions

The Executor MUST halt, write the EXEC packet with the appropriate blocked or
needs-human status, and not force the audit conclusion if any of these occur:

- The audit cannot cite the dispatcher-provided GitHub snapshot from this TASK
  packet or an exact local issue-body artifact without using `gh`, browser,
  network, GitHub API, or a live remote lookup.
- The audit would require editing Rust source/tests, docs, automation,
  workflows, schemas, Cargo metadata, packet templates, or any path outside the
  explicit MAY edit/add surface.
- The audit cannot make one source-grounded recommendation without resolving
  more than one coherent next feature boundary.
- Recording exactly one new task-163 `NEEDS_HUMAN_RECORDED:` marker plus the
  required recommendation block would disturb existing task provenance.
- Another concurrent edit has already recorded the task 163 human marker or
  appended task 164 before this executor can finish.
- Any correction round asks the executor to fix source/test/Cargo/docs or
  otherwise edit a MUST NOT path.
- Any verification gate shows source/test/Cargo/docs/workflow/script/schema/
  template changes or generated artifacts outside current-dispatch ISSUE-421
  scope.

## Planner Notes

This packet intentionally narrows the inherited task-162 self-rearm wording.
Task 163 already exists and is the current dispatch. The active end state is a
brief-enforced human pause: exactly one new task-163 `NEEDS_HUMAN_RECORDED:`
line, one immediately following `Recommendation for human approval` block, and
no task 164.

The executor should treat the final recommendation as a proposal only. It must
not self-arm the next task, implement the proposal, or broaden the audit beyond
the four allowed source/Cargo surfaces named in the user goal.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-421
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
