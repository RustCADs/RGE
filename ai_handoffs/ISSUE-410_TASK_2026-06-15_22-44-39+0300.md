# Task Packet

DISPATCH_ID: ISSUE-410
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_22-44-39+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/tests.rs
- crates/editor-shell/src/lifecycle/mod.rs
- crates/cad-projection/src/lib.rs
- crates/cad-projection/src/render_adapter.rs
- crates/cad-projection/src/projection_structural/mod.rs
STATUS: OPEN

## Goal

Complete audit task 155: read current local source/tests/docs plus the
dispatcher-provided GitHub-state snapshot only, then choose exactly one
smallest bounded FEATURE task 156 that can safely follow the tracked-CAD
liveness inspection work, or record a single `NEEDS_HUMAN_RECORDED:` line if
every candidate crosses a product, policy, or architecture boundary. This is a
source/docs audit and task-selection dispatch, not an implementation dispatch.
It exists to keep the autonomous task chain moving after ISSUE-409 added
tracked-CAD liveness fields to `CadSceneInspection`.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-410_*`
- `ai_dispatch_logs/log_ISSUE-410*`
- `.ai/dispatch-ISSUE-410/**`

### MUST NOT edit
- `crates/**`
- `editor/**`
- `runtime/**`
- `kernel/**`
- `tools/**`
- `docs/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/**`
- `AI_DISPATCH_AUTOMATION.md`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-*.ps1`
- `Register-*.ps1`
- `Get-*.ps1`
- `Watch-*.ps1`
- `Wait-*.ps1`
- `new-handoff.ps1`
- unrelated existing `ai_handoffs/**`, `ai_dispatch_logs/**`, or `.ai/dispatch-*/**` artifacts from other dispatches

### MAY add new files
- `ai_handoffs/ISSUE-410_*`
- `ai_dispatch_logs/log_ISSUE-410*`
- `.ai/dispatch-ISSUE-410/**`

### MUST NOT add new files
- New Rust source or test files under `crates/**`, `editor/**`, `runtime/**`, `kernel/**`, or `tools/**`
- New docs, ADRs, schemas, workflows, scripts, Cargo manifests, lockfiles, packet templates, architecture-lint rules/config, or unrelated handoff/log artifacts
- Any task heading beyond task 156, including a task 157 heading in `.ai/dispatch.tasks.md`

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-410_*
  - ai_dispatch_logs/log_ISSUE-410*
  - .ai/dispatch-ISSUE-410/**
MUST_NOT_EDIT:
  - crates/**
  - editor/**
  - runtime/**
  - kernel/**
  - tools/**
  - docs/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/**
  - AI_DISPATCH_AUTOMATION.md
  - ai_handoffs/AI_HANDOFF_PROTOCOL.md
  - ai_handoffs/templates/**
  - .ai/dispatch.verify.ps1
  - .ai/*.schema.json
  - Invoke-*.ps1
  - Register-*.ps1
  - Get-*.ps1
  - Watch-*.ps1
  - Wait-*.ps1
  - new-handoff.ps1
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A recorded audit in `.ai/dispatch.tasks.md`, `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md` summarizing the source/docs evidence, the dispatcher-snapshot evidence, the selected boundary, and the explicit non-changes.
- `.ai/dispatch.tasks.md` marks task 155 complete and, as the final step, appends exactly one bounded FEATURE task 156 with explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions`, and carries the same Self-re-arm final-step requirement so the chain continues.
- If no bounded in-policy feature exists, `.ai/dispatch.tasks.md` instead appends exactly one `NEEDS_HUMAN_RECORDED:` line with the actual ISO date and a concrete source-grounded reason; no task 156 is appended in that case.
- The EXECUTION_REPORT records the before/after task-heading checks, source greps, dispatcher-snapshot citation, no-implementation proof, no-extra-task proof, and all verification command results.

## Acceptance Criteria

- The executor uses only the dispatcher-provided GitHub-state snapshot copied into the issue body or a local artifact containing the same snapshot for queue-state and already-filed autonomous-task claims. No live `gh`, browser, network, GitHub API, or web lookup is run.
- The audit reads current local source/tests/docs before selecting task 156 or recording `NEEDS_HUMAN_RECORDED:`.
- The candidate comparison covers the post-tracked-CAD liveness boundary, including lifecycle-only cleanup/consistency around stale tracked CAD entity ids after graph restore/projection despawn, making `CadSceneInspection` useful to a future caller without UI wiring, source/test-only gaps exposed by tracked liveness fields, and any smaller current-source CAD/editor-shell boundary.
- Every negative or unchanged current-source claim in the audit includes a practical falsifying search/read command and observed result.
- Exactly one outcome is present after edits: one bounded FEATURE task 156, or one new `NEEDS_HUMAN_RECORDED:` record. There is no task 157 and no second follow-up task.
- No implementation work for task 156 is done. The diff does not touch Rust source/tests, Cargo metadata, workflows, scripts, schemas, packet templates, automation, or unrelated artifacts.

## Dispatcher Snapshot Evidence

Use this snapshot as the only GitHub evidence for this dispatch. It was
generated by `Invoke-AiDispatchAuto.ps1` at
`2026-06-15T22:44:09.6818191+03:00`, before ISSUE-410 was created.

- Repo: `RustCADs/RGE`.
- Open `ai-dispatch` issues before ISSUE-410 was created: none.
- Open failed autonomous issues labelled `ai-auto` plus `ai-dispatch-failed`: none.
- Autonomous issues already filed in the snapshot are all closed and run through ISSUE-409, titled `Add tracked-CAD liveness to CadSceneInspection`.
- There is no dispatcher-snapshot evidence of a pre-existing autonomous issue for the post-ISSUE-409 task 155 audit or for a task 156 follow-up before ISSUE-410 was created.

The executor must cite this section or the exact local issue-body artifact that
contains the same snapshot. It must not attempt to refresh the GitHub state.

## Current-State Claims: Falsification Required

Every negative / "unchanged" assertion this packet makes about current source
or task state lists its inverse-search command and result below. The executor
must repeat the relevant checks immediately before editing because the working
tree may have changed after planning.

- Claim: task 154 and task 155 headings exist in `.ai/dispatch.tasks.md` before this dispatch.
  Falsifying search: `rg -n "^154\.|^155\." .ai/dispatch.tasks.md` -> matched `154. **Add tracked-CAD-entity liveness fields to CadSceneInspection (editor-shell only).**` and `155. **Audit post-tracked-CAD-liveness inspection boundary.**`.
- Claim: no task 156 or task 157 heading exists in `.ai/dispatch.tasks.md` before this dispatch.
  Falsifying search: `rg -n "^156\.|^157\." .ai/dispatch.tasks.md` -> no matches.
- Claim: current source contains the tracked-CAD liveness inspection surface that motivates this audit.
  Falsifying search: `rg -n "CadSceneInspection|cad_scene_inspection|tracked_cad_entity_live|tracked_cad_entity_render_mesh_present|add_cad_cuboid_to_empty_scene|restore_to|despawn_brep_entity|render_mesh_for|BRepHandle" crates/editor-shell/src/lifecycle crates/cad-projection/src` -> matches in `crates/editor-shell/src/lifecycle/commands.rs`, `crates/editor-shell/src/lifecycle/tests.rs`, `crates/editor-shell/src/lifecycle/mod.rs`, and `crates/cad-projection/src/**`.

## Constraints / Non-Goals

- Do not implement task 156 in this dispatch.
- Do not edit Rust source/tests, Cargo metadata, workflows, scripts, schemas, packet templates, architecture lints, automation, or unrelated generated artifacts.
- Do not add UI wiring, menu items, shortcuts, command-palette rows, plugin routes, CommandBus actions, undo/dirty/save behavior, save/load/open/close behavior, camera/navigation behavior, render refresh, render-path behavior, mesh upload, GPU state, CAD-core behavior, or cad-projection behavior.
- Do not use live GitHub/network evidence. The dispatcher snapshot is the only allowed GitHub-state evidence.
- Do not append more than one follow-up. Under the Self-re-arm protocol this audit may append exactly task 156, or exactly one `NEEDS_HUMAN_RECORDED:` line, but not both.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- Before edits: `rg -n "^154\.|^155\.|^156\.|^157\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
- Source audit grep: `rg -n "CadSceneInspection|cad_scene_inspection|tracked_cad_entity|add_cad_cuboid_to_empty_scene|restore_to|despawn_brep_entity|render_mesh_for|BRepHandle" crates/editor-shell/src/lifecycle crates/cad-projection/src`
- CommandBus non-implementation guard: `rg -n "CommandBus|Action|submit\(|undo_command|redo_command|mark_saved_command" crates/editor-actions/src crates/editor-shell/src/lifecycle/commands.rs`
- After edits: `rg -n "^154\.|^155\.|^156\.|^157\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md`
- `git diff --name-only`
- `git diff --check`
- Finalize dry-run for the EXEC packet: run `.\new-handoff.ps1 -Finalize -DryRun` with `-PacketPath` set to the actual ISSUE-410 EXEC packet path.

Expected verification outcomes:

- The before-edit heading check proves task 155 is present and no task 156 or 157 heading pre-exists.
- The after-edit heading check proves task 155 is done and exactly one allowed final-step outcome exists: task 156 or one new `NEEDS_HUMAN_RECORDED:` line; it also proves no task 157 exists.
- `git diff --name-only` lists only the allowed documentation/task files plus generated ISSUE-410 artifacts.
- `git diff --check` exits 0.
- The EXEC packet finalize dry-run exits 0.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- The dispatcher snapshot cannot be cited from the issue body or a local copied artifact without live `gh`, browser, network, GitHub API, or web access.
- The pre-edit heading check shows a pre-existing task 156, task 157, or new live `NEEDS_HUMAN_RECORDED:` marker that would make a single self-rearm outcome ambiguous.
- The audit would require editing any MUST-NOT path or performing implementation work to make the selected feature coherent.
- The smallest coherent follow-up is a batch, roadmap item, or policy/architecture decision rather than one bounded feature dispatch.
- More than one follow-up task would be needed to make the selected boundary safe.
- The canonical verification/correction loop asks the executor to fix an out-of-scope failure in source, Cargo, workflows, automation, schemas, or another MUST-NOT surface. Preserve the audit scope and halt instead.
- Appending exactly one task 156 or exactly one `NEEDS_HUMAN_RECORDED:` line would disturb existing task provenance.

## Planner Notes

This packet intentionally mirrors task 155 from `.ai/dispatch.tasks.md` while
making the GitHub evidence rule self-contained. The preceding feature,
ISSUE-409/task 154, added `tracked_cad_entity_present`,
`tracked_cad_entity_live`, and `tracked_cad_entity_render_mesh_present` to
`CadSceneInspection` in `editor-shell` and tests. This audit must decide the
next smallest feature from current source, not from stale roadmap memory, and
must leave the alternating Self-re-arm chain intact.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-410
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
