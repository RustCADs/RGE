# Correction Packet

DISPATCH_ID: ISSUE-61
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_08-51-10+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_material_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-61_TASK_2026-05-20_08-42-33+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-61_EXEC_2026-05-20_08-50-18+0300.md`
- Latest Review Report: none; this correction is driven by the post-execution canonical verification gate failure.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification finding it addresses.

1. **Normalize material smoke test newline style** - Post-execution verification
   finding: `cargo +nightly fmt --all -- --check` failed with
   `Incorrect newline style in \\?\A:\RCAD\RGE\crates\editor-ui\tests\node_graph_material_smoke.rs`.
   Required change: convert `crates/editor-ui/tests/node_graph_material_smoke.rs`
   to the newline style expected by rustfmt, preserving the file's code content,
   assertions, ordering, and final newline. Do not change any other source,
   manifest, lockfile, doc, script, schema, or packet as part of this
   correction. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and
   shows no remaining newline-style complaint for this file.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred. The only known blocker is the rustfmt newline-style
failure above.

## Updated Acceptance Criteria

Unchanged from Task Packet, with one additional correction-round requirement:
`crates/editor-ui/tests/node_graph_material_smoke.rs` must be newline-normalized
only, with no semantic code changes.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, plus halt if normalizing newline style would require
any code-content change or any edit outside
`crates/editor-ui/tests/node_graph_material_smoke.rs`.

## Planner Notes

The canonical verification gate failed before review because rustfmt rejected
the newline style in an existing editor-ui material smoke test file. This round
authorizes only the mechanical newline normalization required to unblock
formatting and the canonical verification gate. Do not revisit the issue #61
animation smoke implementation unless one of the re-verification gates reports a
new failure directly caused by this correction.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-61
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
