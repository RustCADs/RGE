# Correction Packet

DISPATCH_ID: ISSUE-15
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_09-34-35+0300
RELATED_FILES:
- crates/io-image/src/format_detect.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-15_TASK_2026-05-18_09-27-28+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-15_EXEC_2026-05-18_09-33-52+0300.md`
- Latest Review Report: none; the orchestrated post-execution verification gate failed before a review packet was written.
- Prior Correct Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing less.
Each correction references the specific verification failure it addresses.

1. **Normalize rustfmt newline style** - Post-execution verification failure:
   `cargo +nightly fmt --all -- --check` reported `Incorrect newline style in \\?\A:\RCAD\RGE\crates\io-image\src\format_detect.rs`.
   Required change: normalize only `crates/io-image/src/format_detect.rs` to the newline style expected by the workspace rustfmt configuration. Do not change Rust code semantics, declarations, comments, imports, formatting in unrelated files, dependency metadata, docs, schemas, scripts, or any handoff packets other than the follow-up execution report required by the protocol. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and the diff for `crates/io-image/src/format_detect.rs` is limited to newline-style normalization.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred. The only approved correction is the newline-style fix required for the canonical verification gate to pass.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with one verification-only addition: the repository-level rustfmt newline-style check must pass after normalizing `crates/io-image/src/format_detect.rs`. This correction does not approve any ISSUE-15 feature, dependency, shader, pipeline, or test-scope changes beyond the already executed task.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

If `.ai/dispatch.verify.ps1` cannot be run in the environment, run and document the same canonical gates it would run: format, architecture lints, supply chain, workspace tests, and doctests. Any new failure not caused by the newline-style issue above is a halt condition unless it can be fixed within this packet's approved file envelope.

## Halt Conditions (Updated if Any)

Halt and return to the Planner if satisfying the verification gate appears to require any change other than newline-style normalization in `crates/io-image/src/format_detect.rs`, or if running the correction changes any file outside the approved correction file and the protocol-required follow-up execution report.

## Planner Notes

The prior implementation satisfied the ISSUE-15 task-level verification it reported, but the canonical dispatch verification failed on a workspace rustfmt newline-style error in `crates/io-image/src/format_detect.rs`. This packet approves only the minimal formatting normalization needed to unblock the canonical gate. It does not approve any broader cleanup or any change to the original ISSUE-15 shader-validation implementation.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-15
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
