# Task Packet

DISPATCH_ID: ISSUE-16
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_09-55-54+0300
RELATED_FILES:
- crates/cad-core/src/operators/sweep.rs
STATUS: OPEN

## Goal

Implement GitHub issue #16 by adding canonical per-triangle `TopologyFaceId` labels to `SweepOp::evaluate` in `crates/cad-core/src/operators/sweep.rs`. The dispatch should make Sweep output labeled tessellations using the existing `Tessellation::with_labels` substrate: first cap is face `0`, last cap is face `1`, and side faces are labeled in the same segment-major and edge-major order that side triangles are emitted.

## Scope

### MAY edit
- `crates/cad-core/src/operators/sweep.rs`, including `SweepOp::evaluate`, any required local import adjustment for `TopologyFaceId`, the local `Operator` implementation surface needed to keep labeled-output contracts consistent, and the existing local `#[cfg(test)] mod tests`.

### MUST NOT edit
- Any file other than `crates/cad-core/src/operators/sweep.rs`.
- Any source file outside `crates/cad-core/src/operators/sweep.rs`.
- Cargo manifests, lockfiles, docs, schemas, scripts, CI files, `.gitignore`, architecture-lint code, generated files, or existing handoff packets.
- Any projection, gfx, topology-provider, Boolean, extrude, loft, revolve, round-fillet, or BRep-related file.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-16_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new source files, integration tests, fixtures, docs, schemas, scripts, Cargo entries, architecture-lint files, projection files, gfx files, or generated artifacts.
- No handoff packets other than the protocol-required `ISSUE-16_EXEC` report and its finalize-generated sidecar.

## Deliverables

- `SweepOp::evaluate` MUST return a labeled `Tessellation` via `Tessellation::with_labels`, not an unlabeled `Tessellation::new`, for successful sweep output.
- The emitted `face_labels` vector MUST have exactly one `TopologyFaceId` per output triangle and preserve the existing triangle emission order.
- First-cap triangles MUST all be labeled `TopologyFaceId(0)`.
- Last-cap triangles MUST all be labeled `TopologyFaceId(1)`.
- Side-wall triangles MUST be labeled in emitted segment/edge order. For profile vertex count `n`, path segment index `k`, and profile edge index `i`, both triangles of that emitted side quad MUST be labeled `TopologyFaceId(2 + (k * n + i) as u64)`.
- `SweepOp`'s labeled-output behavior MUST remain internally consistent with the `Operator::output_is_labeled` contract.
- Local focused tests in `crates/cad-core/src/operators/sweep.rs` MUST cover canonical labels for both a 2-point path and a multi-segment path.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `crates/cad-core/src/operators/sweep.rs`.
- `SweepOp::evaluate` successful output is labeled: `mesh.is_labeled()` is true and `mesh.face_labels().is_some()`.
- `face_labels.len() == mesh.triangle_count()` for Sweep output.
- For a square profile along a 2-point path, labels are in this canonical order: first-cap two triangles as `TopologyFaceId(0)`, last-cap two triangles as `TopologyFaceId(1)`, then four side faces in edge order with two triangles each labeled `TopologyFaceId(2)`, `TopologyFaceId(3)`, `TopologyFaceId(4)`, and `TopologyFaceId(5)`.
- For a multi-segment path, tests assert that side labels advance by emitted path segment before profile edge. For example, with `n = 3` and two path segments, side quads are labeled `TopologyFaceId(2)`, `TopologyFaceId(3)`, `TopologyFaceId(4)`, then `TopologyFaceId(5)`, `TopologyFaceId(6)`, `TopologyFaceId(7)`, with two triangles per side quad.
- Existing Sweep geometry counts, validation behavior, winding correction behavior, and structural hash behavior continue to pass.
- No `BRepProvider`, new face-tag enum, projection logic, gfx logic, docs, dependency, Cargo, schema, script, or generated-file work is added.
- `cargo test -p rge-cad-core --lib operators::sweep -- --nocapture` exits 0.

## Constraints / Non-Goals

- Keep the work strictly inside `crates/cad-core/src/operators/sweep.rs`.
- This dispatch adds canonical per-triangle labels to Sweep tessellation output only; it does not add or change BRep provider APIs.
- Do not add `SweepFace`, `SweepFaceId`, or any new face-tag enum.
- Do not add projection hit-testing, cad-projection integration, gfx overlays, rendering behavior, UI behavior, or documentation.
- Do not alter Sweep geometry generation, path/profile validation semantics, monotonic-Z restrictions, vertex ordering, triangle winding, or structural hashing except where unavoidable to construct labels in parallel with the existing triangle order.
- Do not broaden tests outside local `sweep.rs` unit tests.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-cad-core` -> expected exit 0.
- `cargo test -p rge-cad-core --lib operators::sweep -- --nocapture` -> expected exit 0, including focused 2-point and multi-segment Sweep face-label tests.
- `git diff -- crates/cad-core/src/operators/sweep.rs` -> expected diff is limited to Sweep face-label emission, any required local `TopologyFaceId` import, labeled-output contract adjustment, and local Sweep tests.
- `git diff --check -- crates/cad-core/src/operators/sweep.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Any required edit outside `crates/cad-core/src/operators/sweep.rs` appears necessary.
- Passing the focused Sweep tests appears to require changing Sweep geometry, validation semantics, triangle emission order, winding, or structural hashing beyond adding labels in parallel with existing output.
- Implementing the requested labels appears to require adding `BRepProvider`, a new face-tag enum, projection logic, gfx logic, documentation, dependencies, Cargo changes, schemas, scripts, generated files, or tests outside local `sweep.rs`.
- `Tessellation::with_labels` rejects the constructed buffers for a reason other than a local label-count bug within the allowed scope.
- Formatting or the focused Sweep test gate fails for reasons unrelated to this dispatch and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #16. Revision number: 0. There is no prior Claude gate result. Existing `ExtrudeOp` and `LoftOp` already use cap labels `0` and `1` plus side labels after the caps; Sweep should follow the same substrate pattern while extending side identity across multiple emitted path segments.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-16
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
