# Correction Packet

DISPATCH_ID: ISSUE-38
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_06-31-44+0300
RELATED_FILES:
- crates/cad-projection/tests/render_adapter_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-38_TASK_2026-05-19_06-25-04+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-38_EXEC_2026-05-19_06-30-59+0300.md`
- Latest Review Report: none issued; correction is based on the canonical post-execution verification failure.
- Prior Correction Packets: none.
- Verification failure: `cargo +nightly fmt --all -- --check` failed with `Incorrect newline style in \\?\A:\RCAD\RGE\crates\cad-projection\tests\render_adapter_smoke.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific post-execution verification
finding it addresses.

1. **Normalize newline style** - Post-execution verification gate finding:
   `cargo +nightly fmt --all -- --check` rejected
   `crates/cad-projection/tests/render_adapter_smoke.rs` for incorrect newline
   style. Required change: normalize only
   `crates/cad-projection/tests/render_adapter_smoke.rs` to the newline style
   accepted by `cargo +nightly fmt --all -- --check`; do not change Rust logic,
   assertions, test names, imports, comments, packet files, or any other
   source/doc/config file as part of this correction. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0 and the canonical dispatch
   verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred. The only known blocker is the newline-style failure
reported by the canonical post-execution verification gate.

## Updated Acceptance Criteria

Unchanged from Task Packet. This correction only fixes file newline style so
the already implemented ISSUE-38 smoke can pass the formatter and canonical
verification gates.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` - expected exit 0.
- `.ai/dispatch.verify.ps1` - expected exit 0.

If `.ai/dispatch.verify.ps1` fails after the newline-style correction, halt and
report the exact failing step and log tail in the next execution report without
expanding scope.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Additionally, halt if making `cargo +nightly fmt
--all -- --check` pass requires any edit beyond newline normalization in
`crates/cad-projection/tests/render_adapter_smoke.rs`.

## Planner Notes

Do not revisit the test design or broaden the dispatch. The Executor's prior
targeted test passed; the post-execution blocker is purely the formatter's
newline-style check on the one MAY-edit Rust test file.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-38
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
