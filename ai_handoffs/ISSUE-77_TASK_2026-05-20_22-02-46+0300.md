# Task Packet

DISPATCH_ID: ISSUE-77
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_22-02-46+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: OPEN

## Goal

Implement GitHub issue #77 by adding a focused Phase 8 graph-foundation regression for `GraphDiff` after mutating a node payload through `Graph::node_mut`. The dispatch should prove that snapshotting before and after a direct mutable node payload edit reports exactly one node change with the expected old and new payloads, without introducing node/edge add/remove churn or edge-change noise.

## Scope

### MAY edit
- `kernel/graph-foundation/tests/diff_correctness.rs`

### MUST NOT edit
- Any file other than `kernel/graph-foundation/tests/diff_correctness.rs`, except for the protocol-required execution report packet described below.
- Any production/runtime code in `kernel/graph-foundation/src/**`, including `Graph`, `GraphDiff`, `GraphSnapshot`, `NodeId`, `EdgeId`, or `EdgeRecord`.
- Any domain graph crates or graph consumers outside `kernel/graph-foundation`.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`, or existing handoff packets.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-77_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, new integration test files, fixtures, snapshots, docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-77_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add exactly one focused integration test in `kernel/graph-foundation/tests/diff_correctness.rs` for mutating an existing node payload through `Graph::node_mut`.
- The test must snapshot the graph before and after the direct node mutation and compute `GraphDiff::between(&snap1, &snap2)`.
- The test must assert exactly one `changed_nodes` entry for the mutated node, with the expected old and new payloads.
- The test must assert no node additions, no node removals, no edge additions, no edge removals, and no changed edges.
- The test must assert precise `node_change_count()` and `edge_change_count()` results.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `kernel/graph-foundation/tests/diff_correctness.rs`.
- The new regression uses `Graph::node_mut(node_id).unwrap()` or equivalent direct mutable node access; it must not use `replace_node`, remove/reinsert the node, or change production code.
- The test uses an existing-node payload mutation such as changing node `2` from `"beta"` to `"beta-mutated"` after `snap1` is captured and before `snap2` is captured.
- The test includes at least one edge in the graph before snapshotting so the assertions prove node payload mutation does not create edge churn.
- The test asserts:
  - `diff.changed_nodes.len() == 1`
  - `diff.changed_nodes` contains the mutated `NodeId`
  - the changed node tuple preserves the expected old payload and new payload
  - `diff.added_nodes.len() == 0`
  - `diff.removed_nodes.len() == 0`
  - `diff.added_edges.len() == 0`
  - `diff.removed_edges.len() == 0`
  - `diff.changed_edges.len() == 0`
  - `diff.node_change_count() == 1`
  - `diff.edge_change_count() == 0`
- No runtime behavior, public API, dependency, Cargo configuration, documentation, domain graph crate, or source file changes are introduced.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` passes.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify `Graph`, `GraphDiff`, `GraphSnapshot`, graph storage, node mutation, snapshotting, diffing, or any other runtime behavior to satisfy the new test.
- Do not add merge semantics, reconciliation behavior, serialization coverage, traversal coverage, stable hashing coverage, visualization coverage, or architecture policy changes.
- Do not mutate edge payloads or edge endpoints in the new regression; ISSUE-77 is specifically about direct node payload mutation via `Graph::node_mut`.
- Do not add dependencies, fixtures, helper modules, new test files, Cargo manifest entries, docs, scripts, or snapshots.
- Do not rewrite unrelated tests in `diff_correctness.rs` except for minimal local cleanup needed to add the requested regression cleanly.
- Do not commit, stage, push, or create any packet other than the protocol-required execution report and optional EXEC sidecar.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` -> expected exit 0.
- `git diff -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected diff is limited to the requested node payload mutation regression test and any minimal local cleanup needed for that test.
- `git diff --check -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested node mutation regression fails against the existing production implementation and passing it would require changing `Graph`, `GraphDiff`, `GraphSnapshot`, `NodeId`, `EdgeId`, `EdgeRecord`, or any other runtime source.
- Any required edit outside `kernel/graph-foundation/tests/diff_correctness.rs` appears necessary.
- Any new file other than the protocol-required execution report and its finalize-generated sidecar appears necessary.
- The `node_mut` API shape differs from the expected mutable node payload access and the requested coverage cannot be expressed with a test-only edit.
- Implementing the requested coverage appears to require changing edge behavior, edge mutation tests, domain graph crates, or any public API.
- Formatting or tests fail for reasons unrelated to the requested regression and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #77. Revision number: 0. There is no prior Claude gate result. Keep the implementation narrow: add only the direct `Graph::node_mut` payload mutation regression in the existing `diff_correctness` integration test.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-77
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
