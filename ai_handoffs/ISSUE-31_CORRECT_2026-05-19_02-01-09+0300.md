# Correction Packet

DISPATCH_ID: ISSUE-31
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19T02:01:09+03:00
RELATED_FILES:
- crates/cad-projection/tests/face_picking_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-31_TASK_2026-05-19_01-53-33+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-31_EXEC_2026-05-19_01-59-44+0300.md`
- Latest Review Report: none; post-execution verification gate failed before control review could pass.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections: nothing more, nothing less.

1. **Normalize newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with `Incorrect newline style`
   in `crates/cad-projection/tests/face_picking_smoke.rs`. Required change:
   normalize that file's line endings to the Rustfmt-accepted style without
   changing its Rust code, assertions, imports, module comments, or test
   behavior. Acceptance: `cargo +nightly fmt --all -- --check` exits 0.

## Deferred Findings (NOT Approved for This Round)

None. The verification log reports only the newline-style failure above.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this additional correction-round
acceptance bar: the canonical format gate must pass after the line-ending-only
normalization.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0, unless it reaches a new
  unrelated pre-existing failure not caused by this correction; if so, halt and
  report the exact failing step.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus: halt if fixing the format gate would
require any content change beyond line-ending normalization in
`crates/cad-projection/tests/face_picking_smoke.rs`.

## Planner Notes

This is a verification-only correction. Do not modify the ISSUE-31 smoke,
production code, manifests, documentation, scripts, schemas, or any additional
handoff packets. The prior execution appears scoped to the intended
RoundFillet render-adapter smoke; the only known blocker is Rustfmt rejecting
the newline style of `face_picking_smoke.rs` during the canonical repository
format check.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-31
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
