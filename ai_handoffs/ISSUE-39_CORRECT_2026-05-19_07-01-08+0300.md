# Correction Packet

DISPATCH_ID: ISSUE-39
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_07-01-08+0300
RELATED_FILES:
- crates/cad-projection/tests/render_adapter_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-39_TASK_2026-05-19_06-54-43+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-39_EXEC_2026-05-19_07-00-17+0300.md`
- Latest Verification Log: `.ai/dispatch-ISSUE-39/verification.round0.log`
- Latest Review Report: none. The orchestrator stopped before Codex control
  review because canonical verification failed.
- Prior CORRECT packets in this dispatch: none.
- Verification failure: `cargo +nightly fmt --all -- --check` failed with
  `Incorrect newline style in \\?\A:\RCAD\RGE\crates\cad-projection\tests\render_adapter_smoke.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction - nothing more, nothing less.
The correction references the post-execution verification finding that blocked
the dispatch before control review.

1. **Normalize newline style** - Canonical verification failed in
   `cargo +nightly fmt --all -- --check` because
   `crates/cad-projection/tests/render_adapter_smoke.rs` has an incorrect
   newline style. Required change: normalize only
   `crates/cad-projection/tests/render_adapter_smoke.rs` to the newline style
   accepted by `cargo +nightly fmt --all -- --check`. Do not change Rust
   logic, assertions, imports, comments, test names, documentation,
   production code, Cargo metadata, scripts, schemas, or any other source file
   as part of this correction. Acceptance: `cargo +nightly fmt --all -- --check`
   exits 0, and the canonical dispatch verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control review findings exist for this run because verification
failed before control review. The only approved correction is the mechanical
newline-style fix reported by the canonical verification gate.

## Updated Acceptance Criteria

Unchanged from the Task Packet. This correction only removes the repo-wide
formatter blocker so the ISSUE-39 face-picking smoke can proceed through the
canonical verification gate and control review.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-cad-projection --test face_picking_smoke` -> expected
  exit 0, with the new ExtrudeOp top-cap picker smoke executed and passing.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0. This is the dispatch-passing
  gate; the dispatch cannot pass until it succeeds.
- `git status --short --untracked-files=all` -> expected no unexpected changes
  outside `crates/cad-projection/tests/face_picking_smoke.rs`,
  `crates/cad-projection/tests/render_adapter_smoke.rs`, and the required
  ISSUE-39 correction-round execution-report packet.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- Halt and route back to Planner if satisfying the formatter requires any edit
  beyond newline normalization in
  `crates/cad-projection/tests/render_adapter_smoke.rs`.
- Halt and preserve the failing output if the canonical verification gate
  exposes a new, distinct blocking failure after the newline-style correction.

## Planner Notes

Do not revisit the ISSUE-39 picker smoke design or broaden this correction.
The execution report says the focused face-picking test already passed. The
post-execution blocker is the first canonical CI step rejecting newline style
in `render_adapter_smoke.rs`, so this packet approves only that mechanical
normalization and the required re-verification.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-39
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
