# Execution Report

DISPATCH_ID: ISSUE-393
AUTHOR: Executor / Codex
TIMESTAMP: 2026-06-14_13-17-49+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/mod.rs - selected-CAD bounds helper and double-click framing fallback
- crates/editor-shell/src/lifecycle/tests.rs - selected-CAD double-click framing and fallback tests
- .ai/dispatch.tasks.md - marked task 139 done, with no task 140 appended
- Status.md - added ISSUE-393 status snapshot
- HANDOFF.md - added ISSUE-393 handoff snapshot
- plans/BASELINE.md - added update 43 for task 139 completion
- change.md - added ISSUE-393 change-log entry
- ai_handoffs/ISSUE-393_EXEC_2026-06-14_13-17-49+0300.md - execution report
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-393_TASK_2026-06-14_13-03-10+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## Current-State Claims: Falsification Re-run

- Claim: the current viewport double-click implementation frames the whole
  scene by calling `reset_camera()` from the existing left-press handler, while
  face-pick still runs through `should_fire_face_pick` first.
  Re-run before edits:
  `rg -n "handle_viewport_left_press|should_fire_face_pick|reset_camera\(|current_scene_bounds|render_mesh_for|viewport_left_double_click" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> exit 0; matches included `current_scene_bounds`, `render_mesh_for`,
  `handle_viewport_left_press`, `should_fire_face_pick`, and the then-current
  `self.reset_camera()` call in the double-click branch.
- Claim: there is no existing frame-selected command surface to reuse or
  modify.
  Re-run before edits:
  `rg -n "FrameSelected|Frame Selected|frame_selected|Command::FrameSelected" crates/editor-shell/src crates/editor-ui/src crates/editor-egui-host/src`
  -> exit 1, no matches. Re-run after helper rename also exited 1, no matches.
- Claim: the task brief currently has task 138 done, task 139 open, and no
  task 140.
  Re-run before edits:
  `rg -n "^138\.|^139\.|^140\." .ai/dispatch.tasks.md` -> exit 0; task 138
  was done, task 139 was open, and no task 140 matched.
- Claim: `EditorShell::with_world_projection_graph` is single-BRepHandle-only,
  so multi-selected CAD union tests must not broaden the public constructor.
  Re-run before edits:
  `rg -n "with_world_projection_graph|cad_world.query::<BRepHandle>|current_scene_bounds|render_mesh_for" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> exit 0; matches showed `with_world_projection_graph`, the single-entity
  query/assertion, `current_scene_bounds`, and `render_mesh_for`.

## What I Changed

### Source

- `crates/editor-shell/src/lifecycle/mod.rs`: added private
  `selected_cad_scene_bounds()` and `frame_cad_selection_or_reset_camera()`
  helpers. The selected path reads `self.coord.selection`, `self.projection`,
  and `self.cad_world`, resolves selected meshes through
  `CadProjection::render_mesh_for`, computes their union with
  `compute_aabb_union`, and frames with `isometric_camera_for_bounds`.
- `crates/editor-shell/src/lifecycle/mod.rs`: changed only the existing
  qualifying viewport left-double-click branch to call the selected-CAD helper
  before falling back to unchanged scene-wide `reset_camera()`.

### Tests

- `crates/editor-shell/src/lifecycle/tests.rs::viewport_left_double_click_selected_cad_entity_frames_selected_bounds`:
  selected single CAD entity frames its selected mesh bounds instead of the
  scene fallback.
- `crates/editor-shell/src/lifecycle/tests.rs::viewport_left_double_click_multiple_selected_cad_entities_frames_union`:
  multiple selected CAD entities frame the selected-bounds union.
- `crates/editor-shell/src/lifecycle/tests.rs::viewport_left_double_click_cad_no_selection_falls_back_to_scene_bounds`:
  CAD path with no selection preserves scene-wide fallback.
- `crates/editor-shell/src/lifecycle/tests.rs::viewport_left_double_click_unresolved_selected_cad_entity_falls_back_to_scene_bounds`:
  unresolved selected entity falls back to scene-wide framing.
- `crates/editor-shell/src/lifecycle/tests.rs::viewport_left_double_click_selected_cad_single_press_preserves_face_pick_gate`:
  first press preserves the face-pick truth table and does not frame selected
  bounds.

### Docs

- `.ai/dispatch.tasks.md`: marked task 139 done; no task 140 heading was added.
- `Status.md`: added ISSUE-393 selected-CAD double-click framing snapshot.
- `HANDOFF.md`: added matching handoff snapshot for task 139 completion.
- `plans/BASELINE.md`: added update 43 for the Phase 9 editor-usability
  completion.
- `change.md`: added the chronological ISSUE-393 implementation entry.

## Per-File Summary

- `crates/editor-shell/src/lifecycle/mod.rs`: selected-CAD framing remains
  private to viewport double-click. `reset_camera()` itself remains unchanged
  and scene-wide.
- `crates/editor-shell/src/lifecycle/tests.rs`: the multi-CAD fixture uses the
  existing single-BRep constructor first, then adds a second projected entity
  through existing projection APIs inside the crate-private test module.
- Status documents record task completion and explicitly state no task 140 was
  appended.

## Verification Results

- `rg -n "handle_viewport_left_press|ViewportLeftDoubleClick|current_scene_bounds|reset_camera|render_mesh_for|should_fire_face_pick|MouseButton::Left" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> exit 0; matches show the existing detector/left-press path, face-pick
  gate, scene-wide `reset_camera`, and selected-bounds `render_mesh_for` path.
- `rg -n "FrameSelected|Frame Selected|frame_selected|Command::FrameSelected" crates/editor-shell/src crates/editor-ui/src crates/editor-egui-host/src`
  -> exit 1; no matches, as expected.
- `cargo test -p rge-editor-shell --lib viewport_left_double_click` -> exit 0;
  10 passed / 0 failed / 0 ignored.
- `cargo test -p rge-editor-shell --lib reset_camera` -> exit 0; 4 passed /
  0 failed / 0 ignored.
- `cargo test -p rge-editor-shell --lib viewport_cursor_left` -> exit 0;
  6 passed / 0 failed / 0 ignored.
- `cargo test -p rge-editor-shell --lib` -> exit 0; 300 passed / 0 failed /
  1 ignored.
- `cargo check -p rge-editor-shell --lib` -> exit 0.
- `cargo +nightly fmt --all -- --check` -> exit 0 after running
  `cargo +nightly fmt --all` once for wrapping in the new test helper.
- `rg -n "^138\.|^139\.|^140\." .ai/dispatch.tasks.md` -> exit 0; task 138
  done, task 139 done, and no task 140 match.
- `git diff --name-only` -> exit 0; tracked diff before this EXEC packet was
  limited to `.ai/dispatch.tasks.md`, `HANDOFF.md`, `Status.md`, `change.md`,
  `crates/editor-shell/src/lifecycle/mod.rs`,
  `crates/editor-shell/src/lifecycle/tests.rs`, and `plans/BASELINE.md`.
- `git diff --check` -> exit 0; Git printed only existing line-ending
  normalization warnings for some Markdown files.

## Deviations from Task Packet

None. Execution stayed within the Task Packet scope. The active packet allows
ISSUE-393 EXEC `.meta.json` sidecar creation, so this EXEC packet will be
finalized.

## Open Questions for Reviewer

- Please verify that keeping `reset_camera()` unchanged is sufficient to
  preserve View menu/Home scene-wide behavior while the double-click branch is
  now selection-aware.
- Please verify the test fixture's constructor-preserving multi-CAD setup is
  acceptable: it does not broaden `with_world_projection_graph`; it adds the
  second CAD entity only inside crate-private tests through existing projection
  APIs.

## Worktree State

- Tracked files: modified `.ai/dispatch.tasks.md`, `HANDOFF.md`, `Status.md`,
  `change.md`, `crates/editor-shell/src/lifecycle/mod.rs`,
  `crates/editor-shell/src/lifecycle/tests.rs`, and `plans/BASELINE.md`.
- Untracked items: pre-existing `ai_handoffs/ISSUE-393_TASK_2026-06-14_13-03-10+0300.md`,
  pre-existing `ai_handoffs/ISSUE-393_TASK_2026-06-14_13-03-10+0300.meta.json`,
  pre-existing `ai_handoffs/claims/ISSUE-393_2026-06-14_13-03-07-8321856+0300_claim.json`,
  this EXEC packet, and this EXEC packet's `.meta.json` sidecar after
  finalization.
- Branch: `ai-dispatch/ISSUE-393`
- Last commit: `9cb5476fa6d1c5f3aaeb2b556ebb63c27c27600f ai-dispatch ISSUE-392: Audit next Phase 9 task after cursor-left drag cancel`

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-393
AUTHOR: Executor / Codex
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---
