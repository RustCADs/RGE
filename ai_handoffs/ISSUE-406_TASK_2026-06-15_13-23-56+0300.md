# Task Packet

DISPATCH_ID: ISSUE-406
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_13-23-56+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- ai_handoffs/ISSUE-406_*.md
- ai_handoffs/ISSUE-406_*.meta.json
- ai_dispatch_logs/log_*.md
- crates/editor-shell/src/**
- crates/editor-actions/src/**
- crates/cad-core/src/**
- crates/cad-projection/src/**
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
STATUS: OPEN

## Goal

Perform task 151 as a source/docs-read-only audit after the first CAD cuboid add
landed in ISSUE-405. The audit must compare the listed follow-up candidate
classes against current source greps, then update the autonomous task brief with
exactly one bounded FEATURE task 152 or a source-grounded human-stop marker. Do
not implement task 152 in this dispatch.

## Scope

This is an audit-only dispatch. Source, tests, Cargo metadata, workflows,
automation, schemas, and render/CAD behavior are read-only evidence surfaces for
this task, not edit surfaces.

### MAY edit
- `.ai/dispatch.tasks.md`
- `ai_handoffs/ISSUE-406_*.md`
- `ai_handoffs/ISSUE-406_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT edit
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `Invoke-*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-*.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `ai_handoffs/templates/**`
- `AI_DISPATCH_AUTOMATION.md`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- any ADR file
- any architecture-lint rule or config
- any non-ISSUE-406 existing handoff packet, sidecar, audit log, or generated artifact
- plugin runtime, plugin discovery, or plugin loading code
- command routing, shortcut execution, shortcut remapping, shortcut persistence, or fatal shortcut policy
- OS clipboard behavior
- CAD/projection/CommandBus mutation behavior
- undo, dirty-state, save-mark, save, load, open, close, or document authority
- camera behavior, navigation behavior, camera math, viewport hit testing, or face-pick policy
- render architecture, render-path behavior, mesh-upload behavior, or GPU resource lifetime behavior

### MAY add new files
- `ai_handoffs/ISSUE-406_EXEC_*.md`
- `ai_handoffs/ISSUE-406_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/*.schema.json`
- `ai_handoffs/templates/**`
- any ADR, source, test, workflow, schema, automation, architecture-lint, Cargo, or docs file outside the allowed ISSUE-406 artifacts
- any non-ISSUE-406 handoff packet, sidecar, audit log, or generated artifact

### Advisory machine-readable scope envelope

Scope for this dispatch is defined by the concrete MAY/MUST sections above.
Generated ISSUE-406 audit/log artifacts have queue-generated exact filenames,
so advisory machine-readable validation would risk over-authorizing unrelated
logs.

## Deliverables

- An ISSUE-406 EXEC packet containing the source-grounded audit.
- A `.ai/dispatch.tasks.md` update that appends exactly one bounded FEATURE task
  152, or exactly one `NEEDS_HUMAN_RECORDED: ISO-date - reason` line if no
  bounded in-policy next feature exists.
- If task 152 is appended, it must include explicit `MAY edit`, `MUST NOT edit`,
  `Done criteria`, `Verification`, and `Halt conditions` sections.
- If task 152 is appended, it must carry the same Self-re-arm final-step
  requirement so the autonomous task chain continues.
- No implementation of task 152 and no source/test/Cargo/workflow/schema/script
  behavior change.

## Acceptance Criteria

- The audit records the pre-edit task-heading check for
  `^149\.|^150\.|^151\.|^152\.` and proves whether task 152 existed before the
  self-rearm edit.
- Queue and already-filed autonomous-task claims cite only the dispatcher
  GitHub-state snapshot embedded in the ISSUE-406 issue body, or an exact local
  artifact copied from that snapshot. The executor must not call `gh`, browser,
  network tools, or GitHub APIs for those claims.
- The audit compares all three candidate classes from task 151:
  minimal render-path refresh after Phase-1-only window initialization; bounded
  CAD restore semantics cleanup; and strictly headless editor-shell observability
  or test coverage.
- Each candidate class has positive source references and falsifying searches
  for negative claims where practical.
- Exactly one bounded FEATURE task 152 is appended with the required sections
  and copied Self-rearm requirement, or exactly one source-grounded
  `NEEDS_HUMAN_RECORDED` line is appended instead.
- No other task is added, task 152 is not implemented, and all changed files are
  within the MAY-edit/MAY-add surface.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion in the audit must include its inverse
search/read and observed result. The executor must not rely on uncited planning
memory for source-state, queue-state, or task-brief-state claims.

- Claim: At planning time, the task brief had task headings 149, 150, and 151,
  and no task 152 heading.
  Falsifying search: `rg -n "^149\.|^150\.|^151\.|^152\." .ai/dispatch.tasks.md` -> matched task 149 at line 14757, task 150 at line 14864, and task 151 at line 14961; no `^152.` match appeared. `rg -n "^152\." .ai/dispatch.tasks.md` -> exit 1 / no matches.
- Instruction: Any CAD, projection, render, CommandBus, save/load, or
  task-152-feasibility premise belongs in the EXEC audit and must be established
  there with current source reads and inverse searches before use.

## Constraints / Non-Goals

- Do not edit Rust source, Rust tests, Cargo manifests, `Cargo.lock`, workflows,
  automation scripts, schemas, packet templates, architecture-lint config, ADRs,
  status docs, render behavior, CAD behavior, projection behavior, CommandBus
  behavior, undo/dirty/save-load behavior, camera behavior, or viewport behavior.
- Do not implement task 152, scaffold source for task 152, run a design refactor,
  or broaden the first CAD cuboid add behavior.
- Do not convert the headless cuboid add into a menu, shortcut, CommandBus,
  save/load, dirty-state, multi-root, delete, transform, or parameter-editing
  feature.
- Do not use live GitHub, `gh`, browser, network, or GitHub API access for
  queue-state or already-filed-task evidence; use the dispatcher-provided
  snapshot in the issue body.
- Do not append more than one follow-up task or combine multiple candidate
  classes into one oversized task 152.

## Verification Gates

The Executor MUST run and document the result of each gate in the
`EXECUTION_REPORT`:

- `rg -n "^149\.|^150\.|^151\.|^152\." .ai/dispatch.tasks.md` before edits and after edits.
- `rg -n "^152\." .ai/dispatch.tasks.md` before edits and after edits.
- Candidate-class source greps recorded in the audit, including current-source
  reads for `add_cad_cuboid_to_empty_scene`, `CuboidOp`, `begin_operation`,
  `spawn_brep_entity`, `restore_to`, `despawn_brep_entity`, `render_mesh_for`,
  `selected_cad_scene_bounds`, `CommandBus`, `Action`, `submit_action`,
  `undo_command`, `redo_command`, `mark_saved_command`, save/load handlers, and
  menu/shortcut routing as relevant to the rejected/selected candidate boundary.
- `rg -n "add_cad_cuboid_to_empty_scene|CuboidOp|begin_operation|spawn_brep_entity|restore_to|despawn_brep_entity" crates/editor-shell/src`
- `git diff --name-only` with expected changed tracked files limited to
  `.ai/dispatch.tasks.md` and ISSUE-406 generated artifacts.
- `git diff --check`
- `.\new-handoff.ps1 -Finalize -PacketPath` using the actual ISSUE-406 EXEC packet path, with `-DryRun`

Cargo build/test/fmt gates are not required by this packet because this is a
source/docs-read-only audit with no source, test, Cargo, or behavior changes.
If the orchestrator's canonical verification gate runs automatically and reports
a failure outside this dispatch's edit scope, the executor must preserve the
read-only boundary and follow the halt conditions below.

## Halt Conditions

The Executor MUST halt and request fresh guidance if any of the following occur:

- The dispatcher-provided GitHub-state snapshot cannot be cited without live
  `gh`, browser, network, or GitHub API access.
- Completing the audit requires editing any MUST-NOT path or implementing any
  part of task 152.
- The candidate comparison shows that more than one feature follow-up is required
  to make the selected boundary coherent; record `NEEDS_HUMAN_RECORDED` instead
  of forcing a task.
- No bounded task 152 can be specified without crossing a policy,
  product, or architecture boundary; record a source-grounded
  `NEEDS_HUMAN_RECORDED` line instead of forcing a task.
- A pre-existing task 152 heading is found before edits; do not overwrite or
  duplicate it.
- The orchestrator routes a correction that would require source/test/Cargo,
  workflow, automation, schema, render, CAD, projection, CommandBus,
  undo/dirty/save-load, camera, or viewport behavior changes for this read-only
  audit.

## Planner Notes

Use task 151 in `.ai/dispatch.tasks.md` as the source of the candidate list and
self-rearm requirement. Use ISSUE-405 TASK/EXEC packets and current source as
evidence for the predecessor boundary; record the actual reads/greps in the
audit before relying on any statement about menus, shortcuts, CommandBus
authority, save/load, dirty state, multi-root composition, deletion, transforms,
or parameter editing.
This dispatch is only the aftermath audit and next-task selection.

The next task must be a bounded FEATURE task 152 only if the source audit proves
one candidate is independently shippable inside policy. Otherwise the correct
output is a single `NEEDS_HUMAN_RECORDED` marker with a source-grounded reason.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-406
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
