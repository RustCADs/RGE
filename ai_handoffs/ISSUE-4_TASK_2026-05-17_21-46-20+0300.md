# Task Packet

DISPATCH_ID: ISSUE-4
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-17_21-46-20+0300
RELATED_FILES:
- AGENTS.md
- .ai/dispatch.verify.ps1
STATUS: OPEN

## Goal

Add the GitHub issue #4 documentation pointer requested for the dispatch
automation: `AGENTS.md` should name `.ai/dispatch.verify.ps1` in its existing
Pointers list as the canonical verification gate run by the AI dispatch loop.

## Scope

### MAY edit
- AGENTS.md

### MUST NOT edit
- Any existing file other than `AGENTS.md`.
- `.ai/dispatch.verify.ps1`; it is only referenced by the new pointer.
- `AI_DISPATCH_AUTOMATION.md`, `AI_DISPATCH_PARALLEL.md`, `HANDOFF.md`,
  `Status.md`, `change.md`, schemas, scripts, source files, workflow files, and
  `.gitignore`.
- Any existing `ai_handoffs/` packet, including this TASK packet.

### MAY add new files
- The protocol-required `ai_handoffs/ISSUE-4_EXEC_*.md` execution report.
- The matching `ai_handoffs/ISSUE-4_EXEC_*.meta.json` sidecar if generated by
  `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- Any file other than the protocol-required EXECUTION_REPORT packet and its
  matching sidecar.

## Deliverables

- One concise new bullet in the existing `AGENTS.md` "Pointers" list for
  `.ai/dispatch.verify.ps1`.
- The bullet describes `.ai/dispatch.verify.ps1` as the canonical verification
  gate the AI dispatch loop runs, matching the style and brevity of the
  neighboring bullets.
- A completed EXECUTION_REPORT packet documenting the edit and verification.

## Acceptance Criteria

- `AGENTS.md` contains exactly one new pointer bullet for
  `.ai/dispatch.verify.ps1` in the existing "Pointers" list.
- The bullet states that `.ai/dispatch.verify.ps1` is the canonical verification
  gate the AI dispatch loop runs.
- No other existing file is modified.
- No new files are added except the protocol-required EXECUTION_REPORT packet
  and optional matching `.meta.json` sidecar.
- Workspace verification passes with the canonical dispatch verification gate.

## Constraints / Non-Goals

- Do not rewrite, reorganize, or expand `AGENTS.md` beyond the single requested
  pointer bullet.
- Do not modify the verification script, orchestrator, queue runner, protocol,
  schemas, source code, tests, or workflow files.
- Do not add explanatory sections or duplicate the verification script details
  elsewhere in `AGENTS.md`.
- Do not commit, push, stage files, or open a pull request.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- Inspect `AGENTS.md` and confirm the new `.ai/dispatch.verify.ps1` pointer is
  present in the existing "Pointers" list.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\.ai\dispatch.verify.ps1`
  expected exit 0.
- `git status --short --branch` to document the final workspace state; expected
  existing-file modification only in `AGENTS.md`, plus the protocol-required
  EXECUTION_REPORT artifacts.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- The requested pointer cannot be added without editing an existing file other
  than `AGENTS.md`.
- The existing `AGENTS.md` "Pointers" list is missing or has changed enough
  that the requested one-bullet edit is ambiguous.
- `.ai/dispatch.verify.ps1` is missing, renamed, or no longer appears to be the
  canonical dispatch verification gate.
- The canonical verification gate fails or cannot be run; do not make unrelated
  fixes to force it green.
- Any step appears to require source, schema, script, workflow, or broader
  documentation changes outside this dispatch scope.

## Planner Notes

This is a narrow documentation pointer task from GitHub issue #4. The intended
source change is only a single bullet in `AGENTS.md`; protocol packet artifacts
are allowed only because the dispatch loop requires them.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-4
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
