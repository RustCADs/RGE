# Task Packet

DISPATCH_ID: ISSUE-398
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_01-53-21+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- ai_handoffs/ISSUE-398_EXEC_*.md
- ai_handoffs/ISSUE-398_EXEC_*.meta.json
- ai_dispatch_logs/log_*.md
- crates/editor-ui/src/menus/**
- crates/editor-egui-host/src/**
- crates/editor-shell/src/lifecycle/**
- crates/editor-shell/src/render_path.rs
- crates/editor-actions/**
- crates/cad-projection/**
- crates/plugin-discovery/**
- runtime/**
STATUS: OPEN

## Goal

Run task 144 as a docs/source-read-only Phase 9 editor-usability audit after
ISSUE-397 / task 143's wheel double-click reset. Compare the listed remaining
editor-usability candidate classes against current local source, then append
exactly one bounded FEATURE task 145 to `.ai/dispatch.tasks.md`, or record a
single source-grounded `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line if no
bounded in-policy next feature exists. Use only the dispatcher-provided GitHub
state snapshot for queue/already-filed evidence; do not call `gh`, browser,
network, or the GitHub API.

## Scope

This is an implementation-source-read-only audit. Reading Rust source and status
docs is required. Editing production source, tests, Cargo metadata, automation,
schemas, workflows, ADRs, packet templates, or unrelated handoff/log artifacts is
not allowed.

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-398_EXEC_*.md`
- `ai_handoffs/ISSUE-398_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT edit
- Rust source or tests under `crates/**`, `kernel/**`, `editor/**`, `runtime/**`,
  `tools/**`, `plugins/**`, `tests/**`, or `examples/**`
- Cargo manifests or `Cargo.lock`
- `.github/workflows/**`
- `.ai/*.schema.json`
- `.ai/dispatch.verify.ps1`
- dispatch automation, guard, queue, scheduler, watcher, verification, health,
  trend, or handoff scripts
- schemas, ADR files, architecture-lint rules/config, packet templates, or
  unrelated existing handoff/log artifacts
- plugin runtime/discovery/loading code, command routing, shortcut execution,
  remapping/persistence/fatal-policy behavior, OS clipboard behavior,
  CAD/projection/CommandBus mutation, undo/dirty/save-load authority,
  camera/navigation behavior, camera math, viewport hit testing, face-pick
  policy, wheel zoom, right-button orbit math, or middle-button pan math

### MAY add new files
- `ai_handoffs/ISSUE-398_EXEC_*.md`
- `ai_handoffs/ISSUE-398_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files
- Any new Rust source, test, fixture, Cargo, workflow, schema, ADR, plan,
  automation, packet-template, or non-generated documentation file
- Any handoff/log artifact not generated for ISSUE-398

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-398_EXEC_*.md
  - ai_handoffs/ISSUE-398_EXEC_*.meta.json
  - ai_dispatch_logs/log_*.md
MUST_NOT_EDIT:
  - crates/**
  - kernel/**
  - editor/**
  - runtime/**
  - tools/**
  - plugins/**
  - tests/**
  - examples/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/workflows/**
  - .ai/*.schema.json
  - .ai/dispatch.verify.ps1
  - Invoke-*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-*.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
  - docs/adr/**
  - plans/PLAN.md
  - plans/IMPLEMENTATION.md
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A recorded task-144 audit in the allowed docs/brief surface that compares all
  required candidate classes using current local source reads.
- Exactly one bounded FEATURE task 145 appended to `.ai/dispatch.tasks.md`, or
  exactly one `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line appended if no
  bounded in-policy next feature exists.
- If task 145 is appended, it must include explicit `MAY edit`, `MUST NOT edit`,
  `Done criteria`, `Verification`, and `Halt conditions` sections.
- If task 145 is appended, it must carry the same Self-re-arm final-step
  requirement so the next FEATURE appends the next bounded AUDIT task or records
  `NEEDS_HUMAN_RECORDED`.
- A concise status update in the allowed status docs if the audit outcome changes
  the Phase 9/editor-usability state.
- A completed ISSUE-398 EXEC packet describing the audit, source evidence,
  verification, and final task-145-or-human outcome.

## Acceptance Criteria

- The pre-edit task-heading check is recorded using
  `rg -n "^141\.|^142\.|^143\.|^144\.|^145\." .ai/dispatch.tasks.md`.
- Queue/already-filed-task claims cite only the dispatcher-provided snapshot
  from this ISSUE-398 dispatch. The usable snapshot facts are: generated by
  `Invoke-AiDispatchAuto.ps1` at `2026-06-15T01:52:55.9305454+03:00` for
  `RustCADs/RGE`; no open `ai-dispatch` issues before ISSUE-398 was created; no
  open failed autonomous issues; autonomous issues already filed include closed
  ISSUE-397 / task 143. No live `gh`, browser, network, or GitHub API command is
  run for those claims.
- The audit compares these candidate classes from current source and records
  positive source references, and for every negative or unchanged source claim
  either records the falsifying search/read plus observed result or states the
  item as an open question rather than a premise:
  - keybinding remapping, preferences, conflict policy, and fatal-policy
    behavior;
  - host-shell command routing through `MenuCommandHandoff` /
    `EditorShell::route_menu_command`;
  - real plugin command execution beyond the injected extension-command seam;
  - OS or typed clipboard behavior beyond the current shell-local clipboard;
  - CAD/editor mutation through `CommandBus`, projection, undo/dirty, and
    save/load authority;
  - camera/navigation follow-up after wheel zoom, orbit, pan, frame-all,
    selected-CAD framing, cursor grab/release, focus-loss drag cancellation,
    cursor-left cancellation, focus-loss stale double-click reset, and
    mouse-wheel stale double-click reset.
- The selected task 145, if appended, is one bounded FEATURE task only. It must
  not implement itself, open a second follow-up, or require a cross-boundary
  policy/architecture decision to be coherent.
- If no bounded task 145 can be specified safely, the executor appends exactly
  one `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line instead of forcing a
  task.
- No implementation work for task 145 is done, and no Rust/source/test/Cargo
  files are edited.

## Current-State Claims: Falsification Required

This planner packet intentionally avoids negative source claims as premises. The
executor must ground the audit in fresh local source reads and must attach the
inverse search/read that would falsify every negative or "unchanged" source
claim used to select or reject task 145.

- Claim: GitHub queue/already-filed evidence is provided by the dispatcher
  snapshot, not by live GitHub state.
  Falsifying search: not applicable to repository source; executor must cite the
  snapshot facts listed in `Acceptance Criteria` and must not run `gh`, browser,
  network, or GitHub API commands for this evidence.
- Claim: Task 144 is the audit being executed and task 145 is the only allowed
  next task number.
  Falsifying search: `rg -n "^141\.|^142\.|^143\.|^144\.|^145\." .ai/dispatch.tasks.md` -> observed pre-edit result:
  `13514:141. **[DONE 2026-06-14 via ISSUE-395] Reset stale viewport left-double-click state on focus loss.**`;
  `13600:142. **[DONE 2026-06-15 via ISSUE-396] Post-focus-loss-double-click-reset Phase 9 next-task source audit.**`;
  `13788:143. **[DONE 2026-06-15 via ISSUE-397] Reset stale viewport left-double-click state on viewport mouse wheel.**`;
  `13898:144. **Post-wheel-double-click-reset Phase 9 next-task source audit.**`.
  No `145.` match was returned. Executor must record the post-edit result in
  the EXEC packet after appending task 145 or the human-needed marker.

## Constraints / Non-Goals

- Do not implement task 145.
- Do not edit production source, tests, Cargo metadata, workflows, schemas,
  automation scripts, packet templates, ADRs, or unrelated handoff/log artifacts.
- Do not use live GitHub state. The dispatcher-provided snapshot is the only
  authority for queue/already-filed evidence.
- Do not select more than one next feature. If the next coherent work requires a
  batch, policy decision, new architecture boundary, or multiple independent
  changes, record `NEEDS_HUMAN_RECORDED` instead.
- Do not broaden task 145 into shortcut remapping/persistence/fatal-policy,
  plugin discovery/loading/runtime execution, OS clipboard integration,
  CAD/CommandBus mutation, undo/dirty/save-load authority, or camera math unless
  the audit proves a single bounded in-policy feature can be scoped without
  crossing those boundaries.
- Preserve the Self-re-arm chain. If task 145 is appended, its final step must
  require appending exactly one bounded AUDIT task 146 or a single
  `NEEDS_HUMAN_RECORDED` line.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `rg -n "^141\.|^142\.|^143\.|^144\.|^145\." .ai/dispatch.tasks.md` before
  edits and after edits
- Status-doc cross-check against `Status.md`, `HANDOFF.md`,
  `plans/BASELINE.md`, `change.md`, and `.ai/dispatch.tasks.md` for
  `ISSUE-397`, `task 143`, `task 144`, `task 145`, `wheel double-click`,
  and `mouse-wheel`
- Candidate-class source greps/readouts recorded in the audit for each required
  class listed in `Acceptance Criteria`
- `git diff --name-only` showing only allowed docs/brief paths plus generated
  ISSUE-398 artifacts
- `git diff --check`
- Resolve the single `ai_handoffs/ISSUE-398_EXEC_*.md` packet path and run
  `.\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`

Cargo/test/lint gates are not requested for this docs/source-read-only audit.
If the orchestrator's canonical verification gate later runs and fails on a
target outside this task's edit scope, the executor must treat any correction
request to fix source/tests/Cargo/automation as out of scope and halt per the
conditions below.

## Halt Conditions

The Executor MUST halt and request fresh guidance if any of the following occur:

- The dispatcher-provided GitHub-state snapshot cannot be cited without live
  `gh`, browser, network, or GitHub API access.
- The audit would require editing any path outside `MAY edit` or adding any file
  outside `MAY add new files`.
- Any implementation work for task 145 appears necessary during this audit.
- More than one feature follow-up would be required to make the selected
  boundary coherent.
- No bounded task 145 can be specified without crossing policy or architecture
  boundaries; in that case record `NEEDS_HUMAN_RECORDED` instead of appending a
  task.
- Any verification/correction round asks the executor to fix a source, test,
  Cargo, workflow, schema, automation, packet-template, or unrelated artifact
  failure outside this task's docs/source-read-only scope.
- The final `.ai/dispatch.tasks.md` state would contain both a task 145 and a
  `NEEDS_HUMAN_RECORDED` line, more than one task 145, or any task 146.

## Planner Notes

Dispatcher/user context identifies ISSUE-397 / task 143 as the preceding
mouse-wheel stale-double-click reset. Do not treat any detailed wheel-zoom or
no-op behavior as a planner premise; verify current source before recording any
such claim in the audit. Task 144, already present in `.ai/dispatch.tasks.md`,
is the post-wheel-double-click-reset source-read-only audit. This packet
executes that task and carries forward the same self-rearming protocol: the
audit must append one bounded FEATURE task 145 or record a single human-needed
marker.

The dispatcher snapshot in the issue body is the only GitHub evidence source for
this dispatch. Its relevant facts are intentionally repeated in this packet so
the executor does not need to query GitHub: the snapshot was generated at
`2026-06-15T01:52:55.9305454+03:00`, reported no open `ai-dispatch` issues
before ISSUE-398 was created, reported no open failed autonomous issues, and
listed autonomous work through closed ISSUE-397.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-398
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
