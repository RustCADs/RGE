# Correction Packet

DISPATCH_ID: ISSUE-216
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-26_22-41-45+0300
RELATED_FILES:
- ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md
- ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.md
- .ai/dispatch-ISSUE-216/codex.control.round0.json
- .ai/dispatch-ISSUE-216/verification.round0.log
- Status.md
- HANDOFF.md
- crates/rge-data/src/prefab.rs
- crates/rge-data/examples/
- golden-projects/simple-scene/.rge-project
- crates/macros-reflect/tests/
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-216_TASK_2026-05-26_22-15-23+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-216_EXEC_2026-05-26_22-30-28+0300.md`
- Codex Control Review: `.ai/dispatch-ISSUE-216/codex.control.round0.json`
- Verification Log: `.ai/dispatch-ISSUE-216/verification.round0.log`
- Prior Correction Packets: none

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. These corrections are the approved subset from the Codex control review
`required_fixes` list. Do not edit source files, documentation files, schemas,
scripts, prior packets, or logs. The correction target is the next ISSUE-216
EXECUTION_REPORT packet and its normal orchestrator sidecar only.

1. **Status and handoff live evidence** - Control review `required_fixes[0]`.
   Required change: update the next EXEC report to include live line-cited
   evidence from `Status.md` and `HANDOFF.md`, satisfying TASK lines 168-171.
   Do not substitute `plans/BASELINE.md` or `kernel/types/BUDGET.md` for those
   required reads; those files may appear only as supplemental evidence if they
   are still useful. Acceptance: the next EXEC contains explicit `Status.md`
   and `HANDOFF.md` line citations for the state claims it relies on, and no
   text says alternate files replaced those required inspections.

2. **Singular smallest safe follow-up** - Control review `required_fixes[1]`.
   Required change: make `SMALLEST_SAFE_FOLLOW_UP` exactly one action or
   exactly `NEEDS_HUMAN`, as required by TASK lines 127-131. Remove the optional
   docs-only `PREFLIGHT` fallback from the decision block and from open
   questions. Acceptance: the next EXEC names one follow-up only, or says
   `NEEDS_HUMAN` only, with no secondary fallback or optional alternative.

3. **Recent Phase 9 pressure-audit artifact citations** - Control review
   `required_fixes[2]`. Required change: if ISSUE-210, ISSUE-212, or ISSUE-214
   remain in the candidate comparison, add current packet or log line citations
   for each artifact used, satisfying TASK lines 209-211. If any of those
   artifacts are not needed for the comparison, remove the uncited claim instead
   of keeping indirect evidence. Acceptance: every retained ISSUE-210,
   ISSUE-212, or ISSUE-214 comparison point has current packet or log line
   citations.

4. **Typed-schema inspection surfaces** - Control review `required_fixes[3]`.
   Required change: document the required typed-schema inspection surfaces from
   TASK lines 302-306: `crates/rge-data/src/prefab.rs`,
   `crates/rge-data/examples/**`, and
   `golden-projects/simple-scene/.rge-project`. Replace the generic examples
   grep mention with specific line-cited evidence from the files inspected.
   Acceptance: the next EXEC contains line-cited evidence for `prefab.rs`, the
   relevant files under `crates/rge-data/examples/`, and the simple-scene
   `.rge-project` manifest.

5. **Macros-reflect current test-surface citations** - Control review
   `required_fixes[4]`. Required change: where the next EXEC relies on the
   macros-reflect test pilot, replace baseline-only or indirect evidence with
   current file line citations. This must include
   `crates/macros-reflect/tests/fixtures/render_pass.rs` and any other
   `crates/macros-reflect/tests/**` surfaces used to support the claim.
   Acceptance: every macros-reflect test-surface claim retained in the next
   EXEC is backed by current file line citations, not only baseline summaries.

## Deferred Findings (NOT Approved for This Round)

None. The control review did not approve any extra work beyond the five fixes
above, and no optional cleanup is approved for this correction round.

## Updated Acceptance Criteria

Unchanged from the Task Packet. This correction only requires the next EXEC
report to satisfy the Task Packet's evidence, citation, and singular follow-up
requirements that the first EXEC missed.

## Re-Verification Gates

The Executor MUST run or preserve these gates after writing the corrected EXEC
report:

- `.ai/dispatch.verify.ps1` -> expected exit 0 with `VERIFY OK` and all seven
  canonical steps passed.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no source, docs,
  schema, script, or prior-packet edits; only dispatch-owned ISSUE-216 packet
  artifacts may be new or changed.
- Packet validation for the newly written ISSUE-216 EXEC report through the
  orchestrator/new-handoff dry-run path -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet. If any required inspection surface cannot be
read and line-cited without expanding scope, halt in the next EXEC report with
`NEEDS_HUMAN`; do not substitute an unapproved evidence source and do not edit
repository source, docs, schemas, scripts, prior packets, or logs.

## Planner Notes

This correction is report-only. The canonical verification gate already passed
for the read-only audit, and the control failure is about EXEC report
completeness: missing required live citations, an over-broad follow-up field,
uncited recent audit artifacts, omitted typed-schema surfaces, and indirect
macros-reflect evidence. Keep the audit scope unchanged and make the next EXEC
packet citation-complete.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-216
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
