# Correction Packet

DISPATCH_ID: ISSUE-51
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_17-53-56+0300
RELATED_FILES:
- crates/anim-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-51_TASK_2026-05-19_17-49-31+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-51_EXEC_2026-05-19_17-53-17+0300.md`
- Latest Review Report: none generated. The post-execution canonical verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less.

1. **Normalize anim-graph lib.rs newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with
   `Incorrect newline style in \\?\A:\RCAD\RGE\crates\anim-graph\src\lib.rs`.
   Required change: normalize only the line endings in
   `crates/anim-graph/src/lib.rs` to the newline style accepted by `rustfmt`.
   Do not change Rust tokens, formatting, comments, imports, module structure,
   behavior, or any other file. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0, and a whitespace-insensitive
   diff of `crates/anim-graph/src/lib.rs` shows no content change beyond
   newline normalization.

## Deferred Findings (NOT Approved for This Round)

None. No review findings were produced; the only known blocker is the canonical
format gate failure above.

## Updated Acceptance Criteria

Unchanged from the Task Packet except for this correction-specific allowance:
`crates/anim-graph/src/lib.rs` may receive a line-ending-only normalization so
the canonical verification gate can pass. The existing
`crates/material-graph/src/lib.rs` changes from the execution round must not be
expanded.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `git diff --ignore-space-at-eol -- crates/anim-graph/src/lib.rs` -> expected no content diff beyond newline normalization.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with this correction-round addition: halt and
route back to Planner if making `cargo +nightly fmt --all -- --check` pass
would require any non-newline content change in
`crates/anim-graph/src/lib.rs`, any edit to another file, or any
semantic/runtime change.

## Planner Notes

This correction is deliberately narrow. The ISSUE-51 execution report indicates
the requested `MaterialGraph` structural-diff smoke test landed and its local
gate passed, but the orchestrator's canonical verification failed before
control review on a newline-style problem in `crates/anim-graph/src/lib.rs`.
Normalize that file's newline style only, preserve the existing ISSUE-51 test
changes, and rerun the canonical verification gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-51
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---
