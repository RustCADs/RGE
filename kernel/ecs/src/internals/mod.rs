//! `kernel_ecs::internals` — private implementation details.
//!
//! Nothing in this module is part of the public API.  It exists as a home for
//! shared utilities that are used internally by multiple public modules but
//! should not be exposed to consumers of this crate.
//!
//! Currently empty — reserved for future use (e.g., generation-counter
//! utilities, archetype migration helpers).
